# This file was @generated by mochi_gen_py. Do not modify this file by hand!!
#
# Copyright (c) Meta Platforms, Inc. and affiliates.
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#     http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

# pyre-strict

from typing import Any, Callable, Iterator, Optional, Sequence, overload
import numpy as np
import numpy.typing as npt
from . import model as model
from . import prefab as prefab
from . import experimental as experimental
from . import diffsim as diffsim

########################################################################################

# Template specializations
class DynamicArrayActor:
    @overload
    def __init__(self) -> None: ...
    @overload
    def __init__(self, size: int, value: Actor = ...) -> None: ...
    @overload
    def __init__(self, sequence: Sequence[Actor]) -> None: ...
    def __len__(self) -> int: ...
    def __getitem__(self, index: int) -> Actor: ...
    def __setitem__(self, index: int, value: Actor) -> None: ...
    def __iter__(self) -> Iterator[Actor]: ...
    def append(self, value: Actor) -> None: ...
    def clear(self) -> None: ...
    def resize(self, size: int) -> None: ...

type ArrayLikeActor = Sequence[Actor] | DynamicArrayActor | npt.NDArray[Any]

class DynamicArrayActorHandle:
    @overload
    def __init__(self) -> None: ...
    @overload
    def __init__(self, size: int, value: ActorHandle = ...) -> None: ...
    @overload
    def __init__(self, sequence: Sequence[ActorHandle]) -> None: ...
    def __len__(self) -> int: ...
    def __getitem__(self, index: int) -> ActorHandle: ...
    def __setitem__(self, index: int, value: ActorHandle) -> None: ...
    def __iter__(self) -> Iterator[ActorHandle]: ...
    def append(self, value: ActorHandle) -> None: ...
    def clear(self) -> None: ...
    def resize(self, size: int) -> None: ...

type ArrayLikeActorHandle = (
    Sequence[ActorHandle] | DynamicArrayActorHandle | npt.NDArray[Any]
)

class DynamicArrayArticulatedCycleJoint:
    @overload
    def __init__(self) -> None: ...
    @overload
    def __init__(self, size: int, value: ArticulatedCycleJoint = ...) -> None: ...
    @overload
    def __init__(self, sequence: Sequence[ArticulatedCycleJoint]) -> None: ...
    def __len__(self) -> int: ...
    def __getitem__(self, index: int) -> ArticulatedCycleJoint: ...
    def __setitem__(self, index: int, value: ArticulatedCycleJoint) -> None: ...
    def __iter__(self) -> Iterator[ArticulatedCycleJoint]: ...
    def append(self, value: ArticulatedCycleJoint) -> None: ...
    def clear(self) -> None: ...
    def resize(self, size: int) -> None: ...

type ArrayLikeArticulatedCycleJoint = (
    Sequence[ArticulatedCycleJoint] |
    DynamicArrayArticulatedCycleJoint |
    npt.NDArray[Any]
)

class DynamicArrayArticulatedCycleJointParams:
    @overload
    def __init__(self) -> None: ...
    @overload
    def __init__(self, size: int, value: ArticulatedCycleJointParams = ...) -> None: ...
    @overload
    def __init__(self, sequence: Sequence[ArticulatedCycleJointParams]) -> None: ...
    def __len__(self) -> int: ...
    def __getitem__(self, index: int) -> ArticulatedCycleJointParams: ...
    def __setitem__(self, index: int, value: ArticulatedCycleJointParams) -> None: ...
    def __iter__(self) -> Iterator[ArticulatedCycleJointParams]: ...
    def append(self, value: ArticulatedCycleJointParams) -> None: ...
    def clear(self) -> None: ...
    def resize(self, size: int) -> None: ...

type ArrayLikeArticulatedCycleJointParams = (
    Sequence[ArticulatedCycleJointParams] |
    DynamicArrayArticulatedCycleJointParams |
    npt.NDArray[Any]
)

class DynamicArrayArticulatedDofInfo:
    @overload
    def __init__(self) -> None: ...
    @overload
    def __init__(self, size: int, value: ArticulatedDofInfo = ...) -> None: ...
    @overload
    def __init__(self, sequence: Sequence[ArticulatedDofInfo]) -> None: ...
    def __len__(self) -> int: ...
    def __getitem__(self, index: int) -> ArticulatedDofInfo: ...
    def __setitem__(self, index: int, value: ArticulatedDofInfo) -> None: ...
    def __iter__(self) -> Iterator[ArticulatedDofInfo]: ...
    def append(self, value: ArticulatedDofInfo) -> None: ...
    def clear(self) -> None: ...
    def resize(self, size: int) -> None: ...

type ArrayLikeArticulatedDofInfo = (
    Sequence[ArticulatedDofInfo] | DynamicArrayArticulatedDofInfo | npt.NDArray[Any]
)

class DynamicArrayArticulatedJointFrictionParams:
    @overload
    def __init__(self) -> None: ...
    @overload
    def __init__(
        self,
        size: int,
        value: ArticulatedJointFrictionParams = ...,
    ) -> None: ...
    @overload
    def __init__(self, sequence: Sequence[ArticulatedJointFrictionParams]) -> None: ...
    def __len__(self) -> int: ...
    def __getitem__(self, index: int) -> ArticulatedJointFrictionParams: ...
    def __setitem__(
        self,
        index: int,
        value: ArticulatedJointFrictionParams,
    ) -> None: ...
    def __iter__(self) -> Iterator[ArticulatedJointFrictionParams]: ...
    def append(self, value: ArticulatedJointFrictionParams) -> None: ...
    def clear(self) -> None: ...
    def resize(self, size: int) -> None: ...

type ArrayLikeArticulatedJointFrictionParams = (
    Sequence[ArticulatedJointFrictionParams] |
    DynamicArrayArticulatedJointFrictionParams |
    npt.NDArray[Any]
)

class DynamicArrayArticulatedJointParams:
    @overload
    def __init__(self) -> None: ...
    @overload
    def __init__(self, size: int, value: ArticulatedJointParams = ...) -> None: ...
    @overload
    def __init__(self, sequence: Sequence[ArticulatedJointParams]) -> None: ...
    def __len__(self) -> int: ...
    def __getitem__(self, index: int) -> ArticulatedJointParams: ...
    def __setitem__(self, index: int, value: ArticulatedJointParams) -> None: ...
    def __iter__(self) -> Iterator[ArticulatedJointParams]: ...
    def append(self, value: ArticulatedJointParams) -> None: ...
    def clear(self) -> None: ...
    def resize(self, size: int) -> None: ...

type ArrayLikeArticulatedJointParams = (
    Sequence[ArticulatedJointParams] |
    DynamicArrayArticulatedJointParams |
    npt.NDArray[Any]
)

class DynamicArrayArticulatedJointType:
    @overload
    def __init__(self) -> None: ...
    @overload
    def __init__(self, size: int, value: ArticulatedJointType = ...) -> None: ...
    @overload
    def __init__(self, sequence: Sequence[ArticulatedJointType]) -> None: ...
    def __len__(self) -> int: ...
    def __getitem__(self, index: int) -> ArticulatedJointType: ...
    def __setitem__(self, index: int, value: ArticulatedJointType | int) -> None: ...
    def __iter__(self) -> Iterator[ArticulatedJointType]: ...
    def append(self, value: ArticulatedJointType | int) -> None: ...
    def clear(self) -> None: ...
    def resize(self, size: int) -> None: ...

type ArrayLikeArticulatedJointType = (
    Sequence[ArticulatedJointType | int] |
    DynamicArrayArticulatedJointType |
    npt.NDArray[Any]
)

class DynamicArrayArticulatedLinkParams:
    @overload
    def __init__(self) -> None: ...
    @overload
    def __init__(self, size: int, value: ArticulatedLinkParams = ...) -> None: ...
    @overload
    def __init__(self, sequence: Sequence[ArticulatedLinkParams]) -> None: ...
    def __len__(self) -> int: ...
    def __getitem__(self, index: int) -> ArticulatedLinkParams: ...
    def __setitem__(self, index: int, value: ArticulatedLinkParams) -> None: ...
    def __iter__(self) -> Iterator[ArticulatedLinkParams]: ...
    def append(self, value: ArticulatedLinkParams) -> None: ...
    def clear(self) -> None: ...
    def resize(self, size: int) -> None: ...

type ArrayLikeArticulatedLinkParams = (
    Sequence[ArticulatedLinkParams] |
    DynamicArrayArticulatedLinkParams |
    npt.NDArray[Any]
)

class DynamicArrayBlendingData:
    @overload
    def __init__(self) -> None: ...
    @overload
    def __init__(self, size: int, value: BlendingData = ...) -> None: ...
    @overload
    def __init__(self, sequence: Sequence[BlendingData]) -> None: ...
    def __len__(self) -> int: ...
    def __getitem__(self, index: int) -> BlendingData: ...
    def __setitem__(self, index: int, value: BlendingData) -> None: ...
    def __iter__(self) -> Iterator[BlendingData]: ...
    def append(self, value: BlendingData) -> None: ...
    def clear(self) -> None: ...
    def resize(self, size: int) -> None: ...

type ArrayLikeBlendingData = (
    Sequence[BlendingData] | DynamicArrayBlendingData | npt.NDArray[Any]
)

class DynamicArrayBlendingDataView:
    @overload
    def __init__(self) -> None: ...
    @overload
    def __init__(self, size: int, value: BlendingDataView = ...) -> None: ...
    @overload
    def __init__(self, sequence: Sequence[BlendingDataView]) -> None: ...
    def __len__(self) -> int: ...
    def __getitem__(self, index: int) -> BlendingDataView: ...
    def __setitem__(self, index: int, value: BlendingDataView) -> None: ...
    def __iter__(self) -> Iterator[BlendingDataView]: ...
    def append(self, value: BlendingDataView) -> None: ...
    def clear(self) -> None: ...
    def resize(self, size: int) -> None: ...

type ArrayLikeBlendingDataView = (
    Sequence[BlendingDataView] | DynamicArrayBlendingDataView | npt.NDArray[Any]
)

class DynamicArrayBool:
    @overload
    def __init__(self) -> None: ...
    @overload
    def __init__(self, size: int, value: bool = ...) -> None: ...
    @overload
    def __init__(self, sequence: Sequence[bool]) -> None: ...
    def __len__(self) -> int: ...
    def __getitem__(self, index: int) -> bool: ...
    def __setitem__(self, index: int, value: bool) -> None: ...
    def __iter__(self) -> Iterator[bool]: ...
    def append(self, value: bool) -> None: ...
    def clear(self) -> None: ...
    def resize(self, size: int) -> None: ...

type ArrayLikeBool = Sequence[bool] | DynamicArrayBool | npt.NDArray[np.bool_]

class DynamicArrayChar:
    @overload
    def __init__(self) -> None: ...
    @overload
    def __init__(self, size: int, value: str = ...) -> None: ...
    @overload
    def __init__(self, sequence: Sequence[str]) -> None: ...
    def __len__(self) -> int: ...
    def __getitem__(self, index: int) -> str: ...
    def __setitem__(self, index: int, value: str) -> None: ...
    def __iter__(self) -> Iterator[str]: ...
    def append(self, value: str) -> None: ...
    def clear(self) -> None: ...
    def resize(self, size: int) -> None: ...

type ArrayLikeChar = Sequence[str] | DynamicArrayChar | npt.NDArray[np.str_]

class DynamicArrayColor:
    @overload
    def __init__(self) -> None: ...
    @overload
    def __init__(self, size: int, value: Color = ...) -> None: ...
    @overload
    def __init__(self, sequence: Sequence[Color]) -> None: ...
    def __len__(self) -> int: ...
    def __getitem__(self, index: int) -> Color: ...
    def __setitem__(self, index: int, value: ColorLike) -> None: ...
    def __iter__(self) -> Iterator[Color]: ...
    def append(self, value: ColorLike) -> None: ...
    def clear(self) -> None: ...
    def resize(self, size: int) -> None: ...

type ArrayLikeColor = (
    Sequence[ColorLike] | DynamicArrayColor | npt.NDArray[np.unsignedinteger]
)

class DynamicArrayConstraint:
    @overload
    def __init__(self) -> None: ...
    @overload
    def __init__(self, size: int, value: Constraint = ...) -> None: ...
    @overload
    def __init__(self, sequence: Sequence[Constraint]) -> None: ...
    def __len__(self) -> int: ...
    def __getitem__(self, index: int) -> Constraint: ...
    def __setitem__(self, index: int, value: Constraint) -> None: ...
    def __iter__(self) -> Iterator[Constraint]: ...
    def append(self, value: Constraint) -> None: ...
    def clear(self) -> None: ...
    def resize(self, size: int) -> None: ...

type ArrayLikeConstraint = (
    Sequence[Constraint] | DynamicArrayConstraint | npt.NDArray[Any]
)

class DynamicArrayContactPoint:
    @overload
    def __init__(self) -> None: ...
    @overload
    def __init__(self, size: int, value: ContactPoint = ...) -> None: ...
    @overload
    def __init__(self, sequence: Sequence[ContactPoint]) -> None: ...
    def __len__(self) -> int: ...
    def __getitem__(self, index: int) -> ContactPoint: ...
    def __setitem__(self, index: int, value: ContactPoint) -> None: ...
    def __iter__(self) -> Iterator[ContactPoint]: ...
    def append(self, value: ContactPoint) -> None: ...
    def clear(self) -> None: ...
    def resize(self, size: int) -> None: ...

type ArrayLikeContactPoint = (
    Sequence[ContactPoint] | DynamicArrayContactPoint | npt.NDArray[Any]
)

class DynamicArrayInt:
    @overload
    def __init__(self) -> None: ...
    @overload
    def __init__(self, size: int, value: int = ...) -> None: ...
    @overload
    def __init__(self, sequence: Sequence[int]) -> None: ...
    def __len__(self) -> int: ...
    def __getitem__(self, index: int) -> int: ...
    def __setitem__(self, index: int, value: int) -> None: ...
    def __iter__(self) -> Iterator[int]: ...
    def append(self, value: int) -> None: ...
    def clear(self) -> None: ...
    def resize(self, size: int) -> None: ...

type ArrayLikeInt = Sequence[int] | DynamicArrayInt | npt.NDArray[np.signedinteger]

class DynamicArrayNodeContactForce:
    @overload
    def __init__(self) -> None: ...
    @overload
    def __init__(self, size: int, value: NodeContactForce = ...) -> None: ...
    @overload
    def __init__(self, sequence: Sequence[NodeContactForce]) -> None: ...
    def __len__(self) -> int: ...
    def __getitem__(self, index: int) -> NodeContactForce: ...
    def __setitem__(self, index: int, value: NodeContactForce) -> None: ...
    def __iter__(self) -> Iterator[NodeContactForce]: ...
    def append(self, value: NodeContactForce) -> None: ...
    def clear(self) -> None: ...
    def resize(self, size: int) -> None: ...

type ArrayLikeNodeContactForce = (
    Sequence[NodeContactForce] | DynamicArrayNodeContactForce | npt.NDArray[Any]
)

class DynamicArrayPoseConstraintInfo:
    @overload
    def __init__(self) -> None: ...
    @overload
    def __init__(self, size: int, value: PoseConstraintInfo = ...) -> None: ...
    @overload
    def __init__(self, sequence: Sequence[PoseConstraintInfo]) -> None: ...
    def __len__(self) -> int: ...
    def __getitem__(self, index: int) -> PoseConstraintInfo: ...
    def __setitem__(self, index: int, value: PoseConstraintInfo) -> None: ...
    def __iter__(self) -> Iterator[PoseConstraintInfo]: ...
    def append(self, value: PoseConstraintInfo) -> None: ...
    def clear(self) -> None: ...
    def resize(self, size: int) -> None: ...

type ArrayLikePoseConstraintInfo = (
    Sequence[PoseConstraintInfo] | DynamicArrayPoseConstraintInfo | npt.NDArray[Any]
)

class DynamicArrayPoseTrackingParams:
    @overload
    def __init__(self) -> None: ...
    @overload
    def __init__(self, size: int, value: PoseTrackingParams = ...) -> None: ...
    @overload
    def __init__(self, sequence: Sequence[PoseTrackingParams]) -> None: ...
    def __len__(self) -> int: ...
    def __getitem__(self, index: int) -> PoseTrackingParams: ...
    def __setitem__(self, index: int, value: PoseTrackingParams) -> None: ...
    def __iter__(self) -> Iterator[PoseTrackingParams]: ...
    def append(self, value: PoseTrackingParams) -> None: ...
    def clear(self) -> None: ...
    def resize(self, size: int) -> None: ...

type ArrayLikePoseTrackingParams = (
    Sequence[PoseTrackingParams] | DynamicArrayPoseTrackingParams | npt.NDArray[Any]
)

class DynamicArrayReal:
    @overload
    def __init__(self) -> None: ...
    @overload
    def __init__(self, size: int, value: float = ...) -> None: ...
    @overload
    def __init__(self, sequence: Sequence[float]) -> None: ...
    def __len__(self) -> int: ...
    def __getitem__(self, index: int) -> float: ...
    def __setitem__(self, index: int, value: float) -> None: ...
    def __iter__(self) -> Iterator[float]: ...
    def append(self, value: float) -> None: ...
    def clear(self) -> None: ...
    def resize(self, size: int) -> None: ...

type ArrayLikeReal = Sequence[float] | DynamicArrayReal | npt.NDArray[np.floating]

class DynamicArrayReal2:
    @overload
    def __init__(self) -> None: ...
    @overload
    def __init__(self, size: int, value: Real2 = ...) -> None: ...
    @overload
    def __init__(self, sequence: Sequence[Real2]) -> None: ...
    def __len__(self) -> int: ...
    def __getitem__(self, index: int) -> Real2: ...
    def __setitem__(self, index: int, value: Real2Like) -> None: ...
    def __iter__(self) -> Iterator[Real2]: ...
    def append(self, value: Real2Like) -> None: ...
    def clear(self) -> None: ...
    def resize(self, size: int) -> None: ...

type ArrayLikeReal2 = Sequence[Real2Like] | DynamicArrayReal2 | npt.NDArray[np.floating]

class DynamicArrayReal3:
    @overload
    def __init__(self) -> None: ...
    @overload
    def __init__(self, size: int, value: Real3 = ...) -> None: ...
    @overload
    def __init__(self, sequence: Sequence[Real3]) -> None: ...
    def __len__(self) -> int: ...
    def __getitem__(self, index: int) -> Real3: ...
    def __setitem__(self, index: int, value: Real3Like) -> None: ...
    def __iter__(self) -> Iterator[Real3]: ...
    def append(self, value: Real3Like) -> None: ...
    def clear(self) -> None: ...
    def resize(self, size: int) -> None: ...

type ArrayLikeReal3 = Sequence[Real3Like] | DynamicArrayReal3 | npt.NDArray[np.floating]

class DynamicArrayRoutingElement:
    @overload
    def __init__(self) -> None: ...
    @overload
    def __init__(self, size: int, value: RoutingElement = ...) -> None: ...
    @overload
    def __init__(self, sequence: Sequence[RoutingElement]) -> None: ...
    def __len__(self) -> int: ...
    def __getitem__(self, index: int) -> RoutingElement: ...
    def __setitem__(self, index: int, value: RoutingElement) -> None: ...
    def __iter__(self) -> Iterator[RoutingElement]: ...
    def append(self, value: RoutingElement) -> None: ...
    def clear(self) -> None: ...
    def resize(self, size: int) -> None: ...

type ArrayLikeRoutingElement = (
    Sequence[RoutingElement] | DynamicArrayRoutingElement | npt.NDArray[Any]
)

class DynamicArraySoftActorParams:
    @overload
    def __init__(self) -> None: ...
    @overload
    def __init__(self, size: int, value: SoftActorParams = ...) -> None: ...
    @overload
    def __init__(self, sequence: Sequence[SoftActorParams]) -> None: ...
    def __len__(self) -> int: ...
    def __getitem__(self, index: int) -> SoftActorParams: ...
    def __setitem__(self, index: int, value: SoftActorParams) -> None: ...
    def __iter__(self) -> Iterator[SoftActorParams]: ...
    def append(self, value: SoftActorParams) -> None: ...
    def clear(self) -> None: ...
    def resize(self, size: int) -> None: ...

type ArrayLikeSoftActorParams = (
    Sequence[SoftActorParams] | DynamicArraySoftActorParams | npt.NDArray[Any]
)

class DynamicArrayString:
    @overload
    def __init__(self) -> None: ...
    @overload
    def __init__(self, size: int, value: str = ...) -> None: ...
    @overload
    def __init__(self, sequence: Sequence[str]) -> None: ...
    def __len__(self) -> int: ...
    def __getitem__(self, index: int) -> str: ...
    def __setitem__(self, index: int, value: str) -> None: ...
    def __iter__(self) -> Iterator[str]: ...
    def append(self, value: str) -> None: ...
    def clear(self) -> None: ...
    def resize(self, size: int) -> None: ...

type ArrayLikeString = Sequence[str] | DynamicArrayString | npt.NDArray[np.str_]

class DynamicArrayTransformRT:
    @overload
    def __init__(self) -> None: ...
    @overload
    def __init__(self, size: int, value: TransformRT = ...) -> None: ...
    @overload
    def __init__(self, sequence: Sequence[TransformRT]) -> None: ...
    def __len__(self) -> int: ...
    def __getitem__(self, index: int) -> TransformRT: ...
    def __setitem__(self, index: int, value: TransformRT) -> None: ...
    def __iter__(self) -> Iterator[TransformRT]: ...
    def append(self, value: TransformRT) -> None: ...
    def clear(self) -> None: ...
    def resize(self, size: int) -> None: ...

type ArrayLikeTransformRT = (
    Sequence[TransformRT] | DynamicArrayTransformRT | npt.NDArray[Any]
)

class DynamicArrayUint8:
    @overload
    def __init__(self) -> None: ...
    @overload
    def __init__(self, size: int, value: int = ...) -> None: ...
    @overload
    def __init__(self, sequence: Sequence[int]) -> None: ...
    def __len__(self) -> int: ...
    def __getitem__(self, index: int) -> int: ...
    def __setitem__(self, index: int, value: int) -> None: ...
    def __iter__(self) -> Iterator[int]: ...
    def append(self, value: int) -> None: ...
    def clear(self) -> None: ...
    def resize(self, size: int) -> None: ...

type ArrayLikeUint8 = (
    Sequence[int] | DynamicArrayUint8 | npt.NDArray[np.unsignedinteger]
)

class SpanActor:
    def __len__(self) -> int: ...
    def __getitem__(self, index: int) -> Actor: ...
    def __setitem__(self, index: int, value: Actor) -> None: ...
    def __iter__(self) -> Iterator[Actor]: ...

class SpanConstActor:
    def __len__(self) -> int: ...
    def __getitem__(self, index: int) -> Actor: ...
    def __iter__(self) -> Iterator[Actor]: ...

class SpanConstActorConst:
    def __len__(self) -> int: ...
    def __getitem__(self, index: int) -> Actor: ...
    def __iter__(self) -> Iterator[Actor]: ...

class SpanConstActorHandle:
    def __len__(self) -> int: ...
    def __getitem__(self, index: int) -> ActorHandle: ...
    def __iter__(self) -> Iterator[ActorHandle]: ...

class SpanConstArticulatedCycleJoint:
    def __len__(self) -> int: ...
    def __getitem__(self, index: int) -> ArticulatedCycleJoint: ...
    def __iter__(self) -> Iterator[ArticulatedCycleJoint]: ...

class SpanConstArticulatedDofInfo:
    def __len__(self) -> int: ...
    def __getitem__(self, index: int) -> ArticulatedDofInfo: ...
    def __iter__(self) -> Iterator[ArticulatedDofInfo]: ...

class SpanConstArticulatedJointFrictionParams:
    def __len__(self) -> int: ...
    def __getitem__(self, index: int) -> ArticulatedJointFrictionParams: ...
    def __iter__(self) -> Iterator[ArticulatedJointFrictionParams]: ...

class SpanConstArticulatedJointType:
    def __len__(self) -> int: ...
    def __getitem__(self, index: int) -> ArticulatedJointType: ...
    def __iter__(self) -> Iterator[ArticulatedJointType]: ...

class SpanConstChar:
    def __len__(self) -> int: ...
    def __getitem__(self, index: int) -> str: ...
    def __iter__(self) -> Iterator[str]: ...

class SpanConstColor:
    def __len__(self) -> int: ...
    def __getitem__(self, index: int) -> Color: ...
    def __iter__(self) -> Iterator[Color]: ...

class SpanConstContactPoint:
    def __len__(self) -> int: ...
    def __getitem__(self, index: int) -> ContactPoint: ...
    def __iter__(self) -> Iterator[ContactPoint]: ...

class SpanConstInt:
    def __len__(self) -> int: ...
    def __getitem__(self, index: int) -> int: ...
    def __iter__(self) -> Iterator[int]: ...

class SpanConstNodeContactForce:
    def __len__(self) -> int: ...
    def __getitem__(self, index: int) -> NodeContactForce: ...
    def __iter__(self) -> Iterator[NodeContactForce]: ...

class SpanConstPoseConstraintInfo:
    def __len__(self) -> int: ...
    def __getitem__(self, index: int) -> PoseConstraintInfo: ...
    def __iter__(self) -> Iterator[PoseConstraintInfo]: ...

class SpanConstReal:
    def __len__(self) -> int: ...
    def __getitem__(self, index: int) -> float: ...
    def __iter__(self) -> Iterator[float]: ...

class SpanConstReal3:
    def __len__(self) -> int: ...
    def __getitem__(self, index: int) -> Real3: ...
    def __iter__(self) -> Iterator[Real3]: ...

class SpanConstString:
    def __len__(self) -> int: ...
    def __getitem__(self, index: int) -> str: ...
    def __iter__(self) -> Iterator[str]: ...

class SpanConstTransformRT:
    def __len__(self) -> int: ...
    def __getitem__(self, index: int) -> TransformRT: ...
    def __iter__(self) -> Iterator[TransformRT]: ...

class SpanConstUint8:
    def __len__(self) -> int: ...
    def __getitem__(self, index: int) -> int: ...
    def __iter__(self) -> Iterator[int]: ...

class SpanConstraintConst:
    def __len__(self) -> int: ...
    def __getitem__(self, index: int) -> Constraint: ...
    def __iter__(self) -> Iterator[Constraint]: ...

class SpanReal:
    def __len__(self) -> int: ...
    def __getitem__(self, index: int) -> float: ...
    def __setitem__(self, index: int, value: float) -> None: ...
    def __iter__(self) -> Iterator[float]: ...

class SpanReal2:
    def __len__(self) -> int: ...
    def __getitem__(self, index: int) -> Real2: ...
    def __setitem__(self, index: int, value: Real2Like) -> None: ...
    def __iter__(self) -> Iterator[Real2]: ...

class SpanTransformRT:
    def __len__(self) -> int: ...
    def __getitem__(self, index: int) -> TransformRT: ...
    def __setitem__(self, index: int, value: TransformRT) -> None: ...
    def __iter__(self) -> Iterator[TransformRT]: ...

class ArticulatedJointType:
    """Types of joints in an articulated body."""
    FREE: ArticulatedJointType
    """6-DoF joint with unconstrained relative motion between links."""
    PRISMATIC: ArticulatedJointType
    """1-DoF translational joint along a single axis."""
    REVOLUTE: ArticulatedJointType
    """1-DoF rotational joint around a single axis."""
    SPHERICAL: ArticulatedJointType
    """3-DoF rotational ball-and-socket joint."""
    HARD: ArticulatedJointType
    """0-DoF rigid weld joint with no relative motion between links."""
    CYCLE: ArticulatedJointType
    """Cycle-closing joint in a closed-loop articulated topology."""
    COUNT: ArticulatedJointType
    INVALID: ArticulatedJointType
    def __init__(self, value: int) -> None: ...
    @property
    def name(self) -> str: ...
    @property
    def value(self) -> int: ...
    def __int__(self) -> int: ...
    def __index__(self) -> int: ...

class RoutingElementType:
    """Selects how a :class:`~superdex.physics.RoutingElement` is interpreted by a
    spatial tendon.
    """
    WAYPOINT: RoutingElementType
    """A point fixed in a link's local frame. Adjacent waypoints are joined by a
    straight segment whose length contributes to the tendon length.
    """
    LINEAR_JOINT: RoutingElementType
    """A constant moment arm contributing `coefficient * jointCoordinate`. Carries no
    geometry and breaks the polyline between waypoints.
    """
    def __init__(self, value: int) -> None: ...
    @property
    def name(self) -> str: ...
    @property
    def value(self) -> int: ...
    def __int__(self) -> int: ...
    def __index__(self) -> int: ...

class VerbosityLevel:
    """Verbosity levels for solvers and optimizers."""
    SILENT: VerbosityLevel
    """No output."""
    ERROR: VerbosityLevel
    """Only errors."""
    WARNING: VerbosityLevel
    """Errors and warnings."""
    VERBOSE: VerbosityLevel
    """Detailed output including iteration info."""
    COUNT: VerbosityLevel
    """Number of verbosity level enum values."""
    def __init__(self, value: int) -> None: ...
    @property
    def name(self) -> str: ...
    @property
    def value(self) -> int: ...
    def __int__(self) -> int: ...
    def __index__(self) -> int: ...

class ConvergenceStatus:
    """Convergence status of the non-linear solver.

    Note:
        Values are ordered by severity: ``None < Converged < Stopped < Diverged``.
    """
    NONE: ConvergenceStatus
    """Convergence status has not been set."""
    CONVERGED: ConvergenceStatus
    """Solver converged to the requested tolerance."""
    STOPPED: ConvergenceStatus
    """Solver met at least one stopping criterion without converging to the requested
    tolerance.
    """
    DIVERGED: ConvergenceStatus
    """Solver diverged. Some form of solution reset may have been used."""
    COUNT: ConvergenceStatus
    """Number of convergence status enum values."""
    def __init__(self, value: int) -> None: ...
    @property
    def name(self) -> str: ...
    @property
    def value(self) -> int: ...
    def __int__(self) -> int: ...
    def __index__(self) -> int: ...

class SoftMaterialType:
    """Material constitutive models for soft bodies.

    Defines the constitutive response for deformable materials (strain energy and
    stress-strain relationship). Each material has different mechanical properties,
    computational cost, and stability characteristics.

    See Also:
        :class:`~superdex.physics.SoftMaterialParams`,
        :class:`~superdex.physics.MaterialPsdStrategy`
    """
    NEO_HOOKEAN: SoftMaterialType
    """Neo-Hookean hyperelastic material.

    Non-linear hyperelastic material with log-barrier stabilization to handle
    extreme deformations and element inversions robustly. Recommended for most soft
    body simulations requiring large deformations.

    Reference: [Stable Neo-Hookean Flesh Simulation (Smith et al.,
    2018)](https://www.tkim.graphics/NEO/StableNeoHookean2018.pdf)

    See Also:
        :class:`~superdex.physics.NeoHookeanMaterialParams`
    """
    ST_VENANT_KIRCHHOFF: SoftMaterialType
    """Saint Venant-Kirchhoff (StVK) hyperelastic material.

    Classical non-linear hyperelastic material. Simpler than neo-Hookean but may
    exhibit instabilities under large deformations, particularly under large
    compression.

    See Also:
        :class:`~superdex.physics.StVenantKirchhoffMaterialParams`
    """
    LINEAR_ELASTIC: SoftMaterialType
    """Linear elastic material (Hookean).

    Simplest material model with linear stress-strain relationship. Computationally
    fast but only accurate for small deformations.

    Note:
        Not suitable for large deformations.

    Note:
        No PSD enforcement is needed. The Hessian matrix is positive definite
        regardless of the PSD strategy.

    See Also:
        :class:`~superdex.physics.LinearElasticMaterialParams`
    """
    ACTIVE_NEO_HOOKEAN: SoftMaterialType
    """Composite material: passive isotropic elasticity + active anisotropic fiber
    actuation.

    Combines a passive isotropic hyperelastic component (Neo-Hookean) with an active
    anisotropic fiber contraction component (Aniso ARAP). Used to model materials
    with directional actuation like muscle tissue.

    Reference: [Stable Neo-Hookean Flesh Simulation (Smith et al.,
    2018)](https://www.tkim.graphics/NEO/StableNeoHookean2018.pdf) Reference:
    [Anisotropic Elasticity for Inversion-Safety and Element Rehabilitation (Kim et
    al., 2019)](http://tkim.graphics/ANISOTROPY/AnisotropyAndRehab.pdf)

    See Also:
        :class:`~superdex.physics.ActiveNeoHookeanMaterialParams`
    """
    ACTIVE_SHAPE_TARGETING_ARAP: SoftMaterialType
    """Active As-Rigid-As-Possible (ARAP) material with Shape Targeting.

    Active material model based on the concept of "shape targeting". Unlike passive
    models that only react to external loads, it introduces an internal actuation
    mechanism.

    The actuation is defined by the shape target tensor, S_t, which specifies the
    desired local shape of the object. The model then resists deformation from this
    target shape while allowing rigid rotation.

    This approach is effective for simulating biological phenomena like muscle
    contraction.

    Note:
        The "activeness" is controlled externally by setting the shape target tensor
        S_t. The model itself does not have an intrinsic, self-actuating mechanism
        without this input.

        Reference: [Shape Targeting: A Versatile Active Elasticity Constitutive
        Model (Klár et al., 2020)](https://par.nsf.gov/servlets/purl/10230451)

    See Also:
        :class:`~superdex.physics.ActiveShapeTargetingArapMaterialParams`
    """
    ARAP: SoftMaterialType
    """As-Rigid-As-Possible (ARAP) material.

    ARAP's primary goal is to deform the object such that each local region
    undergoes a transformation that is as close to a rigid rotation as possible.

    Because it preserves local shape details well and produces intuitive,
    natural-looking results, ARAP is widely used in computer graphics for
    applications like interactive mesh editing and character posing.

    Reference: [Dynamic Deformables (Kim and Eberle,
    2022)](https://www.tkim.graphics/DYNAMIC_DEFORMABLES/DynamicDeformables.pdf)

    See Also:
        :class:`~superdex.physics.ArapMaterialParams`
    """
    COUNT: SoftMaterialType
    """Number of unique material type enum values."""
    def __init__(self, value: int) -> None: ...
    @property
    def name(self) -> str: ...
    @property
    def value(self) -> int: ...
    def __int__(self) -> int: ...
    def __index__(self) -> int: ...

class MaterialPsdStrategy:
    """Strategy for ensuring positive semi-definite (PSD) Hessian matrices in material
    models.

    The material Hessian (∂P/∂F, where P is the first Piola-Kirchhoff stress and F
    is the deformation gradient) should be PSD for stable simulation. This enum
    defines strategies to enforce this property, balancing computational cost
    against stability and convergence quality.

    Note:
        Different materials support different strategies. Check material-specific
        documentation.

    See Also:
        :class:`~superdex.physics.SoftMaterialType`
    """
    MATERIAL_DEFAULT: MaterialPsdStrategy
    """Use the material's default PSD strategy.

    Note:
        Supported for all material models.

    Note:
        Each material model defines its own recommended default strategy.
    """
    NONE: MaterialPsdStrategy
    """No PSD enforcement. Negative eigenvalues may arise.

    Fastest strategy but may substantially degrade stability of most material
    models.

    Note:
        Supported for all material models.
    """
    PROJECTION: MaterialPsdStrategy
    """Eigenvalue clamping: U * Max(Λ, ε) * U^T.

    Performs eigendecomposition ∂P/∂F = U * Λ * U^T and clamps negative eigenvalues
    to a small positive constant ε. Slower than most other methods, but typically
    ensures best convergence.

    Note:
        Supported for all material models.
    """
    FAST: MaterialPsdStrategy
    """Fast PSD enforcement by dropping problematic terms.

    Faster than :class:`PROJECTION <superdex.physics.MaterialPsdStrategy>` but may
    degrade non-linear solver convergence.

    Note:
        Only supported for :class:`ST_VENANT_KIRCHHOFF
        <superdex.physics.SoftMaterialType>` and :class:`NEO_HOOKEAN
        <superdex.physics.SoftMaterialType>` materials.

    Warning:
        For :class:`ST_VENANT_KIRCHHOFF <superdex.physics.SoftMaterialType>`, does
        not guarantee a PSD Hessian when the Poisson ratio is negative (auxetic
        materials). Use :class:`PROJECTION <superdex.physics.MaterialPsdStrategy>`
        or :class:`ABS_EIGEN_PROJECTION <superdex.physics.MaterialPsdStrategy>`
        instead.
    """
    ABS_EIGEN_PROJECTION: MaterialPsdStrategy
    """Absolute eigenvalue projection: U * Max(``|Λ|``, ε) * U^T.

    Takes absolute value of eigenvalues instead of clamping. Similar cost to
    :class:`PROJECTION <superdex.physics.MaterialPsdStrategy>` but may improve
    stability, especially for highly deformed configurations.

    Note:
        Supported for all material models.

        Reference: [Stabler Neo-Hookean Simulation: Absolute Eigenvalue Filtering
        for Projected Newton (Chen et al.,
        2024)](https://www.cs.columbia.edu/cg/abs-psd/paper.pdf)
    """
    PER_TERM_PROJECTION: MaterialPsdStrategy
    """Per-term projection for composite Hessians.

    Projects each summand of the Hessian separately. For example, if H = H1 + H2,
    projects both H1 and H2 independently.

    Note:
        Only supported for :class:`ACTIVE_SHAPE_TARGETING_ARAP
        <superdex.physics.SoftMaterialType>` material.
    """
    COUNT: MaterialPsdStrategy
    """Number of unique enum values."""
    def __init__(self, value: int) -> None: ...
    @property
    def name(self) -> str: ...
    @property
    def value(self) -> int: ...
    def __int__(self) -> int: ...
    def __index__(self) -> int: ...

class GridSdfResolutionMode:
    """The maximum voxel size for an SDF will be determined by one of these
    measurements scaled by :attr:`~superdex.physics.GridSdfParams.resolution_delta`.
    """
    LARGEST_AXIS: GridSdfResolutionMode
    """Use the largest dimension of the mesh's axis-aligned bounding box (AABB) as
    reference.
    """
    SMALLEST_AXIS: GridSdfResolutionMode
    """Use the smallest dimension of the mesh's AABB as reference."""
    MEAN_AXIS: GridSdfResolutionMode
    """Use the average of all three AABB dimensions as reference."""
    LARGEST_EDGE: GridSdfResolutionMode
    """Use the largest edge length in the mesh as reference."""
    SMALLEST_EDGE: GridSdfResolutionMode
    """Use the smallest edge length in the mesh as reference."""
    MEAN_EDGE: GridSdfResolutionMode
    """Use the average edge length in the mesh as reference."""
    EXPLICIT: GridSdfResolutionMode
    """Use :attr:`~superdex.physics.GridSdfParams.resolution_delta` to set the maximum
    voxel size in meters (no scaling based on the features of the mesh nor AABB).
    """
    COUNT: GridSdfResolutionMode
    """Total number of resolution modes."""
    def __init__(self, value: int) -> None: ...
    @property
    def name(self) -> str: ...
    @property
    def value(self) -> int: ...
    def __int__(self) -> int: ...
    def __index__(self) -> int: ...

class FileFormat:
    """File format options used when saving model data."""
    JSON: FileFormat
    """JSON format (text)."""
    H5: FileFormat
    """HDF5 format (binary). Requires MOCHI_USE_HDF5."""
    COUNT: FileFormat
    def __init__(self, value: int) -> None: ...
    @property
    def name(self) -> str: ...
    @property
    def value(self) -> int: ...
    def __int__(self) -> int: ...
    def __index__(self) -> int: ...

class MeshFileType:
    """Mesh file format hint for shape and model loading."""
    LEGACY: MeshFileType
    """Auto-detect between HDF5 and JSON (default behavior)."""
    PLY: MeshFileType
    """PLY format (Stanford Polygon)."""
    OFF: MeshFileType
    """OFF format (Object File Format)."""
    STL: MeshFileType
    """STL format (Stereolithography)."""
    OBJ: MeshFileType
    """OBJ format (Wavefront)."""
    COUNT: MeshFileType
    def __init__(self, value: int) -> None: ...
    @property
    def name(self) -> str: ...
    @property
    def value(self) -> int: ...
    def __int__(self) -> int: ...
    def __index__(self) -> int: ...

class ColliderType:
    """Collision detection geometry for an :class:`~superdex.physics.Actor`.

    Note:
        This setting controls how OTHER actors detect contact with this actor. It
        does not affect how this actor detects contact with other actors.
    """
    NONE: ColliderType
    """No collision representation. Other actors cannot detect contact with this actor.

    Note:
        It does NOT prevent this actor from detecting contact with other actors.
    """
    AUTO: ColliderType
    """Automatic collider type selection based on the actor's shape.

    Note:
        Mochi will select the most appropriate collider type based on the actor's
        shape and type: - Implicit sphere shapes: :class:`SPHERE
        <superdex.physics.ColliderType>` - Implicit plane shapes: :class:`PLANE
        <superdex.physics.ColliderType>` - Mesh shapes on volumetric actors (e.g.,
        rigid, soft): :class:`SDF <superdex.physics.ColliderType>` - Shell and rod
        actors: :class:`POINT_CLOUD <superdex.physics.ColliderType>`

    Note:
        If Auto is set on an actor, a valid collider type will be assigned even if
        the shape's default collider type is :class:`NONE
        <superdex.physics.ColliderType>`. If you want :class:`NONE
        <superdex.physics.ColliderType>`, set :class:`NONE
        <superdex.physics.ColliderType>` explicitly as your collider type.

    Note:
        After initialization, :meth:`~superdex.physics.Actor.get_collider_type` will
        return the selected type, not :class:`AUTO <superdex.physics.ColliderType>`.
    """
    SPHERE: ColliderType
    """Represent the actor by an approximating sphere derived from its bounding volume.

    Note:
        For shapes with a sphere bounding volume, that sphere is used. For shapes
        with an OBB/AABB bounding volume, the inscribed sphere is used. Otherwise, a
        true bounding sphere is computed.

    Note:
        Fast but only accurate for spherical geometries.

    Note:
        Only supported for rigid actors and articulated links.
    """
    BOX: ColliderType
    """Represent the actor by its axis-aligned bounding box (AABB), in the actor's
    local frame.

    Note:
        Fast but only accurate for box geometries.

    Note:
        Only supported for rigid actors and articulated links.
    """
    MESH: ColliderType
    """[Experimental] Represent the actor by its surface mesh.

    Note:
        Performance is slow.

    Note:
        Only supported for rigid actors and articulated links.

    Warning:
        This is an experimental feature. It may be changed or removed in the future.
        Use at your own risk.
    """
    PLANE: ColliderType
    """Represent the actor as an infinite plane.

    Note:
        Only supported for static rigid actors whose shape is an implicit plane.
    """
    SDF: ColliderType
    """Grid-based Signed Distance Field (SDF) collision representation.

    Note:
        Uses a 3D grid storing the distance to the closest point in the actor's
        surface.

    Note:
        Accurate for closed non-intersecting geometries provided the grid resolution
        is sufficient.

    Note:
        If the grid is not baked in the actor's Shape, it is computed at
        initialization (expensive).

    Note:
        Also supported for soft actors via SDF mapping.

    See Also:
        :class:`~superdex.physics.GridSdfParams`
    """
    POINT_CLOUD: ColliderType
    """Point-cloud collision representation.

    Contacts are detected by proximity queries between collider-side sample points
    and colliding-side sample points. Only other point-cloud colliders can collide
    with this type.
    """
    COUNT: ColliderType
    """Number of collider type enum values."""
    def __init__(self, value: int) -> None: ...
    @property
    def name(self) -> str: ...
    @property
    def value(self) -> int: ...
    def __int__(self) -> int: ...
    def __index__(self) -> int: ...

class LogChannel:
    """Log message channel."""
    VERBOSE: LogChannel
    INFO: LogChannel
    WARNING: LogChannel
    ERROR: LogChannel
    COUNT: LogChannel
    def __init__(self, value: int) -> None: ...
    @property
    def name(self) -> str: ...
    @property
    def value(self) -> int: ...
    def __int__(self) -> int: ...
    def __index__(self) -> int: ...

class CoulombFrictionModel:
    """Coulomb friction regularization model."""
    C1_REGULARIZED: CoulombFrictionModel
    """C1-smoothed Coulomb friction with compact support: friction transitions linearly
    from 0 to full strength over [0,
    :attr:`~superdex.physics.ContactParams.friction_falloff_vel`].
    """
    CINF_REGULARIZED: CoulombFrictionModel
    """C-infinity regularized Coulomb friction with no compact support: friction
    asymptotically approaches full strength.
    :attr:`~superdex.physics.ContactParams.friction_falloff_vel` controls the
    regularization scale.
    """
    COUNT: CoulombFrictionModel
    """Number of friction model enum values."""
    DEFAULT: CoulombFrictionModel
    """Default Coulomb friction model."""
    def __init__(self, value: int) -> None: ...
    @property
    def name(self) -> str: ...
    @property
    def value(self) -> int: ...
    def __int__(self) -> int: ...
    def __index__(self) -> int: ...

class NonLinearSolverType:
    """Non-linear solver algorithm."""
    NEWTON: NonLinearSolverType
    """Newton's method."""
    BFGS: NonLinearSolverType
    """Broyden-Fletcher-Goldfarb-Shanno (BFGS) method.

    Note:
        The initial approximation of the dresidual is the actual dresidual (i.e.,
        the first iteration matches Newton's method).

    Note:
        The algorithm is restarted with the actual dresidual every
        :attr:`~superdex.physics.NonLinearSolverParams.d_residual_assembly_period`
        iterations, or immediately if the line search fails to improve the figure of
        merit it monitors or the linear solver fails to converge.

    See Also:
        :attr:`~superdex.physics.NonLinearSolverParams.d_residual_assembly_period`
    """
    SR1: NonLinearSolverType
    """Symmetric Rank-One (SR1) method.

    Note:
        The initial approximation of the dresidual is the actual dresidual (i.e.,
        the first iteration matches Newton's method).

    Note:
        The algorithm is restarted with the actual dresidual every
        :attr:`~superdex.physics.NonLinearSolverParams.d_residual_assembly_period`
        iterations, or immediately if the line search fails to improve the figure of
        merit it monitors or the linear solver fails to converge.

    See Also:
        :attr:`~superdex.physics.NonLinearSolverParams.d_residual_assembly_period`
    """
    COUNT: NonLinearSolverType
    """Number of non-linear solver type enum values."""
    DEFAULT: NonLinearSolverType
    """Default non-linear solver type."""
    def __init__(self, value: int) -> None: ...
    @property
    def name(self) -> str: ...
    @property
    def value(self) -> int: ...
    def __int__(self) -> int: ...
    def __index__(self) -> int: ...

class NonLinearSolverConvergenceMode:
    """Convergence monitoring mode for the non-linear solver residual.

    Note:
        Convergence is evaluated per simulation island, not scene-wide. Each island
        is solved independently, and the selected mode determines how convergence is
        assessed within that island.

    Note:
        The norm used to monitor divergence via
        :attr:`~superdex.physics.NonLinearSolverParams.abs_div_tol` and
        :attr:`~superdex.physics.NonLinearSolverParams.rel_div_tol` is always the L2
        norm, irrespective of this setting.

    Note:
        The norm used to monitor stagnation via
        :attr:`~superdex.physics.NonLinearSolverParams.rel_step_tol` is always the
        L2 norm, irrespective of this setting.

    See Also:
        :attr:`~superdex.physics.NonLinearSolverParams.abs_tol`,
        :attr:`~superdex.physics.NonLinearSolverParams.rel_tol`
    """
    GLOBAL: NonLinearSolverConvergenceMode
    """Use global, unweighted residual norm for convergence checks within each
    simulation island.

    Convergence is determined by the global residual norm within each simulation
    island: ``|r|`` <= absTol or ``|r|`` <= relTol * ``|r0|``
    """
    PER_ACTOR_WEIGHTED: NonLinearSolverConvergenceMode
    """Use per-actor weighted residual norms for convergence checks.

    Each actor uses a per-actor weighted L2 norm ``|r_a|_W`` = sqrt(Σᵢ wᵢ·rᵢ²),
    where weights normalize force/torque residuals by characteristic force/torque.
    All actors must satisfy their individual criteria: ``|r_a|_W`` <= absTol or
    ``|r_a|_W`` <= relTol * ``|r0_a|_W``

    Note:
        Weights are derived from inertia properties. Actors with zero inertia
        receive uniform weights, which provide no physical normalization. For
        quasi-static problems, :class:`GLOBAL
        <superdex.physics.NonLinearSolverConvergenceMode>` mode is recommended.
    """
    COUNT: NonLinearSolverConvergenceMode
    """Number of convergence monitoring mode enum values."""
    DEFAULT: NonLinearSolverConvergenceMode
    """Default convergence monitoring mode."""
    def __init__(self, value: int) -> None: ...
    @property
    def name(self) -> str: ...
    @property
    def value(self) -> int: ...
    def __int__(self) -> int: ...
    def __index__(self) -> int: ...

class PsdProjectionMode:
    """Positive Semi-Definite (PSD) projection modes for the dresidual matrix."""
    NEVER: PsdProjectionMode
    """Never project to PSD."""
    ALWAYS: PsdProjectionMode
    """Always project to PSD."""
    IF_FAIL_RETRY: PsdProjectionMode
    """If one non-linear iteration fails, retry the iteration projecting to PSD."""
    IF_FAIL_ALWAYS: PsdProjectionMode
    """If one non-linear iteration fails, retry the iteration projecting to PSD and
    continue projecting in all the remaining iterations of the solve.
    """
    COUNT: PsdProjectionMode
    """Number of PSD projection mode enum values."""
    DEFAULT: PsdProjectionMode
    """Default PSD projection mode."""
    def __init__(self, value: int) -> None: ...
    @property
    def name(self) -> str: ...
    @property
    def value(self) -> int: ...
    def __int__(self) -> int: ...
    def __index__(self) -> int: ...

class LineSearchType:
    """Line-search strategy used by the non-linear solver."""
    NONE: LineSearchType
    """No line search. May substantially degrade stability."""
    SIMPLE: LineSearchType
    """Simple line search. Accepts a step if the objective function does not increase
    by more than a specified relative tolerance.

    See Also:
        :attr:`~superdex.physics.NonLinearSolverParams.line_search_max_rel_increase`
    """
    ARMIJO: LineSearchType
    """Line search with Armijo condition.

    See Also:
        :attr:`~superdex.physics.NonLinearSolverParams.line_search_wolfe1`
    """
    WOLFE_WEAK: LineSearchType
    """Line search with weak Wolfe conditions.

    See Also:
        :attr:`~superdex.physics.NonLinearSolverParams.line_search_wolfe1`,
        :attr:`~superdex.physics.NonLinearSolverParams.line_search_wolfe2`
    """
    WOLFE_STRONG: LineSearchType
    """Line search with strong Wolfe conditions.

    See Also:
        :attr:`~superdex.physics.NonLinearSolverParams.line_search_wolfe1`,
        :attr:`~superdex.physics.NonLinearSolverParams.line_search_wolfe2`
    """
    RESIDUAL_NORM: LineSearchType
    """Line search with residual norm condition. Accepts a step if the residual norm
    decreases.

    Note:
        More robust than objective-based line searches (:class:`SIMPLE
        <superdex.physics.LineSearchType>`, :class:`ARMIJO
        <superdex.physics.LineSearchType>`, :class:`WOLFE_WEAK
        <superdex.physics.LineSearchType>`, :class:`WOLFE_STRONG
        <superdex.physics.LineSearchType>`) but may degrade non-linear solver
        convergence in non-convex problems. In highly non-convex regions where the
        residual norm cannot be reduced, it may cause objects to move at slower
        velocity than they should due to the inability to take valid steps. Consider
        using an objective-based line search in that case.
    """
    ARMIJO_OR_RESIDUAL_NORM: LineSearchType
    """Line search that accepts either the Armijo or the residual norm condition.

    Note:
        It combines the robustness of objective-based criteria when the iteration is
        far from the solution, with residual-based criteria when it is near the
        solution (particularly useful with single precision). It requires slightly
        higher cost per line-search iteration.
    """
    COUNT: LineSearchType
    """Number of line search type enum values."""
    DEFAULT: LineSearchType
    """Default line search type."""
    def __init__(self, value: int) -> None: ...
    @property
    def name(self) -> str: ...
    @property
    def value(self) -> int: ...
    def __int__(self) -> int: ...
    def __index__(self) -> int: ...

class LinearToleranceStrategy:
    """Strategy for adapting linear solver tolerance inside non-linear iterations."""
    CONSTANT: LinearToleranceStrategy
    """Constant relative tolerance."""
    EISENSTAT_WALKER1: LinearToleranceStrategy
    """Eisenstat-Walker strategy no. 1.

    Note:
        Should be used with :class:`RESIDUAL_L2
        <superdex.physics.LinearSolverConvergenceNorm>`. Other norms may yield
        suboptimal forcing terms.

    Note:
        Not recommended for use with quasi-Newton methods (e.g., :class:`BFGS
        <superdex.physics.NonLinearSolverType>`, :class:`SR1
        <superdex.physics.NonLinearSolverType>`).

        Reference: [Choosing the Forcing Terms in an Inexact Newton Method, Choice 1
        (Eisenstat and Walker,
        1994)](https://softlib.rice.edu/pub/CRPC-TRs/reports/CRPC-TR94463.pdf)
    """
    EISENSTAT_WALKER2: LinearToleranceStrategy
    """Eisenstat-Walker strategy no. 2.

    Note:
        Not recommended for use with quasi-Newton methods (e.g., :class:`BFGS
        <superdex.physics.NonLinearSolverType>`, :class:`SR1
        <superdex.physics.NonLinearSolverType>`).

        Reference: [Choosing the Forcing Terms in an Inexact Newton Method, Choice 2
        (Eisenstat and Walker,
        1994)](https://softlib.rice.edu/pub/CRPC-TRs/reports/CRPC-TR94463.pdf)
    """
    EISENSTAT_WALKER3: LinearToleranceStrategy
    """Custom Eisenstat-Walker strategy: eta = max(min(1/(2+k), sqrt(``|r|``)), eta_0),
    where `k` is the non-linear iteration number, ``|r|`` is the L2-norm of the
    nonlinear residual, and `eta_0` is
    :attr:`~superdex.physics.LinearSolverParams.rel_tol`.

    Note:
        Should be used with :class:`RESIDUAL_L2
        <superdex.physics.LinearSolverConvergenceNorm>`. Other norms may yield
        suboptimal forcing terms.

    Note:
        Not recommended for use with quasi-Newton methods (e.g., :class:`BFGS
        <superdex.physics.NonLinearSolverType>`, :class:`SR1
        <superdex.physics.NonLinearSolverType>`).
    """
    COUNT: LinearToleranceStrategy
    """Number of linear tolerance strategy enum values."""
    DEFAULT: LinearToleranceStrategy
    """Default linear tolerance strategy."""
    def __init__(self, value: int) -> None: ...
    @property
    def name(self) -> str: ...
    @property
    def value(self) -> int: ...
    def __int__(self) -> int: ...
    def __index__(self) -> int: ...

class LinearSolverType:
    """Linear solver algorithm."""
    CG: LinearSolverType
    """Conjugate Gradient (CG) solver.

    Note:
        Only valid for symmetric positive-definite systems.

    Note:
        Unlike :class:`PARALLEL_CG <superdex.physics.LinearSolverType>`, only
        matrix-vector products are parallelized. CG orthogonalization and
        preconditioner solves are not.
    """
    GMRES: LinearSolverType
    """Generalized Minimal Residual (GMRES) solver."""
    CUDA_CG: LinearSolverType
    """CUDA CG solver.

    Note:
        Only valid for symmetric positive-definite systems.

    Note:
        Requires building with CUDA and a preconditioner with CUDA support.

    See Also:
        :class:`~superdex.physics.PreconditionerType`
    """
    CUDA_GMRES: LinearSolverType
    """CUDA GMRES solver.

    Note:
        Requires building with CUDA and a preconditioner with CUDA support.

    See Also:
        :class:`~superdex.physics.PreconditionerType`
    """
    AUGMENTED_CG: LinearSolverType
    """[Experimental] Augmented CG solver with Krylov subspace recycling.

    Note:
        Only valid for symmetric positive-definite systems.

    Note:
        Requires building with Eigen.

    Warning:
        This is an experimental feature. It may be changed or removed in the future.
        Use at your own risk.
    """
    LDLT: LinearSolverType
    """Direct solver based on the LDLt factorization without pivoting.

    Note:
        Only valid for symmetric systems.

    Note:
        May be unstable for indefinite or ill-conditioned systems.

    Note:
        Uses a dense or a sparse factorization depending on the size and sparsity of
        the system.
    """
    LU: LinearSolverType
    """Direct solver based on the dense LU factorization without pivoting.

    Note:
        May be unstable for ill-conditioned systems.

    Note:
        The cost of the factorization is O(n^3). Will be slow for large systems.

    Note:
        Consider using :class:`LDLT <superdex.physics.LinearSolverType>` if the
        system is symmetric.
    """
    ASYNC_CG: LinearSolverType
    """[Experimental] Asynchronous CG solver.

    Note:
        Only valid for symmetric positive-definite systems.

    Note:
        Async CG is a reformulation of the classical CG algorithm to compute the
        matrix-vector products in parallel to the orthogonalization and convergence
        check.

    Note:
        Equivalent to the classical CG algorithm in exact-precision arithmetic, but
        has inferior stability properties in finite-precision arithmetic.

    Warning:
        This is an experimental feature. It may be changed or removed in the future.
        Use at your own risk.
    """
    PARALLEL_CG: LinearSolverType
    """[Experimental] Parallel CG solver.

    Note:
        Only valid for symmetric positive-definite systems.

    Note:
        Unlike :class:`CG <superdex.physics.LinearSolverType>`, all operations
        (matrix-vector products, preconditioner solves, CG orthogonalization) are
        parallelized.

    Note:
        Requires a preconditioner with a parallel solve.

    Warning:
        Not deterministic.

    Warning:
        This is an experimental feature. It may be changed or removed in the future.
        Use at your own risk.
    """
    MINRES: LinearSolverType
    """Minimal Residual (MINRES) solver.

    Note:
        Only valid for symmetric systems.
    """
    EXPERIMENTAL_CUDA_SPARSE_CHOLESKY: LinearSolverType
    """[Experimental] CUDA sparse Cholesky factorization.

    Note:
        Only valid for sparse or block-sparse symmetric positive-definite systems.

    Note:
        Requires building with CUDA.

    Warning:
        This is an experimental feature. It may be changed or removed in the future.
        Use at your own risk.
    """
    EXPERIMENTAL_CUDA_SPARSE_LDLT: LinearSolverType
    """[Experimental] CUDA sparse LDLt factorization.

    Note:
        Only valid for sparse or block-sparse symmetric systems.

    Note:
        Requires building with CUDA and cuDSS.

    Warning:
        This is an experimental feature. It may be changed or removed in the future.
        Use at your own risk.
    """
    EXPERIMENTAL_CUDA_SPARSE_LU: LinearSolverType
    """[Experimental] CUDA sparse LU factorization.

    Note:
        Only valid for sparse or block-sparse systems.

    Note:
        Requires building with CUDA.

    Warning:
        This is an experimental feature. It may be changed or removed in the future.
        Use at your own risk.
    """
    AUTO: LinearSolverType
    """Let Mochi select the solver type based on the problem."""
    COUNT: LinearSolverType
    """Number of solver type enum values."""
    DEFAULT: LinearSolverType
    """Default solver type."""
    def __init__(self, value: int) -> None: ...
    @property
    def name(self) -> str: ...
    @property
    def value(self) -> int: ...
    def __int__(self) -> int: ...
    def __index__(self) -> int: ...

class PreconditionerType:
    """Preconditioner types for the linear solver.

    Note:
        Only used for iterative solvers.

    Note:
        CUDA iterative solvers only support :class:`NONE
        <superdex.physics.PreconditionerType>`, :class:`JACOBI
        <superdex.physics.PreconditionerType>` and :class:`BLOCK_JACOBI
        <superdex.physics.PreconditionerType>` preconditioners.
    """
    NONE: PreconditionerType
    """No preconditioner."""
    JACOBI: PreconditionerType
    """Jacobi preconditioner.

    Note:
        Only valid for matrices with non-zero diagonal entries.
    """
    BLOCK_JACOBI: PreconditionerType
    """Block Jacobi preconditioner with block size of 3.

    Note:
        Only valid for matrices whose size is a multiple of 3 and whose 3x3 diagonal
        blocks are non-singular.
    """
    SSOR: PreconditionerType
    """Symmetric successive over-relaxation (SSOR) preconditioner.

    Note:
        Only valid for symmetric positive-definite matrices.
    """
    BLOCK_SSOR: PreconditionerType
    """Block symmetric successive over-relaxation (SSOR) preconditioner with block size
    of 3.

    Note:
        Only valid for symmetric positive-definite matrices whose size is a multiple
        of 3 and whose 3x3 diagonal blocks are non-singular.
    """
    LU: PreconditionerType
    """Dense LU factorization without pivoting.

    Note:
        May be unstable for ill-conditioned matrices.

    Note:
        The iterative solver should converge in 1 iteration.
    """
    AMG: PreconditionerType
    """Algebraic multigrid (AMG).

    Note:
        Only valid for 3x3 block sparse symmetric positive-definite matrices with
        non-singular 3x3 diagonal blocks.
    """
    SYM_INVERSE: PreconditionerType
    """Symmetric inverse preconditioner based on the dense LDLt factorization without
    pivoting.

    Note:
        Only valid for symmetric matrices.

    Note:
        May be unstable for indefinite or ill-conditioned matrices.

    Note:
        The iterative solver should converge in 1 iteration.
    """
    LDLT: PreconditionerType
    """Dense LDLt factorization without pivoting.

    Note:
        Only valid for symmetric matrices.

    Note:
        May be unstable for indefinite or ill-conditioned matrices.

    Note:
        The iterative solver should converge in 1 iteration.
    """
    ILU0: PreconditionerType
    """Incomplete LU factorization with zero fill-in."""
    IC0: PreconditionerType
    """Incomplete Cholesky factorization with zero fill-in.

    Note:
        Only valid for symmetric positive-definite matrices.
    """
    PER_ACTOR: PreconditionerType
    """Domain decomposition preconditioner, where each actor is a subdomain.

    Note:
        The most appropriate preconditioner type is used for each actor.
    """
    COLORED_SSOR: PreconditionerType
    """[Experimental] Colored symmetric successive over-relaxation (SSOR)
    preconditioner.

    Note:
        Only valid for symmetric positive-definite matrices.

    Warning:
        This is an experimental feature. It may be changed or removed in the future.
        Use at your own risk.
    """
    COUNT: PreconditionerType
    """Number of preconditioner type enum values."""
    DEFAULT: PreconditionerType
    """Default preconditioner type."""
    def __init__(self, value: int) -> None: ...
    @property
    def name(self) -> str: ...
    @property
    def value(self) -> int: ...
    def __int__(self) -> int: ...
    def __index__(self) -> int: ...

class LinearSolverConvergenceNorm:
    """Norm types for the stopping criteria of the linear solver.

    Note:
        This setting only applies to :class:`CG
        <superdex.physics.LinearSolverType>`, :class:`CUDA_CG
        <superdex.physics.LinearSolverType>`, :class:`PARALLEL_CG
        <superdex.physics.LinearSolverType>` and :class:`ASYNC_CG
        <superdex.physics.LinearSolverType>`. :class:`GMRES
        <superdex.physics.LinearSolverType>`, :class:`CUDA_GMRES
        <superdex.physics.LinearSolverType>`, :class:`MINRES
        <superdex.physics.LinearSolverType>`, and :class:`AUGMENTED_CG
        <superdex.physics.LinearSolverType>` always use the L2-norm of the residual
        regardless of this setting. Direct solvers do not have stop criteria.
    """
    RESIDUAL_L2: LinearSolverConvergenceNorm
    """L2-norm of the residual: ``‖r‖₂``."""
    PRECONDITIONED_RESIDUAL_L2: LinearSolverConvergenceNorm
    """L2-norm of the preconditioned residual: ``‖z‖₂`` where ``z = M⁻¹r``."""
    RESIDUAL_PRECONDITIONER_INDUCED: LinearSolverConvergenceNorm
    """Preconditioner-induced norm of the residual: ``√⟨r, z⟩`` where ``z = M⁻¹r``."""
    COUNT: LinearSolverConvergenceNorm
    """Number of linear solver convergence norm enum values."""
    DEFAULT: LinearSolverConvergenceNorm
    """Default linear solver convergence norm."""
    def __init__(self, value: int) -> None: ...
    @property
    def name(self) -> str: ...
    @property
    def value(self) -> int: ...
    def __int__(self) -> int: ...
    def __index__(self) -> int: ...

class IntegrationMethod:
    """Time-integration method."""
    BACKWARD_EULER: IntegrationMethod
    """Backward Euler: 1 step, 1 stage, 1st order, L-stable. Also known as BDF1 and
    DIRK11.
    """
    BDF2: IntegrationMethod
    """BDF2: 2 steps, 1 stage, 2nd order, A-stable."""
    BDF3: IntegrationMethod
    """BDF3: 3 steps, 1 stage, 3rd order."""
    DIRK22: IntegrationMethod
    """DIRK(2,2): 1 step, 2 stages, 2nd order, L-stable."""
    DIRK23: IntegrationMethod
    """DIRK(2,3): 1 step, 2 stages, 3rd order."""
    DIRK33: IntegrationMethod
    """DIRK(3,3): 1 step, 3 stages, 3rd order, L-stable."""
    SYMPLECTIC_DIRK12: IntegrationMethod
    """Symplectic DIRK(1,2): 1 step, 1 stage, 2nd order, A-stable. Also known as Gauss
    and implicit midpoint.
    """
    SYMPLECTIC_DIRK22: IntegrationMethod
    """Symplectic DIRK(2,2): 1 step, 2 stages, 2nd order, A-stable."""
    COUNT: IntegrationMethod
    """Number of time integration method enum values."""
    DEFAULT: IntegrationMethod
    """Default time integration method."""
    def __init__(self, value: int) -> None: ...
    @property
    def name(self) -> str: ...
    @property
    def value(self) -> int: ...
    def __int__(self) -> int: ...
    def __index__(self) -> int: ...

class CoordinateSpaceAxes:
    """Combination of axis directions for a coordinate space convention.

    Each three-letter name gives the semantic direction of positive X, positive Y,
    and positive Z, respectively. For example, :class:`FLU
    <superdex.physics.CoordinateSpaceAxes>` means X-forward, Y-left, Z-up.
    """
    RUF: CoordinateSpaceAxes
    """X-right, Y-up, Z-forward (Unity)."""
    RUB: CoordinateSpaceAxes
    """X-right, Y-up, Z-backward (OpenGL, Filament)."""
    RDF: CoordinateSpaceAxes
    """X-right, Y-down, Z-forward."""
    RDB: CoordinateSpaceAxes
    """X-right, Y-down, Z-backward."""
    LUF: CoordinateSpaceAxes
    """X-left, Y-up, Z-forward."""
    LUB: CoordinateSpaceAxes
    """X-left, Y-up, Z-backward."""
    LDF: CoordinateSpaceAxes
    """X-left, Y-down, Z-forward."""
    LDB: CoordinateSpaceAxes
    """X-left, Y-down, Z-backward."""
    RFU: CoordinateSpaceAxes
    """X-right, Y-forward, Z-up."""
    RFD: CoordinateSpaceAxes
    """X-right, Y-forward, Z-down."""
    RBU: CoordinateSpaceAxes
    """X-right, Y-backward, Z-up."""
    RBD: CoordinateSpaceAxes
    """X-right, Y-backward, Z-down."""
    LFU: CoordinateSpaceAxes
    """X-left, Y-forward, Z-up."""
    LFD: CoordinateSpaceAxes
    """X-left, Y-forward, Z-down."""
    LBU: CoordinateSpaceAxes
    """X-left, Y-backward, Z-up."""
    LBD: CoordinateSpaceAxes
    """X-left, Y-backward, Z-down."""
    URF: CoordinateSpaceAxes
    """X-up, Y-right, Z-forward."""
    URB: CoordinateSpaceAxes
    """X-up, Y-right, Z-backward."""
    ULF: CoordinateSpaceAxes
    """X-up, Y-left, Z-forward."""
    ULB: CoordinateSpaceAxes
    """X-up, Y-left, Z-backward."""
    DRF: CoordinateSpaceAxes
    """X-down, Y-right, Z-forward."""
    DRB: CoordinateSpaceAxes
    """X-down, Y-right, Z-backward."""
    DLF: CoordinateSpaceAxes
    """X-down, Y-left, Z-forward."""
    DLB: CoordinateSpaceAxes
    """X-down, Y-left, Z-backward."""
    UFR: CoordinateSpaceAxes
    """X-up, Y-forward, Z-right."""
    UFL: CoordinateSpaceAxes
    """X-up, Y-forward, Z-left."""
    UBR: CoordinateSpaceAxes
    """X-up, Y-backward, Z-right."""
    UBL: CoordinateSpaceAxes
    """X-up, Y-backward, Z-left."""
    DFR: CoordinateSpaceAxes
    """X-down, Y-forward, Z-right."""
    DFL: CoordinateSpaceAxes
    """X-down, Y-forward, Z-left."""
    DBR: CoordinateSpaceAxes
    """X-down, Y-backward, Z-right."""
    DBL: CoordinateSpaceAxes
    """X-down, Y-backward, Z-left."""
    FRU: CoordinateSpaceAxes
    """X-forward, Y-right, Z-up (Unreal)."""
    FRD: CoordinateSpaceAxes
    """X-forward, Y-right, Z-down."""
    FLU: CoordinateSpaceAxes
    """X-forward, Y-left, Z-up (Default)."""
    FLD: CoordinateSpaceAxes
    """X-forward, Y-left, Z-down."""
    BRU: CoordinateSpaceAxes
    """X-backward, Y-right, Z-up."""
    BRD: CoordinateSpaceAxes
    """X-backward, Y-right, Z-down."""
    BLU: CoordinateSpaceAxes
    """X-backward, Y-left, Z-up."""
    BLD: CoordinateSpaceAxes
    """X-backward, Y-left, Z-down."""
    FUR: CoordinateSpaceAxes
    """X-forward, Y-up, Z-right."""
    FUL: CoordinateSpaceAxes
    """X-forward, Y-up, Z-left."""
    FDR: CoordinateSpaceAxes
    """X-forward, Y-down, Z-right."""
    FDL: CoordinateSpaceAxes
    """X-forward, Y-down, Z-left."""
    BUR: CoordinateSpaceAxes
    """X-backward, Y-up, Z-right."""
    BUL: CoordinateSpaceAxes
    """X-backward, Y-up, Z-left."""
    BDR: CoordinateSpaceAxes
    """X-backward, Y-down, Z-right."""
    BDL: CoordinateSpaceAxes
    """X-backward, Y-down, Z-left."""
    DEFAULT: CoordinateSpaceAxes
    """Default convention for SuperDex Physics."""
    def __init__(self, value: int) -> None: ...
    @property
    def name(self) -> str: ...
    @property
    def value(self) -> int: ...
    def __int__(self) -> int: ...
    def __index__(self) -> int: ...

class ArticulatedCycleJoint:
    """Defines a cycle-closing joint in an articulated body.

    Cycle joints create closed loops in the kinematic chain, allowing topologies
    beyond simple trees.
    """
    child: int
    """Child link index."""
    parent: int
    """Parent link index."""
    @overload
    def __init__(self) -> None: ...
    @overload
    def __init__(self, child: int = ..., parent: int = ...) -> None: ...
    def __eq__(self, other: object) -> bool: ...
    def __ne__(self, other: object) -> bool: ...

class RoutingElement:
    """One ordered element in a spatial tendon's heterogeneous routing list.

    A :class:`WAYPOINT <superdex.physics.RoutingElementType>` uses :attr:`index` (a
    link index) and :attr:`local_position`; a :class:`LINEAR_JOINT
    <superdex.physics.RoutingElementType>` uses :attr:`index` (a joint index) and
    :attr:`coefficient`. Order matters: a segment forms only between adjacent
    waypoints, so a linear-joint element between two waypoints leaves a gap.
    """
    type: RoutingElementType
    """Selects the interpretation of the remaining fields."""
    index: int
    """Link index for a waypoint, or joint index for a linear-joint element."""
    @property
    def local_position(self) -> Real3:
        """Waypoint position in the link's local frame, the same frame in which the
        link's
        mesh and geometry are authored (the link's root-transform frame) [m]. Only used
        for waypoint elements.
        """
    @local_position.setter
    def local_position(self, value: Real3Like) -> None: ...
    coefficient: float
    """Signed constant moment arm d(displacement)/d(joint DoF) [m / joint DoF]. Only
    used for linear-joint elements. Its sign sets whether the tendon lengthens or
    shortens with the joint DoF.
    """
    @overload
    def __init__(self) -> None: ...
    @overload
    def __init__(
        self,
        type: RoutingElementType | int = ...,
        index: int = ...,
        local_position: Real3Like = ...,
        coefficient: float = ...,
    ) -> None: ...
    def __eq__(self, other: object) -> bool: ...
    def __ne__(self, other: object) -> bool: ...

class BlendingData:
    """Blending data for one source shape within a soft skinned mesh.

    See Also:
        :class:`~superdex.physics.BlendingDataView`
    """
    source_shape: str
    """Name of the soft source shape."""
    @property
    def indices(self) -> DynamicArrayInt:
        """Indices for blending. Size is numNodes * 2."""
    @indices.setter
    def indices(self, value: ArrayLikeInt) -> None: ...
    @property
    def weights(self) -> DynamicArrayReal:
        """Weights for blending. Size is numNodes * 2."""
    @weights.setter
    def weights(self, value: ArrayLikeReal) -> None: ...
    @overload
    def __init__(self) -> None: ...
    @overload
    def __init__(
        self,
        source_shape: str = ...,
        indices: ArrayLikeInt = ...,
        weights: ArrayLikeReal = ...,
    ) -> None: ...
    @overload
    def __init__(self, src: BlendingDataView) -> None:
        """Copy from :class:`~superdex.physics.BlendingDataView`.

        Args:
            src (BlendingDataView): Source data.
        """
    def __eq__(self, other: object) -> bool: ...
    def __ne__(self, other: object) -> bool: ...

class BlendingDataView:
    """Non-owning view of blending data for one source shape within a soft skinned
    mesh.

    See Also:
        :class:`~superdex.physics.BlendingData`
    """
    source_shape: str
    """Name of the soft source shape."""
    @property
    def indices(self) -> SpanConstInt:
        """Indices for blending. Size is numNodes * 2."""
    @indices.setter
    def indices(self, value: ArrayLikeInt) -> None: ...
    @property
    def weights(self) -> SpanConstReal:
        """Weights for blending. Size is numNodes * 2."""
    @weights.setter
    def weights(self, value: ArrayLikeReal) -> None: ...
    @overload
    def __init__(self) -> None: ...
    @overload
    def __init__(
        self,
        source_shape: str = ...,
        indices: ArrayLikeInt = ...,
        weights: ArrayLikeReal = ...,
    ) -> None: ...
    @overload
    def __init__(self, src: BlendingData) -> None:
        """Implicit conversion from :class:`~superdex.physics.BlendingData`.

        Args:
            src (BlendingData): Source data.
        """
    def __eq__(self, other: object) -> bool: ...
    def __ne__(self, other: object) -> bool: ...

class SkinningData:
    """Skinning data for a SuperDex mesh.

    See Also:
        :class:`~superdex.physics.SkinningDataView`
    """
    weights_per_node: int
    """Number of weights and indices per skinned node."""
    @property
    def indices(self) -> DynamicArrayInt:
        """Indices for each node. Size is numNodes * weightsPerNode."""
    @indices.setter
    def indices(self, value: ArrayLikeInt) -> None: ...
    @property
    def weights(self) -> DynamicArrayReal:
        """Weights for each node. Size is numNodes * weightsPerNode."""
    @weights.setter
    def weights(self, value: ArrayLikeReal) -> None: ...
    @overload
    def __init__(self) -> None: ...
    @overload
    def __init__(
        self,
        weights_per_node: int = ...,
        indices: ArrayLikeInt = ...,
        weights: ArrayLikeReal = ...,
    ) -> None: ...
    @overload
    def __init__(self, src: SkinningDataView) -> None:
        """Copy from :class:`~superdex.physics.SkinningDataView`.

        Args:
            src (SkinningDataView): Source data.
        """
    def __eq__(self, other: object) -> bool: ...
    def __ne__(self, other: object) -> bool: ...

class SkinningDataView:
    """Non-owning view of skinning data for a SuperDex mesh.

    See Also:
        :class:`~superdex.physics.SkinningData`
    """
    weights_per_node: int
    """Number of weights and indices per skinned node."""
    @property
    def indices(self) -> SpanConstInt:
        """Indices for each node. Size is numNodes * weightsPerNode."""
    @indices.setter
    def indices(self, value: ArrayLikeInt) -> None: ...
    @property
    def weights(self) -> SpanConstReal:
        """Weights for each node. Size is numNodes * weightsPerNode."""
    @weights.setter
    def weights(self, value: ArrayLikeReal) -> None: ...
    @overload
    def __init__(self) -> None: ...
    @overload
    def __init__(
        self,
        weights_per_node: int = ...,
        indices: ArrayLikeInt = ...,
        weights: ArrayLikeReal = ...,
    ) -> None: ...
    @overload
    def __init__(self, src: SkinningData) -> None:
        """Implicit conversion from :class:`~superdex.physics.SkinningData`.

        Args:
            src (SkinningData): Source data.
        """
    def __eq__(self, other: object) -> bool: ...
    def __ne__(self, other: object) -> bool: ...

class MeshData:
    """Mesh data for a SuperDex actor or model file.

    See Also:
        :class:`~superdex.physics.MeshDataView`
    """
    nodes_per_element: int
    """Number of nodes (vertices) per element.

    Note:
        2 for polyline meshes, 3 for triangle meshes, 4 for tetrahedral meshes.
    """
    @property
    def coordinates(self) -> DynamicArrayReal:
        """Flat array of node coordinate values [m].

        Note:
            Size must be a multiple of 3.
        """
    @coordinates.setter
    def coordinates(self, value: ArrayLikeReal) -> None: ...
    @property
    def connectivity(self) -> DynamicArrayInt:
        """Flat array of node indices forming the elements.

        Note:
            Size must be a multiple of
            :attr:`~superdex.physics.MeshData.nodes_per_element`.
        """
    @connectivity.setter
    def connectivity(self, value: ArrayLikeInt) -> None: ...
    skinning: Optional[SkinningData]
    """Optional skinning data."""
    @overload
    def __init__(self) -> None: ...
    @overload
    def __init__(
        self,
        nodes_per_element: int = ...,
        coordinates: ArrayLikeReal = ...,
        connectivity: ArrayLikeInt = ...,
        skinning: Optional[SkinningData] = ...,
    ) -> None: ...
    @overload
    def __init__(self, src: MeshDataView) -> None:
        """Copy from :class:`~superdex.physics.MeshDataView`.

        Args:
            src (MeshDataView): Source data.
        """
    def __eq__(self, other: object) -> bool: ...
    def __ne__(self, other: object) -> bool: ...
    def get_num_nodes(self) -> int:
        """Return the number of nodes.

        Returns:
            Number of nodes.
        """
    def get_num_elements(self) -> int:
        """Return the number of elements.

        Returns:
            Number of elements.
        """
    def is_empty(self) -> bool:
        """Return true if the mesh is empty.

        Returns:
            True if the mesh is empty.
        """

class MeshDataView:
    """Non-owning view of mesh data for a SuperDex actor or model file.

    See Also:
        :class:`~superdex.physics.MeshData`
    """
    nodes_per_element: int
    """Number of nodes (vertices) per element.

    Note:
        2 for polyline meshes, 3 for triangle meshes, 4 for tetrahedral meshes.
    """
    @property
    def coordinates(self) -> SpanConstReal:
        """Flat array of node coordinate values [m].

        Note:
            Size must be a multiple of 3.
        """
    @coordinates.setter
    def coordinates(self, value: ArrayLikeReal) -> None: ...
    @property
    def connectivity(self) -> SpanConstInt:
        """Flat array of node indices forming the elements.

        Note:
            Size must be a multiple of
            :attr:`~superdex.physics.MeshDataView.nodes_per_element`.
        """
    @connectivity.setter
    def connectivity(self, value: ArrayLikeInt) -> None: ...
    skinning: Optional[SkinningDataView]
    """Optional skinning data.

    Returns:
        Optional skinning data.
    """
    @overload
    def __init__(self) -> None: ...
    @overload
    def __init__(
        self,
        nodes_per_element: int = ...,
        coordinates: ArrayLikeReal = ...,
        connectivity: ArrayLikeInt = ...,
        skinning: Optional[SkinningDataView] = ...,
    ) -> None: ...
    @overload
    def __init__(self, src: MeshData) -> None:
        """Implicit conversion from :class:`~superdex.physics.MeshData`.

        Args:
            src (MeshData): Source data.
        """
    def __eq__(self, other: object) -> bool: ...
    def __ne__(self, other: object) -> bool: ...
    def get_num_nodes(self) -> int:
        """Return the number of nodes.

        Returns:
            Number of nodes.
        """
    def get_num_elements(self) -> int:
        """Return the number of elements.

        Returns:
            Number of elements.
        """
    def is_empty(self) -> bool:
        """Return true if the mesh is empty.

        Returns:
            True if the mesh is empty.
        """

class GridSdfData:
    """Data for a precomputed signed-distance-field grid.

    See Also:
        :class:`~superdex.physics.GridSdfDataView`
    """
    @property
    def dims(self) -> Int3:
        """Dimensions of the SDF grid in X, Y, and Z."""
    @dims.setter
    def dims(self, value: Int3Like) -> None: ...
    @property
    def values(self) -> DynamicArrayReal:
        """Signed distance values [m]. Size must be dims[0] * dims[1] * dims[2]."""
    @values.setter
    def values(self, value: ArrayLikeReal) -> None: ...
    bounds: Aabb
    """Spatial bounds of the SDF grid. Values are distributed uniformly within this
    volume.
    """
    negative_value_bounds: Aabb
    """Spatial bounds of the portion of the SDF grid with negative values.

    Note:
        This is generally the bounds of the mesh for which the SDF grid was
        computed, while the overall grid bounds may be larger due to padding for
        penalty fall-off distance.
    """
    @property
    def scale(self) -> Optional[Real3]:
        """Optional parent-from-grid per-axis scale to apply at runtime.

        Note:
            Applied order is scale, then rotation, then translation.

        Note:
            Typically set when a transform is baked into the containing model.
        """
    @scale.setter
    def scale(self, value: Optional[Real3Like]) -> None: ...
    @property
    def rotation(self) -> Optional[Quaternion]:
        """Optional parent-from-grid rotation to apply at runtime.

        Note:
            Applied order is scale, then rotation, then translation.

        Note:
            Typically set when a transform is baked into the containing model.
        """
    @rotation.setter
    def rotation(self, value: Optional[QuaternionLike]) -> None: ...
    @property
    def translation(self) -> Optional[Real3]:
        """Optional parent-from-grid translation [m] to apply at runtime.

        Note:
            Applied order is scale, then rotation, then translation.

        Note:
            Typically set when a transform is baked into the containing model.
        """
    @translation.setter
    def translation(self, value: Optional[Real3Like]) -> None: ...
    @overload
    def __init__(self) -> None: ...
    @overload
    def __init__(
        self,
        dims: Int3Like = ...,
        values: ArrayLikeReal = ...,
        bounds: Aabb = ...,
        negative_value_bounds: Aabb = ...,
        scale: Optional[Real3Like] = ...,
        rotation: Optional[QuaternionLike] = ...,
        translation: Optional[Real3Like] = ...,
    ) -> None: ...
    @overload
    def __init__(self, src: GridSdfDataView) -> None:
        """Copy from :class:`~superdex.physics.GridSdfDataView`.

        Args:
            src (GridSdfDataView): Source data.
        """
    def __eq__(self, other: object) -> bool: ...
    def __ne__(self, other: object) -> bool: ...

class GridSdfDataView:
    """Non-owning view of data for a precomputed signed-distance-field grid.

    See Also:
        :class:`~superdex.physics.GridSdfData`
    """
    @property
    def dims(self) -> Int3:
        """Dimensions of the SDF grid in X, Y, and Z."""
    @dims.setter
    def dims(self, value: Int3Like) -> None: ...
    @property
    def values(self) -> SpanConstReal:
        """Signed distance values [m]. Size must be dims[0] * dims[1] * dims[2]."""
    @values.setter
    def values(self, value: ArrayLikeReal) -> None: ...
    bounds: Aabb
    """Spatial bounds of the SDF grid. Values are distributed uniformly within this
    volume.
    """
    negative_value_bounds: Aabb
    """Spatial bounds of the portion of the SDF grid with negative values.

    Note:
        This is generally the bounds of the mesh for which the SDF grid was
        computed, while the overall grid bounds may be larger due to padding for
        penalty fall-off distance.
    """
    @property
    def scale(self) -> Optional[Real3]:
        """Optional parent-from-grid per-axis scale to apply at runtime.

        Note:
            Applied order is scale, then rotation, then translation.

        Note:
            Typically set when a transform is baked into the containing model.
        """
    @scale.setter
    def scale(self, value: Optional[Real3Like]) -> None: ...
    @property
    def rotation(self) -> Optional[Quaternion]:
        """Optional parent-from-grid rotation to apply at runtime.

        Note:
            Applied order is scale, then rotation, then translation.

        Note:
            Typically set when a transform is baked into the containing model.
        """
    @rotation.setter
    def rotation(self, value: Optional[QuaternionLike]) -> None: ...
    @property
    def translation(self) -> Optional[Real3]:
        """Optional parent-from-grid translation [m] to apply at runtime.

        Note:
            Applied order is scale, then rotation, then translation.

        Note:
            Typically set when a transform is baked into the containing model.
        """
    @translation.setter
    def translation(self, value: Optional[Real3Like]) -> None: ...
    @overload
    def __init__(self) -> None: ...
    @overload
    def __init__(
        self,
        dims: Int3Like = ...,
        values: ArrayLikeReal = ...,
        bounds: Aabb = ...,
        negative_value_bounds: Aabb = ...,
        scale: Optional[Real3Like] = ...,
        rotation: Optional[QuaternionLike] = ...,
        translation: Optional[Real3Like] = ...,
    ) -> None: ...
    @overload
    def __init__(self, src: GridSdfData) -> None:
        """Implicit conversion from :class:`~superdex.physics.GridSdfData`.

        Args:
            src (GridSdfData): Source data.
        """
    def __eq__(self, other: object) -> bool: ...
    def __ne__(self, other: object) -> bool: ...

class Box:
    """Oriented box represented by center, half extents, and rotation."""
    @property
    def center(self) -> Real3:
        """Center position [m]."""
    @center.setter
    def center(self, value: Real3Like) -> None: ...
    @property
    def half_extents(self) -> Real3:
        """Half-size along each local axis [m]."""
    @half_extents.setter
    def half_extents(self, value: Real3Like) -> None: ...
    @property
    def rotation(self) -> Quaternion:
        """Orientation of the box local frame."""
    @rotation.setter
    def rotation(self, value: QuaternionLike) -> None: ...
    @overload
    def __init__(self) -> None: ...
    @overload
    def __init__(
        self,
        center: Real3Like = ...,
        half_extents: Real3Like = ...,
        rotation: QuaternionLike = ...,
    ) -> None: ...
    def __eq__(self, other: object) -> bool: ...
    def __ne__(self, other: object) -> bool: ...

class Plane:
    """Plane represented by a unit normal and signed distance from the origin."""
    @property
    def normal(self) -> Real3:
        """Unit normal vector."""
    @normal.setter
    def normal(self, value: Real3Like) -> None: ...
    distance: float
    """Signed distance from the origin [m]."""
    @overload
    def __init__(self) -> None: ...
    @overload
    def __init__(self, normal: Real3Like = ..., distance: float = ...) -> None: ...
    def __eq__(self, other: object) -> bool: ...
    def __ne__(self, other: object) -> bool: ...

class Sphere:
    """Sphere represented by center and radius."""
    @property
    def center(self) -> Real3:
        """Center position [m]."""
    @center.setter
    def center(self, value: Real3Like) -> None: ...
    radius: float
    """Radius [m]."""
    @overload
    def __init__(self) -> None: ...
    @overload
    def __init__(self, center: Real3Like = ..., radius: float = ...) -> None: ...
    def __eq__(self, other: object) -> bool: ...
    def __ne__(self, other: object) -> bool: ...

class ModelData:
    """Contents of a SuperDex model file.

    See Also:
        :class:`~superdex.physics.ModelDataView`
    """
    mesh: Optional[MeshData]
    visual_mesh: Optional[MeshData]
    contact_skin_mesh: Optional[MeshData]
    """Optional triangular mesh used for surface queries and, when selected as the
    rod's contact geometry, for contact quadrature.

    The skinning indices reference primary-mesh nodes for triangular and tetrahedral
    meshes, and primary-mesh elements for polylines. Currently consumed only by rod
    actors.
    """
    @property
    def blending(self) -> Optional[DynamicArrayBlendingData]: ...
    @blending.setter
    def blending(self, value: Optional[ArrayLikeBlendingData]) -> None: ...
    @property
    def constrained_nodes(self) -> Optional[DynamicArrayInt]:
        """Indices of mesh nodes that are constrained."""
    @constrained_nodes.setter
    def constrained_nodes(self, value: Optional[ArrayLikeInt]) -> None: ...
    @property
    def element_frame_axes(self) -> Optional[DynamicArrayReal]:
        """Per-element reference frame axes for polyline meshes.

        Flat array of unit vectors (3 reals per element), each orthogonal to its
        element's tangent. Only valid when the mesh is a polyline with
        :attr:`~superdex.physics.MeshData.nodes_per_element` equal to 2.
        """
    @element_frame_axes.setter
    def element_frame_axes(self, value: Optional[ArrayLikeReal]) -> None: ...
    box: Optional[Box]
    plane: Optional[Plane]
    sphere: Optional[Sphere]
    sdf: Optional[GridSdfData]
    material: Optional[PerElementSoftMaterialData]
    @overload
    def __init__(self) -> None: ...
    @overload
    def __init__(
        self,
        mesh: Optional[MeshData] = ...,
        visual_mesh: Optional[MeshData] = ...,
        contact_skin_mesh: Optional[MeshData] = ...,
        blending: Optional[ArrayLikeBlendingData] = ...,
        constrained_nodes: Optional[ArrayLikeInt] = ...,
        element_frame_axes: Optional[ArrayLikeReal] = ...,
        box: Optional[Box] = ...,
        plane: Optional[Plane] = ...,
        sphere: Optional[Sphere] = ...,
        sdf: Optional[GridSdfData] = ...,
        material: Optional[PerElementSoftMaterialData] = ...,
    ) -> None: ...
    @overload
    def __init__(self, src: ModelDataView) -> None:
        """Copy from :class:`~superdex.physics.ModelDataView`.

        Args:
            src (ModelDataView): Source data.
        """
    def __eq__(self, other: object) -> bool: ...
    def __ne__(self, other: object) -> bool: ...

class ModelDataView:
    """Non-owning view of the contents of a SuperDex model file.

    See Also:
        :class:`~superdex.physics.ModelData`
    """
    mesh: Optional[MeshDataView]
    visual_mesh: Optional[MeshDataView]
    contact_skin_mesh: Optional[MeshDataView]
    """Optional triangular mesh used for surface queries and, when selected as the
    rod's contact geometry, for contact quadrature.

    The skinning indices reference primary-mesh nodes for triangular and tetrahedral
    meshes, and primary-mesh elements for polylines. Currently consumed only by rod
    actors.
    """
    @property
    def blending(self) -> Optional[DynamicArrayBlendingDataView]: ...
    @blending.setter
    def blending(self, value: Optional[ArrayLikeBlendingDataView]) -> None: ...
    @property
    def constrained_nodes(self) -> Optional[SpanConstInt]:
        """Indices of mesh nodes that are constrained."""
    @constrained_nodes.setter
    def constrained_nodes(self, value: Optional[ArrayLikeInt]) -> None: ...
    @property
    def element_frame_axes(self) -> Optional[SpanConstReal]:
        """Per-element reference frame axes for polyline meshes.

        Flat array of unit vectors (3 reals per element), each orthogonal to its
        element's tangent. Only valid when the mesh is a polyline with
        :attr:`~superdex.physics.MeshDataView.nodes_per_element` equal to 2.
        """
    @element_frame_axes.setter
    def element_frame_axes(self, value: Optional[ArrayLikeReal]) -> None: ...
    box: Optional[Box]
    plane: Optional[Plane]
    sphere: Optional[Sphere]
    sdf: Optional[GridSdfDataView]
    material: Optional[PerElementSoftMaterialDataView]
    @overload
    def __init__(self) -> None: ...
    @overload
    def __init__(
        self,
        mesh: Optional[MeshDataView] = ...,
        visual_mesh: Optional[MeshDataView] = ...,
        contact_skin_mesh: Optional[MeshDataView] = ...,
        blending: Optional[ArrayLikeBlendingDataView] = ...,
        constrained_nodes: Optional[ArrayLikeInt] = ...,
        element_frame_axes: Optional[ArrayLikeReal] = ...,
        box: Optional[Box] = ...,
        plane: Optional[Plane] = ...,
        sphere: Optional[Sphere] = ...,
        sdf: Optional[GridSdfDataView] = ...,
        material: Optional[PerElementSoftMaterialDataView] = ...,
    ) -> None: ...
    @overload
    def __init__(self, src: ModelData) -> None:
        """Implicit conversion from :class:`~superdex.physics.ModelData`.

        Args:
            src (ModelData): Source data.
        """
    def __eq__(self, other: object) -> bool: ...
    def __ne__(self, other: object) -> bool: ...

class GridSdfParams:
    """Parameters controlling the resolution of grid-based Signed Distance Fields
    (SDF).

    A GridSdf represents the SDF to a closed surface mesh as a 3D grid where each
    vertex stores the signed distance to the surface mesh. The SDF grid resolution
    and bounds are controlled by these parameters.
    """
    resolution_mode: GridSdfResolutionMode
    """Defines the mesh feature used as reference measurement for computing the voxel
    size.

    See Also:
        :attr:`~superdex.physics.GridSdfParams.resolution_delta`
    """
    @property
    def resolution_delta(self) -> Real3:
        """The maximum voxel size will be
        :attr:`~superdex.physics.GridSdfParams.resolution_delta` times the reference
        measurement computed using
        :attr:`~superdex.physics.GridSdfParams.resolution_mode`.

        Note:
            The actual voxel size may be smaller because the number of grid cells will
            be rounded up to an integer value and clamped to a minimum of
            :attr:`~superdex.physics.GridSdfParams.min_grid_resolution`.

        Note:
            Increasing :attr:`~superdex.physics.GridSdfParams.resolution_delta` will
            result in larger voxels. Doing so will make it harder for the SDF to resolve
            fine details of the mesh, but it will save memory and improve SDF generation
            speed.

        Note:
            Decreasing :attr:`~superdex.physics.GridSdfParams.resolution_delta` will
            improve the SDF's ability to resolve fine details, but it will take more
            memory and more time to generate. This cost scales proportional to N^3. Use
            with care.

        See Also:
            :attr:`~superdex.physics.GridSdfParams.resolution_mode`
        """
    @resolution_delta.setter
    def resolution_delta(self, value: Real3Like) -> None: ...
    boundary_padding_dist: float
    """Additional distance (in meters) to expand the mesh's axis-aligned bounding box
    (AABB) when determining the SDF grid bounds.

    The SDF grid will cover the mesh's AABB expanded by this distance in all
    directions. This ensures the SDF has valid distance values even slightly outside
    the mesh's AABB.

    Note:
        In practice, this value should be greater than the value returned by
        :meth:`~superdex.physics.ContactParams.get_penalty_threshold_dist`. If the
        SDF did not have enough padding, then Mochi would have to use a less
        efficient algorithm for collision detection, but it would still work.

    Note:
        When you load a model and bake a uniform scale value less than 1.0, the
        model's pre-computed SDF will also be scaled, resulting in less absolute
        padding. The default padding value is larger than the default contact
        penalty threshold distance for this reason.

    See Also:
        :const:`~superdex.physics.GRID_SDF_DEFAULT_BOUNDARY_PADDING`
    """
    @property
    def min_grid_resolution(self) -> Int3:
        """The minimum grid resolution measured in number of voxels per axis.

        Note:
            This may override the voxel count (and voxel size) computed using
            :attr:`~superdex.physics.GridSdfParams.resolution_mode` and
            :attr:`~superdex.physics.GridSdfParams.resolution_delta`.
        """
    @min_grid_resolution.setter
    def min_grid_resolution(self, value: Int3Like) -> None: ...
    @overload
    def __init__(self) -> None: ...
    @overload
    def __init__(
        self,
        resolution_mode: GridSdfResolutionMode | int = ...,
        resolution_delta: Real3Like = ...,
        boundary_padding_dist: float = ...,
        min_grid_resolution: Int3Like = ...,
    ) -> None: ...

class LinearElasticMaterialParams:
    """Parameters for a linear elastic material model."""
    youngs_modulus: float
    """Young's modulus [Pa]. Must be positive."""
    poisson_ratio: float
    """Poisson's ratio (dimensionless).

    Note:
        Must be in (-1, 0.5). Near 0.5 = nearly incompressible.
    """
    @overload
    def __init__(self) -> None: ...
    @overload
    def __init__(
        self,
        youngs_modulus: float = ...,
        poisson_ratio: float = ...,
    ) -> None: ...

class StVenantKirchhoffMaterialParams:
    """Parameters for the St. Venant-Kirchhoff material model."""
    youngs_modulus: float
    """Young's modulus [Pa]. Must be positive."""
    poisson_ratio: float
    """Poisson's ratio (dimensionless).

    Note:
        Must be in (-1, 0.5). Near 0.5 = nearly incompressible.
    """
    psd_strategy: MaterialPsdStrategy
    """Strategy for ensuring positive semi-definite Hessian matrices.

    See Also:
        :class:`~superdex.physics.MaterialPsdStrategy`
    """
    @overload
    def __init__(self) -> None: ...
    @overload
    def __init__(
        self,
        youngs_modulus: float = ...,
        poisson_ratio: float = ...,
        psd_strategy: MaterialPsdStrategy | int = ...,
    ) -> None: ...

class NeoHookeanMaterialParams:
    """Parameters for the Smith stable Neo-Hookean material model."""
    youngs_modulus: float
    """Young's modulus [Pa]. Must be positive."""
    poisson_ratio: float
    """Poisson's ratio (dimensionless).

    Note:
        Must be in (-1, 0.5). Near 0.5 = nearly incompressible.
    """
    psd_strategy: MaterialPsdStrategy
    """Strategy for ensuring positive semi-definite Hessian matrices.

    See Also:
        :class:`~superdex.physics.MaterialPsdStrategy`
    """
    @overload
    def __init__(self) -> None: ...
    @overload
    def __init__(
        self,
        youngs_modulus: float = ...,
        poisson_ratio: float = ...,
        psd_strategy: MaterialPsdStrategy | int = ...,
    ) -> None: ...

class ActiveAnisoArapMaterialParams:
    """Parameters for the active anisotropic ARAP material component."""
    alpha: float
    """Anisotropic stiffness (α) along fiber direction [Pa].

    Note:
        Must not be negative.
    """
    length: float
    """Anisotropic reference length (dimensionless).

    Note:
        Must not be negative.
    """
    @property
    def aniso_dir(self) -> Real3:
        """Fiber direction unit vector with components (cos(φ)cos(θ), sin(φ),
        cos(φ)sin(θ)).

        See Also:
            :meth:`~superdex.physics.ActiveAnisoArapMaterialParams.compute_fiber_direction`,
            :meth:`~superdex.physics.ActiveAnisoArapMaterialParams.get_theta`,
            :meth:`~superdex.physics.ActiveAnisoArapMaterialParams.get_phi`
        """
    @aniso_dir.setter
    def aniso_dir(self, value: Real3Like) -> None: ...
    psd_strategy: MaterialPsdStrategy
    """Strategy for ensuring positive semi-definite Hessian matrices.

    See Also:
        :class:`~superdex.physics.MaterialPsdStrategy`
    """
    @overload
    def __init__(self) -> None: ...
    @overload
    def __init__(
        self,
        alpha: float = ...,
        length: float = ...,
        aniso_dir: Real3Like = ...,
        psd_strategy: MaterialPsdStrategy | int = ...,
    ) -> None: ...
    def get_theta(self) -> float:
        """Returns the azimuthal angle (θ) [rad] from the stored fiber direction.

        Returns:
            The azimuthal angle.

        See Also:
            :meth:`~superdex.physics.ActiveAnisoArapMaterialParams.compute_fiber_direction`,
            :meth:`~superdex.physics.ActiveAnisoArapMaterialParams.get_phi`
        """
    def get_phi(self) -> float:
        """Returns the elevation angle (φ) [rad] from the stored fiber direction.

        Returns:
            The elevation angle.

        See Also:
            :meth:`~superdex.physics.ActiveAnisoArapMaterialParams.compute_fiber_direction`,
            :meth:`~superdex.physics.ActiveAnisoArapMaterialParams.get_theta`
        """
    @staticmethod
    def compute_fiber_direction(theta: float, phi: float) -> Real3:
        """Computes the fiber direction unit vector from spherical angles.

        Args:
            theta (float): Azimuthal angle (θ) [rad].
            phi (float): Elevation angle (φ) [rad].

        Returns:
            Unit vector: (cos(φ)cos(θ), sin(φ), cos(φ)sin(θ)).
        """

class ActiveNeoHookeanMaterialParams:
    """Parameters for a material combining passive Neo-Hookean and active anisotropic
    terms.
    """
    passive_isotropic: NeoHookeanMaterialParams
    """Passive isotropic hyperelastic component parameters (Neo-Hookean).

    See Also:
        :class:`~superdex.physics.NeoHookeanMaterialParams`
    """
    active_anisotropic: ActiveAnisoArapMaterialParams
    """Active anisotropic fiber contraction component parameters (Active Aniso ARAP).

    See Also:
        :class:`~superdex.physics.ActiveAnisoArapMaterialParams`
    """
    @overload
    def __init__(self) -> None: ...
    @overload
    def __init__(
        self,
        passive_isotropic: NeoHookeanMaterialParams = ...,
        active_anisotropic: ActiveAnisoArapMaterialParams = ...,
    ) -> None: ...

class ArapMaterialParams:
    """Parameters for an as-rigid-as-possible material model."""
    stiffness: float
    """ARAP stiffness parameter (μ) [Pa].

    Note:
        Must be greater than zero.
    """
    psd_strategy: MaterialPsdStrategy
    """Strategy for ensuring positive semi-definite Hessian matrices.

    See Also:
        :class:`~superdex.physics.MaterialPsdStrategy`
    """
    @overload
    def __init__(self) -> None: ...
    @overload
    def __init__(
        self,
        stiffness: float = ...,
        psd_strategy: MaterialPsdStrategy | int = ...,
    ) -> None: ...

class ActiveShapeTargetingArapMaterialParams:
    """Parameters for an active ARAP material targeting a preferred shape tensor."""
    stiffness: float
    """ARAP stiffness parameter (μ) [Pa].

    Note:
        Must be greater than zero.
    """
    @property
    def shape_target_tensor(self) -> Real6:
        """Shape target tensor (6 values).

        Defines the shape target tensor as a symmetric 3x3 matrix added to the identity:
        S_t = I + [[s0, s1, s2], [s1, s3, s4], [s2, s4, s5]]. The shape target tensor
        specifies the desired local shape of the object. The model then resists
        deformation from this target shape while allowing rigid rotation.
        """
    @shape_target_tensor.setter
    def shape_target_tensor(self, value: Real6Like) -> None: ...
    psd_strategy: MaterialPsdStrategy
    """Strategy for ensuring positive semi-definite Hessian matrices.

    See Also:
        :class:`~superdex.physics.MaterialPsdStrategy`
    """
    @overload
    def __init__(self) -> None: ...
    @overload
    def __init__(
        self,
        stiffness: float = ...,
        shape_target_tensor: Real6Like = ...,
        psd_strategy: MaterialPsdStrategy | int = ...,
    ) -> None: ...

class SoftMaterialParams:
    """Parameters for a homogeneous soft material."""
    type: SoftMaterialType
    """Material constitutive model."""
    neo_hookean: NeoHookeanMaterialParams
    """Parameters for the Neo-Hookean material model."""
    st_venant_kirchhoff: StVenantKirchhoffMaterialParams
    """Parameters for the St. Venant-Kirchhoff material model."""
    linear_elastic: LinearElasticMaterialParams
    """Parameters for the Linear Elastic material model."""
    active_neo_hookean: ActiveNeoHookeanMaterialParams
    """Parameters for the Active Neo-Hookean material model."""
    active_shape_targeting_arap: ActiveShapeTargetingArapMaterialParams
    """Parameters for the Active Shape Targeting ARAP material model."""
    arap: ArapMaterialParams
    """Parameters for the ARAP material model."""
    density: float
    """Material density in the undeformed configuration [kg/m³]. Must be positive."""
    mass_damping_coefficient: float
    """Mass damping coefficient [1/s]. Applies a velocity-proportional force `α·M·v`.

    Must be non-negative. Active only when the actor has inertia. A nonzero value
    with no inertia is valid but inactive (logged as a warning at actor
    initialization).
    """
    stiffness_damping_coefficient: float
    """Stiffness damping coefficient `β` [s]. Adds a strain-rate-proportional viscous
    stress.

    Must be non-negative. Active only when the actor has stress. A nonzero value
    with no stress is valid but inactive (logged as a warning at actor
    initialization).

    This is a total-Lagrangian variant of Kelvin–Voigt damping: it adds a viscous
    second Piola–Kirchhoff stress

    ::

          S_visc = β · C₀ : Ė

    where `Ė` is the material time derivative of the Green–Lagrange strain, and `C₀
    = ∂²Ψ/∂E²` is the Lagrangian material stiffness evaluated at zero deformation.

    Note:
        While the parameterization in terms of a timescale β is similar, this
        differs from standard Rayleigh damping using the global assembled stiffness
        matrix; it depends only on strain rate, therefore dissipating energy only
        during deformation.
    """
    stiffness_damping_include_geometric_term: bool
    """[Experimental] Include the geometric term in the stiffness-damping tangent.
    Defaults to `false`.

    Only affects the Jacobian for Newton iteration, not the energy, residual, or
    converged solution. When `false` (default), the viscous tangent uses a cheaper
    quasi-Newton approximation. When `true`, the Newton Jacobian includes a
    geometric term, which is proportional to the per-stage strain increment and may
    improve nonlinear solver convergence in certain scenarios.

    Warning:
        This is an experimental feature. It may be changed or removed in the future.
        Use at your own risk.
    """
    @overload
    def __init__(self) -> None: ...
    @overload
    def __init__(
        self,
        type: SoftMaterialType | int = ...,
        neo_hookean: NeoHookeanMaterialParams = ...,
        st_venant_kirchhoff: StVenantKirchhoffMaterialParams = ...,
        linear_elastic: LinearElasticMaterialParams = ...,
        active_neo_hookean: ActiveNeoHookeanMaterialParams = ...,
        active_shape_targeting_arap: ActiveShapeTargetingArapMaterialParams = ...,
        arap: ArapMaterialParams = ...,
        density: float = ...,
        mass_damping_coefficient: float = ...,
        stiffness_damping_coefficient: float = ...,
        stiffness_damping_include_geometric_term: bool = ...,
    ) -> None: ...

class PerElementSoftMaterialData:
    """Per-element soft material data for heterogeneous materials.

    See Also:
        :class:`~superdex.physics.PerElementSoftMaterialDataView`
    """
    type: SoftMaterialType
    """Material constitutive model."""
    psd_strategy: MaterialPsdStrategy
    """Strategy for ensuring positive semi-definite Hessian matrices.

    See Also:
        :class:`~superdex.physics.MaterialPsdStrategy`
    """
    @property
    def youngs_modulus(self) -> DynamicArrayReal:
        """Young's modulus [Pa]. 1 per element."""
    @youngs_modulus.setter
    def youngs_modulus(self, value: ArrayLikeReal) -> None: ...
    @property
    def poisson_ratio(self) -> DynamicArrayReal:
        """Poisson's ratio (dimensionless). 1 per element."""
    @poisson_ratio.setter
    def poisson_ratio(self, value: ArrayLikeReal) -> None: ...
    @property
    def aniso_alpha(self) -> DynamicArrayReal:
        """Anisotropic stiffness (α) [Pa]. 1 per element."""
    @aniso_alpha.setter
    def aniso_alpha(self, value: ArrayLikeReal) -> None: ...
    @property
    def aniso_length(self) -> DynamicArrayReal:
        """Anisotropic reference length (dimensionless). 1 per element."""
    @aniso_length.setter
    def aniso_length(self, value: ArrayLikeReal) -> None: ...
    @property
    def aniso_theta(self) -> DynamicArrayReal:
        """Fiber azimuthal angle (θ) [rad]. 1 per element."""
    @aniso_theta.setter
    def aniso_theta(self, value: ArrayLikeReal) -> None: ...
    @property
    def aniso_phi(self) -> DynamicArrayReal:
        """Fiber elevation angle (φ) [rad]. 1 per element."""
    @aniso_phi.setter
    def aniso_phi(self, value: ArrayLikeReal) -> None: ...
    @property
    def arap_stiffness(self) -> DynamicArrayReal:
        """ARAP stiffness (μ) [Pa]. 1 per element."""
    @arap_stiffness.setter
    def arap_stiffness(self, value: ArrayLikeReal) -> None: ...
    @property
    def shape_target_tensor(self) -> DynamicArrayReal:
        """Shape target tensor offset in flat symmetric upper-triangle layout.

        Six values per element. Defines S_t = I + [[s0, s1, s2], [s1, s3, s4], [s2, s4,
        s5]].
        """
    @shape_target_tensor.setter
    def shape_target_tensor(self, value: ArrayLikeReal) -> None: ...
    @overload
    def __init__(self) -> None: ...
    @overload
    def __init__(
        self,
        type: SoftMaterialType | int = ...,
        psd_strategy: MaterialPsdStrategy | int = ...,
        youngs_modulus: ArrayLikeReal = ...,
        poisson_ratio: ArrayLikeReal = ...,
        aniso_alpha: ArrayLikeReal = ...,
        aniso_length: ArrayLikeReal = ...,
        aniso_theta: ArrayLikeReal = ...,
        aniso_phi: ArrayLikeReal = ...,
        arap_stiffness: ArrayLikeReal = ...,
        shape_target_tensor: ArrayLikeReal = ...,
    ) -> None: ...
    @overload
    def __init__(self, src: PerElementSoftMaterialDataView) -> None:
        """Copy from :class:`~superdex.physics.PerElementSoftMaterialDataView`.

        Args:
            src (PerElementSoftMaterialDataView): Source data.
        """
    def __eq__(self, other: object) -> bool: ...
    def __ne__(self, other: object) -> bool: ...
    def get_num_elements(self) -> int:
        """Number of elements described by this per-element material field.

        Derived from the per-element array appropriate to
        :attr:`~superdex.physics.PerElementSoftMaterialData.type`:
        :attr:`~superdex.physics.PerElementSoftMaterialData.arap_stiffness` for the
        ARAP-family materials (:class:`ARAP <superdex.physics.SoftMaterialType>` and
        :class:`ACTIVE_SHAPE_TARGETING_ARAP <superdex.physics.SoftMaterialType>`), and
        :attr:`~superdex.physics.PerElementSoftMaterialData.poisson_ratio` for the
        Lame-based materials.

        Returns:
            Number of elements.
        """

class PerElementSoftMaterialDataView:
    """Non-owning view of per-element soft material data.

    See Also:
        :class:`~superdex.physics.PerElementSoftMaterialData`
    """
    type: SoftMaterialType
    """Material constitutive model."""
    psd_strategy: MaterialPsdStrategy
    """Strategy for ensuring positive semi-definite Hessian matrices.

    See Also:
        :class:`~superdex.physics.MaterialPsdStrategy`
    """
    @property
    def youngs_modulus(self) -> SpanConstReal:
        """Young's modulus [Pa]. 1 per element."""
    @youngs_modulus.setter
    def youngs_modulus(self, value: ArrayLikeReal) -> None: ...
    @property
    def poisson_ratio(self) -> SpanConstReal:
        """Poisson's ratio (dimensionless). 1 per element."""
    @poisson_ratio.setter
    def poisson_ratio(self, value: ArrayLikeReal) -> None: ...
    @property
    def aniso_alpha(self) -> SpanConstReal:
        """Anisotropic stiffness (α) [Pa]. 1 per element."""
    @aniso_alpha.setter
    def aniso_alpha(self, value: ArrayLikeReal) -> None: ...
    @property
    def aniso_length(self) -> SpanConstReal:
        """Anisotropic reference length (dimensionless). 1 per element."""
    @aniso_length.setter
    def aniso_length(self, value: ArrayLikeReal) -> None: ...
    @property
    def aniso_theta(self) -> SpanConstReal:
        """Fiber azimuthal angle (θ) [rad]. 1 per element."""
    @aniso_theta.setter
    def aniso_theta(self, value: ArrayLikeReal) -> None: ...
    @property
    def aniso_phi(self) -> SpanConstReal:
        """Fiber elevation angle (φ) [rad]. 1 per element."""
    @aniso_phi.setter
    def aniso_phi(self, value: ArrayLikeReal) -> None: ...
    @property
    def arap_stiffness(self) -> SpanConstReal:
        """ARAP stiffness (μ) [Pa]. 1 per element."""
    @arap_stiffness.setter
    def arap_stiffness(self, value: ArrayLikeReal) -> None: ...
    @property
    def shape_target_tensor(self) -> SpanConstReal:
        """Shape target tensor offset in flat symmetric upper-triangle layout.

        Six values per element. Defines S_t = I + [[s0, s1, s2], [s1, s3, s4], [s2, s4,
        s5]].
        """
    @shape_target_tensor.setter
    def shape_target_tensor(self, value: ArrayLikeReal) -> None: ...
    @overload
    def __init__(self) -> None: ...
    @overload
    def __init__(
        self,
        type: SoftMaterialType | int = ...,
        psd_strategy: MaterialPsdStrategy | int = ...,
        youngs_modulus: ArrayLikeReal = ...,
        poisson_ratio: ArrayLikeReal = ...,
        aniso_alpha: ArrayLikeReal = ...,
        aniso_length: ArrayLikeReal = ...,
        aniso_theta: ArrayLikeReal = ...,
        aniso_phi: ArrayLikeReal = ...,
        arap_stiffness: ArrayLikeReal = ...,
        shape_target_tensor: ArrayLikeReal = ...,
    ) -> None: ...
    @overload
    def __init__(self, src: PerElementSoftMaterialData) -> None:
        """Implicit conversion from
        :class:`~superdex.physics.PerElementSoftMaterialData`.

        Args:
            src (PerElementSoftMaterialData): Source data.
        """
    def __eq__(self, other: object) -> bool: ...
    def __ne__(self, other: object) -> bool: ...

class ContactParams:
    """Parameters for contact mechanics simulation.

    Note:
        For each field without an actor-pair override, contact between a colliding
        actor and a collider uses the collider's contact parameter (not the
        colliding actor's). The exceptions are: - For friction and dissipation
        coefficients (viscousFrictionCoefficient, coulombFrictionCoefficient,
        normalViscousDampingCoefficient), the geometric mean of the colliding and
        collider's coefficients is used. This disables friction/dissipation if
        either of them does. - For penalty coefficient (penaltyCoefficient) and
        friction velocity threshold (frictionFalloffVel), the geometric mean of the
        colliding and collider's values is used, except if the collider is static in
        which case the colliding's values are used.

    See Also:
        :meth:`~superdex.physics.Scene.set_contact_pair_params_override`
    """
    penalty_coefficient: float
    """Stiffness of the contact penalty force [Pa/m].

    Note:
        Must be finite and strictly positive.

    Note:
        Higher penalties create stiffer contacts and reduce penetration.

    Note:
        Arbitrarily large penalties may degrade stability.

    Note:
        The default penalty is appropriate for actors with default density. For
        actors with much higher/lower density than the default density, the penalty
        coefficient may need to be increased/decreased accordingly.

    Note:
        Without an actor-pair override for this field, the value used in a collision
        is the geometric mean of the colliding and collider's coefficients. The
        exception is if the collider is static, in which case the colliding's
        penalty is used.

    Note:
        The penalty coefficient is additionally scaled by length-scale corrections
        when the colliding or collider integrates contact over a non-2D manifold
        (e.g., rod, shell). See
        :attr:`~superdex.physics.ContactParams.colliding_penalty_length_scale`.
    """
    penalty_smoothing_half_distance: float
    """PolyReLU smoothing half-width [m].

    The penalty force transitions from 0 to linear over the decreasing distance
    range (penaltyThreshold, penaltyThreshold - 2 *
    :attr:`~superdex.physics.ContactParams.penalty_smoothing_half_distance`).

    Note:
        Must be finite and not negative.

    Note:
        Larger smoothing distances improve stability but may increase penetration.

    Note:
        Smoothing distance is expected to be small relative to the collider
        geometry.
    """
    penalty_threshold_default: float
    """Default contact detection threshold [m].

    The penalty force transitions from 0 to linear over the decreasing distance
    range (penaltyThreshold, penaltyThreshold - 2 *
    :attr:`~superdex.physics.ContactParams.penalty_smoothing_half_distance`).

    Note:
        Must be finite. Negative values are legal.

    Note:
        If the colliding actor has :class:`NONE <superdex.physics.ColliderType>` or
        :class:`POINT_CLOUD <superdex.physics.ColliderType>`, penaltyThreshold =
        penaltyThresholdDefault +
        :attr:`~superdex.physics.ContactParams.penalty_threshold_extra_padding`.
        Otherwise, penaltyThreshold = penaltyThresholdDefault.

    See Also:
        :attr:`~superdex.physics.ContactParams.penalty_threshold_extra_padding`,
        :meth:`~superdex.physics.ContactParams.get_penalty_threshold_dist`
    """
    penalty_threshold_extra_padding: float
    """Extra padding [m] added to the default contact detection threshold if the
    colliding actor has :class:`NONE <superdex.physics.ColliderType>` or
    :class:`POINT_CLOUD <superdex.physics.ColliderType>`.

    Note:
        Must be finite and not negative.

    Note:
        Extra padding is useful to avoid tunneling through thin actors when the
        other actor has :class:`NONE <superdex.physics.ColliderType>` or
        :class:`POINT_CLOUD <superdex.physics.ColliderType>`.

    See Also:
        :attr:`~superdex.physics.ContactParams.penalty_threshold_default`,
        :meth:`~superdex.physics.ContactParams.get_penalty_threshold_dist`
    """
    friction_with_collider_normal: bool
    """Use collider's normal (normalized SDF gradient) for friction direction if true,
    or the colliding's surface normal at the sample point if false.

    Note:
        For co-dimensional colliding actors with ambiguous normals, the collider's
        normal is always used regardless of frictionWithColliderNormal.
    """
    max_alignment_normals: float
    """Maximum normal alignment threshold.

    Normal alignment is defined as the dot product between colliding and collider
    normals. Contact is disabled for sample points whose normal alignment exceeds
    this threshold. This prevents sample points from being trapped inside the
    collider when penetration is large.

    Note:
        Must be in [-1, 1]. -1 allows contact only for perfectly opposing normals, 1
        allows all contacts.

    Note:
        For co-dimensional colliding actors with ambiguous normals, contact is not
        disabled regardless of maxAlignmentNormals (normal alignment cannot be
        computed).
    """
    viscous_friction_coefficient: float
    """Viscous friction coefficient [s/m].

    Note:
        Friction force is proportional to contact force and tangential velocity.

    Note:
        Must be finite and not negative.

    Note:
        Both viscousFrictionCoefficient and
        :attr:`~superdex.physics.ContactParams.coulomb_friction_coefficient` can be
        >0.

    Note:
        Without an actor-pair override for this field, the value used in a collision
        is the geometric mean of the colliding and collider's coefficients. This
        disables viscous friction if either of them does.
    """
    coulomb_friction_coefficient: float
    """Coulomb friction coefficient (dimensionless).

    Note:
        Must be finite and not negative.

    Note:
        Both :attr:`~superdex.physics.ContactParams.viscous_friction_coefficient`
        and coulombFrictionCoefficient can be >0.

    Note:
        Without an actor-pair override for this field, the value used in a collision
        is the geometric mean of the colliding and collider's coefficients. This
        disables Coulomb friction if either of them does.
    """
    friction_falloff_vel: float
    """Velocity threshold for Coulomb friction smoothing [m/s].

    For C1Regularized, the Coulomb friction force smoothly transitions from 0 to
    full strength as tangential velocity increases from 0 to frictionFalloffVel
    (compact support). For CinfRegularized, the force asymptotically approaches full
    strength with no compact support boundary; frictionFalloffVel controls the
    regularization scale.

    Note:
        Must be finite and not negative. For CinfRegularized, a value of zero is
        clamped internally to avoid numerical issues.

    Note:
        Smaller velocity thresholds improve physical accuracy but may degrade
        stability.

    Note:
        Without an actor-pair override for this field, the value used in a collision
        is the geometric mean of the colliding and collider's thresholds. The
        exception is if the collider is static, in which case the colliding's
        threshold is used.
    """
    normal_viscous_damping_coefficient: float
    """Normal viscous damping coefficient [s/m].

    Damping force in the normal direction, proportional to the elastic normal
    contact force and the normal velocity. Analogous to
    :attr:`~superdex.physics.ContactParams.viscous_friction_coefficient` but acting
    in the normal direction instead of tangentially.

    Note:
        Must be finite and not negative.

    Note:
        Without an actor-pair override for this field, the value used in a collision
        is the geometric mean of the colliding and collider's coefficients. This
        disables normal damping if either of them does.

    Note:
        The resulting coefficient of restitution (CoR) is velocity-dependent. For a
        characteristic impact velocity,
        :func:`~superdex.physics.experimental.calibrate_normal_viscous_damping_coefficient`
        computes the coefficient that approximates a target CoR, while
        :func:`~superdex.physics.experimental.effective_coefficient_of_restitution`
        recovers the CoR produced by a coefficient.

    Note:
        Because the damping force depends on the normal velocity at each contact
        point, it also introduces rolling resistance: a body rolling on a surface
        dissipates energy through the differing normal velocities across its
        contacting region.

    Warning:
        The calibration diverges as CoR approaches zero, which may cause numerical
        problems when approaching fully-inelastic collisions.
    """
    distance_error_bound: float
    """[Experimental] Padding [m] added to the collider's bounding volume for collision
    culling.

    Note:
        Must be finite.

    Note:
        Useful, for example, with approximate SDFs (e.g., deep flow map) to
        compensate for potentially overestimating the true distance.

    Warning:
        This is an experimental feature. It may be changed or removed in the future.
        Use at your own risk.
    """
    obj_scale: float
    """[Experimental] Object scale relative to default size (dimensionless). Used by
    deep flow only.

    Note:
        Must be finite and strictly positive.

    Warning:
        Deep flow is an experimental feature. It may be changed or removed in the
        future. Use at your own risk.
    """
    colliding_penalty_length_scale: float
    """[Experimental] Length scale [m] used to correct the penalty coefficient when the
    colliding body integrates contact traction over a lower-than-two-dimensional
    manifold, such as a rod or point mass. E.g., the penalty is scaled by this value
    if the colliding body lumps contact tractions on a line, or this value squared
    if lumping contact forces on a point.

    Note:
        Must be finite and strictly positive.

    Note:
        This value is not used in the most common case, where contact traction is
        integrated over a two-dimensional surface.

    Note:
        The colliding body's value is always used in a contact pair, because the
        colliding body determines the dimension of the contact traction integral.

    Warning:
        Contact with lower-dimensional bodies is an experimental feature. It may be
        changed or removed in the future. Use at your own risk.
    """
    @overload
    def __init__(self) -> None: ...
    @overload
    def __init__(
        self,
        penalty_coefficient: float = ...,
        penalty_smoothing_half_distance: float = ...,
        penalty_threshold_default: float = ...,
        penalty_threshold_extra_padding: float = ...,
        friction_with_collider_normal: bool = ...,
        max_alignment_normals: float = ...,
        viscous_friction_coefficient: float = ...,
        coulomb_friction_coefficient: float = ...,
        friction_falloff_vel: float = ...,
        normal_viscous_damping_coefficient: float = ...,
        distance_error_bound: float = ...,
        obj_scale: float = ...,
        colliding_penalty_length_scale: float = ...,
    ) -> None: ...
    def get_penalty_threshold_dist(self, add_padding: bool) -> float:
        """Get the total contact detection threshold.

        Args:
            add_padding (bool): If true, includes
                :attr:`~superdex.physics.ContactParams.penalty_threshold_extra_padding`.

        Returns:
            Total contact detection threshold [m].
        """

class ArticulatedJointFrictionParams:
    """Per-joint friction parameters for articulated bodies.

    Viscous and dry friction force/torque contributions are added together. -
    Viscous friction force/torque is proportional to velocity with coefficient
    :attr:`viscous`. - The static-friction peak/breakaway force/torque is given by
    :attr:`coulomb` + :attr:`stiction_extra`. Static friction is regularized to
    allow slight slippage at speeds of up to :attr:`falloff_vel`. - Above the
    smoothing threshold ``|v|`` > :attr:`falloff_vel`, dynamic dry friction
    force/torque (excluding additional viscous friction) has magnitude
    :attr:`coulomb` + :attr:`stiction_extra` * exp(-pow((``|v|`` -
    :attr:`falloff_vel`) / :attr:`stribeck_vel`, 2)) when :attr:`stribeck_vel` is
    positive. If it is zero, the Stribeck term is omitted and the magnitude is
    :attr:`coulomb`. Here ``|v|`` is the magnitude of the relative (linear or
    angular) velocity. This causes friction to decrease smoothly from the peak
    static value to :attr:`coulomb` at high velocities.
    """
    viscous: float
    """Viscous friction coefficient [N·s/m or N·m·s/rad].

    Note:
        Default is zero (no viscous friction).
    """
    coulomb: float
    """Coulomb friction coefficient [N or N·m].

    Note:
        Default is zero (no Coulomb friction).
    """
    falloff_vel: float
    """Velocity threshold for dry friction smoothing [m/s or rad/s].

    The dry friction force/torque smoothly transitions from 0 to the peak value as
    (linear or angular) velocity increases from 0 to
    :attr:`~superdex.physics.ArticulatedJointFrictionParams.falloff_vel`.

    Note:
        Smaller velocity thresholds improve physical accuracy but may degrade
        stability.
    """
    stiction_extra: float
    """[Experimental] Extra stiction force/torque [N or N·m] representing the
    difference between static and dynamic friction.

    Note:
        Must not be negative. The
        :attr:`~superdex.physics.ArticulatedJointFrictionParams.coulomb` represents
        dynamic friction in the high-velocity limit, and peak static friction is
        :attr:`~superdex.physics.ArticulatedJointFrictionParams.coulomb` +
        :attr:`~superdex.physics.ArticulatedJointFrictionParams.stiction_extra`.

    Note:
        Defaults to zero (no difference between static and dynamic friction).

    Warning:
        This is an experimental feature. It may be changed or removed in the future.
        Use at your own risk.

    Warning:
        Nonzero values may harm convergence.

    Warning:
        Nonzero extra stiction with zero Stribeck velocity results in a nonsmooth
        force.
    """
    stribeck_vel: float
    """[Experimental] Stribeck velocity [m/s or rad/s] governing how sharply friction
    transitions from static to dynamic coefficients as velocity increases.

    Note:
        Must not be negative. A smaller value means a sharper transition.

    Note:
        Defaults to zero.

    Note:
        This has no effect if
        :attr:`~superdex.physics.ArticulatedJointFrictionParams.stiction_extra` is
        zero.

    Warning:
        This is an experimental feature. It may be changed or removed in the future.
        Use at your own risk.

    Warning:
        Nonzero values may harm convergence.

    Warning:
        Nonzero extra stiction with zero Stribeck velocity results in a nonsmooth
        force.
    """
    @overload
    def __init__(self) -> None: ...
    @overload
    def __init__(
        self,
        viscous: float = ...,
        coulomb: float = ...,
        falloff_vel: float = ...,
        stiction_extra: float = ...,
        stribeck_vel: float = ...,
    ) -> None: ...
    def __eq__(self, other: object) -> bool: ...
    def __ne__(self, other: object) -> bool: ...

class ArticulatedDofInfo:
    """Degree-of-freedom layout information for one articulated joint."""
    offset: int
    """Offset of this joint's DoFs in the flattened DoF array."""
    trans_size: int
    """Number of translational DoFs for this joint."""
    rot_size: int
    """Number of rotational DoFs for this joint."""
    @overload
    def __init__(self) -> None: ...
    @overload
    def __init__(
        self,
        offset: int = ...,
        trans_size: int = ...,
        rot_size: int = ...,
    ) -> None: ...
    def get_trans_offset(self) -> int:
        """Offset of this joint's translational DoFs in the flattened DoF array.

        Returns:
            The translational DoF offset (equal to
            :attr:`~superdex.physics.ArticulatedDofInfo.offset`).
        """
    def get_rot_offset(self) -> int:
        """Offset of this joint's rotational DoFs in the flattened DoF array.

        Returns:
            The rotational DoF offset
            (:attr:`~superdex.physics.ArticulatedDofInfo.offset` +
            :attr:`~superdex.physics.ArticulatedDofInfo.trans_size`).
        """
    def get_size(self) -> int:
        """Total number of DoFs for this joint.

        Returns:
            The total DoF count (:attr:`~superdex.physics.ArticulatedDofInfo.trans_size`
            + :attr:`~superdex.physics.ArticulatedDofInfo.rot_size`).
        """

class SaturationHessianParams:
    """[Experimental] Controls whether force-saturation terms use a fitted quadratic
    Hessian independently for each saturation pathway.

    For each flag, `true` selects the fitted Hessian, which is more stable. `false`
    selects the exact analytical Hessian, which may converge faster but is less
    stable.

    Note:
        When a flag is `false`, the solver will try first with the true Hessian for
        that pathway. If the Newton iteration fails to improve, all flags are set to
        `true` and the iteration is retried with fitted Hessians for all pathways.

    Warning:
        This is an experimental feature. It may be changed or removed in the future.
        Use at your own risk.
    """
    contact_friction: bool
    """Use fitted Hessian for contact-friction saturation."""
    joint_friction: bool
    """Use fitted Hessian for joint-friction saturation."""
    constraint_saturation: bool
    """Use fitted Hessian for constraint saturation."""
    @overload
    def __init__(self) -> None: ...
    @overload
    def __init__(
        self,
        contact_friction: bool = ...,
        joint_friction: bool = ...,
        constraint_saturation: bool = ...,
    ) -> None: ...
    @staticmethod
    def all(value: bool) -> SaturationHessianParams:
        """Construct with all three flags set to the given value.

        Args:
            value (bool): Value assigned to all three flags.

        Returns:
            A :class:`~superdex.physics.SaturationHessianParams` with every flag set to
            ``value``.
        """
    def all_true(self) -> bool:
        """Return true iff all three flags are true.

        Returns:
            True iff all three flags are true.
        """

class ExperimentalEvalParams:
    """[Experimental] Evaluation settings common to the full scene.

    They tune the evaluation of internal models (contact, constraints, etc).

    Warning:
        This is an experimental feature. It may be changed or removed in the future.
        Use at your own risk.
    """
    explicit_normals: bool
    """Use explicit normals (from stage-start kinematics) for the evaluation of
    friction.

    Whether to treat the colliding and collider normals explicitly (using
    stage-start kinematics) or implicitly (using current kinematics) for the
    evaluation of alignment and the friction plane.

    Note:
        The SDF gradient for the normal collision force is always implicit.

    Note:
        explicitNormals = true and implicitNormalForceForDissipation = false
        produces contact residuals that are the exact gradients of the contact
        merit. This improves convergence guarantees of the Newton solve, but may be
        less stable due to the explicit treatment.

    Note:
        A differentiable scene requires explicitNormals = true.
    """
    fade_friction: bool
    """Fade friction coefficient based on normal alignment.

    When enabled, friction is scaled by (maxAlignmentNormals - alignment) /
    (maxAlignmentNormals + 1), where "alignment" is defined as the dot product
    between colliding and collider normals.

    Note:
        For co-dimensional colliding actors with ambiguous normals, friction fading
        is disabled regardless of fadeFriction (normal alignment cannot be
        computed).

    Note:
        fadeFriction = true adds a non-integrable term to the residual unless
        :attr:`~superdex.physics.ExperimentalEvalParams.explicit_normals` = true.

    See Also:
        :attr:`~superdex.physics.ContactParams.max_alignment_normals`
    """
    implicit_normal_force_for_dissipation: bool
    """Treat the normal contact force implicitly (if true) or explicitly (if false) for
    dissipative contact terms.

    Note:
        Implicit treatment improves stability (especially with high-order time
        integrators) but the resulting dissipative force is not integrable, hence it
        cannot be derived from an objective function. This (a) may hurt convergence
        with an objective-based line search, and (b) makes the force dresidual
        non-symmetric (a symmetric approximation is used). With
        implicitNormalForceForDissipation = false, the distance used for the
        explicit normal force is approximate when
        :attr:`~superdex.physics.ExperimentalEvalParams.explicit_normals` is false,
        but accurate when it is true.

    Note:
        Dissipative contact terms include Coulomb friction, viscous friction, and
        normal viscous damping. Using an implicit normal force for normal damping
        can improve the accuracy of the effective coefficient of restitution at a
        given time step size.
    """
    fitted_saturation_hessian: SaturationHessianParams
    """Controls whether force-saturation terms use a fitted quadratic Hessian (more
    stable) or the exact analytical Hessian (faster convergence but less stable),
    independently for each saturation pathway.

    Note:
        For any flag set to false, the solver will try first with the true Hessian.
        If it fails, it will retry with the fitted Hessian.
    """
    friction_model: CoulombFrictionModel
    """Selects which Coulomb friction smoothing model to use.

    C1Regularized (default) preserves existing behavior exactly. CinfRegularized is
    an additional model that can be selected at runtime without affecting
    C1Regularized users.
    """
    consistency_res_norm: bool
    """Validate that the residual is the gradient of the objective by a directional
    finite-difference consistency check.

    Note:
        This operation is expensive and should only be used for debugging.
    """
    consistency_res_norm_step: float
    """Step size for finite-difference consistency check."""
    @overload
    def __init__(self) -> None: ...
    @overload
    def __init__(
        self,
        explicit_normals: bool = ...,
        fade_friction: bool = ...,
        implicit_normal_force_for_dissipation: bool = ...,
        fitted_saturation_hessian: SaturationHessianParams = ...,
        friction_model: CoulombFrictionModel | int = ...,
        consistency_res_norm: bool = ...,
        consistency_res_norm_step: float = ...,
    ) -> None: ...

class NonLinearSolverParams:
    """Parameters for non-linear implicit solves."""
    solver_type: NonLinearSolverType
    """Non-linear solver type."""
    d_residual_assembly_period: int
    """Every how many non-linear iterations to assemble the dresidual matrix.

    Note:
        For Newton's method, set to >1 to reuse the dresidual across iterations.

    Note:
        For quasi-Newton methods (e.g., BFGS, SR1), it must be >1 and indicates
        every how many iterations to restart the algorithm with the actual
        dresidual.
    """
    max_iter: int
    """Maximum number of non-linear iterations."""
    max_elapsed_time_seconds: float
    """Maximum elapsed time [s].

    Note:
        The solve terminates if the elapsed time exceeds this threshold.

    Note:
        0 means no time limit.
    """
    convergence_mode: NonLinearSolverConvergenceMode
    """Convergence monitoring mode.

    Note:
        Applies to :attr:`~superdex.physics.NonLinearSolverParams.abs_tol` and
        :attr:`~superdex.physics.NonLinearSolverParams.rel_tol`.
    """
    abs_tol: float
    """Absolute residual norm tolerance for convergence."""
    rel_tol: float
    """Relative residual norm tolerance for convergence, relative to the initial
    residual.
    """
    rel_step_tol: float
    """Relative tolerance on the L2 norm of the raw linear-solve increment before line
    search scaling.

    Note:
        The solve terminates with :class:`STOPPED
        <superdex.physics.ConvergenceStatus>` status if ``|dx|/|x|`` is below this
        threshold (i.e., the step norm is below this fraction of the current
        solution norm).

    Note:
        0 disables this criterion.

    Note:
        Default is :const:`~superdex.physics.DEFAULT_REL_STEP_TOL`.
    """
    stop_if_no_improvement: bool
    """Stop the solve if the line search figure of merit does not improve from the
    previous iteration.
    """
    psd_proj_mode: PsdProjectionMode
    """Positive Semi-Definite (PSD) projection mode for the dresidual matrix."""
    gradient_descent_fallback: bool
    """Fall back to gradient descent direction if the solver search direction fails."""
    explosion_control: bool
    """Enable heuristic explosion prevention."""
    abs_div_tol: float
    """Absolute divergence tolerance.

    Note:
        Triggers explosion control if residual norm exceeds this value.

    Note:
        Used only if
        :attr:`~superdex.physics.NonLinearSolverParams.explosion_control` is true.
    """
    rel_div_tol: float
    """Relative divergence tolerance, relative to the initial residual.

    Note:
        Triggers explosion control if relative residual norm exceeds this value.

    Note:
        Used only if
        :attr:`~superdex.physics.NonLinearSolverParams.explosion_control` is true.
    """
    line_search_max_iter: int
    """Maximum number of line search iterations.

    Note:
        Must be >= 1 unless
        :attr:`~superdex.physics.NonLinearSolverParams.line_search_type` is
        :class:`NONE <superdex.physics.LineSearchType>`.
    """
    line_search_alpha: float
    """Step length reduction factor for line search.

    Note:
        Must be in (0, 1).
    """
    line_search_wolfe1: float
    """Wolfe condition parameter c1 (sufficient decrease).

    Note:
        Must be in (0, 1).

    Note:
        Used only by :class:`ARMIJO <superdex.physics.LineSearchType>`,
        :class:`WOLFE_WEAK <superdex.physics.LineSearchType>`, :class:`WOLFE_STRONG
        <superdex.physics.LineSearchType>`, and :class:`ARMIJO_OR_RESIDUAL_NORM
        <superdex.physics.LineSearchType>` line search types.
    """
    line_search_wolfe2: float
    """Wolfe condition parameter c2 (curvature).

    Note:
        Must be in
        (:attr:`~superdex.physics.NonLinearSolverParams.line_search_wolfe1`, 1).

    Note:
        Used only by :class:`WOLFE_WEAK <superdex.physics.LineSearchType>` and
        :class:`WOLFE_STRONG <superdex.physics.LineSearchType>` line search types.
    """
    line_search_max_rel_increase: float
    """Maximum relative increase in the objective to accept the step.

    Note:
        Only applies to :class:`SIMPLE <superdex.physics.LineSearchType>`.
    """
    line_search_type: LineSearchType
    """Line search type."""
    linear_tolerance_strategy: LinearToleranceStrategy
    """Strategy for adaptive linear solver tolerance (forcing term)."""
    verbosity: VerbosityLevel
    """Verbosity level for logging output."""
    @overload
    def __init__(self) -> None: ...
    @overload
    def __init__(
        self,
        solver_type: NonLinearSolverType | int = ...,
        d_residual_assembly_period: int = ...,
        max_iter: int = ...,
        max_elapsed_time_seconds: float = ...,
        convergence_mode: NonLinearSolverConvergenceMode | int = ...,
        abs_tol: float = ...,
        rel_tol: float = ...,
        rel_step_tol: float = ...,
        stop_if_no_improvement: bool = ...,
        psd_proj_mode: PsdProjectionMode | int = ...,
        gradient_descent_fallback: bool = ...,
        explosion_control: bool = ...,
        abs_div_tol: float = ...,
        rel_div_tol: float = ...,
        line_search_max_iter: int = ...,
        line_search_alpha: float = ...,
        line_search_wolfe1: float = ...,
        line_search_wolfe2: float = ...,
        line_search_max_rel_increase: float = ...,
        line_search_type: LineSearchType | int = ...,
        linear_tolerance_strategy: LinearToleranceStrategy | int = ...,
        verbosity: VerbosityLevel | int = ...,
    ) -> None: ...

class LinearSolverParams:
    """Parameters for linear solves."""
    solver_type: LinearSolverType
    """Linear solver type."""
    preconditioner_type: PreconditionerType
    """Preconditioner type.

    Note:
        Applies only to iterative solvers.
    """
    norm_type: LinearSolverConvergenceNorm
    """Norm used for the stopping criteria.

    Note:
        Applies only to the solvers documented in
        :class:`~superdex.physics.LinearSolverConvergenceNorm`.
    """
    abs_tol: float
    """Absolute residual norm tolerance for convergence.

    Note:
        Applies only to iterative solvers.
    """
    rel_tol: float
    """Relative residual norm tolerance for convergence, relative to the initial
    residual.

    Note:
        Applies only to iterative solvers.

    Note:
        Ignored when using :class:`EISENSTAT_WALKER1
        <superdex.physics.LinearToleranceStrategy>` or :class:`EISENSTAT_WALKER2
        <superdex.physics.LinearToleranceStrategy>` in the non-linear solver.

    Note:
        For :class:`EISENSTAT_WALKER3 <superdex.physics.LinearToleranceStrategy>`,
        this is used as a lower bound on the adaptive tolerance in every non-linear
        iteration.
    """
    rel_div_tol: float
    """Relative residual norm tolerance for divergence, relative to the initial
    residual.

    Note:
        Applies only to iterative solvers.
    """
    max_iter: int
    """Maximum number of iterations for iterative solvers.

    Note:
        Applies only to iterative solvers.

    Note:
        Must be positive or :const:`~superdex.physics.AUTO_LINEAR_SOLVER_MAX_ITER`.

    Note:
        :const:`~superdex.physics.AUTO_LINEAR_SOLVER_MAX_ITER` lets Mochi select the
        maximum number of iterations based on the problem.
    """
    restart_size: int
    """Krylov subspace dimension before restarting.

    Note:
        Applies only to GMRES solvers (e.g., :class:`GMRES
        <superdex.physics.LinearSolverType>`, :class:`CUDA_GMRES
        <superdex.physics.LinearSolverType>`).
    """
    abort_if_not_spd: bool
    """Abort solve if matrix is not SPD.

    Note:
        Applies only to iterative SPD solvers (e.g., :class:`CG
        <superdex.physics.LinearSolverType>`, :class:`CUDA_CG
        <superdex.physics.LinearSolverType>`, :class:`AUGMENTED_CG
        <superdex.physics.LinearSolverType>`).
    """
    verbosity: VerbosityLevel
    """Verbosity level for logging output."""
    @overload
    def __init__(self) -> None: ...
    @overload
    def __init__(
        self,
        solver_type: LinearSolverType | int = ...,
        preconditioner_type: PreconditionerType | int = ...,
        norm_type: LinearSolverConvergenceNorm | int = ...,
        abs_tol: float = ...,
        rel_tol: float = ...,
        rel_div_tol: float = ...,
        max_iter: int = ...,
        restart_size: int = ...,
        abort_if_not_spd: bool = ...,
        verbosity: VerbosityLevel | int = ...,
    ) -> None: ...

class CoordinateSpace:
    """Coordinate-axis convention and linear unit scale."""
    axes: CoordinateSpaceAxes
    """Semantic direction of the positive X, Y, and Z axes."""
    units_per_meter: float
    """Length of one meter, expressed in this space's linear units.

    Note:
        Must be positive and finite. For example, 100 for a space measured in
        centimeters.
    """
    @overload
    def __init__(self) -> None: ...
    @overload
    def __init__(
        self,
        axes: CoordinateSpaceAxes | int = ...,
        units_per_meter: float = ...,
    ) -> None: ...
    def __eq__(self, other: object) -> bool: ...
    def __ne__(self, other: object) -> bool: ...
    def validate(self) -> None:
        """Validate that :attr:`~superdex.physics.CoordinateSpace.axes` is a complete
        convention and :attr:`~superdex.physics.CoordinateSpace.units_per_meter` is
        finite and positive.

        Raises:
            :class:`~superdex.physics.Error`: If an error occurs.
        """
    @staticmethod
    def default() -> CoordinateSpace:
        """Return SuperDex Physics' default coordinate space convention.

        Returns:
            X-forward, Y-left, Z-up, in meters.
        """
    @staticmethod
    def unreal() -> CoordinateSpace:
        """Return Unreal Engine's coordinate space convention.

        Returns:
            X-forward, Y-right, Z-up, in centimeters.
        """
    @staticmethod
    def unity() -> CoordinateSpace:
        """Return Unity's coordinate space convention.

        Returns:
            X-right, Y-up, Z-forward, in meters.
        """
    @staticmethod
    def open_gl() -> CoordinateSpace:
        """Return OpenGL's coordinate space convention.

        Returns:
            X-right, Y-up, Z-backward, in meters.
        """
    @staticmethod
    def filament() -> CoordinateSpace:
        """Return Filament's coordinate space convention.

        Returns:
            X-right, Y-up, Z-backward, in meters.
        """
    def get_right(self) -> Real3:
        """Return the unit vector pointing right, in this space's own coordinates.

        Returns:
            The unit vector pointing right, in this space's own coordinates.
        """
    def get_up(self) -> Real3:
        """Return the unit vector pointing up, in this space's own coordinates.

        Returns:
            The unit vector pointing up, in this space's own coordinates.
        """
    def get_forward(self) -> Real3:
        """Return the unit vector pointing forward, in this space's own coordinates.

        Returns:
            The unit vector pointing forward, in this space's own coordinates.
        """

REAL3_ZEROS: Real3
"""The zero vector (0, 0, 0)."""

REAL3_ONES: Real3
"""The ones vector (1, 1, 1)."""

REAL3_X_AXIS: Real3
"""Unit vector along the X axis (1, 0, 0)."""

REAL3_Y_AXIS: Real3
"""Unit vector along the Y axis (0, 1, 0)."""

REAL3_Z_AXIS: Real3
"""Unit vector along the Z axis (0, 0, 1)."""

MINUS2_PI_PLUS2_PI: Real2
"""The range [-2π, 2π]."""

UNIT_INTERVAL: Real2
"""The unit interval [0, 1]."""

MESH_DATA_SPACE_DIM: int
"""Spatial dimensionality of mesh data."""

GRID_SDF_DEFAULT_BOUNDARY_PADDING: float
"""Default boundary padding distance [m] for grid-based SDF construction."""

DEFAULT_DENSITY: float
"""Default material density [kg/m³]."""

DEFAULT_REL_STEP_TOL: float
"""Default tolerance for the L2 norm of the non-linear solver's raw linear-solve
increment. Avoids attempting to solve beyond the floating-point noise floor.

See Also:
    :attr:`~superdex.physics.NonLinearSolverParams.rel_step_tol`
"""

AUTO_LINEAR_SOLVER_MAX_ITER: int
"""Sentinel value to automatically select the maximum number of linear solver
iterations.
"""

class Handle:
    """Generic 64-bit handle providing base functionality for strongly-typed handles."""
    type ValueType = int
    """Raw 64-bit identifier type."""
    type ValueTypeLike = int
    value: Handle.ValueType
    """Raw 64-bit identifier. Zero indicates an invalid handle."""
    @overload
    def __init__(self) -> None: ...
    @overload
    def __init__(self, value: Handle.ValueTypeLike = ...) -> None: ...
    @overload
    def __init__(self, raw: Handle.ValueTypeLike) -> None:
        """Construct a handle from its raw 64-bit identifier.

        Args:
            raw (ValueTypeLike): The raw handle identifier.
        """
    def __eq__(self, other: object) -> bool: ...
    def __ne__(self, other: object) -> bool: ...
    def __lt__(self, other: Handle) -> bool: ...
    def __hash__(self) -> int: ...
    def is_valid(self) -> bool:
        """Check whether the handle is valid (i.e., its value is non-zero).

        Returns:
            True if the handle is valid, false otherwise.
        """
    def get_hash(self) -> int:
        """Compute a hash of the handle for use in unordered containers.

        Returns:
            The hash of the handle's raw identifier.
        """

class SceneHandle(Handle):
    """Unique identifier for a :class:`~superdex.physics.Scene` within a Mochi context.
    """
    @overload
    def __init__(self) -> None: ...
    @overload
    def __init__(self, value: Handle.ValueTypeLike = ...) -> None: ...

class ActorHandle(Handle):
    """Unique identifier for an :class:`~superdex.physics.Actor` within a
    :class:`~superdex.physics.Scene`.

    Note:
        An :class:`~superdex.physics.ActorHandle` can only be used with the
        :class:`~superdex.physics.Scene` that created it.

    Note:
        Creating and destroying many actors in the same scene may eventually cause
        an :class:`~superdex.physics.ActorHandle` value from a previously destroyed
        actor to be re-used.
    """
    @overload
    def __init__(self) -> None: ...
    @overload
    def __init__(self, value: Handle.ValueTypeLike = ...) -> None: ...

class ShapeHandle(Handle):
    """Handle to a shape (mesh, sphere, plane, etc.) within a Mochi context.

    The shape remains in memory while referenced by any handle, actor, or file cache
    entry. In C++ and Python, :class:`~superdex.physics.ShapeHandle` is
    reference-counted and releases its reference automatically when destroyed. In
    the C API, you must call :func:`~superdex.physics.release_shape` when done with
    the handle.

    See Also:
        :func:`~superdex.physics.release_shape`
    """
    @overload
    def __init__(self) -> None: ...
    @overload
    def __init__(self, value: Handle.ValueTypeLike = ...) -> None: ...

class CallbackHandle(Handle):
    """Unique identifier for a callback registration within a
    :class:`~superdex.physics.Scene`.

    Note:
        A :class:`~superdex.physics.CallbackHandle` can only be used with the
        :class:`~superdex.physics.Scene` that created it.
    """
    @overload
    def __init__(self) -> None: ...
    @overload
    def __init__(self, value: Handle.ValueTypeLike = ...) -> None: ...

class ConstraintHandle(Handle):
    """Unique identifier for a :class:`~superdex.physics.Constraint` within a
    :class:`~superdex.physics.Scene`.

    Note:
        A :class:`~superdex.physics.ConstraintHandle` can only be used with the
        :class:`~superdex.physics.Scene` that created it.

    Note:
        Creating and destroying many constraints in the same scene may eventually
        cause a :class:`~superdex.physics.ConstraintHandle` value from a previously
        destroyed constraint to be re-used.
    """
    @overload
    def __init__(self) -> None: ...
    @overload
    def __init__(self, value: Handle.ValueTypeLike = ...) -> None: ...

class StateHandle(Handle):
    """Unique identifier for a captured simulation state within a
    :class:`~superdex.physics.Scene`.

    Note:
        A :class:`~superdex.physics.StateHandle` can only be used with the
        :class:`~superdex.physics.Scene` that created it.

    See Also:
        :meth:`~superdex.physics.Scene.capture_state`,
        :meth:`~superdex.physics.Scene.restore_state`,
        :meth:`~superdex.physics.Scene.release_state`
    """
    @overload
    def __init__(self) -> None: ...
    @overload
    def __init__(self, value: Handle.ValueTypeLike = ...) -> None: ...

class QueryHandle(Handle):
    """Unique identifier for a query within a :class:`~superdex.physics.Scene`.

    Note:
        A :class:`~superdex.physics.QueryHandle` can only be used with the
        :class:`~superdex.physics.Scene` that created it.

    See Also:
        :meth:`~superdex.physics.Actor.register_query`,
        :meth:`~superdex.physics.Actor.cancel_query`,
        :meth:`~superdex.physics.Constraint.register_query`,
        :meth:`~superdex.physics.Constraint.cancel_query`
    """
    @overload
    def __init__(self) -> None: ...
    @overload
    def __init__(self, value: Handle.ValueTypeLike = ...) -> None: ...

INVALID_HANDLE: int
"""Default value for an invalid handle."""

class ActorType:
    """Type of actor.

    See Also:
        :class:`~superdex.physics.Actor`, :meth:`~superdex.physics.Actor.get_type`
    """
    NONE: ActorType
    """Invalid actor type."""
    RIGID: ActorType
    """Rigid body actor."""
    SOFT: ActorType
    """Volumetric deformable actor."""
    ARTICULATED: ActorType
    """Articulated actor composed of rigid links connected by joints."""
    SHELL: ActorType
    """[Experimental] Thin-shell actor for modeling cloth, membranes, and other
    thin-walled objects.

    Warning:
        This is an experimental feature. It may be changed or removed in the future.
        Use at your own risk.
    """
    ROD: ActorType
    """[Experimental] Rod actor for thin elastic rods.

    Warning:
        This is an experimental feature. It may be changed or removed in the future.
        Use at your own risk.
    """
    COUNT: ActorType
    """Number of actor type enum values."""
    def __init__(self, value: int) -> None: ...
    @property
    def name(self) -> str: ...
    @property
    def value(self) -> int: ...
    def __int__(self) -> int: ...
    def __index__(self) -> int: ...

class BoundarySubsamplingStrategy:
    """Strategy for selecting the active boundary faces when subsampling boundary
    integrals.
    """
    UNIFORM_PROBABILITY: BoundarySubsamplingStrategy
    """Select boundary faces with uniform probability.

    Note:
        Results in sample density following mesh resolution. Areas with more
        triangles have more samples.

    Note:
        Active boundary faces are selected deterministically using uniformly-spaced
        indices.
    """
    AREA_PROPORTIONAL: BoundarySubsamplingStrategy
    """Select boundary faces with probability proportional to their area.

    Note:
        Larger faces have higher selection probability.

    Note:
        Results in more uniform spatial distribution of sample points, independent
        of mesh resolution.
    """
    COUNT: BoundarySubsamplingStrategy
    """Number of boundary subsampling strategy enum values."""
    def __init__(self, value: int) -> None: ...
    @property
    def name(self) -> str: ...
    @property
    def value(self) -> int: ...
    def __int__(self) -> int: ...
    def __index__(self) -> int: ...

class ActorBoundaryElementType:
    """Two-dimensional discretization element types for actor boundary integrals.

    Note:
        PxQy indicates x-th order elements with y quadrature points.
    """
    P1Q1: ActorBoundaryElementType
    """Linear elements (P1), Dunavant quadrature with 1 point (Q1)."""
    P1Q3: ActorBoundaryElementType
    """Linear elements (P1), Dunavant quadrature with 3 points (Q3)."""
    P1Q6: ActorBoundaryElementType
    """Linear elements (P1), Dunavant quadrature with 6 points (Q6)."""
    EXPERIMENTAL_P1Q7: ActorBoundaryElementType
    """[Experimental] Linear elements (P1), Lyness-Jespersen quadrature with 7 points
    (Q7).

    Warning:
        This is an experimental feature. It may be changed or removed in the future.
        Use at your own risk.
    """
    EXPERIMENTAL_P1Q12: ActorBoundaryElementType
    """[Experimental] Linear elements (P1), Dunavant quadrature with 12 points (Q12).

    Warning:
        This is an experimental feature. It may be changed or removed in the future.
        Use at your own risk.
    """
    EXPERIMENTAL_P1Q16: ActorBoundaryElementType
    """[Experimental] Linear elements (P1), Lyness-Jespersen quadrature with 16 points
    (Q16).

    Warning:
        This is an experimental feature. It may be changed or removed in the future.
        Use at your own risk.
    """
    COUNT: ActorBoundaryElementType
    """Number of actor boundary element type enum values."""
    DEFAULT: ActorBoundaryElementType
    """Default actor boundary element type."""
    def __init__(self, value: int) -> None: ...
    @property
    def name(self) -> str: ...
    @property
    def value(self) -> int: ...
    def __int__(self) -> int: ...
    def __index__(self) -> int: ...

class ActorSegmentElementType:
    """One-dimensional discretization element types for actor segment integrals.

    Note:
        PxQy indicates x-th order elements with y quadrature points.
    """
    P1Q1: ActorSegmentElementType
    """Linear elements (P1), Gauss-Legendre quadrature with 1 point (Q1)."""
    P1Q2: ActorSegmentElementType
    """Linear elements (P1), Gauss-Legendre quadrature with 2 points (Q2)."""
    P1Q3: ActorSegmentElementType
    """Linear elements (P1), Gauss-Legendre quadrature with 3 points (Q3)."""
    COUNT: ActorSegmentElementType
    """Number of actor segment element type enum values."""
    DEFAULT: ActorSegmentElementType
    """Default actor segment element type."""
    def __init__(self, value: int) -> None: ...
    @property
    def name(self) -> str: ...
    @property
    def value(self) -> int: ...
    def __int__(self) -> int: ...
    def __index__(self) -> int: ...

class ConstraintType:
    """Type of constraint acting on actors' degrees of freedom.

    Each constraint type enforces specific relationships between actor DoFs, such as
    fixing positions, limiting joint ranges, or maintaining rigid connections. The
    type determines which constraint methods are applicable and what parameters are
    required.

    See Also:
        :class:`~superdex.physics.Constraint`,
        :meth:`~superdex.physics.Constraint.get_type`
    """
    NONE: ConstraintType
    """Invalid constraint type."""
    ARTICULATED_SINGLE_DOF_RANGE: ConstraintType
    """Range constraint on a single articulated DoF. Limits the DoF value to [min,
    max].

    See Also:
        :meth:`~superdex.physics.Scene.create_articulated_single_dof_range_constraint`,
        :class:`~superdex.physics.ArticulatedSingleDofRangeConstraintParams`
    """
    ARTICULATED3D_ROTATION_RANGE: ConstraintType
    """Range constraint on the DoFs of an articulated 3D rotation. Limits DoF values to
    [min, max].

    See Also:
        :meth:`~superdex.physics.Scene.create_articulated3d_rotation_range_constraint`,
        :class:`~superdex.physics.Articulated3dRotationRangeConstraintParams`
    """
    ARTICULATED_SINGLE_DOF_TARGET: ConstraintType
    """Target constraint on a single articulated DoF. Drives the DoF toward a target
    value.

    See Also:
        :meth:`~superdex.physics.Scene.create_articulated_single_dof_target_constraint`,
        :class:`~superdex.physics.ArticulatedSingleDofTargetConstraintParams`
    """
    ARTICULATED3D_ROTATION_TARGET: ConstraintType
    """Target constraint on the DoFs of an articulated 3D rotation. Drives rotation
    DoFs toward target values.

    See Also:
        :meth:`~superdex.physics.Scene.create_articulated3d_rotation_target_constraint`,
        :class:`~superdex.physics.Articulated3dRotationTargetConstraintParams`
    """
    JOINT_ROTATION_RANGE: ConstraintType
    """Range constraint on joint rotation. Limits joint angles to specified bounds.

    See Also:
        :meth:`~superdex.physics.Scene.create_joint_rotation_range_constraint`,
        :class:`~superdex.physics.JointRotationRangeConstraintParams`
    """
    JOINT_ROTATION_TRACKING: ConstraintType
    """Tracking constraint on joint rotation. Drives joint toward target orientation.

    See Also:
        :meth:`~superdex.physics.Scene.create_joint_rotation_tracking_constraint`,
        :class:`~superdex.physics.JointRotationTrackingConstraintParams`
    """
    RIGID_PIVOT_POSITION: ConstraintType
    """Position constraint on a rigid actor's pivot point. Fixes or tracks a
    world-space position.

    See Also:
        :meth:`~superdex.physics.Scene.create_rigid_pivot_position_constraint`,
        :class:`~superdex.physics.RigidPivotPositionConstraintParams`
    """
    RIGID_PIVOT_TO_RIGID_TARGET: ConstraintType
    """Position constraint that drives a rigid actor's pivot point toward where that
    same local point would be if the actor's center-of-mass frame matched a target
    transform.

    See Also:
        :meth:`~superdex.physics.Scene.create_rigid_pivot_to_rigid_target_constraint`,
        :class:`~superdex.physics.RigidPivotToRigidTargetConstraintParams`
    """
    RIGID_PIVOT_ROTATION: ConstraintType
    """Rotation constraint on a rigid actor's pivot frame. Fixes or tracks a
    world-space orientation.

    See Also:
        :meth:`~superdex.physics.Scene.create_rigid_pivot_rotation_constraint`,
        :class:`~superdex.physics.RigidPivotRotationConstraintParams`
    """
    RIGID_PRISMATIC_JOINT: ConstraintType
    """Translational prismatic joint constraint between two rigid actors. Constrains
    translation to a single axis. Does not constrain rotation. Pair with
    :class:`JOINT_ROTATION_TRACKING <superdex.physics.ConstraintType>` for a full
    prismatic joint.

    See Also:
        :meth:`~superdex.physics.Scene.create_rigid_prismatic_joint_constraint`,
        :class:`~superdex.physics.RigidPrismaticJointConstraintParams`
    """
    RIGID_SPHERICAL_JOINT: ConstraintType
    """Spherical joint constraint between two rigid actors. Allows rotation but no
    translation.

    See Also:
        :meth:`~superdex.physics.Scene.create_rigid_spherical_joint_constraint`,
        :class:`~superdex.physics.RigidSphericalJointConstraintParams`
    """
    DEFORMABLE_NODE_POSITION: ConstraintType
    """Position constraint on a deformable actor node. Fixes or tracks node position in
    world space.

    See Also:
        :meth:`~superdex.physics.Scene.create_deformable_node_position_constraint`,
        :class:`~superdex.physics.DeformableNodePositionConstraintParams`
    """
    DEFORMABLE_NODE_TO_RIGID: ConstraintType
    """Constraint connecting a deformable node to a rigid actor. Attaches deformable
    body to rigid body.

    See Also:
        :meth:`~superdex.physics.Scene.create_deformable_node_to_rigid_constraint`,
        :class:`~superdex.physics.DeformableNodeToRigidConstraintParams`
    """
    DEFORMABLE_NODE_TO_DEFORMABLE_NODE: ConstraintType
    """Constraint connecting two deformable actor nodes.

    See Also:
        :meth:`~superdex.physics.Scene.create_deformable_node_to_deformable_node_constraint`,
        :class:`~superdex.physics.DeformableNodeToDeformableNodeConstraintParams`
    """
    ROD_ELEMENT_ROTATION_TO_RIGID: ConstraintType
    """Rotation constraint between a rigid actor and a rod element. Tracks relative
    rotation.

    See Also:
        :meth:`~superdex.physics.Scene.create_rod_element_rotation_to_rigid_constraint`,
        :class:`~superdex.physics.RodElementRotationToRigidConstraintParams`
    """
    COUNT: ConstraintType
    """Number of constraint type enum values."""
    def __init__(self, value: int) -> None: ...
    @property
    def name(self) -> str: ...
    @property
    def value(self) -> int: ...
    def __int__(self) -> int: ...
    def __index__(self) -> int: ...

class PoseConstraintType:
    """Type of constraint in an articulated pose controller.

    Defines the functional purpose of a pose constraint, not its internal
    implementation. Pose controllers use these constraints to track target
    configurations for articulated actors.

    See Also:
        :class:`~superdex.physics.PoseConstraintInfo`,
        :class:`~superdex.physics.PoseTrackingParams`,
        :class:`~superdex.physics.PoseControllerParams`
    """
    JOINT: PoseConstraintType
    """Joint constraint."""
    LINK_TRANSLATION: PoseConstraintType
    """Link translation constraint. Controls the position of a link."""
    LINK_ROTATION: PoseConstraintType
    """Link rotation constraint. Controls the orientation of a link."""
    COUNT: PoseConstraintType
    """Number of pose constraint type enum values."""
    INVALID: PoseConstraintType
    """Invalid constraint type."""
    def __init__(self, value: int) -> None: ...
    @property
    def name(self) -> str: ...
    @property
    def value(self) -> int: ...
    def __int__(self) -> int: ...
    def __index__(self) -> int: ...

class QueryType:
    """Type of query data to compute for an actor or constraint.

    Queries enable on-demand computation of simulation data. Some queries allow
    immediate retrieval of data; others must be registered beforehand to have the
    data computed and available for retrieval after the next step. Queries are
    reference counted, so multiple registrations are acceptable.

    Note:
        Not all query types are supported for all actor/constraint types. Use
        :meth:`~superdex.physics.Actor.is_query_supported` or
        :meth:`~superdex.physics.Constraint.is_query_supported` to check at runtime
        whether a specific query type is supported for a given actor/constraint.

    See Also:
        :meth:`~superdex.physics.Actor.register_query`,
        :meth:`~superdex.physics.Actor.register_query_and_compute`,
        :meth:`~superdex.physics.Constraint.register_query`
    """
    NODE_POSITIONS: QueryType
    """Positions of all mesh nodes. Available via
    :meth:`~superdex.physics.Actor.get_node_positions_local`.

    Note:
        Only supported for standalone soft actors, nested soft actors, and shell
        actors.
    """
    ELEMENTS_DEFORMATION_GRADIENT: QueryType
    """Deformation gradients for elements in deformable actors. Available via
    :meth:`~superdex.physics.Actor.get_elements_deformation_gradient`.

    Note:
        Only supported for standalone and nested soft actors (except ROMs).
    """
    SURFACE_NODE_POSITIONS: QueryType
    """Positions of surface mesh nodes. Available via
    :meth:`~superdex.physics.Actor.get_surface_mesh_node_positions_local`.

    Note:
        Not supported for actors without a surface mesh, such as rod actors or rigid
        actors without a mesh (e.g. with an implicit shape).
    """
    SURFACE_NODE_NORMALS: QueryType
    """Normals of surface mesh nodes. Available via
    :meth:`~superdex.physics.Actor.get_surface_mesh_node_normals_local`.

    Note:
        Not supported for actors without a surface mesh, such as rod actors or rigid
        actors without a mesh (e.g. with an implicit shape).
    """
    VISUAL_NODE_POSITIONS: QueryType
    """Positions of visual mesh nodes. Available via
    :meth:`~superdex.physics.Actor.get_visual_mesh_node_positions_local`.

    Note:
        Only supported for actors with a visual mesh. Deformable actors also require
        a visual mesh embedding.
    """
    VISUAL_NODE_NORMALS: QueryType
    """Normals of visual mesh nodes. Available via
    :meth:`~superdex.physics.Actor.get_visual_mesh_node_normals_local`.

    Note:
        Only supported for actors with a visual mesh. Deformable actors also require
        a visual mesh embedding.
    """
    CONTACT_POINTS: QueryType
    """Contact points between actors. Available via
    :meth:`~superdex.physics.Actor.get_contact_points_world`.

    Note:
        Immediate computation, i.e.
        :meth:`~superdex.physics.Actor.register_query_and_compute`, is not
        supported.

    Note:
        Not supported for actors without contact sample points or with far SDF
        evaluation enabled.
    """
    ELASTIC_ENERGY: QueryType
    """Elastic energy of a deformable actor. Available via
    :meth:`~superdex.physics.Actor.get_elastic_energy`.

    Note:
        Only supported for standalone and nested soft actors.
    """
    NODE_CONTACT_FORCES: QueryType
    """Contact forces per node. Available via
    :meth:`~superdex.physics.Actor.get_node_contact_forces_world`.

    Note:
        Immediate computation, i.e.
        :meth:`~superdex.physics.Actor.register_query_and_compute`, is not
        supported.

    Note:
        Not supported for rod actors, actors without contact sample points or actors
        with far SDF evaluation enabled.
    """
    CONSTRAINT_FORCE: QueryType
    """Force/torque applied by a constraint. Available via
    :meth:`~superdex.physics.Constraint.get_force`.

    Note:
        Only applicable to :class:`~superdex.physics.Constraint` objects.

    Note:
        Immediate computation, i.e.
        :meth:`~superdex.physics.Actor.register_query_and_compute`, is not
        supported.
    """
    ARTICULATED_CONTROLLER_FORCE: QueryType
    """Generalized forces applied by an articulated pose controller. Available via
    :meth:`~superdex.physics.Actor.get_articulated_controller_force`.

    Note:
        Only applicable to articulated actors with a pose controller.

    Note:
        Immediate computation, i.e.
        :meth:`~superdex.physics.Actor.register_query_and_compute`, is not
        supported.
    """
    SDF_DISTANCES: QueryType
    """Signed distance field values for contact samples. Available via
    :meth:`~superdex.physics.Actor.get_sdf_distances`.

    Note:
        Immediate computation, i.e.
        :meth:`~superdex.physics.Actor.register_query_and_compute`, is not
        supported.
    """
    TOTAL_CONTACT_FORCE: QueryType
    """Total contact force and torque acting on an actor. Available via
    :meth:`~superdex.physics.Actor.get_contact_force_world` and
    :meth:`~superdex.physics.Actor.get_contact_torque_world`.

    Note:
        Immediate computation, i.e.
        :meth:`~superdex.physics.Actor.register_query_and_compute`, is not
        supported.

    Note:
        Not supported for actors without contact sample points or with far SDF
        evaluation enabled.
    """
    COUNT: QueryType
    """Number of query type enum values."""
    def __init__(self, value: int) -> None: ...
    @property
    def name(self) -> str: ...
    @property
    def value(self) -> int: ...
    def __int__(self) -> int: ...
    def __index__(self) -> int: ...

class IncludeNestedActors:
    """Specifies whether actor-contact filtering also applies to nested actors.

    See Also:
        :meth:`~superdex.physics.Scene.enable_actor_contact_asymmetric`,
        :meth:`~superdex.physics.Scene.enable_actor_contact_symmetric`
    """
    NO: IncludeNestedActors
    """Affect only the exact actor handles passed to the API."""
    YES: IncludeNestedActors
    """Affect the exact actor and, for parent actors, its nested actors.

    Note:
        Actors without nested actors are affected as exact actors.
    """
    COUNT: IncludeNestedActors
    """Number of :class:`~superdex.physics.IncludeNestedActors` values."""
    def __init__(self, value: int) -> None: ...
    @property
    def name(self) -> str: ...
    @property
    def value(self) -> int: ...
    def __int__(self) -> int: ...
    def __index__(self) -> int: ...

class SolverParams:
    """Simulation solver configuration parameters."""
    non_linear_solver: NonLinearSolverParams
    """Nonlinear solver settings."""
    linear_solver: LinearSolverParams
    """Linear solver settings."""
    integration_method: IntegrationMethod
    """Time integration method."""
    experimental_eval: ExperimentalEvalParams
    """[Experimental] Evaluation settings common to the full scene.

    Warning:
        This is an experimental feature. It may be changed or removed in the future.
        Use at your own risk.
    """
    @overload
    def __init__(self) -> None: ...
    @overload
    def __init__(
        self,
        non_linear_solver: NonLinearSolverParams = ...,
        linear_solver: LinearSolverParams = ...,
        integration_method: IntegrationMethod | int = ...,
        experimental_eval: ExperimentalEvalParams = ...,
    ) -> None: ...
    def __eq__(self, other: object) -> bool: ...
    def __ne__(self, other: object) -> bool: ...

class RecenteringParams:
    """Parameters controlling recentering behavior for standalone soft actors.

    Recentering automatically updates the root transform as the actor's "rigid
    pivot" (typically near the center of mass) moves, with corresponding adjustments
    to local-space coordinates.

    Note:
        When recentering is disabled, local-space displacement values can become
        very large if the actor moves far from its starting position. This may be
        problematic due to finite-precision arithmetic.

    Note:
        When recentering is enabled, the root transform rotates and translates to
        follow the actor, and local-space displacements reflect only deformation,
        not global rotation or translation.

    See Also:
        :meth:`~superdex.physics.Actor.get_recentering_params`,
        :meth:`~superdex.physics.Actor.set_recentering_params`
    """
    use_recentering: bool
    """Whether recentering is enabled."""
    rotation_epsilon_deg: float
    """Minimum rotation threshold [degrees] to apply recentering.

    Note:
        Recentering is applied if either rotation or translation change more than
        their respective thresholds.
    """
    translation_epsilon: float
    """Minimum translation threshold [m] to apply recentering.

    Note:
        Recentering is applied if either rotation or translation change more than
        their respective thresholds.
    """
    @overload
    def __init__(self) -> None: ...
    @overload
    def __init__(
        self,
        use_recentering: bool = ...,
        rotation_epsilon_deg: float = ...,
        translation_epsilon: float = ...,
    ) -> None: ...

class BoundarySubsamplingParams:
    """Parameters for subsampling boundary integrals such as contact.

    Subsampling reduces the number of boundary sample points used for contact and
    other boundary integrals, which can improve performance at the cost of accuracy.
    """
    subsampling_density: float
    """Subsampling density in the range [0, 1].

    Note:
        0 means no sample points are used. 1 means all sample points are used.
    """
    strategy: BoundarySubsamplingStrategy
    """Strategy for determining which sample points to use."""
    @overload
    def __init__(self) -> None: ...
    @overload
    def __init__(
        self,
        subsampling_density: float = ...,
        strategy: BoundarySubsamplingStrategy | int = ...,
    ) -> None: ...
    def __eq__(self, other: object) -> bool: ...
    def __ne__(self, other: object) -> bool: ...

class ContactPairParamsOverride:
    """Optional contact-response parameter replacements for an unordered actor pair.

    Each present field replaces the value normally combined from the two actors. An
    absent field retains the existing combination rule.

    Note:
        Present fields have the same validity requirements as the corresponding
        :class:`~superdex.physics.ContactParams` fields. A present zero is a value
        subject to those requirements, not an absent field.
    """
    penalty_coefficient: Optional[float]
    """Pair penalty coefficient [Pa/m] before role-dependent dimensional corrections.

    See Also:
        :attr:`~superdex.physics.ContactParams.penalty_coefficient`
    """
    friction_falloff_vel: Optional[float]
    """Friction falloff velocity [m/s].

    See Also:
        :attr:`~superdex.physics.ContactParams.friction_falloff_vel`
    """
    viscous_friction_coefficient: Optional[float]
    """Pair viscous friction coefficient [s/m].

    See Also:
        :attr:`~superdex.physics.ContactParams.viscous_friction_coefficient`
    """
    coulomb_friction_coefficient: Optional[float]
    """Pair Coulomb friction coefficient.

    See Also:
        :attr:`~superdex.physics.ContactParams.coulomb_friction_coefficient`
    """
    normal_viscous_damping_coefficient: Optional[float]
    """Pair normal viscous damping coefficient [s/m].

    See Also:
        :attr:`~superdex.physics.ContactParams.normal_viscous_damping_coefficient`
    """
    @overload
    def __init__(self) -> None: ...
    @overload
    def __init__(
        self,
        penalty_coefficient: Optional[float] = ...,
        friction_falloff_vel: Optional[float] = ...,
        viscous_friction_coefficient: Optional[float] = ...,
        coulomb_friction_coefficient: Optional[float] = ...,
        normal_viscous_damping_coefficient: Optional[float] = ...,
    ) -> None: ...
    def __eq__(self, other: object) -> bool: ...
    def __ne__(self, other: object) -> bool: ...

class ArticulatedShapeInfo:
    """Information describing an articulated shape's kinematic structure.

    Provides read-only access to an articulated shape's links, joints, and
    hierarchy. This information is immutable once the shape is created and defines
    the articulation's topology and configuration limits.

    Note:
        Number of joints = number of links + number of cycle joints.

    See Also:
        :meth:`~superdex.physics.Actor.get_articulated_shape_info`,
        :func:`~superdex.physics.get_articulated_shape_info`
    """
    @property
    def root_from_links_at_rest(self) -> SpanConstTransformRT:
        """Rotation and translation of each link in rest configuration relative to the
        local reference system of the articulated shape. Size must equal the number of
        links.
        """
    @root_from_links_at_rest.setter
    def root_from_links_at_rest(self, value: ArrayLikeTransformRT) -> None: ...
    @property
    def link_names(self) -> SpanConstString:
        """Name of each link."""
    @link_names.setter
    def link_names(self, value: ArrayLikeString) -> None: ...
    @property
    def parents(self) -> SpanConstInt:
        """Parent link indices defining a tree-like joint structure with no cycles. Size
        must equal the number of links.

        Note:
            ``parent[i] < i`` (parent must come before child in the array).

        Note:
            Use -1 for links with no parent.
        """
    @parents.setter
    def parents(self, value: ArrayLikeInt) -> None: ...
    @property
    def joint_types(self) -> SpanConstArticulatedJointType:
        """Type of each joint. Size must be numLinks + numCycles.

        See Also:
            :class:`~superdex.physics.ArticulatedJointType`
        """
    @joint_types.setter
    def joint_types(self, value: ArrayLikeArticulatedJointType) -> None: ...
    @property
    def cycles(self) -> SpanConstArticulatedCycleJoint:
        """Cycle joints creating closed kinematic loops in the articulated body.

        Note:
            Empty if no cycles exist (i.e., the kinematic structure is purely
            tree-like).

        Note:
            Cycle entries appear at indices [numLinks, numLinks + numCycles) of the
            per-joint arrays
            (:attr:`~superdex.physics.ArticulatedShapeInfo.joint_types`,
            :attr:`~superdex.physics.ArticulatedShapeInfo.joint_axes`,
            :attr:`~superdex.physics.ArticulatedShapeInfo.joint_from_child_link`,
            :attr:`~superdex.physics.ArticulatedShapeInfo.parent_link_from_joint`,
            :attr:`~superdex.physics.ArticulatedShapeInfo.joint_min_limits`,
            :attr:`~superdex.physics.ArticulatedShapeInfo.joint_max_limits`,
            :attr:`~superdex.physics.ArticulatedShapeInfo.joint_names`).

        See Also:
            :class:`~superdex.physics.ArticulatedCycleJoint`
        """
    @cycles.setter
    def cycles(self, value: ArrayLikeArticulatedCycleJoint) -> None: ...
    @property
    def joint_axes(self) -> SpanConstReal3:
        """Axis of each joint in the joint's local frame. Size must be numLinks +
        numCycles.
        """
    @joint_axes.setter
    def joint_axes(self, value: ArrayLikeReal3) -> None: ...
    @property
    def dof_info(self) -> SpanConstArticulatedDofInfo:
        """DoF information of each joint (offset within the full pose, number of
        translation DoFs, number of rotation DoFs). Size must be numLinks + numCycles.
        """
    @dof_info.setter
    def dof_info(self, value: ArrayLikeArticulatedDofInfo) -> None: ...
    @property
    def joint_from_child_link(self) -> SpanConstTransformRT:
        """Transform from each joint's child link to the joint frame (rotation +
        translation). Size must be numLinks + numCycles.

        This is the transform from the child link to the joint.
        :attr:`~superdex.physics.ArticulatedLinkParams.parent_joint_from_link` for tree
        joints and
        :attr:`~superdex.physics.ArticulatedCycleJointParams.joint_from_child_link` for
        cycle joints. The joint axis
        (:attr:`~superdex.physics.ArticulatedShapeInfo.joint_axes`) and joint motion are
        interpreted in this joint frame.
        """
    @joint_from_child_link.setter
    def joint_from_child_link(self, value: ArrayLikeTransformRT) -> None: ...
    @property
    def parent_link_from_joint(self) -> SpanConstTransformRT:
        """Joint frame of each joint expressed in its parent link's local frame
        (rotation +
        translation). Size must be numLinks + numCycles.

        This is the transform from the joint to the parent link, corresponding to
        :attr:`~superdex.physics.ArticulatedJointParams.parent_link_from_joint`.
        """
    @parent_link_from_joint.setter
    def parent_link_from_joint(self, value: ArrayLikeTransformRT) -> None: ...
    @property
    def joint_min_limits(self) -> SpanConstReal3:
        """Minimum joint limits for each joint DoF. Size must be numLinks + numCycles,
        or
        empty if no joint has limits.

        Limits are defined for either all joints or none. Joints without minimum limits
        are indicated by
        :attr:`~superdex.physics.ArticulatedShapeInfo.joint_min_limits`\\[i] =
        -mochi::kInf3. For 1D joints, limits are the scalar minimum limits multiplied by
        the joint axis. For 3D joints, each component defines the limit along one of the
        axes of the joint.

        Note:
            The definition of joint limits for spherical joints may be complex, and it
            may be easier to model them as 3 co-located revolute joints.

        Note:
            Size of :attr:`~superdex.physics.ArticulatedShapeInfo.joint_min_limits` and
            :attr:`~superdex.physics.ArticulatedShapeInfo.joint_max_limits` must be the
            same.
        """
    @joint_min_limits.setter
    def joint_min_limits(self, value: ArrayLikeReal3) -> None: ...
    @property
    def joint_max_limits(self) -> SpanConstReal3:
        """Maximum joint limits for each joint DoF. Size must be numLinks + numCycles,
        or
        empty if no joint has limits.

        Limits are defined for either all joints or none. Joints without maximum limits
        are indicated by
        :attr:`~superdex.physics.ArticulatedShapeInfo.joint_max_limits`\\[i] =
        mochi::kInf3. For 1D joints, limits are the scalar maximum limits multiplied by
        the joint axis. For 3D joints, each component defines the limit along one of the
        axes of the joint.

        Note:
            The definition of joint limits for spherical joints may be complex, and it
            may be easier to model them as 3 co-located revolute joints.

        Note:
            Size of :attr:`~superdex.physics.ArticulatedShapeInfo.joint_min_limits` and
            :attr:`~superdex.physics.ArticulatedShapeInfo.joint_max_limits` must be the
            same.
        """
    @joint_max_limits.setter
    def joint_max_limits(self, value: ArrayLikeReal3) -> None: ...
    @property
    def joint_names(self) -> SpanConstString:
        """Name of each joint. Size must be numLinks + numCycles."""
    @joint_names.setter
    def joint_names(self, value: ArrayLikeString) -> None: ...
    @overload
    def __init__(self) -> None: ...
    @overload
    def __init__(
        self,
        root_from_links_at_rest: ArrayLikeTransformRT = ...,
        link_names: ArrayLikeString = ...,
        parents: ArrayLikeInt = ...,
        joint_types: ArrayLikeArticulatedJointType = ...,
        cycles: ArrayLikeArticulatedCycleJoint = ...,
        joint_axes: ArrayLikeReal3 = ...,
        dof_info: ArrayLikeArticulatedDofInfo = ...,
        joint_from_child_link: ArrayLikeTransformRT = ...,
        parent_link_from_joint: ArrayLikeTransformRT = ...,
        joint_min_limits: ArrayLikeReal3 = ...,
        joint_max_limits: ArrayLikeReal3 = ...,
        joint_names: ArrayLikeString = ...,
    ) -> None: ...

class StepInfo:
    """Information passed to per-step callbacks.

    See Also:
        :meth:`~superdex.physics.Scene.register_pre_step_callback`,
        :meth:`~superdex.physics.Scene.register_post_step_callback`
    """
    scene: Scene
    """Pointer to the :class:`~superdex.physics.Scene` being stepped."""
    time_step_sec: float
    """Time step size [s] for this simulation step."""
    @overload
    def __init__(self) -> None: ...
    @overload
    def __init__(self, scene: Scene = ..., time_step_sec: float = ...) -> None: ...

class RigidActorParams:
    """Parameters to create a rigid body actor.

    Rigid actors behave as rigid bodies with 6 degrees of freedom (3 translational,
    3 rotational). They can be static (fixed in space) or dynamic (affected by
    forces and collisions).

    Note:
        Also used to create links in articulated bodies.

    See Also:
        :meth:`~superdex.physics.Scene.create_rigid_actor`,
        :class:`~superdex.physics.ArticulatedActorParams`
    """
    name: str
    """Optional actor name. Uniqueness of names is not enforced."""
    layer: str
    """Contact layer name for filtering contacts between actors.

    See Also:
        :meth:`~superdex.physics.Scene.enable_layer_contact_asymmetric`,
        :meth:`~superdex.physics.Scene.enable_layer_contact_symmetric`,
        :meth:`~superdex.physics.Scene.enable_actor_contact_asymmetric`,
        :meth:`~superdex.physics.Scene.enable_actor_contact_symmetric`
    """
    shape: ShapeHandle
    """Shape handle defining the actor's geometry and other metadata.

    Note:
        A shape can be shared by multiple actors, even actors in different scenes.

    Note:
        Dynamic rigid actors require a shape with a surface mesh.

    See Also:
        :class:`~superdex.physics.ShapeHandle`,
        :func:`~superdex.physics.load_shape_from_file`
    """
    world_from_local: TransformRT
    """Initial rotation and translation of the actor's local frame with respect to
    world frame.

    Note:
        The actor's center of mass may be located elsewhere, since it is not
        necessarily at the origin of the actor's local coordinate frame.

    See Also:
        :attr:`~superdex.physics.RigidActorParams.center_of_mass`
    """
    collider_type: ColliderType
    """Collision detection geometry.

    Note:
        Determines how OTHER actors detect contact with this actor. It does not
        affect how this actor detects contact with other actors.

    See Also:
        :class:`~superdex.physics.ColliderType`
    """
    is_static: bool
    """If true, actor will be static (fixed in space). If false, actor will be dynamic.
    """
    contact: ContactParams
    """Contact mechanics parameters.

    See Also:
        :class:`~superdex.physics.ContactParams`
    """
    sdf: GridSdfParams
    """Parameters used to construct a grid-based Signed Distance Field (SDF) if the
    shape doesn't already have one.

    Note:
        Ignored if :attr:`~superdex.physics.RigidActorParams.collider_type` is not
        :class:`SDF <superdex.physics.ColliderType>`.

    Note:
        Ignored if the shape already has a grid-based SDF.
    """
    has_gravity: bool
    """If true, actor is affected by gravity.

    See Also:
        :meth:`~superdex.physics.Scene.set_gravity`
    """
    density: Optional[float]
    """Uniform density [kg/m³] of the actor. Must be positive if provided.

    Note:
        Setting both density and mass is not recommended. If both are set, mass
        takes precedence and density is ignored.

    Note:
        If only density is set, mass is computed automatically from volume.

    Note:
        If neither is set, :const:`~superdex.physics.DEFAULT_DENSITY` is used.

    See Also:
        :attr:`~superdex.physics.RigidActorParams.mass`
    """
    mass: Optional[float]
    """Total mass [kg] of the actor. Must be positive if provided.

    Note:
        Setting both mass and density is not recommended. If both are set, mass
        takes precedence and density is ignored.

    Note:
        If only mass is set, density is computed automatically from volume.

    See Also:
        :attr:`~superdex.physics.RigidActorParams.density`
    """
    @property
    def center_of_mass(self) -> Optional[Real3]:
        """Position [m] of the center of mass in the actor's local frame.

        Note:
            If not set, computed automatically from geometry.

        Note:
            Usually combined with explicit momentOfInertia.

        See Also:
            :attr:`~superdex.physics.RigidActorParams.moment_of_inertia`
        """
    @center_of_mass.setter
    def center_of_mass(self, value: Optional[Real3Like]) -> None: ...
    @property
    def moment_of_inertia(self) -> Optional[Real6]:
        """Moment of inertia tensor [kg·m²] at the center of mass in the actor's local
        frame.

        Stored as [ixx, ixy, ixz, iyy, iyz, izz] using negative tensor notation:

        ::

                 [ixx, ixy, ixz]   [ +∫(y²+z²)dm,  -∫(xy)dm,    -∫(xz)dm    ]
             I = [ixy, iyy, iyz] = [ -∫(xy)dm,     +∫(x²+z²)dm, -∫(yz)dm    ]
                 [ixz, iyz, izz]   [ -∫(xz)dm,     -∫(yz)dm,    +∫(x²+y²)dm ]

        Note:
            Uses negative tensor notation consistent with e.g. robotics URDF files.

        Note:
            Many CAD tools (e.g., Solidworks) use positive tensor notation requiring
            negation of off-diagonal terms. See
            https://www.mathworks.com/help/sm/ug/specify-custom-inertia.html.

        Note:
            Must be finite. A finite but physically invalid tensor (negative principal
            moments or one violating the triangle inequality) is accepted with a
            warning, not rejected.

        Note:
            If not set, computed automatically from geometry and density.

        Note:
            Usually combined with explicit centerOfMass.

        See Also:
            :attr:`~superdex.physics.RigidActorParams.center_of_mass`
        """
    @moment_of_inertia.setter
    def moment_of_inertia(self, value: Optional[Real6Like]) -> None: ...
    boundary_element_type: ActorBoundaryElementType
    """Finite element type for boundary discretization.

    Note:
        Affects accuracy and performance of contact and boundary integrals.

    See Also:
        :class:`~superdex.physics.ActorBoundaryElementType`
    """
    boundary_subsampling: Optional[BoundarySubsamplingParams]
    """Optional subsampling for boundary integrals such as contact.

    Note:
        Reduces computational cost by using fewer sample points.

    Note:
        For best performance, combine subsampling with
        :attr:`~superdex.physics.RigidActorParams.boundary_element_type` =
        :class:`P1Q1 <superdex.physics.ActorBoundaryElementType>`.

    See Also:
        :class:`~superdex.physics.BoundarySubsamplingParams`
    """
    @property
    def linear_velocity(self) -> Optional[Real3]:
        """Optional initial linear velocity of the center of mass in world frame [m/s],
        zero otherwise.

        Note:
            Not valid for static rigid actors or articulated links.

        See Also:
            :meth:`~superdex.physics.Actor.get_linear_velocity`,
            :meth:`~superdex.physics.Actor.set_velocity`
        """
    @linear_velocity.setter
    def linear_velocity(self, value: Optional[Real3Like]) -> None: ...
    @property
    def angular_velocity(self) -> Optional[Real3]:
        """Optional initial angular velocity in world frame [rad/s], zero otherwise.

        Note:
            Not valid for static rigid actors or articulated links.

        See Also:
            :meth:`~superdex.physics.Actor.get_angular_velocity`,
            :meth:`~superdex.physics.Actor.set_velocity`
        """
    @angular_velocity.setter
    def angular_velocity(self, value: Optional[Real3Like]) -> None: ...
    @overload
    def __init__(self) -> None: ...
    @overload
    def __init__(
        self,
        name: str = ...,
        layer: str = ...,
        shape: ShapeHandle = ...,
        world_from_local: TransformRT = ...,
        collider_type: ColliderType | int = ...,
        is_static: bool = ...,
        contact: ContactParams = ...,
        sdf: GridSdfParams = ...,
        has_gravity: bool = ...,
        density: Optional[float] = ...,
        mass: Optional[float] = ...,
        center_of_mass: Optional[Real3Like] = ...,
        moment_of_inertia: Optional[Real6Like] = ...,
        boundary_element_type: ActorBoundaryElementType | int = ...,
        boundary_subsampling: Optional[BoundarySubsamplingParams] = ...,
        linear_velocity: Optional[Real3Like] = ...,
        angular_velocity: Optional[Real3Like] = ...,
    ) -> None: ...
    def __eq__(self, other: object) -> bool: ...
    def __ne__(self, other: object) -> bool: ...

class SoftActorParams:
    """Parameters to create a soft deformable actor.

    Soft actors are volumetric deformable bodies simulated using the Finite Element
    Method (FEM).

    Note:
        Soft actors created via :meth:`~superdex.physics.Scene.create_soft_actor`
        act as colliding actors via their own surface sample points but not as
        colliders. To create a soft actor with a collider, use
        :func:`~superdex.physics.experimental.create_soft_actor` with an explicit
        :attr:`~superdex.physics.experimental.ExperimentalSoftActorParams.collider_type`.

    See Also:
        :meth:`~superdex.physics.Scene.create_soft_actor`,
        :func:`~superdex.physics.experimental.create_soft_actor`,
        :class:`~superdex.physics.experimental.ExperimentalSoftActorParams`
    """
    name: str
    """Optional actor name. Uniqueness of names is not enforced.

    Note:
        When used in :attr:`~superdex.physics.SoftSkinnedActorParams.soft_params`,
        this is interpreted as a nested soft local name. See
        :attr:`~superdex.physics.SoftSkinnedActorParams.soft_params` for additional
        requirements and defaulting behavior.
    """
    layer: str
    """Contact layer name for filtering contacts between actors.

    See Also:
        :meth:`~superdex.physics.Scene.enable_layer_contact_asymmetric`,
        :meth:`~superdex.physics.Scene.enable_layer_contact_symmetric`,
        :meth:`~superdex.physics.Scene.enable_actor_contact_asymmetric`,
        :meth:`~superdex.physics.Scene.enable_actor_contact_symmetric`
    """
    world_from_local: TransformRT
    """Initial rotation and translation of the actor's local frame with respect to
    world frame.

    Note:
        When this struct is used as an entry of
        :attr:`~superdex.physics.SoftSkinnedActorParams.soft_params`, this transform
        must be identity. The soft actor's shape must be defined directly in the
        reference frame of the articulated actor (see
        :attr:`~superdex.physics.SoftSkinnedActorParams.skeleton_params`). The
        soft-skinned actor's placement in the scene world is provided by
        :attr:`~superdex.physics.ArticulatedActorParams.world_from_root` in
        :attr:`~superdex.physics.SoftSkinnedActorParams.skeleton_params`.
    """
    shape: ShapeHandle
    """Shape handle defining the actor's geometry and other metadata.

    Note:
        Must be a TetrahedralMeshShape for soft actors.

    Note:
        A shape can be shared by multiple actors, even actors in different scenes.

    See Also:
        :class:`~superdex.physics.ShapeHandle`,
        :func:`~superdex.physics.load_shape_from_file`
    """
    material: SoftMaterialParams
    """Uniform material properties.

    Note:
        If the shape contains per-element material data of the same
        :class:`~superdex.physics.SoftMaterialType`, the per-element values from the
        shape take precedence over and replace these uniform values. If the shape's
        per-element material type differs, these uniform values are used instead.

    Note:
        The actor uses a single PSD strategy for all elements. By default it is the
        :attr:`~superdex.physics.SoftActorParams.material`\\'s PSD strategy. If the
        shape supplies per-element material data of a matching
        :class:`~superdex.physics.SoftMaterialType` whose
        :attr:`~superdex.physics.PerElementSoftMaterialData.psd_strategy` is
        specified (i.e. not :class:`MATERIAL_DEFAULT
        <superdex.physics.MaterialPsdStrategy>`), that value takes precedence. If
        the :attr:`~superdex.physics.SoftActorParams.material`\\'s PSD strategy is
        also specified, the two must be equal.

    Note:
        Non-uniform material properties can also be applied after the actor is
        created. See experimental::SetSoftMaterialParamsField.

    See Also:
        :meth:`~superdex.physics.Actor.set_soft_material_params`,
        :func:`~superdex.physics.experimental.set_soft_material_params_field`
    """
    contact: ContactParams
    """Contact mechanics parameters.

    See Also:
        :class:`~superdex.physics.ContactParams`
    """
    has_gravity: bool
    """If true, actor is affected by gravity.

    Note:
        Unless the soft actor is part of a soft-skinned actor
        (:attr:`~superdex.physics.SoftSkinnedActorParams.soft_params`), at least one
        of :attr:`~superdex.physics.SoftActorParams.has_gravity`,
        :attr:`~superdex.physics.SoftActorParams.has_inertia` or
        :attr:`~superdex.physics.SoftActorParams.has_stress` must be enabled.

    See Also:
        :meth:`~superdex.physics.Scene.set_gravity`
    """
    has_inertia: bool
    """If true, actor is affected by inertial forces.

    Note:
        Disable for quasi-static simulations.

    Note:
        Unless the soft actor is part of a soft-skinned actor
        (:attr:`~superdex.physics.SoftSkinnedActorParams.soft_params`), at least one
        of :attr:`~superdex.physics.SoftActorParams.has_gravity`,
        :attr:`~superdex.physics.SoftActorParams.has_inertia` or
        :attr:`~superdex.physics.SoftActorParams.has_stress` must be enabled.
    """
    has_stress: bool
    """If true, actor is affected by internal stress/strain forces.

    Note:
        Unless the soft actor is part of a soft-skinned actor
        (:attr:`~superdex.physics.SoftSkinnedActorParams.soft_params`), at least one
        of :attr:`~superdex.physics.SoftActorParams.has_gravity`,
        :attr:`~superdex.physics.SoftActorParams.has_inertia` or
        :attr:`~superdex.physics.SoftActorParams.has_stress` must be enabled.
    """
    boundary_element_type: ActorBoundaryElementType
    """Finite element type for boundary discretization.

    Note:
        Affects accuracy and performance of contact and boundary integrals.

    See Also:
        :class:`~superdex.physics.ActorBoundaryElementType`
    """
    @overload
    def __init__(self) -> None: ...
    @overload
    def __init__(
        self,
        name: str = ...,
        layer: str = ...,
        world_from_local: TransformRT = ...,
        shape: ShapeHandle = ...,
        material: SoftMaterialParams = ...,
        contact: ContactParams = ...,
        has_gravity: bool = ...,
        has_inertia: bool = ...,
        has_stress: bool = ...,
        boundary_element_type: ActorBoundaryElementType | int = ...,
    ) -> None: ...
    def __eq__(self, other: object) -> bool: ...
    def __ne__(self, other: object) -> bool: ...

class ArticulatedJointParams:
    """Parameters describing a single joint within an articulated actor.

    Each joint connects a link to its parent in the kinematic tree. Joints are
    referenced positionally — the i-th entry of
    :attr:`~superdex.physics.ArticulatedActorParams.joints` corresponds to the joint
    connecting the i-th link to its parent. Cycle-closing joints are declared
    separately via :attr:`~superdex.physics.ArticulatedActorParams.cycles`.

    See Also:
        :class:`~superdex.physics.ArticulatedActorParams`,
        :class:`~superdex.physics.ArticulatedLinkParams`,
        :class:`~superdex.physics.ArticulatedJointType`
    """
    name: str
    """Joint name. Must be unique among joints of the same articulated actor.

    Note:
        If left empty, a unique default name (e.g., "joint_0", "joint_1", ...) is
        assigned automatically.
    """
    type: ArticulatedJointType
    """Joint type.

    Note:
        Default :class:`INVALID <superdex.physics.ArticulatedJointType>` is a
        sentinel. The user must overwrite it.

    Note:
        :class:`CYCLE <superdex.physics.ArticulatedJointType>` is reserved for
        cycle-closing joints. Use
        :attr:`~superdex.physics.ArticulatedActorParams.cycles` for cycle joints.

    Note:
        Static links can be modeled using a :class:`HARD
        <superdex.physics.ArticulatedJointType>` joint whose parent is either -1 or
        another static link.
    """
    parent_link_from_joint: TransformRT
    """Rotation and translation of the joint with respect to the parent link."""
    @property
    def axis(self) -> Real3:
        """Local axis of motion for the joint, in the joint's local frame.

        Note:
            Only used for :class:`PRISMATIC <superdex.physics.ArticulatedJointType>` and
            :class:`REVOLUTE <superdex.physics.ArticulatedJointType>` joints. Ignored
            (and reset to zero) for other joint types.

        Note:
            For Prismatic/Revolute joints, must be non-zero and finite. The vector is
            automatically normalized to unit length on actor creation.
        """
    @axis.setter
    def axis(self, value: Real3Like) -> None: ...
    friction: ArticulatedJointFrictionParams
    """Joint friction parameters.

    Note:
        Ignored for :class:`FREE <superdex.physics.ArticulatedJointType>` and
        :class:`HARD <superdex.physics.ArticulatedJointType>` joints.
    """
    inertia: Optional[float]
    """Joint inertia coefficient. Units are [kg] for translation DoFs and [kg·m²] for
    rotation DoFs.

    Note:
        Default is zero joint inertia.

    Note:
        Ignored for :class:`FREE <superdex.physics.ArticulatedJointType>` and
        :class:`HARD <superdex.physics.ArticulatedJointType>` joints.
    """
    @property
    def min_limit(self) -> Optional[Real3]:
        """Minimum joint limits for each DoF. Units are [m] for translation DoFs and
        [rad]
        for rotation DoFs.

        For 1D joints, limits must be defined by multiplying the scalar min limits by
        the joint axis. For 3D joints, each component must define the limit along one of
        the axes of the joint.

        Note:
            The definition of joint limits for spherical joints may be complex, and it
            may be easier to model them as 3 co-located revolute joints.
        """
    @min_limit.setter
    def min_limit(self, value: Optional[Real3Like]) -> None: ...
    @property
    def max_limit(self) -> Optional[Real3]:
        """Maximum joint limits for each DoF. Units are [m] for translation DoFs and
        [rad]
        for rotation DoFs.

        For 1D joints, limits must be defined by multiplying the scalar max limits by
        the joint axis. For 3D joints, each component must define the limit along one of
        the axes of the joint.

        Note:
            The definition of joint limits for spherical joints may be complex, and it
            may be easier to model them as 3 co-located revolute joints.
        """
    @max_limit.setter
    def max_limit(self, value: Optional[Real3Like]) -> None: ...
    limit_stiffness: float
    """Stiffness coefficient [N/m or N·m/rad] for the joint limit constraints.

    Note:
        Ignored for joints without limits.
    """
    limit_damping: float
    """Damping coefficient [N·s/m or N·m·s/rad] for the joint limit constraints.

    Note:
        Ignored for joints without limits.
    """
    @overload
    def __init__(self) -> None: ...
    @overload
    def __init__(
        self,
        name: str = ...,
        type: ArticulatedJointType | int = ...,
        parent_link_from_joint: TransformRT = ...,
        axis: Real3Like = ...,
        friction: ArticulatedJointFrictionParams = ...,
        inertia: Optional[float] = ...,
        min_limit: Optional[Real3Like] = ...,
        max_limit: Optional[Real3Like] = ...,
        limit_stiffness: float = ...,
        limit_damping: float = ...,
    ) -> None: ...
    def __eq__(self, other: object) -> bool: ...
    def __ne__(self, other: object) -> bool: ...

class ArticulatedLinkParams:
    """Parameters describing a single rigid link within an articulated actor.

    Each link is a rigid body in the articulation's kinematic tree. Mirrors
    :class:`~superdex.physics.RigidActorParams` for the per-link rigid-body
    properties (shape, mass, inertia, contact), and adds tree-structure fields
    (:attr:`parent_link`, :attr:`parent_joint_from_link`). Pose and velocity at
    creation time come from the articulated body, not from per-link fields.

    See Also:
        :class:`~superdex.physics.ArticulatedActorParams`,
        :class:`~superdex.physics.ArticulatedJointParams`,
        :class:`~superdex.physics.RigidActorParams`
    """
    name: str
    """Link local name.

    Note:
        Must be unique among links of the same articulated actor. For soft-skinned
        actors, it must also not collide with nested soft actor local names.

    Note:
        Must not contain forward slash, backslash, or embedded NUL characters, which
        are reserved for hierarchy paths or incompatible with actor path formatting.

    Note:
        If left empty, a unique default name (e.g., "link_0", "link_1", ...) is
        assigned automatically.

    Note:
        In the scene the corresponding actor is named
        "articulatedActorName/linkLocalName". If the articulated actor name is
        empty, link actors use "unnamed_articulation/linkLocalName".
    """
    parent_link: int
    """Index of the parent link in
    :attr:`~superdex.physics.ArticulatedActorParams.links`, or -1 for the root.

    Note:
        Links must be listed in parent-first order: ``parentLink < i`` for the i-th
        link.

    Note:
        The root link (index 0) must have ``parentLink == -1``.
    """
    parent_joint_from_link: TransformRT
    """Rotation and translation of the link with respect to the parent joint.

    Warning:
        The rotation must be identity. Non-identity rotations are not supported yet.
    """
    shape: ShapeHandle
    """Shape handle defining the actor's geometry and other metadata.

    Note:
        A shape can be shared by multiple actors, even actors in different scenes.

    Note:
        Dynamic links require a shape with a surface mesh when link contact is
        enabled. An articulated actor enables link contact when no skin shape is
        provided. A soft-skinned actor enables link contact when
        :attr:`~superdex.physics.SoftSkinnedActorParams.enable_colliding_links` is
        true.

    See Also:
        :class:`~superdex.physics.ShapeHandle`,
        :func:`~superdex.physics.load_shape_from_file`
    """
    layer: str
    """Contact layer name for filtering contacts between actors.

    See Also:
        :meth:`~superdex.physics.Scene.enable_layer_contact_asymmetric`,
        :meth:`~superdex.physics.Scene.enable_layer_contact_symmetric`,
        :meth:`~superdex.physics.Scene.enable_actor_contact_asymmetric`,
        :meth:`~superdex.physics.Scene.enable_actor_contact_symmetric`
    """
    collider_type: ColliderType
    """Collision detection geometry.

    Note:
        Determines how OTHER actors detect contact with this actor. It does not
        affect how this actor detects contact with other actors.
    """
    contact: ContactParams
    """Contact mechanics parameters."""
    has_gravity: bool
    """If true, the link actor will be affected by gravity.

    See Also:
        :meth:`~superdex.physics.Scene.set_gravity`
    """
    density: Optional[float]
    """Uniform density [kg/m³] of the actor. Must be positive if provided.

    Note:
        Specify either density or mass, but not both.

    Note:
        If density is set, mass is computed automatically from volume (and vice
        versa).

    Note:
        If neither is set, :const:`~superdex.physics.DEFAULT_DENSITY` is used.

    See Also:
        :attr:`~superdex.physics.ArticulatedLinkParams.mass`
    """
    mass: Optional[float]
    """Total mass [kg] of the actor. Must be positive if provided.

    Note:
        Specify either mass or density, but not both.

    Note:
        If mass is set, density is computed automatically from volume (and vice
        versa).

    See Also:
        :attr:`~superdex.physics.ArticulatedLinkParams.density`
    """
    @property
    def center_of_mass(self) -> Optional[Real3]:
        """Position [m] of the center of mass in the link actor's local frame.

        Note:
            If not set, computed automatically from geometry.

        Note:
            Usually combined with explicit momentOfInertia.

        See Also:
            :attr:`~superdex.physics.ArticulatedLinkParams.moment_of_inertia`
        """
    @center_of_mass.setter
    def center_of_mass(self, value: Optional[Real3Like]) -> None: ...
    @property
    def moment_of_inertia(self) -> Optional[Real6]:
        """Moment of inertia tensor [kg·m²] at the center of mass in the actor's local
        frame.

        Stored as [ixx, ixy, ixz, iyy, iyz, izz] using negative tensor notation:

        ::

                 [ixx, ixy, ixz]   [ +∫(y²+z²)dm,  -∫(xy)dm,    -∫(xz)dm    ]
             I = [ixy, iyy, iyz] = [ -∫(xy)dm,     +∫(x²+z²)dm, -∫(yz)dm    ]
                 [ixz, iyz, izz]   [ -∫(xz)dm,     -∫(yz)dm,    +∫(x²+y²)dm ]

        Note:
            Uses negative tensor notation consistent with e.g. robotics URDF files.

        Note:
            Many CAD tools (e.g., Solidworks) use positive tensor notation requiring
            negation of off-diagonal terms. See
            https://www.mathworks.com/help/sm/ug/specify-custom-inertia.html.

        Note:
            Must be finite. A finite but physically invalid tensor (negative principal
            moments or one violating the triangle inequality) is accepted with a
            warning, not rejected.

        Note:
            If not set, computed automatically from geometry and density.

        Note:
            Usually combined with explicit centerOfMass.

        See Also:
            :attr:`~superdex.physics.ArticulatedLinkParams.center_of_mass`
        """
    @moment_of_inertia.setter
    def moment_of_inertia(self, value: Optional[Real6Like]) -> None: ...
    boundary_element_type: ActorBoundaryElementType
    """Finite element type for boundary discretization.

    Note:
        Affects accuracy and performance of contact and boundary integrals.
    """
    boundary_subsampling: Optional[BoundarySubsamplingParams]
    """Optional subsampling for boundary integrals such as contact.

    Note:
        Reduces computational cost by using fewer sample points.

    Note:
        For best performance, combine subsampling with
        :attr:`~superdex.physics.ArticulatedLinkParams.boundary_element_type` =
        :class:`P1Q1 <superdex.physics.ActorBoundaryElementType>`.
    """
    @overload
    def __init__(self) -> None: ...
    @overload
    def __init__(
        self,
        name: str = ...,
        parent_link: int = ...,
        parent_joint_from_link: TransformRT = ...,
        shape: ShapeHandle = ...,
        layer: str = ...,
        collider_type: ColliderType | int = ...,
        contact: ContactParams = ...,
        has_gravity: bool = ...,
        density: Optional[float] = ...,
        mass: Optional[float] = ...,
        center_of_mass: Optional[Real3Like] = ...,
        moment_of_inertia: Optional[Real6Like] = ...,
        boundary_element_type: ActorBoundaryElementType | int = ...,
        boundary_subsampling: Optional[BoundarySubsamplingParams] = ...,
    ) -> None: ...
    def __eq__(self, other: object) -> bool: ...
    def __ne__(self, other: object) -> bool: ...

class ArticulatedSkinParams:
    """Optional skinned mesh attached to an articulated actor for collision and
    rendering.

    The skin is a triangular or tetrahedral mesh deformed by the articulated links
    and, for a blended skin, the nested soft actors. The articulated actor acts as a
    colliding actor through the skin's surface: contact sample points on that
    surface are tested against other actors' collider geometry. The skin does not
    provide collider geometry, so other actors' contact sample points are not tested
    against it.

    See Also:
        :attr:`~superdex.physics.ArticulatedActorParams.skin`
    """
    shape: ShapeHandle
    """Shape handle defining the skinned triangular or tetrahedral mesh.

    Note:
        A shape can be shared by multiple actors, even actors in different scenes.

    See Also:
        :class:`~superdex.physics.ShapeHandle`,
        :func:`~superdex.physics.load_shape_from_file`
    """
    layer: str
    """Contact layer name for filtering contacts between actors.

    See Also:
        :meth:`~superdex.physics.Scene.enable_layer_contact_asymmetric`,
        :meth:`~superdex.physics.Scene.enable_layer_contact_symmetric`,
        :meth:`~superdex.physics.Scene.enable_actor_contact_asymmetric`,
        :meth:`~superdex.physics.Scene.enable_actor_contact_symmetric`
    """
    contact: ContactParams
    """Contact mechanics parameters."""
    boundary_element_type: ActorBoundaryElementType
    """Finite element type for boundary discretization.

    Note:
        Affects accuracy and performance of contact and boundary integrals.
    """
    boundary_subsampling: Optional[BoundarySubsamplingParams]
    """Optional subsampling for boundary integrals such as contact.

    Note:
        Reduces computational cost by using fewer sample points.

    Note:
        For best performance, combine subsampling with
        :attr:`~superdex.physics.ArticulatedSkinParams.boundary_element_type` =
        :class:`P1Q1 <superdex.physics.ActorBoundaryElementType>`.
    """
    @overload
    def __init__(self) -> None: ...
    @overload
    def __init__(
        self,
        shape: ShapeHandle = ...,
        layer: str = ...,
        contact: ContactParams = ...,
        boundary_element_type: ActorBoundaryElementType | int = ...,
        boundary_subsampling: Optional[BoundarySubsamplingParams] = ...,
    ) -> None: ...
    def __eq__(self, other: object) -> bool: ...
    def __ne__(self, other: object) -> bool: ...

class ArticulatedCycleJointParams:
    """Parameters for a cycle-closing joint within an articulated actor.

    Cycle joints close loops in the kinematic chain, enabling topologies beyond
    simple trees. Implemented as a soft spherical joint constraint (see
    :attr:`joint_from_child_link` and :attr:`stiffness`).

    See Also:
        :attr:`~superdex.physics.ArticulatedActorParams.cycles`,
        :class:`~superdex.physics.ArticulatedCycleJoint`
    """
    parent_link: int
    """Parent link index."""
    child_link: int
    """Child link index."""
    joint_from_child_link: TransformRT
    """Position and rotation of the cycle joint constraint in the child link's local
    frame.

    Note:
        A cycle joint is implemented using a spherical joint constraint. The
        :attr:`~superdex.physics.ArticulatedCycleJointParams.joint_from_child_link`
        translation defines the pivot point, in the child link's local frame.
        Rotation is currently unused, but it could affect joint limits in the
        future.
    """
    stiffness: float
    """Stiffness [N/m] for the cycle joint constraint."""
    @overload
    def __init__(self) -> None: ...
    @overload
    def __init__(
        self,
        parent_link: int = ...,
        child_link: int = ...,
        joint_from_child_link: TransformRT = ...,
        stiffness: float = ...,
    ) -> None: ...
    def __eq__(self, other: object) -> bool: ...
    def __ne__(self, other: object) -> bool: ...

class ArticulatedActorParams:
    """Parameters to create an articulated actor.

    Articulated actors are composed of rigid links connected by joints, forming a
    kinematic tree with optional cycle joints. Per-link properties are supplied via
    :attr:`links`, per-joint properties via :attr:`joints`, and cycle-closing joints
    via :attr:`cycles`. An optional skinned surface mesh can be attached via
    :attr:`skin`.

    Contact is automatically disabled between links that are: (a) directly adjacent,
    (b) reachable via hard joints, or (c) reachable via (dummy) shapeless links. To
    enable or disable contact for specific pairs of links, see
    :meth:`~superdex.physics.Scene.enable_actor_contact_symmetric`.

    See Also:
        :meth:`~superdex.physics.Scene.create_articulated_actor`
    """
    name: str
    """Name of the articulated actor.

    Note:
        Uniqueness of names is not enforced.

    Note:
        Link actor names have the form "articulatedActorName/linkLocalName", where
        `linkLocalName` is the link's local name. If the articulated actor name is
        empty, link actors use "unnamed_articulation" as the parent name.
    """
    world_from_root: TransformRT
    """Initial rotation and translation of the articulated actor's local frame with
    respect to world frame.
    """
    @property
    def cycles(self) -> DynamicArrayArticulatedCycleJointParams:
        """Optional cycle joints creating closed kinematic loops. Empty if no cycles
        exist.
        """
    @cycles.setter
    def cycles(self, value: ArrayLikeArticulatedCycleJointParams) -> None: ...
    @property
    def joints(self) -> DynamicArrayArticulatedJointParams:
        """Joint parameters (one per joint).

        Note:
            Size must match :attr:`~superdex.physics.ArticulatedActorParams.links`.
        """
    @joints.setter
    def joints(self, value: ArrayLikeArticulatedJointParams) -> None: ...
    @property
    def links(self) -> DynamicArrayArticulatedLinkParams:
        """Link actor parameters (one per link). Must contain at least one link.

        Note:
            Size must match :attr:`~superdex.physics.ArticulatedActorParams.joints`.

        Note:
            Links must be listed in parent-first order: the root link is at index 0 with
            `parentLink == -1`, and any other link's `parentLink` must be smaller than
            its own index.
        """
    @links.setter
    def links(self, value: ArrayLikeArticulatedLinkParams) -> None: ...
    skin: Optional[ArticulatedSkinParams]
    """Optional skinned mesh parameters."""
    @property
    def joint_velocities(self) -> Optional[DynamicArrayReal]:
        """Optional initial joint velocity per DoF [m/s or rad/s].

        See Also:
            :meth:`~superdex.physics.Actor.get_articulated_joint_velocities`,
            :meth:`~superdex.physics.Actor.set_articulated_joint_velocities`
        """
    @joint_velocities.setter
    def joint_velocities(self, value: Optional[ArrayLikeReal]) -> None: ...
    @overload
    def __init__(self) -> None: ...
    @overload
    def __init__(
        self,
        name: str = ...,
        world_from_root: TransformRT = ...,
        cycles: ArrayLikeArticulatedCycleJointParams = ...,
        joints: ArrayLikeArticulatedJointParams = ...,
        links: ArrayLikeArticulatedLinkParams = ...,
        skin: Optional[ArticulatedSkinParams] = ...,
        joint_velocities: Optional[ArrayLikeReal] = ...,
    ) -> None: ...
    def __eq__(self, other: object) -> bool: ...
    def __ne__(self, other: object) -> bool: ...

class SoftSkinnedActorParams:
    """Parameters to create soft-skinned actors with a shared articulated skeleton.

    Soft-skinned actors combine deformable soft bodies with an articulated skeleton,
    allowing realistic tissue simulation that follows skeletal motion.

    See Also:
        :meth:`~superdex.physics.Scene.create_soft_skinned_actor`
    """
    skeleton_params: ArticulatedActorParams
    """Parameters for the articulated skeleton.

    Note:
        If a skin shape is provided, the skin mesh acts as the colliding surface,
        not the surfaces of individual nested soft actors. The skin shape must
        include blending data to define how the skinned surfaces of the nested soft
        actors are blended with the overarching skin. Each nested soft actor must be
        linked to its corresponding blending data via its effective nested soft
        local name after default-name assignment.

    See Also:
        :class:`~superdex.physics.ArticulatedActorParams`,
        :class:`~superdex.physics.ArticulatedSkinParams`
    """
    @property
    def soft_params(self) -> DynamicArraySoftActorParams:
        """Parameters for the nested soft actors.

        Note:
            If a skin shape is provided, the skin mesh acts as the colliding surface,
            not the surfaces of individual nested soft actors.

        Note:
            If a skin shape is provided, dynamic hyper-reduction is not allowed on the
            nested soft actors.

        Note:
            If a :class:`~superdex.physics.SoftActorParams` entry has its hasInertia set
            to true, inertia is applied in the unposed configuration of that actor
            (prior to skinning) and this struct's hasInertia must be false.

        Note:
            If a :class:`~superdex.physics.SoftActorParams` entry has its hasStress set
            to true, elasticity is applied in the unposed configuration of that actor
            (prior to skinning) and this struct's hasStress must be false.

        Note:
            Each :class:`~superdex.physics.SoftActorParams` entry must have its
            hasGravity set to false (use this struct's hasGravity instead).

        Note:
            Each nested soft actor must have some DoFs constrained. This can be
            implemented (a) by constraining nodes in the
            :class:`~superdex.physics.SoftActorParams` shape for FOM actors, or (b) by
            constraining the ROM subspace for ROM actors.

        Note:
            Each nested soft actor must have at least one energy term enabled: either
            this struct's :attr:`~superdex.physics.SoftSkinnedActorParams.has_gravity`,
            :attr:`~superdex.physics.SoftSkinnedActorParams.has_inertia` or
            :attr:`~superdex.physics.SoftSkinnedActorParams.has_stress` (applied to
            every entry), or that entry's own
            :attr:`~superdex.physics.SoftActorParams.has_inertia` or
            :attr:`~superdex.physics.SoftActorParams.has_stress`.

        Note:
            Each entry's :attr:`~superdex.physics.SoftActorParams.name` is a nested soft
            local name. Explicit non-empty names must be unique across skeleton link
            local names and other nested soft local names, and must not contain forward
            slash, backslash, or embedded NUL characters.

        Note:
            Empty names are assigned deterministically after reserving all skeleton link
            names and all explicit nested soft names: an empty entry at index `i` uses
            `soft_i` if available; otherwise it uses the first available `soft_N` found
            by scanning upward from `N = 0`.

        Note:
            Each entry's :attr:`~superdex.physics.SoftActorParams.world_from_local` must
            be identity. Each nested soft actor's shape must be defined directly in the
            reference frame of the articulated actor. The soft-skinned actor's placement
            in scene world is provided by
            :attr:`~superdex.physics.ArticulatedActorParams.world_from_root` from
            :attr:`~superdex.physics.SoftSkinnedActorParams.skeleton_params`.

        See Also:
            :attr:`~superdex.physics.SoftSkinnedActorParams.has_gravity`,
            :attr:`~superdex.physics.SoftSkinnedActorParams.has_inertia`,
            :attr:`~superdex.physics.SoftSkinnedActorParams.has_stress`
        """
    @soft_params.setter
    def soft_params(self, value: ArrayLikeSoftActorParams) -> None: ...
    @property
    def soft_attach_links(self) -> DynamicArrayString:
        """Optional local names of the rigid links where nested soft actors are
        attached.

        Note:
            If empty, nested soft actor shapes must include skinning data.

        Note:
            If provided, must be 1-to-1 with
            :attr:`~superdex.physics.SoftSkinnedActorParams.soft_params`. Each entry
            must be non-empty and must match a skeleton link local name.

        Note:
            Contact is automatically disabled between nested soft actors and their
            attachment links.
        """
    @soft_attach_links.setter
    def soft_attach_links(self, value: ArrayLikeString) -> None: ...
    enable_colliding_links: bool
    """Enable internal skeleton links as colliding actors.

    Note:
        If false, only nested soft actors act as colliding actors when no skin is
        present; otherwise, only the articulated actor acts as a colliding actor
        through its skin.

    Note:
        This setting does not affect whether the links act as colliders.
    """
    has_gravity: bool
    """Enable gravity evaluation of nested soft actors on posed/skinned positions.

    Note:
        Each :class:`~superdex.physics.SoftActorParams` entry must have its
        hasGravity set to false.

    See Also:
        :meth:`~superdex.physics.Scene.set_gravity`
    """
    has_inertia: bool
    """Enable inertia evaluation of nested soft actors on posed/skinned positions.

    Note:
        If true, each :class:`~superdex.physics.SoftActorParams` entry must have its
        hasInertia set to false.

    Note:
        If true, inertia is applied in the posed (post-skinning) configuration. This
        is the physically correct behavior.

    Note:
        If false, inertia is applied in the unposed configuration for each
        :class:`~superdex.physics.SoftActorParams` entry that has its hasInertia set
        to true. This is an approximation but may improve performance.
    """
    has_stress: bool
    """Enable elasticity evaluation of nested soft actors on posed/skinned positions.

    Note:
        If true, each :class:`~superdex.physics.SoftActorParams` entry must have its
        hasStress set to false.

    Note:
        If true, stress forces are applied in the posed (post-skinning)
        configuration. This is the physically correct behavior.

    Note:
        If false, stress forces are applied in the unposed configuration for each
        :class:`~superdex.physics.SoftActorParams` entry that has its hasStress set
        to true. This is an approximation but may improve performance.
    """
    @overload
    def __init__(self) -> None: ...
    @overload
    def __init__(
        self,
        skeleton_params: ArticulatedActorParams = ...,
        soft_params: ArrayLikeSoftActorParams = ...,
        soft_attach_links: ArrayLikeString = ...,
        enable_colliding_links: bool = ...,
        has_gravity: bool = ...,
        has_inertia: bool = ...,
        has_stress: bool = ...,
    ) -> None: ...

class ConstraintParams:
    """Base constraint parameters.

    All constraint types inherit from this struct and add their specific parameters.
    Constraints apply forces and torques using a spring-damper model. Without
    saturation, the stiffness contribution is proportional to displacement or
    angular deviation, and the damping contribution is proportional to the
    corresponding linear or angular velocity. Their respective coefficients are
    :attr:`stiffness` and :attr:`damping`. :attr:`saturation` limits only the
    stiffness contribution.

    Note:
        SuperDex Physics uses the International System of Units (SI) by default.
        Other units can be used, but doing so requires overwriting all dimensional
        default parameters (material, contact, constraints, solver, etc.) to ensure
        all units remain consistent.

    See Also:
        :class:`~superdex.physics.ConstraintType`
    """
    stiffness: float
    """Constraint stiffness coefficient. [N/m] for translation constraints, [N·m/rad]
    for rotation constraints. Must be non-negative and finite.
    """
    damping: float
    """Constraint damping coefficient. [N·s/m] for translation constraints, [N·m·s/rad]
    for rotation constraints. Must be non-negative and finite.
    """
    saturation: float
    """Saturation distance [m] or angle [rad]. A negative value disables saturation.
    Must be finite and non-zero.

    Note:
        When enabled, saturation smoothly limits the magnitude of the elastic
        (stiffness) contribution to
        :attr:`~superdex.physics.ConstraintParams.stiffness` *
        :attr:`~superdex.physics.ConstraintParams.saturation`. The damping
        contribution is separate and is not limited by saturation.
    """
    @overload
    def __init__(self) -> None: ...
    @overload
    def __init__(
        self,
        stiffness: float = ...,
        damping: float = ...,
        saturation: float = ...,
    ) -> None: ...
    def __eq__(self, other: object) -> bool: ...
    def __ne__(self, other: object) -> bool: ...

class RigidSphericalJointConstraintParams(ConstraintParams):
    """Parameters for creating a spherical joint between two rigid actors.

    See Also:
        :meth:`~superdex.physics.Scene.create_rigid_spherical_joint_constraint`,
        :class:`RIGID_SPHERICAL_JOINT <superdex.physics.ConstraintType>`
    """
    @property
    def local_pos_a(self) -> Real3:
        """Joint position [m] for rigid actor A in actor A's local frame."""
    @local_pos_a.setter
    def local_pos_a(self, value: Real3Like) -> None: ...
    @property
    def local_pos_b(self) -> Real3:
        """Joint position [m] for rigid actor B in actor B's local frame."""
    @local_pos_b.setter
    def local_pos_b(self, value: Real3Like) -> None: ...
    actor_a: ActorHandle
    """Actor handle identifying rigid actor A."""
    actor_b: ActorHandle
    """Actor handle identifying rigid actor B."""
    @overload
    def __init__(self) -> None: ...
    @overload
    def __init__(
        self,
        stiffness: float = ...,
        damping: float = ...,
        saturation: float = ...,
        local_pos_a: Real3Like = ...,
        local_pos_b: Real3Like = ...,
        actor_a: ActorHandle = ...,
        actor_b: ActorHandle = ...,
    ) -> None: ...

class RigidPrismaticJointConstraintParams(ConstraintParams):
    """Parameters for creating a prismatic joint between two rigid actors.

    Note:
        Constrains translation perpendicular to the free axis (and optionally limits
        translation along it). Does not constrain rotation.

    Note:
        It must be complemented with
        :class:`~superdex.physics.JointRotationTrackingConstraintParams` to
        constrain the rotation and implement a full prismatic joint.

    See Also:
        :meth:`~superdex.physics.Scene.create_rigid_prismatic_joint_constraint`,
        :class:`RIGID_PRISMATIC_JOINT <superdex.physics.ConstraintType>`,
        :class:`~superdex.physics.JointRotationTrackingConstraintParams`
    """
    @property
    def free_axis(self) -> Real3:
        """Axis of free translation, specified in the world frame at constraint creation
        time.

        Note:
            The axis is captured at creation and thereafter rigidly attached to actor A:
            it rotates with actor A rather than remaining fixed in world space.

        Note:
            Must be non-zero and finite.

        Note:
            Its magnitude is ignored (normalized internally).
        """
    @free_axis.setter
    def free_axis(self, value: Real3Like) -> None: ...
    actor_a: ActorHandle
    """Actor handle identifying rigid actor A."""
    actor_b: ActorHandle
    """Actor handle identifying rigid actor B."""
    max: Optional[float]
    """Maximum translation [m] relative to rest position. Empty means unbounded."""
    min: Optional[float]
    """Minimum translation [m] relative to rest position. Empty means unbounded."""
    @overload
    def __init__(self) -> None: ...
    @overload
    def __init__(
        self,
        stiffness: float = ...,
        damping: float = ...,
        saturation: float = ...,
        free_axis: Real3Like = ...,
        actor_a: ActorHandle = ...,
        actor_b: ActorHandle = ...,
        max: Optional[float] = ...,
        min: Optional[float] = ...,
    ) -> None: ...

class DeformableNodeToDeformableNodeConstraintParams(ConstraintParams):
    """Parameters for creating a constraint coupling a node in a deformable actor to a
    node in another deformable actor.

    See Also:
        :meth:`~superdex.physics.Scene.create_deformable_node_to_deformable_node_constraint`,
        :class:`DEFORMABLE_NODE_TO_DEFORMABLE_NODE
        <superdex.physics.ConstraintType>`
    """
    node_index_a: int
    """Node index in deformable actor A's reference simulation mesh, as returned by
    :meth:`~superdex.physics.Actor.get_mesh`. Ignored if
    :attr:`~superdex.physics.DeformableNodeToDeformableNodeConstraintParams.find_closest`
    is true; otherwise it must identify a node in that mesh.
    """
    node_index_b: int
    """Node index in deformable actor B's reference simulation mesh, as returned by
    :meth:`~superdex.physics.Actor.get_mesh`. Ignored if
    :attr:`~superdex.physics.DeformableNodeToDeformableNodeConstraintParams.find_closest`
    is true; otherwise it must identify a node in that mesh.
    """
    actor_a: ActorHandle
    """Actor handle identifying deformable actor A."""
    actor_b: ActorHandle
    """Actor handle identifying deformable actor B."""
    find_closest: bool
    """Whether to automatically select a boundary node on each actor.

    Note:
        When true, selection is based on proximity to the world-space midpoint of
        the actors' root-frame origins and occurs at constraint creation using their
        undeformed reference simulation meshes.
        :attr:`~superdex.physics.DeformableNodeToDeformableNodeConstraintParams.node_index_a`
        and
        :attr:`~superdex.physics.DeformableNodeToDeformableNodeConstraintParams.node_index_b`
        are ignored.

    Note:
        When true, both actors must be soft actors (shell and rod actors are not
        supported).
    """
    @overload
    def __init__(self) -> None: ...
    @overload
    def __init__(
        self,
        stiffness: float = ...,
        damping: float = ...,
        saturation: float = ...,
        node_index_a: int = ...,
        node_index_b: int = ...,
        actor_a: ActorHandle = ...,
        actor_b: ActorHandle = ...,
        find_closest: bool = ...,
    ) -> None: ...

class RodElementRotationToRigidConstraintParams(ConstraintParams):
    """Parameters for creating a tracking constraint on the relative rotation between a
    rigid actor and a rod element.

    Forces the relative rotation between the rigid actor and a specified rod element
    to track a target rotation. The target rotation is initialized to the relative
    rotation observed at constraint creation time, and can be updated at runtime via
    :meth:`~superdex.physics.Constraint.set_target_rotation`.

    See Also:
        :meth:`~superdex.physics.Scene.create_rod_element_rotation_to_rigid_constraint`,
        :class:`ROD_ELEMENT_ROTATION_TO_RIGID <superdex.physics.ConstraintType>`,
        :meth:`~superdex.physics.Constraint.set_target_rotation`
    """
    @property
    def ref_frame_rot_vec(self) -> Real3:
        """Joint reference-frame orientation in world coordinates at constraint
        creation,
        expressed as a rotation vector [rad].

        Note:
            After creation, the frame remains fixed relative to the rigid actor.
        """
    @ref_frame_rot_vec.setter
    def ref_frame_rot_vec(self, value: Real3Like) -> None: ...
    rigid_actor: ActorHandle
    """Actor handle identifying rigid actor."""
    rod_actor: ActorHandle
    """Actor handle identifying rod actor."""
    element_index: int
    """Index of the element in the rod actor."""
    @overload
    def __init__(self) -> None: ...
    @overload
    def __init__(
        self,
        stiffness: float = ...,
        damping: float = ...,
        saturation: float = ...,
        ref_frame_rot_vec: Real3Like = ...,
        rigid_actor: ActorHandle = ...,
        rod_actor: ActorHandle = ...,
        element_index: int = ...,
    ) -> None: ...

class DeformableNodeToRigidConstraintParams(ConstraintParams):
    """Parameters for creating a constraint coupling a deformable node to a rigid
    actor.

    See Also:
        :meth:`~superdex.physics.Scene.create_deformable_node_to_rigid_constraint`,
        :class:`DEFORMABLE_NODE_TO_RIGID <superdex.physics.ConstraintType>`
    """
    @property
    def rigid_local_pos(self) -> Real3:
        """Coupling position [m] in rigid actor's local frame.

        Note:
            When
            :attr:`~superdex.physics.DeformableNodeToRigidConstraintParams.fix_to_deformable_pos`
            is true, this field is ignored and the coupling position is auto-computed
            based on the deformable node's initial position in the rigid actor's frame.

        Note:
            When
            :attr:`~superdex.physics.DeformableNodeToRigidConstraintParams.find_closest`
            is also true, this field is still used as the search point that selects
            which deformable node is picked.
        """
    @rigid_local_pos.setter
    def rigid_local_pos(self, value: Real3Like) -> None: ...
    deformable_node_index: int
    """Node index in the deformable actor's reference simulation mesh, as returned by
    :meth:`~superdex.physics.Actor.get_mesh`. Ignored if
    :attr:`~superdex.physics.DeformableNodeToRigidConstraintParams.find_closest` is
    true; otherwise it must identify a node in that mesh.
    """
    rigid_actor: ActorHandle
    """Actor handle identifying the rigid actor."""
    deformable_actor: ActorHandle
    """Actor handle identifying the deformable actor."""
    find_closest: bool
    """Whether to automatically select a boundary node of the deformable actor.

    Note:
        When true, selection is based on proximity to
        :attr:`~superdex.physics.DeformableNodeToRigidConstraintParams.rigid_local_pos`
        transformed to world space by the rigid actor's transform at constraint
        creation. Selection uses the deformable actor's undeformed reference
        simulation mesh, and
        :attr:`~superdex.physics.DeformableNodeToRigidConstraintParams.deformable_node_index`
        is ignored.

    Note:
        When true, the deformable actor must be a soft actor (shell and rod actors
        are not supported).
    """
    fix_to_deformable_pos: bool
    """When true, the coupling position on the rigid actor is auto-computed to match
    the deformable node's initial position (in the rigid actor's frame), so the
    constraint starts at zero deviation, and
    :attr:`~superdex.physics.DeformableNodeToRigidConstraintParams.rigid_local_pos`
    is ignored.

    Note:
        When
        :attr:`~superdex.physics.DeformableNodeToRigidConstraintParams.find_closest`
        is also true,
        :attr:`~superdex.physics.DeformableNodeToRigidConstraintParams.rigid_local_pos`
        is still used as the search point that selects which deformable node is
        picked.
    """
    @overload
    def __init__(self) -> None: ...
    @overload
    def __init__(
        self,
        stiffness: float = ...,
        damping: float = ...,
        saturation: float = ...,
        rigid_local_pos: Real3Like = ...,
        deformable_node_index: int = ...,
        rigid_actor: ActorHandle = ...,
        deformable_actor: ActorHandle = ...,
        find_closest: bool = ...,
        fix_to_deformable_pos: bool = ...,
    ) -> None: ...

class JointRotationRangeConstraintParams(ConstraintParams):
    """Parameters for creating a joint rotation range constraint between two rigid
    actors.

    Constrains the relative rotation between two rigid actors to remain within
    specified bounds. The relative rotation is expressed in a joint reference frame
    (captured at constraint creation time and rigidly attached to actor A
    thereafter), and each component of its rotation-vector (axis-angle)
    representation is independently constrained to its [min, max] range.

    See Also:
        :meth:`~superdex.physics.Scene.create_joint_rotation_range_constraint`,
        :class:`JOINT_ROTATION_RANGE <superdex.physics.ConstraintType>`
    """
    @property
    def ref_frame_rot_vec(self) -> Real3:
        """Joint reference frame specified as a rotation vector [rad] with respect to
        world
        frame.
        """
    @ref_frame_rot_vec.setter
    def ref_frame_rot_vec(self, value: Real3Like) -> None: ...
    @property
    def angle_range_x(self) -> Real2:
        """Allowed range [min, max] [rad] for the X component of the relative rotation
        vector (axis-angle), expressed in the joint reference frame.
        """
    @angle_range_x.setter
    def angle_range_x(self, value: Real2Like) -> None: ...
    @property
    def angle_range_y(self) -> Real2:
        """Allowed range [min, max] [rad] for the Y component of the relative rotation
        vector (axis-angle), expressed in the joint reference frame.
        """
    @angle_range_y.setter
    def angle_range_y(self, value: Real2Like) -> None: ...
    @property
    def angle_range_z(self) -> Real2:
        """Allowed range [min, max] [rad] for the Z component of the relative rotation
        vector (axis-angle), expressed in the joint reference frame.
        """
    @angle_range_z.setter
    def angle_range_z(self, value: Real2Like) -> None: ...
    actor_a: ActorHandle
    """Actor handle identifying rigid actor A."""
    actor_b: ActorHandle
    """Actor handle identifying rigid actor B."""
    range_around_rest: bool
    """When true, angle limits are relative to the actors' relative rotation at
    constraint creation time. When false, limits are absolute in the joint reference
    frame.
    """
    @overload
    def __init__(self) -> None: ...
    @overload
    def __init__(
        self,
        stiffness: float = ...,
        damping: float = ...,
        saturation: float = ...,
        ref_frame_rot_vec: Real3Like = ...,
        angle_range_x: Real2Like = ...,
        angle_range_y: Real2Like = ...,
        angle_range_z: Real2Like = ...,
        actor_a: ActorHandle = ...,
        actor_b: ActorHandle = ...,
        range_around_rest: bool = ...,
    ) -> None: ...

class RigidPivotPositionConstraintParams(ConstraintParams):
    """Parameters for creating a position constraint on a rigid actor's pivot point.

    Couples a point on a rigid actor (expressed in the actor's local coordinates and
    referred to as "pivot") to a specific target position in world coordinates.

    See Also:
        :meth:`~superdex.physics.Scene.create_rigid_pivot_position_constraint`,
        :class:`RIGID_PIVOT_POSITION <superdex.physics.ConstraintType>`
    """
    @property
    def target_position(self) -> Real3:
        """Target position in world frame [m]."""
    @target_position.setter
    def target_position(self, value: Real3Like) -> None: ...
    @property
    def local_position(self) -> Real3:
        """Pivot point in rigid actor's local frame [m]."""
    @local_position.setter
    def local_position(self, value: Real3Like) -> None: ...
    actor: ActorHandle
    """Actor handle identifying the rigid actor."""
    @overload
    def __init__(self) -> None: ...
    @overload
    def __init__(
        self,
        stiffness: float = ...,
        damping: float = ...,
        saturation: float = ...,
        target_position: Real3Like = ...,
        local_position: Real3Like = ...,
        actor: ActorHandle = ...,
    ) -> None: ...

class RigidPivotToRigidTargetConstraintParams(ConstraintParams):
    """Parameters for creating a constraint on a rigid actor's pivot point. The target
    is expressed as the center-of-mass transform of the actor.

    Couples a point on a rigid actor (expressed in the actor's local coordinates and
    referred to as "pivot") to the world position of the pivot under a target
    center-of-mass transform.

    See Also:
        :meth:`~superdex.physics.Scene.create_rigid_pivot_to_rigid_target_constraint`,
        :class:`RIGID_PIVOT_TO_RIGID_TARGET <superdex.physics.ConstraintType>`
    """
    target_transform: TransformRT
    """Target center-of-mass transform."""
    @property
    def local_position(self) -> Real3:
        """Pivot point in rigid actor's local frame [m]."""
    @local_position.setter
    def local_position(self, value: Real3Like) -> None: ...
    actor: ActorHandle
    """Actor handle identifying the rigid actor."""
    @overload
    def __init__(self) -> None: ...
    @overload
    def __init__(
        self,
        stiffness: float = ...,
        damping: float = ...,
        saturation: float = ...,
        target_transform: TransformRT = ...,
        local_position: Real3Like = ...,
        actor: ActorHandle = ...,
    ) -> None: ...

class RigidPivotRotationConstraintParams(ConstraintParams):
    """Parameters for creating a rotation constraint on a rigid actor's pivot frame.

    Couples a frame on a rigid actor (expressed in the actor's local coordinates and
    referred to as "pivot") to a specific target rotation in world coordinates.

    See Also:
        :meth:`~superdex.physics.Scene.create_rigid_pivot_rotation_constraint`,
        :class:`RIGID_PIVOT_ROTATION <superdex.physics.ConstraintType>`
    """
    @property
    def target_rotation(self) -> Real3:
        """Target rotation in world coordinates, as a rotation vector [rad]."""
    @target_rotation.setter
    def target_rotation(self, value: Real3Like) -> None: ...
    @property
    def local_rotation(self) -> Real3:
        """Pivot frame in rigid actor's local coordinates, as a rotation vector [rad].
        """
    @local_rotation.setter
    def local_rotation(self, value: Real3Like) -> None: ...
    actor: ActorHandle
    """Actor handle identifying the rigid actor."""
    @overload
    def __init__(self) -> None: ...
    @overload
    def __init__(
        self,
        stiffness: float = ...,
        damping: float = ...,
        saturation: float = ...,
        target_rotation: Real3Like = ...,
        local_rotation: Real3Like = ...,
        actor: ActorHandle = ...,
    ) -> None: ...

class DeformableNodePositionConstraintParams(ConstraintParams):
    """Parameters for creating a position constraint on a deformable actor node.

    See Also:
        :meth:`~superdex.physics.Scene.create_deformable_node_position_constraint`,
        :class:`DEFORMABLE_NODE_POSITION <superdex.physics.ConstraintType>`
    """
    node_index: int
    """Node index in the deformable actor's reference simulation mesh, as returned by
    :meth:`~superdex.physics.Actor.get_mesh`. Must identify a node in that mesh.
    """
    @property
    def position(self) -> Real3:
        """Target position for the node in world frame [m]."""
    @position.setter
    def position(self, value: Real3Like) -> None: ...
    actor: ActorHandle
    """Actor handle identifying the deformable actor."""
    @overload
    def __init__(self) -> None: ...
    @overload
    def __init__(
        self,
        stiffness: float = ...,
        damping: float = ...,
        saturation: float = ...,
        node_index: int = ...,
        position: Real3Like = ...,
        actor: ActorHandle = ...,
    ) -> None: ...
    def __eq__(self, other: object) -> bool: ...
    def __ne__(self, other: object) -> bool: ...

class JointRotationTrackingConstraintParams(ConstraintParams):
    """Parameters for creating a joint rotation tracking constraint between two rigid
    actors.

    Forces the relative rotation between two rigid actors to track a target
    rotation. The target rotation is initialized to the relative rotation observed
    at constraint creation time, and can be updated at runtime via
    :meth:`~superdex.physics.Constraint.set_target_rotation`. The relative rotation
    vector is expressed in a reference frame specified by :attr:`ref_frame_rot_vec`.

    Note:
        Sometimes used in combination with
        :class:`~superdex.physics.RigidPrismaticJointConstraintParams` to fully
        constrain rotation in a prismatic joint.

    See Also:
        :meth:`~superdex.physics.Scene.create_joint_rotation_tracking_constraint`,
        :class:`JOINT_ROTATION_TRACKING <superdex.physics.ConstraintType>`,
        :class:`~superdex.physics.RigidPrismaticJointConstraintParams`,
        :meth:`~superdex.physics.Constraint.set_target_rotation`
    """
    @property
    def ref_frame_rot_vec(self) -> Real3:
        """Joint reference-frame orientation in world coordinates at constraint
        creation,
        expressed as a rotation vector [rad].

        Note:
            After creation, the frame remains fixed relative to actor A.
        """
    @ref_frame_rot_vec.setter
    def ref_frame_rot_vec(self, value: Real3Like) -> None: ...
    actor_a: ActorHandle
    """Actor handle identifying rigid actor A."""
    actor_b: ActorHandle
    """Actor handle identifying rigid actor B."""
    @overload
    def __init__(self) -> None: ...
    @overload
    def __init__(
        self,
        stiffness: float = ...,
        damping: float = ...,
        saturation: float = ...,
        ref_frame_rot_vec: Real3Like = ...,
        actor_a: ActorHandle = ...,
        actor_b: ActorHandle = ...,
    ) -> None: ...

class ArticulatedSingleDofTargetConstraintParams(ConstraintParams):
    """Parameters for creating a target constraint on a single articulated DoF.

    Forces the specified degree of freedom in a joint of an articulated actor to
    track a given target value. This drives the joint toward a desired
    configuration.

    Note:
        It applies only to translation DoFs (free or prismatic joints) or single-DoF
        rotation joints (revolute). For 3D rotation joints (spherical or free), use
        instead
        :class:`~superdex.physics.Articulated3dRotationTargetConstraintParams`.

    See Also:
        :meth:`~superdex.physics.Scene.create_articulated_single_dof_target_constraint`,
        :class:`ARTICULATED_SINGLE_DOF_TARGET <superdex.physics.ConstraintType>`
    """
    actor: ActorHandle
    """Actor handle identifying the articulated actor."""
    joint_index: int
    """Index of the joint in the articulated actor."""
    dof_index: int
    """DoF index within the joint (0-based). Translation DoFs come first, then rotation
    DoFs. For free joints, only translation indices are accepted (use
    :class:`~superdex.physics.Articulated3dRotationTargetConstraintParams` to target
    the 3D rotation).
    """
    target_value: float
    """Target value for the joint DoF. Units are [m] for translation DoFs, [rad] for
    rotation DoFs.
    """
    @overload
    def __init__(self) -> None: ...
    @overload
    def __init__(
        self,
        stiffness: float = ...,
        damping: float = ...,
        saturation: float = ...,
        actor: ActorHandle = ...,
        joint_index: int = ...,
        dof_index: int = ...,
        target_value: float = ...,
    ) -> None: ...

class Articulated3dRotationTargetConstraintParams(ConstraintParams):
    """Parameters for creating a target constraint on 3D rotation DoFs of an
    articulated actor.

    Forces the rotation degrees of freedom of a spherical or free joint in an
    articulated actor to track a given target orientation. This drives the joint
    toward a desired rotational configuration.

    Note:
        Applies only to joints with 3D rotation DoFs (spherical joints, or the
        rotational part of free joints).

    See Also:
        :meth:`~superdex.physics.Scene.create_articulated3d_rotation_target_constraint`,
        :class:`ARTICULATED3D_ROTATION_TARGET <superdex.physics.ConstraintType>`
    """
    actor: ActorHandle
    """Actor handle identifying the articulated actor."""
    joint_index: int
    """Index of the joint in the articulated actor."""
    @property
    def target(self) -> Quaternion:
        """Target rotation relative to the joint's rest orientation, expressed in the
        joint's local frame as a quaternion [x, y, z, w]. The identity quaternion
        corresponds to the rest orientation.

        Note:
            Must be finite and non-zero. Normalized before use.
        """
    @target.setter
    def target(self, value: QuaternionLike) -> None: ...
    @overload
    def __init__(self) -> None: ...
    @overload
    def __init__(
        self,
        stiffness: float = ...,
        damping: float = ...,
        saturation: float = ...,
        actor: ActorHandle = ...,
        joint_index: int = ...,
        target: QuaternionLike = ...,
    ) -> None: ...

class ArticulatedSingleDofRangeConstraintParams(ConstraintParams):
    """Parameters for creating a range constraint on a single articulated DoF.

    Limits a degree of freedom of a joint in an articulated actor to remain within
    specified [min, max] bounds. This prevents the joint from exceeding its
    mechanical limits.

    Note:
        It applies only to translation DoFs (free or prismatic joints) or single-DoF
        rotation joints (revolute). For 3D rotation joints (spherical or free), use
        instead
        :class:`~superdex.physics.Articulated3dRotationRangeConstraintParams`.

    See Also:
        :meth:`~superdex.physics.Scene.create_articulated_single_dof_range_constraint`,
        :class:`ARTICULATED_SINGLE_DOF_RANGE <superdex.physics.ConstraintType>`
    """
    actor: ActorHandle
    """Actor handle identifying the articulated actor."""
    joint_index: int
    """Index of the joint in the articulated actor."""
    dof_index: int
    """DoF index within the joint (0-based). Translation DoFs come first, then rotation
    DoFs. For free joints, only translation indices are accepted (use
    :class:`~superdex.physics.Articulated3dRotationRangeConstraintParams` to
    constrain the 3D rotation).
    """
    min_value: float
    """Minimum value for the DoF. [m] for translation DoFs, [rad] for rotation DoFs."""
    max_value: float
    """Maximum value for the DoF. [m] for translation DoFs, [rad] for rotation DoFs."""
    @overload
    def __init__(self) -> None: ...
    @overload
    def __init__(
        self,
        stiffness: float = ...,
        damping: float = ...,
        saturation: float = ...,
        actor: ActorHandle = ...,
        joint_index: int = ...,
        dof_index: int = ...,
        min_value: float = ...,
        max_value: float = ...,
    ) -> None: ...

class Articulated3dRotationRangeConstraintParams(ConstraintParams):
    """Parameters for creating a range constraint on a 3D-rotation articulated joint.

    Constrains each component of a spherical or free joint's local rotation vector
    (rotation axis multiplied by rotation angle) to its corresponding [min, max]
    range. Use this constraint to model the joint's mechanical rotation limits.

    See Also:
        :meth:`~superdex.physics.Scene.create_articulated3d_rotation_range_constraint`,
        :class:`ARTICULATED3D_ROTATION_RANGE <superdex.physics.ConstraintType>`
    """
    actor: ActorHandle
    """Actor handle identifying the articulated actor."""
    joint_index: int
    """Index of the joint in the articulated actor."""
    @property
    def min_values(self) -> Real3:
        """Minimum allowed value [rad] for each component of the joint-local rotation
        vector.
        """
    @min_values.setter
    def min_values(self, value: Real3Like) -> None: ...
    @property
    def max_values(self) -> Real3:
        """Maximum allowed value [rad] for each component of the joint-local rotation
        vector.
        """
    @max_values.setter
    def max_values(self, value: Real3Like) -> None: ...
    @overload
    def __init__(self) -> None: ...
    @overload
    def __init__(
        self,
        stiffness: float = ...,
        damping: float = ...,
        saturation: float = ...,
        actor: ActorHandle = ...,
        joint_index: int = ...,
        min_values: Real3Like = ...,
        max_values: Real3Like = ...,
    ) -> None: ...

class ContactPoint:
    """Information about a contact point between two actors.

    Describes a single contact or near-contact point between two actors. Contains
    geometric information (positions, normal, distance), kinematic data
    (velocities), and dynamic data (force). Also includes finite element
    discretization information for accurate force interpolation.

    Note:
        Available via :meth:`~superdex.physics.Actor.get_contact_points_world` after
        registering :class:`CONTACT_POINTS <superdex.physics.QueryType>`.

    See Also:
        :class:`CONTACT_POINTS <superdex.physics.QueryType>`,
        :meth:`~superdex.physics.Actor.register_query`,
        :meth:`~superdex.physics.Actor.get_contact_points_world`
    """
    actor_a: ActorHandle
    """Handle of the colliding actor.

    Note:
        When calling :meth:`~superdex.physics.Actor.get_contact_points_world`, the
        actor being queried may be reported as either
        :attr:`~superdex.physics.ContactPoint.actor_a` or
        :attr:`~superdex.physics.ContactPoint.actor_b` in this struct, depending on
        the direction of the contact test.

    Note:
        May be the same as :attr:`~superdex.physics.ContactPoint.actor_b` in the
        case of self-contact.
    """
    actor_b: ActorHandle
    """Handle of the collider actor.

    Note:
        When calling :meth:`~superdex.physics.Actor.get_contact_points_world`, the
        actor being queried may be reported as either
        :attr:`~superdex.physics.ContactPoint.actor_a` or
        :attr:`~superdex.physics.ContactPoint.actor_b` in this struct, depending on
        the direction of the contact test.

    Note:
        May be the same as :attr:`~superdex.physics.ContactPoint.actor_a` in the
        case of self-contact.
    """
    distance: float
    """Signed separation distance [m] between
    :attr:`~superdex.physics.ContactPoint.pos_a` (on
    :attr:`~superdex.physics.ContactPoint.actor_a`) and the closest point on the
    surface of :attr:`~superdex.physics.ContactPoint.actor_b`. Negative if the point
    is inside (i.e. actors overlap).
    """
    @property
    def pos_a(self) -> Real3:
        """Contact position [m] on :attr:`~superdex.physics.ContactPoint.actor_a` in
        world
        frame.
        """
    @pos_a.setter
    def pos_a(self, value: Real3Like) -> None: ...
    @property
    def pos_b(self) -> Real3:
        """Approximate contact position [m] on
        :attr:`~superdex.physics.ContactPoint.actor_b` in world frame.

        Note:
            Equivalent to (posA - normal * distance).
        """
    @pos_b.setter
    def pos_b(self, value: Real3Like) -> None: ...
    @property
    def normal(self) -> Real3:
        """Contact normal in world frame pointing away from
        :attr:`~superdex.physics.ContactPoint.actor_b`. Unit length.
        """
    @normal.setter
    def normal(self, value: Real3Like) -> None: ...
    @property
    def force(self) -> Real3:
        """Contact force [N] applied to :attr:`~superdex.physics.ContactPoint.actor_a`
        from
        :attr:`~superdex.physics.ContactPoint.actor_b` in world frame.
        """
    @force.setter
    def force(self, value: Real3Like) -> None: ...
    @property
    def point_velocity_a(self) -> Real3:
        """Velocity [m/s] of the contact point on
        :attr:`~superdex.physics.ContactPoint.actor_a` in world frame.
        """
    @point_velocity_a.setter
    def point_velocity_a(self, value: Real3Like) -> None: ...
    @property
    def point_velocity_b(self) -> Real3:
        """Velocity [m/s] of the contact point on
        :attr:`~superdex.physics.ContactPoint.actor_b` in world frame.
        """
    @point_velocity_b.setter
    def point_velocity_b(self, value: Real3Like) -> None: ...
    sample_index: int
    """Sample point index of :attr:`~superdex.physics.ContactPoint.actor_a`."""
    int_weight: float
    """Quadrature integration weight of the sample point on the colliding manifold of
    :attr:`~superdex.physics.ContactPoint.actor_a`.

    Note:
        Units depend on the dimensionality of the colliding manifold of
        :attr:`~superdex.physics.ContactPoint.actor_a`: [m²] for surface contact
        (e.g., rigid, articulated, soft, shell, and rod actors using contact-skin
        contact), or [m] for rod actors using centerline contact.

    Note:
        For surface contact, represents the surface area corresponding to the sample
        point. For rod centerline contact, represents the arc length.

    Note:
        Useful for contact area/length estimation and other post-processing.

    Note:
        :attr:`~superdex.physics.ContactPoint.force` is already weighted by this
        value.
    """
    element_index: int
    """Element index on the surface mesh of
    :attr:`~superdex.physics.ContactPoint.actor_a`.

    Note:
        For rod actors, this field is not populated and is reported as 0. Use
        :attr:`~superdex.physics.ContactPoint.sample_index` to identify the contact
        location.
    """
    @property
    def parametric_coords(self) -> Real3:
        """Parametric coordinates within the surface element of
        :attr:`~superdex.physics.ContactPoint.actor_a`.

        Note:
            Corresponds to finite-element basis function values at the contact sample
            quadrature point.

        Note:
            For linear finite elements, these match barycentric coordinates.

        Note:
            Allows interpolation of functions defined on triangle vertices.

        Note:
            For rod actors, this field is not populated and is reported as {0, 0, 0}.
            Use :attr:`~superdex.physics.ContactPoint.sample_index` to identify the
            contact location.
        """
    @parametric_coords.setter
    def parametric_coords(self, value: Real3Like) -> None: ...
    @overload
    def __init__(self) -> None: ...
    @overload
    def __init__(
        self,
        actor_a: ActorHandle = ...,
        actor_b: ActorHandle = ...,
        distance: float = ...,
        pos_a: Real3Like = ...,
        pos_b: Real3Like = ...,
        normal: Real3Like = ...,
        force: Real3Like = ...,
        point_velocity_a: Real3Like = ...,
        point_velocity_b: Real3Like = ...,
        sample_index: int = ...,
        int_weight: float = ...,
        element_index: int = ...,
        parametric_coords: Real3Like = ...,
    ) -> None: ...

class NodeContactForce:
    """Contact force applied to a specific node.

    Note:
        Available via :meth:`~superdex.physics.Actor.get_node_contact_forces_world`
        after registering :class:`NODE_CONTACT_FORCES <superdex.physics.QueryType>`.

    See Also:
        :class:`NODE_CONTACT_FORCES <superdex.physics.QueryType>`,
        :meth:`~superdex.physics.Actor.register_query`,
        :meth:`~superdex.physics.Actor.get_node_contact_forces_world`
    """
    index: int
    """Node index in the volumetric mesh (for actors with a volumetric mesh) or surface
    mesh (for actors without a volumetric mesh).
    """
    @property
    def force(self) -> Real3:
        """Contact force [N] applied to the node."""
    @force.setter
    def force(self, value: Real3Like) -> None: ...
    @overload
    def __init__(self) -> None: ...
    @overload
    def __init__(self, index: int = ..., force: Real3Like = ...) -> None: ...

class SdfDistances:
    """SDF distance information for active contact sample points.

    Stores signed distance field (SDF) values and gradients for contact sample
    points. Negative distances indicate penetration into the surface.

    Note:
        Available via :meth:`~superdex.physics.Actor.get_sdf_distances` after
        registering :class:`SDF_DISTANCES <superdex.physics.QueryType>`.

    See Also:
        :class:`SDF_DISTANCES <superdex.physics.QueryType>`,
        :meth:`~superdex.physics.Actor.register_query`,
        :meth:`~superdex.physics.Actor.get_sdf_distances`
    """
    @property
    def sample_indices(self) -> SpanConstInt:
        """Indices into the actor's contact sample points array."""
    @sample_indices.setter
    def sample_indices(self, value: ArrayLikeInt) -> None: ...
    @property
    def world_positions(self) -> SpanConstReal3:
        """Sample positions [m] in world frame."""
    @world_positions.setter
    def world_positions(self, value: ArrayLikeReal3) -> None: ...
    @property
    def distances(self) -> SpanConstReal:
        """Signed distances [m] to the closest surface.

        Note:
            Negative values indicate penetration inside the surface.
        """
    @distances.setter
    def distances(self, value: ArrayLikeReal) -> None: ...
    @property
    def distance_grads(self) -> SpanConstReal3:
        """SDF gradients at sample positions, in world frame."""
    @distance_grads.setter
    def distance_grads(self, value: ArrayLikeReal3) -> None: ...
    max_sdf_far_distance_evaluation: float
    """Maximum distance [m] at which SDF evaluation was performed.

    Note:
        Non-zero only if the actor has far SDF evaluation enabled.
    """
    @overload
    def __init__(self) -> None: ...
    @overload
    def __init__(
        self,
        sample_indices: ArrayLikeInt = ...,
        world_positions: ArrayLikeReal3 = ...,
        distances: ArrayLikeReal = ...,
        distance_grads: ArrayLikeReal3 = ...,
        max_sdf_far_distance_evaluation: float = ...,
    ) -> None: ...

class PerformanceStats:
    """Timing and profiling metrics from the last simulation step.

    See Also:
        :meth:`~superdex.physics.Scene.get_performance_stats`
    """
    total_step_duration_sec: float
    """Total wall-clock time [s] spent in the last call to
    :meth:`~superdex.physics.Scene.step`.

    Note:
        Includes time spent in user-defined pre-step and post-step callbacks.
    """
    solve_step_duration_sec: float
    """Wall-clock time [s] spent solving physics in the last call to
    :meth:`~superdex.physics.Scene.step`.

    Note:
        Does not include time spent in user-defined pre-step and post-step
        callbacks.
    """
    pre_step_callbacks_duration_sec: float
    """Wall-clock time [s] spent executing user-defined pre-step callbacks in the last
    call to :meth:`~superdex.physics.Scene.step`.
    """
    post_step_callbacks_duration_sec: float
    """Wall-clock time [s] spent executing user-defined post-step callbacks in the last
    call to :meth:`~superdex.physics.Scene.step`.
    """
    recording_step_duration_sec: float
    """Wall-clock time [s] spent on recording in the last call to
    :meth:`~superdex.physics.Scene.step`.
    """
    @overload
    def __init__(self) -> None: ...
    @overload
    def __init__(
        self,
        total_step_duration_sec: float = ...,
        solve_step_duration_sec: float = ...,
        pre_step_callbacks_duration_sec: float = ...,
        post_step_callbacks_duration_sec: float = ...,
        recording_step_duration_sec: float = ...,
    ) -> None: ...
    def __eq__(self, other: object) -> bool: ...
    def __ne__(self, other: object) -> bool: ...

class SolverStats:
    """Solver convergence metrics from the last simulation step.

    See Also:
        :meth:`~superdex.physics.Scene.get_solver_stats`
    """
    max_non_linear_iters: int
    """Maximum number of non-linear solver iterations across all islands and
    integration stages in the last call to :meth:`~superdex.physics.Scene.step`.
    """
    residual_norm: float
    """Aggregate residual norm across all islands and integration stages in the last
    call to :meth:`~superdex.physics.Scene.step`. Computed as the root mean square
    across integration stages of the L2 norm of per-island residual norms.
    """
    max_line_search_iters: int
    """Maximum number of line-search iterations per Newton solve across all islands and
    integration stages in the last call to :meth:`~superdex.physics.Scene.step`.
    """
    convergence_status: ConvergenceStatus
    """Aggregate convergence status of the scene in the last call to
    :meth:`~superdex.physics.Scene.step`.

    Returns the worst convergence status across all dynamic actors: :class:`DIVERGED
    <superdex.physics.ConvergenceStatus>` if any actor diverged, :class:`STOPPED
    <superdex.physics.ConvergenceStatus>` if any actor's solver was stopped,
    :class:`CONVERGED <superdex.physics.ConvergenceStatus>` if all dynamic actors
    converged, :class:`NONE <superdex.physics.ConvergenceStatus>` if no dynamic
    actors exist, :meth:`~superdex.physics.Scene.step` has not been called yet or
    the last call to :meth:`~superdex.physics.Scene.step` was with zero time step.

    See Also:
        :class:`~superdex.physics.ConvergenceStatus`,
        :meth:`~superdex.physics.Actor.get_convergence_status`
    """
    @overload
    def __init__(self) -> None: ...
    @overload
    def __init__(
        self,
        max_non_linear_iters: int = ...,
        residual_norm: float = ...,
        max_line_search_iters: int = ...,
        convergence_status: ConvergenceStatus | int = ...,
    ) -> None: ...
    def __eq__(self, other: object) -> bool: ...
    def __ne__(self, other: object) -> bool: ...

class RecordingParams:
    """Parameters controlling what data gets included in a scene recording.

    Warning:
        The recording system may undergo a substantial refactor to expand its
        capabilities. Its API and recorded file format may change in future
        releases.

    See Also:
        :meth:`~superdex.physics.Scene.start_recording`,
        :meth:`~superdex.physics.Scene.stop_recording`
    """
    record_actor_meshes: bool
    """Include the meshes (if any) when recording actor creation events."""
    record_actor_local_to_global_map: bool
    """Include the local-to-global map (if any) when recording actor creation events."""
    record_actor_mass_matrix: bool
    """Include the mass matrix (if any) when recording actor creation events."""
    record_target_state: bool
    """Record articulated pose controller targets and tracking parameters every step."""
    record_dynamic_actor_state: bool
    """Record the state of dynamic actors every step.

    This includes position, rotation, and velocity. For soft actors, it includes the
    node displacements and velocities.
    """
    record_static_actor_state: bool
    """Record the state of static actors every step. This includes position and
    rotation.
    """
    record_contact_points: bool
    """Record results from :class:`CONTACT_POINTS <superdex.physics.QueryType>` for
    each dynamic actor every step.
    """
    record_node_contact_forces: bool
    """Record results for :class:`NODE_CONTACT_FORCES <superdex.physics.QueryType>` for
    each soft actor every step.
    """
    record_sdf_distances: bool
    """Record SDF distances for :class:`SDF_DISTANCES <superdex.physics.QueryType>` for
    each dynamic actor every step.
    """
    @overload
    def __init__(self) -> None: ...
    @overload
    def __init__(
        self,
        record_actor_meshes: bool = ...,
        record_actor_local_to_global_map: bool = ...,
        record_actor_mass_matrix: bool = ...,
        record_target_state: bool = ...,
        record_dynamic_actor_state: bool = ...,
        record_static_actor_state: bool = ...,
        record_contact_points: bool = ...,
        record_node_contact_forces: bool = ...,
        record_sdf_distances: bool = ...,
    ) -> None: ...
    @staticmethod
    def all(enabled: bool = ...) -> RecordingParams:
        """Return :class:`~superdex.physics.RecordingParams` with all boolean options
        set
        to the given value.

        Args:
            enabled (bool): True to enable all options (maximum data), false to disable
                (minimum data).

        Returns:
            The initialized :class:`~superdex.physics.RecordingParams`.
        """

class PoseTrackingParams:
    """Parameters for pose controller tracking constraints.

    Defines spring-damper parameters for individual pose tracking constraints in an
    articulated actor's pose controller. Each constraint acts on a joint rotation,
    link translation, or link rotation.

    See Also:
        :meth:`~superdex.physics.Actor.get_articulated_pose_controller_params`,
        :meth:`~superdex.physics.Actor.set_articulated_pose_controller_params`,
        :class:`~superdex.physics.PoseControllerParams`
    """
    stiffness: float
    """Constraint stiffness.

    Note:
        Units depend on the DoF kind: [N/m] for translation DoFs, [N·m/rad] for
        rotation DoFs.

    Note:
        Must be non-negative and finite.
    """
    damping: float
    """Constraint damping.

    Note:
        Units depend on the DoF kind: [N·s/m] for translation DoFs, [N·m·s/rad] for
        rotation DoFs.

    Note:
        Must be non-negative and finite.
    """
    saturation: float
    """Saturation distance [m] or angle [rad]. A negative value disables saturation.

    Note:
        When enabled, saturation smoothly limits the magnitude of the elastic
        (stiffness) contribution to
        :attr:`~superdex.physics.PoseTrackingParams.stiffness` *
        :attr:`~superdex.physics.PoseTrackingParams.saturation`. The damping
        contribution is separate and is not limited by saturation.

    Note:
        Must be finite and non-zero.
    """
    @overload
    def __init__(self) -> None: ...
    @overload
    def __init__(
        self,
        stiffness: float = ...,
        damping: float = ...,
        saturation: float = ...,
    ) -> None: ...
    def __eq__(self, other: object) -> bool: ...
    def __ne__(self, other: object) -> bool: ...

class PoseControllerParams:
    """Parameters to create a pose controller for an articulated actor.

    Note:
        Each tracking array must be empty, have size 1, or have size equal to the
        number of links. Empty arrays use zero-gain defaults. Size-1 arrays are
        broadcast to all applicable constraints. Full-size arrays are indexed by
        link.

    Note:
        Empty controllers are allowed. Constraints are created even when stiffness
        and damping are both zero, and can be accessed/updated via
        :meth:`~superdex.physics.Actor.get_articulated_pose_constraints`,
        :meth:`~superdex.physics.Actor.get_articulated_pose_controller_params` and
        :meth:`~superdex.physics.Actor.set_articulated_pose_controller_params`.

    Note:
        Control of cycle joints is not supported.

    See Also:
        :meth:`~superdex.physics.Actor.add_articulated_pose_controller`,
        :class:`~superdex.physics.PoseTrackingParams`
    """
    @property
    def link_pos_tracking(self) -> DynamicArrayPoseTrackingParams:
        """Tracking parameters for link position constraints."""
    @link_pos_tracking.setter
    def link_pos_tracking(self, value: ArrayLikePoseTrackingParams) -> None: ...
    @property
    def link_rot_tracking(self) -> DynamicArrayPoseTrackingParams:
        """Tracking parameters for link rotation constraints."""
    @link_rot_tracking.setter
    def link_rot_tracking(self, value: ArrayLikePoseTrackingParams) -> None: ...
    @property
    def joint_tracking(self) -> DynamicArrayPoseTrackingParams:
        """Tracking parameters for joint constraints."""
    @joint_tracking.setter
    def joint_tracking(self, value: ArrayLikePoseTrackingParams) -> None: ...
    @overload
    def __init__(self) -> None: ...
    @overload
    def __init__(
        self,
        link_pos_tracking: ArrayLikePoseTrackingParams = ...,
        link_rot_tracking: ArrayLikePoseTrackingParams = ...,
        joint_tracking: ArrayLikePoseTrackingParams = ...,
    ) -> None: ...
    @overload
    def __init__(self, num_links: int) -> None:
        """Construct with all tracking arrays pre-sized to numLinks default-constructed
        PoseTrackingParams (zero gains).

        Args:
            num_links (int): Number of links the controller will track.
        """

class PoseConstraintInfo:
    """Information about a single pose constraint in a pose controller.

    See Also:
        :class:`~superdex.physics.PoseConstraintType`,
        :meth:`~superdex.physics.Actor.get_articulated_pose_constraints`
    """
    handle: ConstraintHandle
    """Handle to the underlying constraint."""
    type: PoseConstraintType
    """Type of constraint."""
    link: int
    """Index of the target link in the articulated body."""
    parent: int
    """Index of the parent link, or -1 if none."""
    @overload
    def __init__(self) -> None: ...
    @overload
    def __init__(
        self,
        handle: ConstraintHandle = ...,
        type: PoseConstraintType | int = ...,
        link: int = ...,
        parent: int = ...,
    ) -> None: ...

class DebugDrawSpheres:
    """Sphere primitives for debug visualization."""
    @property
    def positions(self) -> SpanConstReal3:
        """Sphere center positions [m] in world frame."""
    @positions.setter
    def positions(self, value: ArrayLikeReal3) -> None: ...
    @property
    def radii(self) -> SpanConstReal:
        """Sphere radii [m]."""
    @radii.setter
    def radii(self, value: ArrayLikeReal) -> None: ...
    @property
    def colors(self) -> SpanConstColor:
        """Sphere colors."""
    @colors.setter
    def colors(self, value: ArrayLikeColor) -> None: ...
    @overload
    def __init__(self) -> None: ...
    @overload
    def __init__(
        self,
        positions: ArrayLikeReal3 = ...,
        radii: ArrayLikeReal = ...,
        colors: ArrayLikeColor = ...,
    ) -> None: ...
    def __eq__(self, other: object) -> bool: ...
    def __ne__(self, other: object) -> bool: ...

class DebugDrawLineVertices:
    """Line primitives for debug visualization.

    Vertices are stored as a line list: every two consecutive vertices form one line
    segment (positions[0]–positions[1] is the first segment,
    positions[2]–positions[3] is the second, and so on). The total number of line
    segments is half the size of :attr:`positions`.
    """
    @property
    def positions(self) -> SpanConstReal3:
        """Line vertex positions [m] in world frame."""
    @positions.setter
    def positions(self, value: ArrayLikeReal3) -> None: ...
    @property
    def colors(self) -> SpanConstColor:
        """Line vertex colors."""
    @colors.setter
    def colors(self, value: ArrayLikeColor) -> None: ...
    @overload
    def __init__(self) -> None: ...
    @overload
    def __init__(
        self,
        positions: ArrayLikeReal3 = ...,
        colors: ArrayLikeColor = ...,
    ) -> None: ...
    def __eq__(self, other: object) -> bool: ...
    def __ne__(self, other: object) -> bool: ...

class DebugDrawData:
    """Collection of debug draw primitives.

    Contains all geometric primitives to be rendered by the client's rendering
    system. Data is valid until the next call to
    :meth:`~superdex.physics.DebugDraw.gather_data` or
    :class:`~superdex.physics.Scene` modification.
    """
    line_vertices: DebugDrawLineVertices
    """Line primitives."""
    spheres: DebugDrawSpheres
    """Sphere primitives."""
    @overload
    def __init__(self) -> None: ...
    @overload
    def __init__(
        self,
        line_vertices: DebugDrawLineVertices = ...,
        spheres: DebugDrawSpheres = ...,
    ) -> None: ...
    def __eq__(self, other: object) -> bool: ...
    def __ne__(self, other: object) -> bool: ...

def set_is_single_threaded(is_single_threaded: bool) -> None:
    """Enable or disable single-threaded mode.

    In single-threaded mode, each new task will be executed on the calling thread,
    even if other threads are available. This may hurt performance, but it is useful
    for debugging and batch simulation.

    Enabling single-threaded mode does not, however, terminate tasks that are
    already running asynchronously. Such tasks will run to completion. For example,
    if you have an ``mochi::AsyncScene``, it will continue to step asynchronously,
    but each step will do its work on that one thread.

    Args:
        is_single_threaded (bool): True to enable single-threaded mode. False to
            disable.

    Note:
        Call on any thread.

    Warning:
        This documentation references the following C++ API, which is not available
        in Python: ``mochi::AsyncScene``.

    See Also:
        :func:`~superdex.physics.is_single_threaded`
    """

def is_single_threaded() -> bool:
    """Return true if running in single-threaded mode.

    Returns true when the Mochi context was created with zero worker threads (see
    ``mochi::CreateContext``) or when single-threaded mode has been explicitly
    enabled via :func:`~superdex.physics.set_is_single_threaded`.

    Returns:
        True if running in single-threaded mode.

    Note:
        Call on any thread.

    Warning:
        This documentation references the following C++ API, which is not available
        in Python: ``mochi::CreateContext``.

    See Also:
        :func:`~superdex.physics.set_is_single_threaded`, ``mochi::CreateContext``
    """

def get_num_threads() -> int:
    """Return the number of worker threads available to execute asynchronous tasks.

    Returns:
        The configured worker-thread count (see ``mochi::CreateContext``), or zero
        if single-threaded mode is enabled (see
        :func:`~superdex.physics.set_is_single_threaded`).

    Note:
        Call on any thread.

    Warning:
        This documentation references the following C++ API, which is not available
        in Python: ``mochi::CreateContext``.

    See Also:
        ``mochi::CreateContext``, :func:`~superdex.physics.is_single_threaded`
    """

def enable_log_channel(channel: LogChannel | int, enable: bool) -> None:
    """Enable or disable log messages on the specified channel.

    When a channel is disabled, messages on that channel are discarded before
    reaching the logging callback (see :func:`~superdex.physics.set_log_callback`).

    Args:
        channel (LogChannel | int): The log channel to enable or disable.
        enable (bool): True to enable the channel, false to disable it.

    Note:
        By default, all channels are enabled except for :class:`VERBOSE
        <superdex.physics.LogChannel>`.

    Note:
        Call on any thread.

    See Also:
        :func:`~superdex.physics.is_log_channel_enabled`,
        :func:`~superdex.physics.set_log_callback`
    """

def is_log_channel_enabled(channel: LogChannel | int) -> bool:
    """Return true if messages on the specified log channel are enabled.

    Args:
        channel (LogChannel | int): The log channel to query.

    Returns:
        True if the channel is enabled, false otherwise.

    Note:
        Call on any thread.

    See Also:
        :func:`~superdex.physics.enable_log_channel`
    """

def set_log_callback(callback: Callable[[LogChannel, str, str, int], None]) -> None:
    """Optionally provide a callback function to redirect SuperDex Physics logging. The
    default function outputs to stdout and to the debugger (if attached).

    Args:
        callback (Callable[[LogChannel, str, str, int], None]): Function to handle
            logging, or None to restore the default logging function.

    Note:
        Call on any thread.

    Warning:
        This documentation references the following C++ API, which is not available
        in Python: ``mochi::Context::GetLogCallback``.

    Warning:
        Your callback function may be called on any thread.

    See Also:
        ``mochi::Context::GetLogCallback``
    """

@overload
def load_shape_from_file(file_path: str) -> ShapeHandle:
    """Load a shape from a file of supported format.

    Supported formats: JSON (.mochi.json), HDF5 (.mochi.h5), OBJ (.obj), OFF (.off),
    PLY (.ply), and STL (.stl).

    Args:
        file_path (str): File path to load (case sensitive on some filesystems).

    Returns:
        A :class:`~superdex.physics.ShapeHandle` that can be used to create actors.

    Raises:
        :class:`~superdex.physics.Error`: If an error occurs.

    Note:
        The shape remains in memory while referenced by any handle or actor. In the
        C API, you must release the shape explicitly by calling
        :func:`~superdex.physics.release_shape`. In C++ and Python, this is
        optional, since :class:`~superdex.physics.ShapeHandle` cleanup is automatic.

    Note:
        Call on any thread.

    See Also:
        :class:`~superdex.physics.ShapeHandle`,
        :func:`~superdex.physics.release_shape`,
        :func:`~superdex.physics.load_shape_from_bytes`
    """

@overload
def load_shape_from_file(file_path: str, bake_scale: Real3Like) -> ShapeHandle:
    """Load a shape from a file of supported format, and bake in some scale.

    Supported formats: JSON (.mochi.json), HDF5 (.mochi.h5), OBJ (.obj), OFF (.off),
    PLY (.ply), and STL (.stl).

    Args:
        file_path (str): File path to load (case sensitive on some filesystems).
        bake_scale (Real3Like): Scale to bake into the shape or (1, 1, 1) for no
            change. Negative scale results in mirroring.

    Returns:
        A :class:`~superdex.physics.ShapeHandle` that can be used to create actors.

    Raises:
        :class:`~superdex.physics.Error`: If an error occurs.

    Note:
        The shape remains in memory while referenced by any handle or actor. In the
        C API, you must release the shape explicitly by calling
        :func:`~superdex.physics.release_shape`. In C++ and Python, this is
        optional, since :class:`~superdex.physics.ShapeHandle` cleanup is automatic.

    Note:
        Some shape data cannot bake arbitrary non-uniform scale. Unsupported scale
        values are rejected.

    Note:
        Precomputed grid SDF data is preserved only when ``bake_scale`` is uniform
        by absolute value. Non-uniform scale by absolute value discards the
        precomputed SDF. If an SDF collider later requires SDF data, SuperDex
        Physics regenerates it from the transformed mesh at runtime, which may be
        expensive.

    Note:
        If the loaded model has
        :attr:`~superdex.physics.ModelData.element_frame_axes`, ``bake_scale``
        transforms those axes as normal directions using the inverse-transpose of
        the scale transform, then normalize them.

    Note:
        Call on any thread.

    See Also:
        :class:`~superdex.physics.ShapeHandle`,
        :func:`~superdex.physics.release_shape`,
        :func:`~superdex.physics.load_shape_from_bytes`
    """

@overload
def load_shape_from_file(file_path: str, bake_transform: TransformRT) -> ShapeHandle:
    """Load a shape from a file of supported format, and bake in rotation and
    translation.

    Supported formats: JSON (.mochi.json), HDF5 (.mochi.h5), OBJ (.obj), OFF (.off),
    PLY (.ply), and STL (.stl).

    Args:
        file_path (str): File path to load (case sensitive on some filesystems).
        bake_transform (TransformRT): Transform to bake into the shape, or the
            identity transform for no change.

    Returns:
        A :class:`~superdex.physics.ShapeHandle` that can be used to create actors.

    Raises:
        :class:`~superdex.physics.Error`: If an error occurs.

    Note:
        The shape remains in memory while referenced by any handle or actor. In the
        C API, you must release the shape explicitly by calling
        :func:`~superdex.physics.release_shape`. In C++ and Python, this is
        optional, since :class:`~superdex.physics.ShapeHandle` cleanup is automatic.

    Note:
        If the loaded model has
        :attr:`~superdex.physics.ModelData.element_frame_axes`, ``bake_transform``
        transforms those axes as normal directions using the inverse-transpose of
        the rotation transform, then normalize them.

    Note:
        Call on any thread.

    See Also:
        :class:`~superdex.physics.ShapeHandle`,
        :func:`~superdex.physics.release_shape`,
        :func:`~superdex.physics.load_shape_from_bytes`
    """

@overload
def load_shape_from_file(
    file_path: str,
    bake_scale: Real3Like,
    bake_transform: TransformRT,
) -> ShapeHandle:
    """Load a shape from a file of supported format, and bake in some scale, rotation,
    and translation.

    Supported formats: JSON (.mochi.json), HDF5 (.mochi.h5), OBJ (.obj), OFF (.off),
    PLY (.ply), and STL (.stl).

    Args:
        file_path (str): File path to load (case sensitive on some filesystems).
        bake_scale (Real3Like): Scale to bake into the shape or (1, 1, 1) for no
            change. Negative scale results in mirroring.
        bake_transform (TransformRT): Transform to bake into the shape, or the
            identity transform for no change.

    Returns:
        A :class:`~superdex.physics.ShapeHandle` that can be used to create actors.

    Raises:
        :class:`~superdex.physics.Error`: If an error occurs.

    Note:
        The shape remains in memory while referenced by any handle or actor. In the
        C API, you must release the shape explicitly by calling
        :func:`~superdex.physics.release_shape`. In C++ and Python, this is
        optional, since :class:`~superdex.physics.ShapeHandle` cleanup is automatic.

    Note:
        Some shape data cannot bake arbitrary non-uniform scale. Unsupported scale
        values are rejected.

    Note:
        Precomputed grid SDF data is preserved only when ``bake_scale`` is uniform
        by absolute value. Non-uniform scale by absolute value discards the
        precomputed SDF. If an SDF collider later requires SDF data, SuperDex
        Physics regenerates it from the transformed mesh at runtime, which may be
        expensive.

    Note:
        If the loaded model has
        :attr:`~superdex.physics.ModelData.element_frame_axes`, ``bake_scale`` and
        ``bake_transform`` transform those axes as normal directions using the
        inverse-transpose of the scale-rotation transform, then normalize them.

    Note:
        Call on any thread.

    See Also:
        :class:`~superdex.physics.ShapeHandle`,
        :func:`~superdex.physics.release_shape`,
        :func:`~superdex.physics.load_shape_from_bytes`
    """

@overload
def load_shape_from_bytes(file_data: ArrayLikeChar) -> ShapeHandle:
    """Load a shape from a file of supported format, where the file has already been
    loaded into memory.

    Supported formats: JSON (.mochi.json) and HDF5 (.mochi.h5), auto-detected from
    header bytes. To load a surface mesh format (OBJ, PLY, OFF, STL) from memory,
    use the overload that takes an explicit :class:`~superdex.physics.MeshFileType`.

    Args:
        file_data (ArrayLikeChar): File contents as an in-memory byte array.

    Returns:
        A :class:`~superdex.physics.ShapeHandle` that can be used to create actors.

    Raises:
        :class:`~superdex.physics.Error`: If an error occurs.

    Note:
        The shape remains in memory while referenced by any handle or actor. In the
        C API, you must release the shape explicitly by calling
        :func:`~superdex.physics.release_shape`. In C++ and Python, this is
        optional, since :class:`~superdex.physics.ShapeHandle` cleanup is automatic.

    Note:
        Call on any thread.

    See Also:
        :class:`~superdex.physics.ShapeHandle`,
        :func:`~superdex.physics.release_shape`,
        :func:`~superdex.physics.load_shape_from_file`
    """

@overload
def load_shape_from_bytes(
    file_data: ArrayLikeChar,
    bake_scale: Real3Like,
) -> ShapeHandle:
    """Load a shape from a file of supported format, where the file has already been
    loaded into memory, and baking in some scale.

    Supported formats: JSON (.mochi.json) and HDF5 (.mochi.h5), auto-detected from
    header bytes. To load a surface mesh format (OBJ, PLY, OFF, STL) from memory,
    use the overload that takes an explicit :class:`~superdex.physics.MeshFileType`.

    Args:
        file_data (ArrayLikeChar): File contents as an in-memory byte array.
        bake_scale (Real3Like): Scale to bake into the shape or (1, 1, 1) for no
            change. Negative scale results in mirroring.

    Returns:
        A :class:`~superdex.physics.ShapeHandle` that can be used to create actors.

    Raises:
        :class:`~superdex.physics.Error`: If an error occurs.

    Note:
        The shape remains in memory while referenced by any handle or actor. In the
        C API, you must release the shape explicitly by calling
        :func:`~superdex.physics.release_shape`. In C++ and Python, this is
        optional, since :class:`~superdex.physics.ShapeHandle` cleanup is automatic.

    Note:
        Some shape data cannot bake arbitrary non-uniform scale. Unsupported scale
        values are rejected.

    Note:
        Precomputed grid SDF data is preserved only when ``bake_scale`` is uniform
        by absolute value. Non-uniform scale by absolute value discards the
        precomputed SDF. If an SDF collider later requires SDF data, SuperDex
        Physics regenerates it from the transformed mesh at runtime, which may be
        expensive.

    Note:
        If the loaded model has
        :attr:`~superdex.physics.ModelData.element_frame_axes`, ``bake_scale``
        transforms those axes as normal directions using the inverse-transpose of
        the scale transform, then normalize them.

    Note:
        Call on any thread.

    See Also:
        :class:`~superdex.physics.ShapeHandle`,
        :func:`~superdex.physics.release_shape`,
        :func:`~superdex.physics.load_shape_from_file`
    """

@overload
def load_shape_from_bytes(
    file_data: ArrayLikeChar,
    bake_transform: TransformRT,
) -> ShapeHandle:
    """Load a shape from a file of supported format, where the file has already been
    loaded into memory, and baking in rotation and translation.

    Supported formats: JSON (.mochi.json) and HDF5 (.mochi.h5), auto-detected from
    header bytes. To load a surface mesh format (OBJ, PLY, OFF, STL) from memory,
    use the overload that takes an explicit :class:`~superdex.physics.MeshFileType`.

    Args:
        file_data (ArrayLikeChar): File contents as an in-memory byte array.
        bake_transform (TransformRT): Transform to bake into the shape, or the
            identity transform for no change.

    Returns:
        A :class:`~superdex.physics.ShapeHandle` that can be used to create actors.

    Raises:
        :class:`~superdex.physics.Error`: If an error occurs.

    Note:
        The shape remains in memory while referenced by any handle or actor. In the
        C API, you must release the shape explicitly by calling
        :func:`~superdex.physics.release_shape`. In C++ and Python, this is
        optional, since :class:`~superdex.physics.ShapeHandle` cleanup is automatic.

    Note:
        If the loaded model has
        :attr:`~superdex.physics.ModelData.element_frame_axes`, ``bake_transform``
        transforms those axes as normal directions using the inverse-transpose of
        the rotation transform, then normalize them.

    Note:
        Call on any thread.

    See Also:
        :class:`~superdex.physics.ShapeHandle`,
        :func:`~superdex.physics.release_shape`,
        :func:`~superdex.physics.load_shape_from_file`
    """

@overload
def load_shape_from_bytes(
    file_data: ArrayLikeChar,
    bake_scale: Real3Like,
    bake_transform: TransformRT,
) -> ShapeHandle:
    """Load a shape from a file of supported format, where the file has already been
    loaded into memory, and baking in some scale, rotation, and translation.

    Supported formats: JSON (.mochi.json) and HDF5 (.mochi.h5), auto-detected from
    header bytes. To load a surface mesh format (OBJ, PLY, OFF, STL) from memory,
    use the overload that takes an explicit :class:`~superdex.physics.MeshFileType`.

    Args:
        file_data (ArrayLikeChar): File contents as an in-memory byte array.
        bake_scale (Real3Like): Scale to bake into the shape or (1, 1, 1) for no
            change. Negative scale results in mirroring.
        bake_transform (TransformRT): Transform to bake into the shape, or the
            identity transform for no change.

    Returns:
        A :class:`~superdex.physics.ShapeHandle` that can be used to create actors.

    Raises:
        :class:`~superdex.physics.Error`: If an error occurs.

    Note:
        The shape remains in memory while referenced by any handle or actor. In the
        C API, you must release the shape explicitly by calling
        :func:`~superdex.physics.release_shape`. In C++ and Python, this is
        optional, since :class:`~superdex.physics.ShapeHandle` cleanup is automatic.

    Note:
        Some shape data cannot bake arbitrary non-uniform scale. Unsupported scale
        values are rejected.

    Note:
        Precomputed grid SDF data is preserved only when ``bake_scale`` is uniform
        by absolute value. Non-uniform scale by absolute value discards the
        precomputed SDF. If an SDF collider later requires SDF data, SuperDex
        Physics regenerates it from the transformed mesh at runtime, which may be
        expensive.

    Note:
        If the loaded model has
        :attr:`~superdex.physics.ModelData.element_frame_axes`, ``bake_scale`` and
        ``bake_transform`` transform those axes as normal directions using the
        inverse-transpose of the scale-rotation transform, then normalize them.

    Note:
        Call on any thread.

    See Also:
        :class:`~superdex.physics.ShapeHandle`,
        :func:`~superdex.physics.release_shape`,
        :func:`~superdex.physics.load_shape_from_file`
    """

@overload
def load_shape_from_bytes(
    file_data: ArrayLikeChar,
    format: MeshFileType | int,
) -> ShapeHandle:
    """Load a shape from a file of supported format, where the file has already been
    loaded into memory, with an explicit mesh format hint.

    Args:
        file_data (ArrayLikeChar): File contents as an in-memory byte array.
        format (MeshFileType | int): Mesh file format hint. When :class:`LEGACY
            <superdex.physics.MeshFileType>`, auto-detects between HDF5 and JSON via
            header bytes. When a surface mesh format (:class:`PLY
            <superdex.physics.MeshFileType>`, :class:`OFF
            <superdex.physics.MeshFileType>`, :class:`STL
            <superdex.physics.MeshFileType>`, :class:`OBJ
            <superdex.physics.MeshFileType>`), dispatches directly to that reader.

    Returns:
        A :class:`~superdex.physics.ShapeHandle` that can be used to create actors.

    Raises:
        :class:`~superdex.physics.Error`: If an error occurs.

    Note:
        The shape remains in memory while referenced by any handle or actor. In the
        C API, you must release the shape explicitly by calling
        :func:`~superdex.physics.release_shape`. In C++ and Python, this is
        optional, since :class:`~superdex.physics.ShapeHandle` cleanup is automatic.

    Note:
        Call on any thread.

    See Also:
        :class:`~superdex.physics.ShapeHandle`,
        :func:`~superdex.physics.release_shape`,
        :func:`~superdex.physics.load_shape_from_file`
    """

@overload
def load_shape_from_bytes(
    file_data: ArrayLikeChar,
    format: MeshFileType | int,
    bake_scale: Real3Like,
) -> ShapeHandle:
    """Load a shape from a file of supported format, where the file has already been
    loaded into memory, with an explicit mesh format hint, and baking in some scale.

    Args:
        file_data (ArrayLikeChar): File contents as an in-memory byte array.
        format (MeshFileType | int): Mesh file format hint. When :class:`LEGACY
            <superdex.physics.MeshFileType>`, auto-detects between HDF5 and JSON via
            header bytes. When a surface mesh format (:class:`PLY
            <superdex.physics.MeshFileType>`, :class:`OFF
            <superdex.physics.MeshFileType>`, :class:`STL
            <superdex.physics.MeshFileType>`, :class:`OBJ
            <superdex.physics.MeshFileType>`), dispatches directly to that reader.
        bake_scale (Real3Like): Scale to bake into the shape or (1, 1, 1) for no
            change. Negative scale results in mirroring.

    Returns:
        A :class:`~superdex.physics.ShapeHandle` that can be used to create actors.

    Raises:
        :class:`~superdex.physics.Error`: If an error occurs.

    Note:
        The shape remains in memory while referenced by any handle or actor. In the
        C API, you must release the shape explicitly by calling
        :func:`~superdex.physics.release_shape`. In C++ and Python, this is
        optional, since :class:`~superdex.physics.ShapeHandle` cleanup is automatic.

    Note:
        Some shape data cannot bake arbitrary non-uniform scale. Unsupported scale
        values are rejected.

    Note:
        Precomputed grid SDF data is preserved only when ``bake_scale`` is uniform
        by absolute value. Non-uniform scale by absolute value discards the
        precomputed SDF. If an SDF collider later requires SDF data, SuperDex
        Physics regenerates it from the transformed mesh at runtime, which may be
        expensive.

    Note:
        If the loaded model has
        :attr:`~superdex.physics.ModelData.element_frame_axes`, ``bake_scale``
        transforms those axes as normal directions using the inverse-transpose of
        the scale transform, then normalize them.

    Note:
        Call on any thread.

    See Also:
        :class:`~superdex.physics.ShapeHandle`,
        :func:`~superdex.physics.release_shape`,
        :func:`~superdex.physics.load_shape_from_file`
    """

@overload
def load_shape_from_bytes(
    file_data: ArrayLikeChar,
    format: MeshFileType | int,
    bake_transform: TransformRT,
) -> ShapeHandle:
    """Load a shape from a file of supported format, where the file has already been
    loaded into memory, with an explicit mesh format hint, and baking in rotation
    and translation.

    Args:
        file_data (ArrayLikeChar): File contents as an in-memory byte array.
        format (MeshFileType | int): Mesh file format hint. When :class:`LEGACY
            <superdex.physics.MeshFileType>`, auto-detects between HDF5 and JSON via
            header bytes. When a surface mesh format (:class:`PLY
            <superdex.physics.MeshFileType>`, :class:`OFF
            <superdex.physics.MeshFileType>`, :class:`STL
            <superdex.physics.MeshFileType>`, :class:`OBJ
            <superdex.physics.MeshFileType>`), dispatches directly to that reader.
        bake_transform (TransformRT): Transform to bake into the shape, or the
            identity transform for no change.

    Returns:
        A :class:`~superdex.physics.ShapeHandle` that can be used to create actors.

    Raises:
        :class:`~superdex.physics.Error`: If an error occurs.

    Note:
        The shape remains in memory while referenced by any handle or actor. In the
        C API, you must release the shape explicitly by calling
        :func:`~superdex.physics.release_shape`. In C++ and Python, this is
        optional, since :class:`~superdex.physics.ShapeHandle` cleanup is automatic.

    Note:
        If the loaded model has
        :attr:`~superdex.physics.ModelData.element_frame_axes`, ``bake_transform``
        transforms those axes as normal directions using the inverse-transpose of
        the rotation transform, then normalize them.

    Note:
        Call on any thread.

    See Also:
        :class:`~superdex.physics.ShapeHandle`,
        :func:`~superdex.physics.release_shape`,
        :func:`~superdex.physics.load_shape_from_file`
    """

@overload
def load_shape_from_bytes(
    file_data: ArrayLikeChar,
    format: MeshFileType | int,
    bake_scale: Real3Like,
    bake_transform: TransformRT,
) -> ShapeHandle:
    """Load a shape from a file of supported format, where the file has already been
    loaded into memory, with an explicit mesh format hint, and baking in some scale,
    rotation, and translation.

    Args:
        file_data (ArrayLikeChar): File contents as an in-memory byte array.
        format (MeshFileType | int): Mesh file format hint. When :class:`LEGACY
            <superdex.physics.MeshFileType>`, auto-detects between HDF5 and JSON via
            header bytes. When a surface mesh format (:class:`PLY
            <superdex.physics.MeshFileType>`, :class:`OFF
            <superdex.physics.MeshFileType>`, :class:`STL
            <superdex.physics.MeshFileType>`, :class:`OBJ
            <superdex.physics.MeshFileType>`), dispatches directly to that reader.
        bake_scale (Real3Like): Scale to bake into the shape or (1, 1, 1) for no
            change. Negative scale results in mirroring.
        bake_transform (TransformRT): Transform to bake into the shape, or the
            identity transform for no change.

    Returns:
        A :class:`~superdex.physics.ShapeHandle` that can be used to create actors.

    Raises:
        :class:`~superdex.physics.Error`: If an error occurs.

    Note:
        The shape remains in memory while referenced by any handle or actor. In the
        C API, you must release the shape explicitly by calling
        :func:`~superdex.physics.release_shape`. In C++ and Python, this is
        optional, since :class:`~superdex.physics.ShapeHandle` cleanup is automatic.

    Note:
        Some shape data cannot bake arbitrary non-uniform scale. Unsupported scale
        values are rejected.

    Note:
        Precomputed grid SDF data is preserved only when ``bake_scale`` is uniform
        by absolute value. Non-uniform scale by absolute value discards the
        precomputed SDF. If an SDF collider later requires SDF data, SuperDex
        Physics regenerates it from the transformed mesh at runtime, which may be
        expensive.

    Note:
        If the loaded model has
        :attr:`~superdex.physics.ModelData.element_frame_axes`, ``bake_scale`` and
        ``bake_transform`` transform those axes as normal directions using the
        inverse-transpose of the scale-rotation transform, then normalize them.

    Note:
        Call on any thread.

    See Also:
        :class:`~superdex.physics.ShapeHandle`,
        :func:`~superdex.physics.release_shape`,
        :func:`~superdex.physics.load_shape_from_file`
    """

@overload
def create_mesh_shape(mesh: MeshData) -> ShapeHandle:
    """Create a shape using in-memory mesh data, so that it can be used to create
    actors.

    Args:
        mesh (MeshData): The mesh data, which SuperDex Physics copies.

    Returns:
        A :class:`~superdex.physics.ShapeHandle` that can be used to create actors.

    Raises:
        :class:`~superdex.physics.Error`: If an error occurs.

    Note:
        The shape remains in memory while referenced by any handle or actor. In the
        C API, you must release the shape explicitly by calling
        :func:`~superdex.physics.release_shape`. In C++ and Python, this is
        optional, since :class:`~superdex.physics.ShapeHandle` cleanup is automatic.

    Note:
        Call on any thread.

    Note:
        Can be a tetrahedral mesh, a triangular mesh, or a polyline mesh, depending
        on the value of :attr:`~superdex.physics.MeshData.nodes_per_element`.

    See Also:
        :class:`~superdex.physics.ShapeHandle`,
        :func:`~superdex.physics.release_shape`
    """
@overload
def create_mesh_shape(
    nodes_per_element: int = ...,
    coordinates: ArrayLikeReal = ...,
    connectivity: ArrayLikeInt = ...,
    skinning: Optional[SkinningData] = ...,
) -> ShapeHandle: ...

@overload
def create_mesh_shape(mesh: MeshDataView) -> ShapeHandle:
    """Create a shape using in-memory mesh data, so that it can be used to create
    actors.

    Args:
        mesh (MeshDataView): A non-owning view of the mesh data, which SuperDex
            Physics copies.

    Returns:
        A :class:`~superdex.physics.ShapeHandle` that can be used to create actors.

    Raises:
        :class:`~superdex.physics.Error`: If an error occurs.

    Note:
        The shape remains in memory while referenced by any handle or actor. In the
        C API, you must release the shape explicitly by calling
        :func:`~superdex.physics.release_shape`. In C++ and Python, this is
        optional, since :class:`~superdex.physics.ShapeHandle` cleanup is automatic.

    Note:
        Call on any thread.

    Note:
        Can be a tetrahedral mesh, a triangular mesh, or a polyline mesh, depending
        on the value of :attr:`~superdex.physics.MeshDataView.nodes_per_element`.

    See Also:
        :class:`~superdex.physics.ShapeHandle`,
        :func:`~superdex.physics.release_shape`
    """

@overload
def create_model_shape(model: ModelData) -> ShapeHandle:
    """Create a shape using in-memory model data, so that it can be used to create
    actors.

    Args:
        model (ModelData): The model data, which SuperDex Physics copies.

    Returns:
        A :class:`~superdex.physics.ShapeHandle` that can be used to create actors.

    Raises:
        :class:`~superdex.physics.Error`: If an error occurs.

    Note:
        The shape remains in memory while referenced by any handle or actor. In the
        C API, you must release the shape explicitly by calling
        :func:`~superdex.physics.release_shape`. In C++ and Python, this is
        optional, since :class:`~superdex.physics.ShapeHandle` cleanup is automatic.

    Note:
        Call on any thread.

    Note:
        Can be a mesh or an implicit shape, depending on the contents.

    See Also:
        :class:`~superdex.physics.ShapeHandle`,
        :func:`~superdex.physics.release_shape`
    """
@overload
def create_model_shape(
    mesh: Optional[MeshData] = ...,
    visual_mesh: Optional[MeshData] = ...,
    contact_skin_mesh: Optional[MeshData] = ...,
    blending: Optional[ArrayLikeBlendingData] = ...,
    constrained_nodes: Optional[ArrayLikeInt] = ...,
    element_frame_axes: Optional[ArrayLikeReal] = ...,
    box: Optional[Box] = ...,
    plane: Optional[Plane] = ...,
    sphere: Optional[Sphere] = ...,
    sdf: Optional[GridSdfData] = ...,
    material: Optional[PerElementSoftMaterialData] = ...,
) -> ShapeHandle: ...

@overload
def create_model_shape(model: ModelDataView) -> ShapeHandle:
    """Create a shape using in-memory model data, so that it can be used to create
    actors.

    Args:
        model (ModelDataView): A non-owning view of the model data, which SuperDex
            Physics copies.

    Returns:
        A :class:`~superdex.physics.ShapeHandle` that can be used to create actors.

    Raises:
        :class:`~superdex.physics.Error`: If an error occurs.

    Note:
        The shape remains in memory while referenced by any handle or actor. In the
        C API, you must release the shape explicitly by calling
        :func:`~superdex.physics.release_shape`. In C++ and Python, this is
        optional, since :class:`~superdex.physics.ShapeHandle` cleanup is automatic.

    Note:
        Call on any thread.

    Note:
        Can be a mesh or an implicit shape, depending on the contents.

    See Also:
        :class:`~superdex.physics.ShapeHandle`,
        :func:`~superdex.physics.release_shape`
    """

def create_tet_mesh_shape(
    coordinates: ArrayLikeReal,
    connectivity: ArrayLikeInt,
) -> ShapeHandle:
    """Create a tetrahedral mesh shape using in-memory data.

    Args:
        coordinates (ArrayLikeReal): Node positions [m] (3 values per node)
        connectivity (ArrayLikeInt): Node indices (4 values per tetrahedron).

    Returns:
        A :class:`~superdex.physics.ShapeHandle` that can be used to create actors.

    Raises:
        :class:`~superdex.physics.Error`: If an error occurs.

    Note:
        The shape remains in memory while referenced by any handle or actor. In the
        C API, you must release the shape explicitly by calling
        :func:`~superdex.physics.release_shape`. In C++ and Python, this is
        optional, since :class:`~superdex.physics.ShapeHandle` cleanup is automatic.

    Note:
        Call on any thread.

    Note:
        "Vertex" is another word for "node" in this context.

    See Also:
        :class:`~superdex.physics.ShapeHandle`,
        :func:`~superdex.physics.release_shape`
    """

def create_tri_mesh_shape(
    coordinates: ArrayLikeReal,
    connectivity: ArrayLikeInt,
) -> ShapeHandle:
    """Create a triangle mesh shape using in-memory data.

    Args:
        coordinates (ArrayLikeReal): Node positions [m] (3 values per node).
        connectivity (ArrayLikeInt): Node indices (3 values per triangle).

    Returns:
        A :class:`~superdex.physics.ShapeHandle` that can be used to create actors.

    Raises:
        :class:`~superdex.physics.Error`: If an error occurs.

    Note:
        The shape remains in memory while referenced by any handle or actor. In the
        C API, you must release the shape explicitly by calling
        :func:`~superdex.physics.release_shape`. In C++ and Python, this is
        optional, since :class:`~superdex.physics.ShapeHandle` cleanup is automatic.

    Note:
        Call on any thread.

    Note:
        "Vertex" is another word for "node" in this context.

    See Also:
        :class:`~superdex.physics.ShapeHandle`,
        :func:`~superdex.physics.release_shape`
    """

def create_sphere_shape(center: Real3Like, radius: float) -> ShapeHandle:
    """Create an implicit sphere shape.

    Args:
        center (Real3Like): Center of the sphere [m], in the shape's local frame.
        radius (float): Radius of the sphere [m]

    Returns:
        A :class:`~superdex.physics.ShapeHandle` that can be used to create rigid
        actors.

    Raises:
        :class:`~superdex.physics.Error`: If an error occurs.

    Note:
        The shape remains in memory while referenced by any handle or actor. In the
        C API, you must release the shape explicitly by calling
        :func:`~superdex.physics.release_shape`. In C++ and Python, this is
        optional, since :class:`~superdex.physics.ShapeHandle` cleanup is automatic.

    Note:
        Call on any thread.

    See Also:
        :class:`~superdex.physics.ShapeHandle`,
        :func:`~superdex.physics.release_shape`
    """

def create_plane_shape(normal: Real3Like, distance: float) -> ShapeHandle:
    """Create an implicit plane with infinite extents.

    Args:
        normal (Real3Like): Normal vector of the plane, in the shape's local frame.
            Need not be unit length; it is normalized internally.
        distance (float): Signed offset [m] from the origin to the plane, measured
            along the (normalized) ``normal``. The plane equation is
            dot(normalize(normal), x) = distance.

    Returns:
        A :class:`~superdex.physics.ShapeHandle` that can be used to create rigid
        actors.

    Raises:
        :class:`~superdex.physics.Error`: If an error occurs.

    Note:
        The shape remains in memory while referenced by any handle or actor. In the
        C API, you must release the shape explicitly by calling
        :func:`~superdex.physics.release_shape`. In C++ and Python, this is
        optional, since :class:`~superdex.physics.ShapeHandle` cleanup is automatic.

    Note:
        Call on any thread.

    See Also:
        :class:`~superdex.physics.ShapeHandle`,
        :func:`~superdex.physics.release_shape`
    """

def get_shape_mesh(shape: ShapeHandle) -> MeshDataView:
    """Get a view of the shape's main mesh data.

    For tetrahedral mesh shapes, returns the volume mesh (4 nodes per element). For
    triangular mesh shapes, returns the surface mesh (3 nodes per element). For
    polyline shapes, returns the polyline mesh (2 nodes per element). For shapes
    without a main mesh, including implicit shapes such as spheres and planes,
    returns an empty view.

    Args:
        shape (ShapeHandle): Handle to a valid shape.

    Returns:
        A non-owning view of the shape's main mesh data, or an empty view if the
        shape has no mesh.

    Raises:
        :class:`~superdex.physics.Error`: If an error occurs.

    Note:
        Can be called on any thread.

    Note:
        The returned view will be invalid after the shape handle has been released.

    See Also:
        :class:`~superdex.physics.MeshDataView`,
        :func:`~superdex.physics.get_shape_surface_mesh`,
        :func:`~superdex.physics.get_shape_visual_mesh`,
        :meth:`~superdex.physics.Actor.get_mesh`
    """

def get_shape_surface_mesh(shape: ShapeHandle) -> MeshDataView:
    """Get a view of the shape's surface mesh data.

    Returns a triangle mesh (3 nodes per element) representing the shape's surface.
    The coordinate array contains exactly the surface nodes referenced by the
    returned connectivity; nodes present in the underlying main mesh but not
    referenced by any surface triangle are omitted. Connectivity values are indices
    into this returned coordinate array. For polyline shapes with an authored
    contact skin, returns that skin. For shapes without a surface mesh, returns an
    empty view.

    Args:
        shape (ShapeHandle): Handle to a valid shape.

    Returns:
        A non-owning view of the shape's surface mesh data, or an empty view if the
        shape has no surface mesh.

    Raises:
        :class:`~superdex.physics.Error`: If an error occurs.

    Note:
        For triangular mesh shapes, this may differ from
        :func:`~superdex.physics.get_shape_mesh` if the main mesh contains
        unreferenced nodes. For tetrahedral mesh shapes, this is the boundary
        surface of the volume mesh, with non-boundary volume nodes omitted.

    Note:
        Can be called on any thread.

    Note:
        The returned view will be invalid after the shape handle has been released.

    See Also:
        :class:`~superdex.physics.MeshDataView`,
        :func:`~superdex.physics.get_shape_mesh`,
        :func:`~superdex.physics.get_shape_visual_mesh`,
        :meth:`~superdex.physics.Actor.get_surface_mesh`
    """

def get_shape_visual_mesh(shape: ShapeHandle) -> MeshDataView:
    """Get a view of the shape's visual mesh data, including skinning data if
    available.

    Returns a triangle mesh (3 nodes per element) intended for visual rendering.
    Coordinates and connectivity are returned in visual-mesh node-index space,
    including any visual mesh nodes not referenced by the visual connectivity. The
    mesh may include linear skinning data for deformation. For shapes without a
    visual mesh, returns an empty view.

    Args:
        shape (ShapeHandle): Handle to a valid shape.

    Returns:
        A non-owning view of the shape's visual mesh data, or an empty view if the
        shape has no visual mesh.

    Raises:
        :class:`~superdex.physics.Error`: If an error occurs.

    Note:
        Can be called on any thread.

    Note:
        The returned view will be invalid after the shape handle has been released.

    Note:
        When linear visual-mesh skinning data is present, skinning indices refer to
        the node ordering returned by :func:`~superdex.physics.get_shape_mesh`, not
        to the compact surface-node ordering returned by
        :func:`~superdex.physics.get_shape_surface_mesh`. Rod visual mesh embeddings
        are nonlinear and are not exposed through this linear skinning field.

    See Also:
        :class:`~superdex.physics.MeshDataView`,
        :func:`~superdex.physics.get_shape_mesh`,
        :func:`~superdex.physics.get_shape_surface_mesh`,
        :meth:`~superdex.physics.Actor.get_visual_mesh`
    """

def get_shape_aabb(shape: ShapeHandle) -> Aabb:
    """Get an axis-aligned bounding box (AABB) that contains the shape, in its local
    coordinate frame.

    Args:
        shape (ShapeHandle): Handle to a valid shape.

    Returns:
        The axis-aligned bounding box of the shape in local coordinates.

    Raises:
        :class:`~superdex.physics.Error`: If an error occurs.

    Note:
        Call on any thread.

    Note:
        Sets an error for shapes that do not have a bounding volume (e.g.,
        articulated shapes — query their link shapes individually instead).
    """

def get_articulated_shape_info(shape: ShapeHandle) -> ArticulatedShapeInfo:
    """Get information about an articulated shape.

    Args:
        shape (ShapeHandle): Handle to an articulated shape.

    Returns:
        The :class:`~superdex.physics.ArticulatedShapeInfo` for the specified shape.

    Raises:
        :class:`~superdex.physics.Error`: If an error occurs.

    Note:
        Only supported for articulated shapes. Sets error otherwise.

    Note:
        Can be called on any thread.

    Note:
        The returned information will be invalid after the shape handle has been
        released.

    See Also:
        :class:`~superdex.physics.ArticulatedShapeInfo`,
        :meth:`~superdex.physics.Actor.get_articulated_shape_info`
    """

def release_shape(shape: ShapeHandle) -> None:
    """Release a shape handle.

    In the C API, this must be called for every
    :class:`~superdex.physics.ShapeHandle` when you are done with it. In C++ and
    Python, this is optional because :class:`~superdex.physics.ShapeHandle` releases
    its reference automatically when destroyed. The shape memory is freed when it is
    no longer referenced by any handle, actor, or file cache entry.

    Args:
        shape (ShapeHandle): Handle to the shape to release.

    Note:
        Call on any thread.

    Note:
        If the shape handle is invalid or already released, then it will be ignored.

    See Also:
        :class:`~superdex.physics.ShapeHandle`,
        :func:`~superdex.physics.is_file_cache_enabled`
    """

def get_num_shapes() -> int:
    """Get the number of shape handles that have been created/loaded and have not yet
    been released.

    Returns:
        The number of active shape handles.

    Note:
        The file cache does not affect this value.

    Note:
        Call on any thread.

    See Also:
        :func:`~superdex.physics.is_file_cache_enabled`
    """

def enable_file_cache(enable: bool) -> None:
    """Enable or disable the file cache.

    When enabled, shape memory will be preserved even after the
    :class:`~superdex.physics.ShapeHandle` has been released. Subsequent requests
    for the same file (with the same baked-in scale and transform) will be fast
    because the data will already be loaded. This can be an important optimization,
    but beware that it could waste memory if it preserves shapes that are no longer
    needed.

    Args:
        enable (bool): True to enable, false to disable the feature.

    Note:
        Call on any thread.

    Note:
        Disabling the cache (passing false) immediately releases all currently
        cached shapes, equivalent to calling
        :func:`~superdex.physics.clear_file_cache`.

    See Also:
        :func:`~superdex.physics.is_file_cache_enabled`,
        :func:`~superdex.physics.clear_file_cache`,
        :func:`~superdex.physics.clear_file_from_cache`
    """

def is_file_cache_enabled() -> bool:
    """Return true if the file cache feature is enabled.

    Returns:
        True if the file cache is enabled, false otherwise.

    Note:
        Call on any thread.
    """

def clear_file_cache() -> None:
    """Release all shapes that are being preserved by the file cache (if any).

    Use this to free up memory, or to force subsequent file requests to be loaded
    from disk, in case those files had been modified.

    Note:
        Call on any thread.

    See Also:
        :func:`~superdex.physics.is_file_cache_enabled`,
        :func:`~superdex.physics.clear_file_from_cache`
    """

def clear_file_from_cache(file_path: str) -> None:
    """Release all cached entries for a given shape file.

    Use this to free up memory, or to force the next file request to be loaded from
    disk, in case the file had been modified. All entries for ``file_path`` are
    removed, including every variant produced by different bake scale/transform
    combinations.

    Args:
        file_path (str): Path to a file that may have been loaded. The match is
            case-sensitive and must equal the path used at load time exactly.

    Note:
        Call on any thread.

    See Also:
        :func:`~superdex.physics.is_file_cache_enabled`,
        :func:`~superdex.physics.clear_file_cache`
    """

def create_scene(name: str) -> Scene:
    """Create a new :class:`~superdex.physics.Scene`.

    A scene is a collection of actors and constraints, which can be stepped to
    advance the simulation. Each scene is independent. Actors from one scene cannot
    affect another scene.

    You can create a new scene on any thread, but that scene must only be accessed
    from one thread at a time. All reads and writes to the scene must be synchronous
    and well ordered. However, if you have multiple scenes, then it is safe to step
    them concurrently.

    Args:
        name (str): Name of the new scene (for debugging).

    Returns:
        Pointer to the new scene. Call :func:`~superdex.physics.destroy_scene` when
        done with it.

    Note:
        Call on any thread.

    See Also:
        :func:`~superdex.physics.destroy_scene`
    """

def destroy_scene(scene: Optional[Scene]) -> None:
    """Immediately destroy a scene and all its actors and constraints.

    Args:
        scene (Optional[Scene]): Pointer to the scene to destroy.

    Note:
        No-op if ``scene`` is None or unknown to this Mochi context.

    Note:
        Call on any thread.

    Note:
        Any remaining scenes will be cleaned up automatically when the Mochi context
        is destroyed.

    Warning:
        This documentation references the following C++ APIs, which are not
        available in Python: ``mochi::AsyncScene``,
        ``mochi::Context::DestroyAsyncScene``.

    Warning:
        Do not dereference pointers to the scene, actors, constraints, etc. after
        this call.

    Warning:
        If ``scene`` is owned by an ``mochi::AsyncScene``, the call is a no-op (a
        warning is logged). Call ``mochi::Context::DestroyAsyncScene`` instead.

    See Also:
        :func:`~superdex.physics.create_scene`,
        ``mochi::Context::DestroyAsyncScene``
    """

def get_scene(handle: SceneHandle) -> Optional[Scene]:
    """Get a pointer to a scene by its handle.

    Args:
        handle (SceneHandle): Handle to the scene.

    Returns:
        Pointer to the scene, or None if the handle is invalid in this context.

    Note:
        Call on any thread.
    """

def is_valid_scene(scene: Optional[Scene]) -> bool:
    """Check if a scene is valid (not destroyed), and belongs to this Mochi context.

    Args:
        scene (Optional[Scene]): Pointer to the scene to check.

    Returns:
        True if the scene is valid and belongs to this Mochi context.

    Note:
        Call on any thread.

    See Also:
        :func:`~superdex.physics.create_scene`,
        :func:`~superdex.physics.destroy_scene`
    """

def get_debug_server() -> DebugServer:
    """Get the debug server for this context.

    Returns:
        Reference to the :class:`~superdex.physics.DebugServer`, which accepts
        connections from external debug tools.

    See Also:
        :class:`~superdex.physics.DebugServer`
    """

class Actor:
    """Represents a simulated body (actor) in a scene.

    Note:
        Every body in the simulation is an actor.

    Note:
        Actors always belong to exactly one scene.

    Note:
        Actors can be static or dynamic.

    Warning:
        Concurrent access to the actor or its scene is illegal.
    """
    def get_handle(self) -> ActorHandle:
        """Get the actor's handle.

        Returns:
            Actor's handle.

        Note:
            Handles are safe to store and remain valid throughout the actor lifespan.

        Note:
            Call :meth:`~superdex.physics.Scene.get_actor` to look up the actor's
            pointer from the actor's handle.

        See Also:
            :meth:`~superdex.physics.Scene.get_actor`
        """
    def get_scene(self) -> Scene:
        """Get the :class:`~superdex.physics.Scene` that this actor belongs to.

        Returns:
            Pointer to the scene that this actor belongs to.
        """
    def get_name(self) -> str:
        """Get the name of the actor.

        Returns:
            Name of the actor.

        Note:
            Actor names are not guaranteed to be unique.
        """
    def get_type(self) -> ActorType:
        """Get the type of the actor.

        Returns:
            Type of the actor.
        """
    def get_convergence_status(self) -> ConvergenceStatus:
        """Get the convergence status of the actor in the most recent simulation step.

        Returns:
            Convergence status of the actor in the most recent simulation step.

        Note:
            Static actors do not participate in the solver and may return :class:`NONE
            <superdex.physics.ConvergenceStatus>`.

        See Also:
            :class:`~superdex.physics.ConvergenceStatus`,
            :attr:`~superdex.physics.SolverStats.convergence_status`
        """
    def is_static(self) -> bool:
        """Check whether the actor is static.

        Returns:
            True if the actor is static. False if it's dynamic.
        """
    def get_contact_params(self) -> ContactParams:
        """Get the contact parameters of the actor.

        Returns:
            Contact parameters of the actor.

        Raises:
            :class:`~superdex.physics.Error`: If an error occurs.

        Note:
            Reports an error if the actor has no contact parameters.
        """
    def set_contact_params(self, params: ContactParams) -> None:
        """Set the contact parameters of the actor.

        Args:
            params (ContactParams): Contact parameters to set.

        Raises:
            :class:`~superdex.physics.Error`: If an error occurs.
        """
    def get_collider_type(self) -> ColliderType:
        """Get the collider type of the actor.

        Returns:
            Collider type of the actor.

        Note:
            If :class:`AUTO <superdex.physics.ColliderType>` was specified when creating
            the actor, this method returns the collider type that was automatically
            selected based on the actor's shape, not Auto.
        """
    def has_root_transform(self) -> bool:
        """Check if the actor has a root transform.

        Returns:
            True if the actor has a root transform. False otherwise.

        Note:
            Non-rendered actors may not have a root transform.
        """
    def get_root_transform(self) -> TransformRT:
        """Get the actor's world-from-local transform (root transform).

        Returns:
            The actor's world-from-local transform.

        Note:
            Returns an identity transform when
            :meth:`~superdex.physics.Actor.has_root_transform` returns false.

        Note:
            For a top-level articulated actor, the local frame is the articulation root
            frame, not the root link's frame. For a nested link actor, the local frame
            is that link's frame.
        """
    def set_root_transform(self, world_from_local: TransformRT) -> None:
        """Set the actor's world-from-local transform (root transform).

        Args:
            world_from_local (TransformRT): World-space transform to set.

        Raises:
            :class:`~superdex.physics.Error`: If an error occurs.

        Note:
            Not allowed on nested link actors
            (:meth:`~superdex.physics.Actor.get_nested_link_actors`) or nested soft
            actors (:meth:`~superdex.physics.Actor.get_nested_soft_actors`).

        Note:
            For a top-level articulated actor, the local frame is the articulation root
            frame, not the root link's frame.

        Note:
            For dynamic actors, it does not change velocity. For static actors, however,
            it changes velocity, because it is implicitly defined by the root transform
            in consecutive time steps. To cancel the velocity of a static actor, use
            :meth:`~superdex.physics.Actor.set_velocity`.

        Note:
            Resets multi-step time integrators, e.g. BDF2 falls back to backward Euler
            in the first time step after this method is called.
        """
    def get_rigid_center_of_mass_local(self) -> Real3:
        """Get the position of the center of mass in the actor's local coordinate frame
        (:meth:`~superdex.physics.Actor.get_root_transform`).

        Returns:
            Position vector of the center of mass in the actor's local coordinate frame.

        Raises:
            :class:`~superdex.physics.Error`: If an error occurs.

        Note:
            Only supported for dynamic rigid actors (including articulated links) and
            soft actors.

        See Also:
            :meth:`~superdex.physics.Actor.get_root_transform`
        """
    def get_center_of_mass_transform(self) -> TransformRT:
        """Get the world-space transform of the actor expressed at the center of mass.

        The rotation is the same as in
        :meth:`~superdex.physics.Actor.get_root_transform`, but the translation accounts
        for the local position of the center of mass with respect to the root transform.

        Returns:
            Transform of the actor at the center of mass.

        Raises:
            :class:`~superdex.physics.Error`: If an error occurs.

        Note:
            Only supported for dynamic rigid actors.

        See Also:
            :meth:`~superdex.physics.Actor.set_center_of_mass_transform`,
            :meth:`~superdex.physics.Actor.get_root_transform`
        """
    def set_center_of_mass_transform(self, world_from_com: TransformRT) -> None:
        """Set the world-space transform of the actor expressed at the center of mass.

        Args:
            world_from_com (TransformRT): World-space transform at the center of mass to
                set.

        Raises:
            :class:`~superdex.physics.Error`: If an error occurs.

        Note:
            Only supported for standalone dynamic rigid actors.

        Note:
            Does not change velocity.

        Note:
            Resets multi-step time integrators, e.g. BDF2 falls back to backward Euler
            in the first time step after this method is called.

        See Also:
            :meth:`~superdex.physics.Actor.get_center_of_mass_transform`,
            :meth:`~superdex.physics.Actor.set_root_transform`
        """
    def get_rigid_moment_of_inertia_local(self) -> Real6:
        """Get the moment of inertia tensor of the actor at the center of mass, aligned
        with the actor's local reference system (root transform), and using *negative*
        tensor notation.

        ::

                 [ixx, ixy, ixz]   [ +∫(y²+z²)dm, -∫(xy)dm,    -∫(xz)dm     ]
             I = [ixy, iyy, iyz] = [ -∫(xy)dm,    +∫(x²+z²)dm, -∫(yz)dm     ]
                 [ixz, iyz, izz]   [ -∫(xz)dm,    -∫(yz)dm,    +∫(x²+y²)dm  ]

        Returns:
            Upper-right triangle in the form [ixx, ixy, ixz, iyy, iyz, izz]. Units:
            kg·m².

        Raises:
            :class:`~superdex.physics.Error`: If an error occurs.

        Note:
            Only supported for dynamic rigid actors.
        """
    def set_velocity(self, linear_vel: Real3Like, angular_vel: Real3Like) -> None:
        """Set the world-frame linear and angular velocity of the actor at the center of
        mass.

        Args:
            linear_vel (Real3Like): Linear velocity [m/s] of the center of mass in world
                frame.
            angular_vel (Real3Like): Angular velocity [rad/s] at the center of mass in
                world frame.

        Raises:
            :class:`~superdex.physics.Error`: If an error occurs.

        Note:
            Only supported for standalone rigid actors and standalone soft actors
            (except ROMs).

        Note:
            For static actors, it only supports setting zero velocity.

        Note:
            For standalone soft actors, the angular velocity must be zero.

        Note:
            Resets multi-step time integrators, e.g. BDF2 falls back to backward Euler
            in the first time step after this method is called.
        """
    def get_linear_velocity(self) -> Real3:
        """Get the linear velocity of the actor in world frame.

        Returns:
            Linear velocity in world frame [m/s].

        Raises:
            :class:`~superdex.physics.Error`: If an error occurs.

        Note:
            Only supported for rigid and soft actors.

        Note:
            For rigid actors, it returns the velocity of the center of mass.

        Note:
            For soft actors, it returns an estimate of the linear velocity of the root
            transform. Requires recentering to be enabled for accurate results.

        Note:
            Not supported for ROM actors.
        """
    def get_angular_velocity(self) -> Real3:
        """Get the angular velocity of the actor in world frame.

        Returns:
            Angular velocity in world frame [rad/s].

        Raises:
            :class:`~superdex.physics.Error`: If an error occurs.

        Note:
            Only supported for rigid and soft actors.

        Note:
            For rigid actors, it returns the actual angular velocity.

        Note:
            For soft actors, it returns an estimate of the angular velocity of the root
            transform. Requires recentering to be enabled for accurate results.

        Note:
            Not supported for ROM actors.
        """
    def get_mass(self) -> float:
        """Get the total mass of the actor.

        Returns:
            Mass [kg] of the actor.

        Raises:
            :class:`~superdex.physics.Error`: If an error occurs.

        Note:
            Not supported for static actors.
        """
    def get_density(self) -> float:
        """Get the density of the actor in the undeformed configuration.

        Returns:
            Density [kg/m³] of the actor.

        Raises:
            :class:`~superdex.physics.Error`: If an error occurs.

        Note:
            Only supported for rigid actors (except static rigid actors), articulated
            links and soft actors.

        See Also:
            :meth:`~superdex.physics.Actor.set_density`,
            :meth:`~superdex.physics.Actor.get_soft_material_params`
        """
    def set_density(self, density: float) -> None:
        """Set the density of the actor in the undeformed configuration.

        Args:
            density (float): Density [kg/m³] to set. Must be positive.

        Raises:
            :class:`~superdex.physics.Error`: If an error occurs.

        Note:
            Only supported for rigid actors (except static rigid actors), articulated
            links and soft actors.

        Note:
            The moment of inertia is updated based on the new density. The center of
            mass is left unchanged.

        See Also:
            :meth:`~superdex.physics.Actor.get_density`,
            :meth:`~superdex.physics.Actor.set_inertia_properties`,
            :meth:`~superdex.physics.Actor.set_soft_material_params`
        """
    def set_inertia_properties(
        self,
        mass: float,
        center_of_mass: Real3Like,
        moment_of_inertia: Real6Like,
    ) -> None:
        """Set the mass properties of the actor.

        Directly sets mass, center of mass position in local coordinates, and moment of
        inertia tensor at the center of mass. This bypasses density-based computation
        and allows specifying exact inertia from CAD data or external measurements.

        Args:
            mass (float): Mass [kg] to set. Must be positive and finite.
            center_of_mass (Real3Like): Position of center of mass [m] in actor's local
                coordinate frame (:meth:`~superdex.physics.Actor.get_root_transform`).
            moment_of_inertia (Real6Like): Moment of inertia tensor at center of mass
                [kg·m²], upper-right triangle [ixx, ixy, ixz, iyy, iyz, izz] using
                negative tensor notation consistent with
                :meth:`~superdex.physics.Actor.get_rigid_moment_of_inertia_local`. Must
                be finite.

        Raises:
            :class:`~superdex.physics.Error`: If an error occurs.

        Note:
            Only supported for standalone dynamic rigid actors.

        Note:
            A finite but physically invalid moment of inertia tensor (negative principal
            moments or one violating the triangle inequality) is accepted with a
            warning, not rejected.

        Note:
            Re-anchors the density reference state. Subsequent
            :meth:`~superdex.physics.Actor.set_density` calls rescale mass and moment of
            inertia proportionally about the new values; center of mass is unchanged by
            :meth:`~superdex.physics.Actor.set_density`.

        Note:
            After this call, :meth:`~superdex.physics.Actor.get_density` returns bulk
            density mass / volume.

        See Also:
            :meth:`~superdex.physics.Actor.get_mass`,
            :meth:`~superdex.physics.Actor.get_density`,
            :meth:`~superdex.physics.Actor.set_density`,
            :meth:`~superdex.physics.Actor.get_rigid_center_of_mass_local`,
            :meth:`~superdex.physics.Actor.get_rigid_moment_of_inertia_local`
        """
    def get_elastic_energy(self) -> float:
        """Get the elastic energy of the actor, measured as the difference with respect
        to
        the rest state.

        Returns:
            Elastic energy [J] of the actor.

        Raises:
            :class:`~superdex.physics.Error`: If an error occurs.

        Note:
            Requires registering :class:`ELASTIC_ENERGY <superdex.physics.QueryType>`
            before the simulation step. Results are available after the simulation step
            completes.

        Note:
            Requires the actor to support :class:`ELASTIC_ENERGY
            <superdex.physics.QueryType>`.

        See Also:
            :meth:`~superdex.physics.Actor.register_query`, :class:`ELASTIC_ENERGY
            <superdex.physics.QueryType>`
        """
    def get_recentering_params(self) -> RecenteringParams:
        """Gets the recentering parameters of the actor.

        Returns:
            Recentering parameters of the actor.

        Note:
            Recentering is only supported for standalone soft actors. For other actor
            types, returns default recentering parameters with
            :attr:`~superdex.physics.RecenteringParams.use_recentering` set to false.

        See Also:
            :class:`~superdex.physics.RecenteringParams`,
            :meth:`~superdex.physics.Actor.set_recentering_params`
        """
    def set_recentering_params(self, params: RecenteringParams) -> None:
        """Set the recentering parameters of the actor.

        When enabled, the root transform automatically moves as the actor's "rigid
        pivot" (typically near the center of mass) moves.

        Args:
            params (RecenteringParams): Recentering parameters to set.

        Raises:
            :class:`~superdex.physics.Error`: If an error occurs.

        Note:
            Only supported for standalone soft actors.

        See Also:
            :class:`~superdex.physics.RecenteringParams`,
            :meth:`~superdex.physics.Actor.get_recentering_params`
        """
    def get_displacements(self) -> SpanConstReal:
        """Get the node displacements of the actor in the actor's local frame, relative
        to
        reference (rest) positions.

        Returns:
            Node displacements in the actor's local frame. Length is 3 × the number of
            nodes, except for rod actors, where it is 4 × the number of nodes (3
            displacement [m] + 1 twist [rad] per node). The number of nodes is available
            from :meth:`~superdex.physics.Actor.get_mesh`.

        Raises:
            :class:`~superdex.physics.Error`: If an error occurs.

        Note:
            Only supported for standalone soft actors, nested soft actors, shell actors,
            rod actors, and articulated actors with a skin mesh.

        Note:
            For articulated actors, returns displacements of the skin mesh.

        Note:
            For nested soft actors, returns post-skinning displacements, which may
            differ from the pre-skinning elastic deformation component written by
            :meth:`~superdex.physics.Actor.set_displacements`.

        See Also:
            :meth:`~superdex.physics.Actor.set_displacements`,
            :meth:`~superdex.physics.Actor.get_node_positions_local`
        """
    def set_displacements(self, displacements: ArrayLikeReal) -> None:
        """Set the node displacements in the actor's local frame, relative to reference
        (rest) positions.

        Args:
            displacements (ArrayLikeReal): Node displacements [m] to set in the actor's
                local frame. Length must be 3 × the number of nodes. The number of nodes
                is available from :meth:`~superdex.physics.Actor.get_mesh`.

        Raises:
            :class:`~superdex.physics.Error`: If an error occurs.

        Note:
            Only supported for standalone soft actors, nested soft actors, and shell
            actors.

        Note:
            For nested soft actors, displacements are the elastic deformation component
            only (before skinning is applied).

        Note:
            Does not change the elastic velocity DoFs, but changes the skinned node
            velocities of nested soft actors.

        Note:
            Resets multi-step time integrators, e.g. BDF2 falls back to backward Euler
            in the first time step after this method is called.

        See Also:
            :meth:`~superdex.physics.Actor.get_displacements`,
            :meth:`~superdex.physics.Actor.set_node_positions_local`
        """
    def get_soft_material_params(self) -> SoftMaterialParams:
        """Get the material parameters of a soft actor.

        Returns:
            Material parameters of the actor.

        Raises:
            :class:`~superdex.physics.Error`: If an error occurs.

        Note:
            Only supported for soft actors.

        See Also:
            :meth:`~superdex.physics.Actor.set_soft_material_params`,
            :func:`~superdex.physics.experimental.get_soft_material_params_field`,
            :meth:`~superdex.physics.Actor.get_density`
        """
    def set_soft_material_params(self, params: SoftMaterialParams) -> None:
        """Set the material parameters of a soft actor.

        Args:
            params (SoftMaterialParams): Material parameters to set.

        Raises:
            :class:`~superdex.physics.Error`: If an error occurs.

        Note:
            Only supported for soft actors.

        Note:
            If the :class:`ELASTIC_ENERGY <superdex.physics.QueryType>` query is active,
            invalidates the elastic energy baseline. One simulation step must complete
            before :meth:`~superdex.physics.Actor.get_elastic_energy` returns valid
            results.

        See Also:
            :meth:`~superdex.physics.Actor.get_soft_material_params`,
            :func:`~superdex.physics.experimental.set_soft_material_params_field`,
            :meth:`~superdex.physics.Actor.set_density`
        """
    def get_mesh(self) -> MeshDataView:
        """Get a view of the actor's reference simulation mesh.

        For soft actors, returns the tetrahedral mesh. For shell actors, returns the
        triangle mesh. For rod actors, returns the polyline mesh. For articulated actors
        with a skin mesh, returns the skin mesh. For articulated actors without a skin
        mesh, returns an empty view. For soft-skinned actors, returns the blended skin
        mesh when one is present and an empty view otherwise. For rigid actors whose
        shape is a tetrahedral or triangular mesh, returns their surface simulation
        mesh. For rigid actors with an implicit shape, such as spheres and planes,
        returns an empty view.

        Returns:
            A non-owning view of the actor's reference simulation mesh, or an empty view
            if the actor has no simulation mesh.

        Note:
            The returned coordinates are reference positions in the actor's local frame.
            They do not include deformation for deformable actors.

        Note:
            For rigid actors with tetrahedral or triangular mesh shapes, returns the
            same compact surface view as
            :meth:`~superdex.physics.Actor.get_surface_mesh`. For tetrahedral meshes
            this is the boundary surface. For triangular meshes, nodes not referenced by
            any surface triangle are omitted and remaining nodes may be reindexed. Use
            :meth:`~superdex.physics.Actor.get_reference_shape` and
            :func:`~superdex.physics.get_shape_mesh` to retrieve the original authored
            shape mesh.

        Note:
            The returned view remains valid until the actor is destroyed.

        See Also:
            :class:`~superdex.physics.MeshDataView`,
            :meth:`~superdex.physics.Actor.get_surface_mesh`,
            :meth:`~superdex.physics.Actor.get_visual_mesh`,
            :func:`~superdex.physics.get_shape_mesh`
        """
    def get_surface_mesh(self) -> MeshDataView:
        """Get a view of the actor's reference surface mesh.

        Returns a compact triangle mesh for the actor's surface. For volumetric
        simulation meshes, the surface is the boundary triangles. For triangular
        simulation meshes, the surface has the same triangle elements as the simulation
        mesh, but nodes not referenced by any surface triangle are omitted and remaining
        nodes may be reindexed. For rod actors with an authored contact skin, returns
        that skin regardless of the actor's selected collision representation. Returns
        an empty view if the actor does not have a surface mesh.

        Returns:
            A non-owning view of the actor's reference surface mesh, or an empty view if
            the actor has no surface mesh.

        Note:
            The returned coordinates are reference surface-mesh positions in the actor's
            local frame. They do not include deformation for deformable actors.

        Note:
            The returned mesh uses compact surface-node ordering. Connectivity indices
            refer to this ordering, which may differ from the node ordering returned by
            :meth:`~superdex.physics.Actor.get_mesh`. Interior nodes and nodes not
            referenced by any surface triangle are excluded. Results from
            :meth:`~superdex.physics.Actor.get_surface_mesh_node_positions_local` and
            :meth:`~superdex.physics.Actor.get_surface_mesh_node_normals_local` use this
            same surface ordering.

        Note:
            The returned view remains valid until the actor is destroyed.

        See Also:
            :class:`~superdex.physics.MeshDataView`,
            :meth:`~superdex.physics.Actor.get_mesh`,
            :meth:`~superdex.physics.Actor.get_visual_mesh`,
            :meth:`~superdex.physics.Actor.get_surface_mesh_node_positions_local`,
            :meth:`~superdex.physics.Actor.get_surface_mesh_node_normals_local`,
            :func:`~superdex.physics.get_shape_surface_mesh`
        """
    def get_visual_mesh(self) -> MeshDataView:
        """Get a view of the actor's reference visual mesh.

        Returns a triangle mesh intended for visual rendering. The mesh may include
        linear visual-mesh skinning data when available. Returns an empty view if the
        actor does not have a visual mesh.

        Returns:
            A non-owning view of the actor's reference visual mesh, or an empty view if
            the actor has no visual mesh.

        Note:
            The returned coordinates are reference visual-mesh positions in the actor's
            local frame. They do not include deformation for deformable actors.

        Note:
            Includes visual mesh nodes not referenced by any visual triangle. Unlike
            surface mesh getters, visual mesh getters do not omit unreferenced nodes.

        Note:
            When linear visual-mesh skinning data is present, skinning indices refer to
            the node ordering returned by :meth:`~superdex.physics.Actor.get_mesh`, not
            to the compact surface-node ordering returned by
            :meth:`~superdex.physics.Actor.get_surface_mesh`. Rod visual mesh embeddings
            are nonlinear and are not exposed through this linear skinning field.

        Note:
            The returned view remains valid until the actor is destroyed.

        See Also:
            :class:`~superdex.physics.MeshDataView`,
            :meth:`~superdex.physics.Actor.get_mesh`,
            :meth:`~superdex.physics.Actor.get_surface_mesh`,
            :meth:`~superdex.physics.Actor.get_visual_mesh_node_positions_local`,
            :meth:`~superdex.physics.Actor.get_visual_mesh_node_normals_local`,
            :func:`~superdex.physics.get_shape_visual_mesh`
        """
    def get_surface_mesh_node_positions_local(self) -> SpanConstReal:
        """Get the current position of the nodes (reference + displacements) of the
        surface
        mesh in the actor's local frame.

        Returns:
            Current node positions [m] of the surface mesh in the actor's local frame (3
            values per node: x, y, z). The returned node list contains exactly the nodes
            referenced by :meth:`~superdex.physics.Actor.get_surface_mesh` and is 1-to-1
            with :meth:`~superdex.physics.Actor.get_surface_mesh_node_normals_local`.

        Raises:
            :class:`~superdex.physics.Error`: If an error occurs.

        Note:
            Requires registering :class:`SURFACE_NODE_POSITIONS
            <superdex.physics.QueryType>` before the simulation step. Results are
            available after the simulation step completes.

        Note:
            Requires the actor to support :class:`SURFACE_NODE_POSITIONS
            <superdex.physics.QueryType>`.

        Note:
            The node ordering corresponds to the mesh connectivity from
            :meth:`~superdex.physics.Actor.get_surface_mesh`.

        Note:
            Supported for rod actors whose shape has an authored contact skin,
            regardless of whether that skin is selected for collision.

        See Also:
            :meth:`~superdex.physics.Actor.register_query`,
            :class:`SURFACE_NODE_POSITIONS <superdex.physics.QueryType>`,
            :meth:`~superdex.physics.Actor.get_surface_mesh`,
            :meth:`~superdex.physics.Actor.get_surface_mesh_node_normals_local`
        """
    def get_surface_mesh_node_normals_local(self) -> SpanConstReal:
        """Get the normal direction for each node of the surface mesh in the actor's
        local
        frame.

        Returns:
            Span of normals (3 values per node) in the actor's local frame, computed by
            normalizing the area-weighted sum of adjacent triangle normals. The returned
            node list contains exactly the nodes referenced by
            :meth:`~superdex.physics.Actor.get_surface_mesh` and is 1-to-1 with
            :meth:`~superdex.physics.Actor.get_surface_mesh_node_positions_local`.

        Raises:
            :class:`~superdex.physics.Error`: If an error occurs.

        Note:
            Requires registering :class:`SURFACE_NODE_NORMALS
            <superdex.physics.QueryType>` before the simulation step. Results are
            available after the simulation step completes.

        Note:
            Requires the actor to support :class:`SURFACE_NODE_NORMALS
            <superdex.physics.QueryType>`.

        Note:
            The node ordering corresponds to the mesh connectivity from
            :meth:`~superdex.physics.Actor.get_surface_mesh`.

        Note:
            Supported for rod actors whose shape has an authored contact skin,
            regardless of whether that skin is selected for collision.

        See Also:
            :meth:`~superdex.physics.Actor.register_query`, :class:`SURFACE_NODE_NORMALS
            <superdex.physics.QueryType>`,
            :meth:`~superdex.physics.Actor.get_surface_mesh`,
            :meth:`~superdex.physics.Actor.get_surface_mesh_node_positions_local`
        """
    def get_reference_shape(self) -> ShapeHandle:
        """Get a new :class:`~superdex.physics.ShapeHandle` referencing the same shape
        data
        used to create this actor.

        The returned handle is a new reference to the same underlying shape data. It
        will have a different numeric value from the
        :class:`~superdex.physics.ShapeHandle` originally used to create the actor. The
        new handle represents a new request to keep the shape data in memory — the shape
        will remain valid as long as any handle, actor, or file cache entry references
        it.

        Returns:
            A new :class:`~superdex.physics.ShapeHandle` referencing the actor's shape
            data.

        Raises:
            :class:`~superdex.physics.Error`: If an error occurs.

        Note:
            In C++ and Python, the returned :class:`~superdex.physics.ShapeHandle` is
            reference-counted and releases its reference automatically when destroyed.
            In the C API, you must call :func:`~superdex.physics.release_shape` when
            done with the handle to avoid leaks.

        See Also:
            :func:`~superdex.physics.release_shape`,
            :func:`~superdex.physics.load_shape_from_file`
        """
    def get_contact_layer(self) -> str:
        """Get the name of the contact layer of the actor.

        Returns:
            Contact layer name, or an empty string if the actor has no contact layer.

        See Also:
            :meth:`~superdex.physics.Actor.set_contact_layer`,
            :meth:`~superdex.physics.Scene.enable_layer_contact_asymmetric`,
            :meth:`~superdex.physics.Scene.enable_layer_contact_symmetric`
        """
    def set_contact_layer(self, layer: str) -> None:
        """Set the name of the contact layer of the actor.

        Args:
            layer (str): Contact layer name to set.

        See Also:
            :meth:`~superdex.physics.Actor.get_contact_layer`,
            :meth:`~superdex.physics.Scene.enable_layer_contact_asymmetric`,
            :meth:`~superdex.physics.Scene.enable_layer_contact_symmetric`
        """
    def get_num_dofs(self) -> int:
        """Get the number of degrees of freedom (DoFs) of the actor.

        Returns:
            Number of degrees of freedom of the actor.

        Note:
            Returns zero for actors without degrees of freedom (e.g., static actors).
        """
    def get_dof_values(
        self,
        dof_indices: ArrayLikeInt,
        out_dof_values: ArrayLikeReal,
    ) -> None:
        """Get the current value of the actor's degrees of freedom (DoFs) corresponding
        to
        the specified indices.

        Args:
            dof_indices (ArrayLikeInt): DoF indices to query. Each index must be in the
                range [0, :meth:`~superdex.physics.Actor.get_num_dofs`). If empty, get
                the value of all DoFs.
            out_dof_values (ArrayLikeReal): Output buffer for the DoF values. Size must
                equal :meth:`~superdex.physics.Actor.get_num_dofs` when ``dof_indices``
                is empty, or the size of ``dof_indices`` otherwise.

        Raises:
            :class:`~superdex.physics.Error`: If an error occurs.

        Note:
            Not supported for static actors, even when both input and output spans are
            empty.

        See Also:
            :meth:`~superdex.physics.Actor.get_num_dofs`
        """
    @overload
    def query_nodes_in_volume_local(
        self,
        volume: Aabb,
        boundary_only: bool,
        callback: Callable[[int, Real3], None],
    ) -> None:
        """Find the actor's nodes that are inside an axis-aligned bounding box (AABB).

        Args:
            volume (Aabb): Axis-aligned bounding box in the actor's local frame.
            boundary_only (bool): If true, only boundary nodes are considered.
            callback (Callable[[int, Real3], None]): Callback called for each node
                found. The position is the current node position [m] in the actor's
                local frame. The node index uses
                :meth:`~superdex.physics.Actor.get_mesh` ordering, including when
                ``boundary_only`` is true.

        Raises:
            :class:`~superdex.physics.Error`: If an error occurs.

        Note:
            When ``boundary_only`` is false, this function supports only soft and shell
            actors. When true, it also supports articulated actors with a skin mesh,
            soft-skinned actors with a blended skin mesh, and rigid actors (dynamic or
            static) with tetrahedral or triangular mesh shapes. Rod actors and actors
            without a simulation mesh are not supported.

        Note:
            Requires query registration before the simulation step. Register
            :class:`NODE_POSITIONS <superdex.physics.QueryType>` when ``boundary_only``
            is false, or :class:`SURFACE_NODE_POSITIONS <superdex.physics.QueryType>`
            when ``boundary_only`` is true. Results are available after the simulation
            step completes.

        Warning:
            This is a synchronous call and may be expensive.
        """
    @overload
    def query_nodes_in_volume_local(
        self,
        volume: Obb,
        boundary_only: bool,
        callback: Callable[[int, Real3], None],
    ) -> None:
        """Find the actor's nodes that are inside an oriented bounding box (OBB).

        Args:
            volume (Obb): Oriented bounding box in the actor's local frame.
            boundary_only (bool): If true, only boundary nodes are considered.
            callback (Callable[[int, Real3], None]): Callback called for each node
                found. The position is the current node position [m] in the actor's
                local frame. The node index uses
                :meth:`~superdex.physics.Actor.get_mesh` ordering, including when
                ``boundary_only`` is true.

        Raises:
            :class:`~superdex.physics.Error`: If an error occurs.

        Note:
            When ``boundary_only`` is false, this function supports only soft and shell
            actors. When true, it also supports articulated actors with a skin mesh,
            soft-skinned actors with a blended skin mesh, and rigid actors (dynamic or
            static) with tetrahedral or triangular mesh shapes. Rod actors and actors
            without a simulation mesh are not supported.

        Note:
            Requires query registration before the simulation step. Register
            :class:`NODE_POSITIONS <superdex.physics.QueryType>` when ``boundary_only``
            is false, or :class:`SURFACE_NODE_POSITIONS <superdex.physics.QueryType>`
            when ``boundary_only`` is true. Results are available after the simulation
            step completes.

        Warning:
            This is a synchronous call and may be expensive.
        """
    @overload
    def query_nodes_in_volume_local(
        self,
        volume: Sphere,
        boundary_only: bool,
        callback: Callable[[int, Real3], None],
    ) -> None:
        """Find the actor's nodes that are inside a sphere.

        Args:
            volume (Sphere): Sphere in the actor's local frame.
            boundary_only (bool): If true, only boundary nodes are considered.
            callback (Callable[[int, Real3], None]): Callback called for each node
                found. The position is the current node position [m] in the actor's
                local frame. The node index uses
                :meth:`~superdex.physics.Actor.get_mesh` ordering, including when
                ``boundary_only`` is true.

        Raises:
            :class:`~superdex.physics.Error`: If an error occurs.

        Note:
            When ``boundary_only`` is false, this function supports only soft and shell
            actors. When true, it also supports articulated actors with a skin mesh,
            soft-skinned actors with a blended skin mesh, and rigid actors (dynamic or
            static) with tetrahedral or triangular mesh shapes. Rod actors and actors
            without a simulation mesh are not supported.

        Note:
            Requires query registration before the simulation step. Register
            :class:`NODE_POSITIONS <superdex.physics.QueryType>` when ``boundary_only``
            is false, or :class:`SURFACE_NODE_POSITIONS <superdex.physics.QueryType>`
            when ``boundary_only`` is true. Results are available after the simulation
            step completes.

        Warning:
            This is a synchronous call and may be expensive.
        """
    def get_points_distance_to_surface(
        self,
        points_world: ArrayLikeReal3,
        out_distances: ArrayLikeReal,
    ) -> None:
        """Query the signed distance to the closest point on the actor's surface.

        Args:
            points_world (ArrayLikeReal3): Point cloud in world frame.
            out_distances (ArrayLikeReal): Output distances [m], in the same order as
                the input points. Must be the same size as the input points. Negative
                values indicate points inside the actor.

        Raises:
            :class:`~superdex.physics.Error`: If an error occurs.

        Note:
            Requires the actor to have a collider.

        Note:
            For actors with SDF collider, the reported distance is only correct inside
            the actor's bounding box. Outside the bounding box, it is an upper bound
            based on the triangle inequality.

        Note:
            This function is not supported for soft actors or for the experimental
            :class:`POINT_CLOUD <superdex.physics.ColliderType>`.
        """
    def get_aabb_local(self) -> Aabb:
        """Get an axis-aligned bounding box (AABB) that contains the full volume of the
        actor, in the actor's local frame.

        Returns:
            AABB in the actor's local frame containing the full actor volume.

        Raises:
            :class:`~superdex.physics.Error`: If an error occurs.
        """
    def get_aabb_world(self) -> Aabb:
        """Get an axis-aligned bounding box (AABB) that contains the full volume of the
        actor, in world frame.

        Returns:
            AABB in world frame containing the full actor volume.

        Raises:
            :class:`~superdex.physics.Error`: If an error occurs.
        """
    def get_node_positions_local(self) -> SpanConstReal:
        """Get the current position (reference + displacements) of each node in the
        actor's
        local frame.

        Returns:
            Flattened node positions [m] in the actor's local frame (3 values per node:
            x, y, z).

        Raises:
            :class:`~superdex.physics.Error`: If an error occurs.

        Note:
            Requires registering :class:`NODE_POSITIONS <superdex.physics.QueryType>`
            before the simulation step. Results are available after the simulation step
            completes.

        Note:
            Requires the actor to support :class:`NODE_POSITIONS
            <superdex.physics.QueryType>`.

        See Also:
            :meth:`~superdex.physics.Actor.register_query`, :class:`NODE_POSITIONS
            <superdex.physics.QueryType>`
        """
    def set_zero_displacements_and_velocities(self) -> None:
        """Set all local displacement and velocity DoFs to zero.

        Raises:
            :class:`~superdex.physics.Error`: If an error occurs.

        Note:
            Only supported for standalone soft actors, nested soft actors, shell actors,
            and rod actors.

        Note:
            For nested soft actors, it zeroes only elastic displacement and velocity.
            Skeleton-driven displacement and velocity are preserved.

        Note:
            For rod actors, it zeroes both the translational displacement DoFs and the
            twist-angle DoFs.

        Note:
            Resets multi-step time integrators, e.g. BDF2 falls back to backward Euler
            in the first time step after this method is called.
        """
    def get_elements_deformation_gradient(self) -> SpanConstReal:
        """Get the deformation gradient of each element.

        Returns:
            Span of deformation gradients. The deformation gradient F of element e is
            stored at index e*9 in row-major format: F_00, F_01, F_02, F_10, F_11, F_12,
            F_20, F_21, F_22.

        Raises:
            :class:`~superdex.physics.Error`: If an error occurs.

        Note:
            Requires registering :class:`ELEMENTS_DEFORMATION_GRADIENT
            <superdex.physics.QueryType>` before the simulation step. Results are
            available after the simulation step completes.

        Note:
            Requires the actor to support :class:`ELEMENTS_DEFORMATION_GRADIENT
            <superdex.physics.QueryType>`.

        See Also:
            :meth:`~superdex.physics.Actor.register_query`,
            :class:`ELEMENTS_DEFORMATION_GRADIENT <superdex.physics.QueryType>`
        """
    def set_node_positions_local(self, positions_local: ArrayLikeReal) -> None:
        """Set the current position of each node (reference + displacements) in the
        actor's
        local frame.

        Args:
            positions_local (ArrayLikeReal): Node positions [m] to set (3 values per
                node: x, y, z) in the actor's local frame.

        Raises:
            :class:`~superdex.physics.Error`: If an error occurs.

        Note:
            Only supported for standalone soft actors and shell actors.

        Note:
            Does not change node velocities.

        Note:
            Resets multi-step time integrators, e.g. BDF2 falls back to backward Euler
            in the first time step after this method is called.
        """
    def set_node_velocities_local(self, velocities_local: ArrayLikeReal) -> None:
        """Set the current velocity of each node in the actor's local frame.

        Args:
            velocities_local (ArrayLikeReal): Node velocities to set in the actor's
                local frame.

        Raises:
            :class:`~superdex.physics.Error`: If an error occurs.

        Note:
            Only supported for standalone soft actors, shell actors, and rod actors.

        Note:
            For standalone soft actors and shell actors: 3 values per node [m/s] — (vx,
            vy, vz).

        Note:
            For rod actors: 4 values per node — (vx, vy, vz) in [m/s] plus a twist rate
            [rad/s].

        Note:
            Resets multi-step time integrators, e.g. BDF2 falls back to backward Euler
            in the first time step after this method is called.
        """
    def get_visual_mesh_node_positions_local(self) -> SpanConstReal:
        """Get the current position (reference + displacements) of the nodes of the
        visual
        mesh in the actor's local frame.

        Returns:
            Current node positions [m] of the visual mesh in the actor's local frame (3
            values per node: x, y, z). The number of nodes is available via
            :meth:`~superdex.physics.Actor.get_visual_mesh`. The order of the nodes is
            1-to-1 with
            :meth:`~superdex.physics.Actor.get_visual_mesh_node_normals_local`.

        Raises:
            :class:`~superdex.physics.Error`: If an error occurs.

        Note:
            Includes visual mesh nodes not referenced by any visual triangle. Unlike
            surface mesh getters, visual mesh getters do not omit unreferenced nodes.

        Note:
            Requires registering :class:`VISUAL_NODE_POSITIONS
            <superdex.physics.QueryType>` before the simulation step. Results are
            available after the simulation step completes.

        Note:
            Requires the actor to support :class:`VISUAL_NODE_POSITIONS
            <superdex.physics.QueryType>`.

        See Also:
            :meth:`~superdex.physics.Actor.register_query`,
            :class:`VISUAL_NODE_POSITIONS <superdex.physics.QueryType>`,
            :meth:`~superdex.physics.Actor.get_visual_mesh`,
            :meth:`~superdex.physics.Actor.get_visual_mesh_node_normals_local`
        """
    def get_visual_mesh_node_normals_local(self) -> SpanConstReal:
        """Get the normal direction to each node of the visual mesh in the actor's local
        frame.

        Returns:
            Flattened normals (3 values per node: nx, ny, nz) in the actor's local
            frame. The number of nodes is available via
            :meth:`~superdex.physics.Actor.get_visual_mesh`. The order of the nodes is
            1-to-1 with
            :meth:`~superdex.physics.Actor.get_visual_mesh_node_positions_local`.
            Normals for visual nodes not referenced by any visual triangle are zero.

        Raises:
            :class:`~superdex.physics.Error`: If an error occurs.

        Note:
            Includes visual mesh nodes not referenced by any visual triangle. Unlike
            surface mesh getters, visual mesh getters do not omit unreferenced nodes.

        Note:
            Requires registering :class:`VISUAL_NODE_NORMALS
            <superdex.physics.QueryType>` before the simulation step. Results are
            available after the simulation step completes.

        Note:
            Requires the actor to support :class:`VISUAL_NODE_NORMALS
            <superdex.physics.QueryType>`.

        See Also:
            :meth:`~superdex.physics.Actor.register_query`, :class:`VISUAL_NODE_NORMALS
            <superdex.physics.QueryType>`,
            :meth:`~superdex.physics.Actor.get_visual_mesh`,
            :meth:`~superdex.physics.Actor.get_visual_mesh_node_positions_local`
        """
    def get_boundary_condition_dof_indices(self) -> SpanConstInt:
        """Get the DoF indices that are constrained by boundary conditions.

        Returns:
            Span of DoF indices with boundary conditions applied.

        Note:
            See :meth:`~superdex.physics.Actor.add_boundary_condition_dofs_world` for
            the DoF layout for each actor type.

        Note:
            The returned span contains every boundary-condition entry added through the
            public boundary-condition functions that has not been cleared, in the order
            the entries were added, including repeated DoF indices. Permanent and
            clearable entries are both included and are not distinguishable. Boundary
            conditions applied automatically by the engine are not included.

        Note:
            Adding boundary conditions or calling
            :meth:`~superdex.physics.Actor.clear_boundary_conditions` may invalidate the
            returned span. Do not retain it across either operation. Permanent entries
            are retained when :meth:`~superdex.physics.Actor.clear_boundary_conditions`
            is called, but their positions within subsequently returned spans may
            change. Do not cache those positions across a call to
            :meth:`~superdex.physics.Actor.clear_boundary_conditions`.

        See Also:
            :meth:`~superdex.physics.Actor.get_boundary_condition_dof_values_world`,
            :meth:`~superdex.physics.Actor.add_boundary_condition_dofs_world`
        """
    def get_boundary_condition_dof_values_world(self) -> SpanConstReal:
        """Get the target values of DoFs fixed by boundary conditions.

        Returns:
            Span of DoF target values in the actor-specific representations documented
            by :meth:`~superdex.physics.Actor.add_boundary_condition_dofs_world`.

        Note:
            1-to-1 correspondence with
            :meth:`~superdex.physics.Actor.get_boundary_condition_dof_indices`.

        Note:
            Adding boundary conditions or calling
            :meth:`~superdex.physics.Actor.clear_boundary_conditions` may invalidate the
            returned span. Do not retain it across either operation. Permanent entries
            are retained when :meth:`~superdex.physics.Actor.clear_boundary_conditions`
            is called, but their positions within subsequently returned spans may
            change. Do not cache those positions across a call to
            :meth:`~superdex.physics.Actor.clear_boundary_conditions`.

        See Also:
            :meth:`~superdex.physics.Actor.get_boundary_condition_dof_indices`,
            :meth:`~superdex.physics.Actor.add_boundary_condition_dofs_world`
        """
    def add_boundary_condition_dofs_world(
        self,
        dof_indices: ArrayLikeInt,
        dof_values_world: ArrayLikeReal,
    ) -> None:
        """Add boundary conditions to specified DoFs.

        Args:
            dof_indices (ArrayLikeInt): DoF indices to constrain.
            dof_values_world (ArrayLikeReal): Target values in the actor-specific
                representations described below. Length must equal ``dof_indices``.

        Raises:
            :class:`~superdex.physics.Error`: If an error occurs.

        Note:
            Not supported for nested soft actors (see
            :meth:`~superdex.physics.Actor.get_nested_soft_actors`).

        Note:
            Not supported in differentiable scenes
            (:func:`~superdex.physics.diffsim.make_scene_differentiable`) or for ROM
            actors.

        Note:
            For soft and shell actors, each node has 3 consecutive DoFs containing its
            target x, y, and z position [m] in the world frame. All 3 must be specified
            together. Consider
            :meth:`~superdex.physics.Actor.add_boundary_condition_nodes_world` as a more
            convenient alternative.

        Note:
            For rigid and articulated actors, the DoF indices within each call must be
            sorted in strictly increasing order (no duplicate indices).

        Note:
            Rigid actors have 6 DoFs: indices 0-2 are the target world-space
            center-of-mass position [m], and indices 3-5 are the target world-from-local
            orientation as a rotation vector (axis multiplied by angle [rad]). Either
            all or none of the 3 rotation DoFs must be specified.

        Note:
            Articulated actors have a number of DoFs that depends on the number of
            joints and the DoFs per joint. When setting boundary conditions on the
            rotation of a free or spherical joint, either all or none of the 3 DoFs must
            be specified.

        Note:
            Rod actors have 4 DoFs per node: the first 3 values are the target node x,
            y, and z position [m] in the world frame, followed by the next element's
            scalar twist angle [rad]. (For open rods, the last DoF, i.e., the final
            node's twist DoF, is included for padding only, does not affect the physics,
            and is automatically constrained to zero. This automatic constraint is not
            reported by
            :meth:`~superdex.physics.Actor.get_boundary_condition_dof_indices`. For
            closed-loop rods, all twist DoFs are physical.) Boundary conditions on a rod
            node's position must specify all 3 consecutive displacement DoFs for that
            node. Only zero values are allowed for the scalar twist angle in this
            function, with the interpretation that the element's cross-section material
            axes rotate by only the minimal amount needed to remain orthogonal to the
            rod's centerline. More elaborate control over rod element rotations should
            use constraints instead of boundary conditions.

        Note:
            Boundary condition APIs add to existing boundary conditions; they do not
            replace existing entries. If a DoF has more than one entry, getter APIs
            return all entries in the order they were added, and the most recently added
            value is the effective value during the step.

        Note:
            Boundary conditions can be cleared with
            :meth:`~superdex.physics.Actor.clear_boundary_conditions`.

        Warning:
            Constrained 3D rotations may exhibit small numerical differences due to
            differences in the internal representation.

        Warning:
            Adding a boundary condition to the same DoF more than once is legal — the
            most recently added value wins — but strongly discouraged. Each call adds an
            entry, so repeatedly re-adding without an intervening
            :meth:`~superdex.physics.Actor.clear_boundary_conditions` grows the per-step
            cost without bound. To update a boundary condition, call
            :meth:`~superdex.physics.Actor.clear_boundary_conditions` and add it again.

        See Also:
            :meth:`~superdex.physics.Actor.clear_boundary_conditions`,
            :meth:`~superdex.physics.Actor.add_boundary_condition_nodes_world`,
            :meth:`~superdex.physics.Actor.add_boundary_condition_dofs_world_permanent`,
            :meth:`~superdex.physics.Actor.get_boundary_condition_dof_indices`,
            :meth:`~superdex.physics.Actor.get_boundary_condition_dof_values_world`
        """
    def add_boundary_condition_dofs_world_permanent(
        self,
        dof_indices: ArrayLikeInt,
        dof_values_world: ArrayLikeReal,
    ) -> None:
        """Add permanent boundary conditions to specified DoFs.

        Args:
            dof_indices (ArrayLikeInt): DoF indices to constrain.
            dof_values_world (ArrayLikeReal): Target values in the actor-specific
                representations documented by
                :meth:`~superdex.physics.Actor.add_boundary_condition_dofs_world`.
                Length must equal ``dof_indices``.

        Raises:
            :class:`~superdex.physics.Error`: If an error occurs.

        Note:
            Not supported for nested soft actors (see
            :meth:`~superdex.physics.Actor.get_nested_soft_actors`).

        Note:
            Not supported in differentiable scenes
            (:func:`~superdex.physics.diffsim.make_scene_differentiable`) or for ROM
            actors.

        Note:
            Same as :meth:`~superdex.physics.Actor.add_boundary_condition_dofs_world`,
            except boundary conditions cannot be cleared.

        Note:
            If a clearable entry is later added for the same DoF, that clearable entry
            is the effective value until
            :meth:`~superdex.physics.Actor.clear_boundary_conditions` removes it. The
            permanent entry is then the effective value again.

        Warning:
            Permanent boundary conditions are retained for the lifetime of the actor and
            cannot be removed by
            :meth:`~superdex.physics.Actor.clear_boundary_conditions`. Adding a
            permanent boundary condition to the same DoF more than once is legal but
            strongly discouraged. The duplicates can never be cleared and irreversibly
            grow the per-step cost. Add each permanent boundary condition only once.

        See Also:
            :meth:`~superdex.physics.Actor.add_boundary_condition_dofs_world`
        """
    def add_boundary_condition_nodes_world(
        self,
        node_indices: ArrayLikeInt,
        node_positions_world: ArrayLikeReal,
    ) -> None:
        """Add world-space boundary conditions for specified nodes.

        Args:
            node_indices (ArrayLikeInt): Node indices to constrain.
            node_positions_world (ArrayLikeReal): Target positions [m] in world frame.
                Length must be 3 times the size of ``node_indices``.

        Raises:
            :class:`~superdex.physics.Error`: If an error occurs.

        Note:
            Only supported for standalone soft actors and shell actors.

        Note:
            Not supported in differentiable scenes
            (:func:`~superdex.physics.diffsim.make_scene_differentiable`) or for ROM
            actors.

        Note:
            Boundary conditions can be cleared with
            :meth:`~superdex.physics.Actor.clear_boundary_conditions`.

        Note:
            Equivalent to calling
            :meth:`~superdex.physics.Actor.add_boundary_condition_dofs_world` with DoF
            indices (3*i, 3*i+1, 3*i+2) for each node i.

        Note:
            This API adds to existing boundary conditions. See
            :meth:`~superdex.physics.Actor.add_boundary_condition_dofs_world` for
            behavior when a DoF has more than one entry.

        Warning:
            Adding boundary conditions to the same nodes repeatedly without clearing
            grows the per-step cost without bound. See
            :meth:`~superdex.physics.Actor.add_boundary_condition_dofs_world`.

        See Also:
            :meth:`~superdex.physics.Actor.clear_boundary_conditions`,
            :meth:`~superdex.physics.Actor.add_boundary_condition_dofs_world`,
            :meth:`~superdex.physics.Actor.add_boundary_condition_nodes_world_permanent`,
            :meth:`~superdex.physics.Actor.get_boundary_condition_dof_indices`,
            :meth:`~superdex.physics.Actor.get_boundary_condition_dof_values_world`
        """
    def add_boundary_condition_nodes_world_permanent(
        self,
        node_indices: ArrayLikeInt,
        node_positions_world: ArrayLikeReal,
    ) -> None:
        """Add permanent world-space boundary conditions for specified nodes.

        Args:
            node_indices (ArrayLikeInt): Node indices to constrain.
            node_positions_world (ArrayLikeReal): Target positions [m] in world frame.
                Length must be 3 times the size of ``node_indices``.

        Raises:
            :class:`~superdex.physics.Error`: If an error occurs.

        Note:
            Only supported for standalone soft actors and shell actors.

        Note:
            Not supported in differentiable scenes
            (:func:`~superdex.physics.diffsim.make_scene_differentiable`) or for ROM
            actors.

        Note:
            Same as :meth:`~superdex.physics.Actor.add_boundary_condition_nodes_world`,
            except boundary conditions cannot be cleared.

        Note:
            See
            :meth:`~superdex.physics.Actor.add_boundary_condition_dofs_world_permanent`
            for interaction with clearable entries later added to the same DoF.

        Warning:
            Add each permanent boundary condition only once. Duplicates can never be
            cleared and irreversibly grow the per-step cost. See
            :meth:`~superdex.physics.Actor.add_boundary_condition_dofs_world_permanent`.

        See Also:
            :meth:`~superdex.physics.Actor.add_boundary_condition_nodes_world`,
            :meth:`~superdex.physics.Actor.add_boundary_condition_dofs_world_permanent`
        """
    def add_boundary_condition_constrained_nodes_at_rest(self) -> None:
        """Fix the "constrained nodes" at their world-space reference positions.

        Raises:
            :class:`~superdex.physics.Error`: If an error occurs.

        Note:
            Only supported for standalone soft actors and shell actors.

        Note:
            Not supported in differentiable scenes
            (:func:`~superdex.physics.diffsim.make_scene_differentiable`) or for ROM
            actors.

        Note:
            Boundary conditions can be cleared with
            :meth:`~superdex.physics.Actor.clear_boundary_conditions`.

        Note:
            Applies to constrained nodes specified by the actor's mesh. Has no effect if
            none are specified.

        Note:
            Each target position is computed by transforming the node's mesh reference
            coordinate by the actor's root transform.

        Note:
            This API adds to existing boundary conditions. See
            :meth:`~superdex.physics.Actor.add_boundary_condition_dofs_world` for
            behavior when a DoF has more than one entry.

        Warning:
            If the actor's mesh has constrained nodes, calling this repeatedly without
            clearing re-adds the same boundary conditions and grows the per-step cost
            without bound. See
            :meth:`~superdex.physics.Actor.add_boundary_condition_dofs_world`.

        See Also:
            :meth:`~superdex.physics.Actor.clear_boundary_conditions`,
            :meth:`~superdex.physics.Actor.add_boundary_condition_constrained_nodes_at_rest_permanent`,
            :meth:`~superdex.physics.Actor.add_boundary_condition_dofs_world`
        """
    def add_boundary_condition_constrained_nodes_at_rest_permanent(self) -> None:
        """Permanently fix the "constrained nodes" at their world-space reference
        positions.

        Raises:
            :class:`~superdex.physics.Error`: If an error occurs.

        Note:
            Only supported for standalone soft actors and shell actors.

        Note:
            Not supported in differentiable scenes
            (:func:`~superdex.physics.diffsim.make_scene_differentiable`) or for ROM
            actors.

        Note:
            Same as
            :meth:`~superdex.physics.Actor.add_boundary_condition_constrained_nodes_at_rest`,
            except boundary conditions cannot be cleared.

        Note:
            See
            :meth:`~superdex.physics.Actor.add_boundary_condition_dofs_world_permanent`
            for interaction with clearable entries later added to the same DoF.

        Warning:
            If the actor's mesh has constrained nodes, each call adds a permanent
            boundary condition for every constrained node. Call this at most once.
            Repeated calls create duplicates that cannot be removed and irreversibly
            grow the per-step cost. See
            :meth:`~superdex.physics.Actor.add_boundary_condition_dofs_world_permanent`.

        See Also:
            :meth:`~superdex.physics.Actor.add_boundary_condition_constrained_nodes_at_rest`,
            :meth:`~superdex.physics.Actor.add_boundary_condition_dofs_world_permanent`
        """
    def clear_boundary_conditions(self) -> None:
        """Remove all clearable boundary conditions previously added.

        Note:
            To replace existing clearable boundary conditions, call this before adding
            the new boundary-condition set.

        Note:
            May invalidate spans previously returned by
            :meth:`~superdex.physics.Actor.get_boundary_condition_dof_indices` and
            :meth:`~superdex.physics.Actor.get_boundary_condition_dof_values_world`. Do
            not retain them across this call.

        Note:
            Permanent entries are retained, but this call may change their positions in
            spans returned by
            :meth:`~superdex.physics.Actor.get_boundary_condition_dof_indices` and
            :meth:`~superdex.physics.Actor.get_boundary_condition_dof_values_world`. Do
            not cache those positions across this call.

        See Also:
            :meth:`~superdex.physics.Actor.add_boundary_condition_dofs_world`,
            :meth:`~superdex.physics.Actor.add_boundary_condition_nodes_world`,
            :meth:`~superdex.physics.Actor.add_boundary_condition_constrained_nodes_at_rest`
        """
    def set_external_forces_on_dofs(
        self,
        dof_indices: ArrayLikeInt,
        force_values: ArrayLikeReal,
    ) -> None:
        """Set external forces on specified DoFs.

        Args:
            dof_indices (ArrayLikeInt): DoF indices to apply forces to. Must be unique.
            force_values (ArrayLikeReal): Force values [N] for translation DoFs, torque
                values [N·m] for rotation DoFs. Length must equal the number of DoF
                indices.

        Raises:
            :class:`~superdex.physics.Error`: If an error occurs.

        Note:
            Only supported for rigid, articulated, rod, and shell actors.

        Note:
            DoF indices use the same per-actor-type index layout documented by
            :meth:`~superdex.physics.Actor.add_boundary_condition_dofs_world`.

        Note:
            External forces can be cleared with
            :meth:`~superdex.physics.Actor.clear_external_forces`.

        Note:
            Replaces all previously set external forces, including external forces on
            DoFs not included in ``dof_indices``. Passing empty ``dof_indices`` and
            ``force_values`` is equivalent to calling
            :meth:`~superdex.physics.Actor.clear_external_forces`.

        Note:
            For rigid and shell actors, values are interpreted in the world frame.

        Note:
            For articulated actors, values are generalized forces in the actor's
            joint-DoF coordinate basis.

        Note:
            For rod actors, forces on displacement DoFs are specified in the world
            frame, while (generalized) forces (scalar torques) on element twist-angle
            DoFs are always oriented along the corresponding element's current
            orientation.

        Note:
            For open (non-closed-loop) rod actors, the last DoF is padding that does not
            participate in physics, so it is invalid to apply a torque to it. For
            closed-loop rods, the last DoF is a physical twist angle.

        See Also:
            :meth:`~superdex.physics.Actor.clear_external_forces`,
            :meth:`~superdex.physics.Actor.get_external_forces`,
            :meth:`~superdex.physics.Actor.add_boundary_condition_dofs_world`
        """
    def clear_external_forces(self) -> None:
        """Remove all the external forces previously added.

        See Also:
            :meth:`~superdex.physics.Actor.set_external_forces_on_dofs`
        """
    def get_external_forces(self, out_forces: ArrayLikeReal) -> None:
        """Get the external forces currently set on this actor as a dense array over all
        DoFs.

        Args:
            out_forces (ArrayLikeReal): Dense output array of force values [N] for
                translation DoFs and torque values [N·m] for rotation DoFs. One entry
                per DoF. Size must equal :meth:`~superdex.physics.Actor.get_num_dofs`.
                DoFs with no external force set are filled with zero.

        Raises:
            :class:`~superdex.physics.Error`: If an error occurs.

        Note:
            Returned values use the same frame convention as
            :meth:`~superdex.physics.Actor.set_external_forces_on_dofs`.

        See Also:
            :meth:`~superdex.physics.Actor.set_external_forces_on_dofs`,
            :meth:`~superdex.physics.Actor.clear_external_forces`
        """
    def get_contact_points_world(self) -> SpanConstContactPoint:
        """Get all the active contact points of the actor.

        Returns:
            Span of active contact points.

        Raises:
            :class:`~superdex.physics.Error`: If an error occurs.

        Note:
            Requires registering :class:`CONTACT_POINTS <superdex.physics.QueryType>`
            before the simulation step. Results are available after the simulation step
            completes.

        Note:
            Requires the actor to support :class:`CONTACT_POINTS
            <superdex.physics.QueryType>`.

        Note:
            If the solver diverged in the last simulation step, results may not be
            available or may be stale.

        See Also:
            :meth:`~superdex.physics.Actor.register_query`,
            :class:`~superdex.physics.ContactPoint`, :class:`CONTACT_POINTS
            <superdex.physics.QueryType>`
        """
    def get_node_contact_forces_world(self) -> SpanConstNodeContactForce:
        """Get contact forces on nodes.

        Returns:
            Span of per-node contact forces [N] in world frame.

        Raises:
            :class:`~superdex.physics.Error`: If an error occurs.

        Note:
            Requires registering :class:`NODE_CONTACT_FORCES
            <superdex.physics.QueryType>` before the simulation step. Results are
            available after the simulation step completes.

        Note:
            Requires the actor to support :class:`NODE_CONTACT_FORCES
            <superdex.physics.QueryType>`.

        Note:
            If the solver diverged in the last simulation step, results may not be
            available or may be stale.

        See Also:
            :meth:`~superdex.physics.Actor.register_query`,
            :class:`~superdex.physics.NodeContactForce`, :class:`NODE_CONTACT_FORCES
            <superdex.physics.QueryType>`
        """
    def get_contact_force_world(self) -> Real3:
        """Get the total contact force on the actor.

        Returns:
            Total contact force [N] in world frame.

        Raises:
            :class:`~superdex.physics.Error`: If an error occurs.

        Note:
            Requires registering :class:`TOTAL_CONTACT_FORCE
            <superdex.physics.QueryType>` before the simulation step. Results are
            available after the simulation step completes.

        Note:
            Requires the actor to support :class:`TOTAL_CONTACT_FORCE
            <superdex.physics.QueryType>`.

        Note:
            If the solver diverged in the last simulation step, results may not be
            available or may be stale.

        See Also:
            :meth:`~superdex.physics.Actor.register_query`,
            :meth:`~superdex.physics.Actor.get_contact_torque_world`,
            :meth:`~superdex.physics.Actor.get_contact_force_from_actor_world`,
            :class:`TOTAL_CONTACT_FORCE <superdex.physics.QueryType>`
        """
    def get_contact_torque_world(self) -> Real3:
        """Get the total contact torque about the center of mass.

        Returns:
            Total contact torque [N·m] in world frame, about the center of mass.

        Raises:
            :class:`~superdex.physics.Error`: If an error occurs.

        Note:
            Requires registering :class:`TOTAL_CONTACT_FORCE
            <superdex.physics.QueryType>` before the simulation step. Results are
            available after the simulation step completes.

        Note:
            Only supported for rigid actors and articulated links that support
            :class:`TOTAL_CONTACT_FORCE <superdex.physics.QueryType>`.

        Note:
            If the solver diverged in the last simulation step, results may not be
            available or may be stale.

        See Also:
            :meth:`~superdex.physics.Actor.register_query`,
            :meth:`~superdex.physics.Actor.get_contact_force_world`,
            :class:`TOTAL_CONTACT_FORCE <superdex.physics.QueryType>`
        """
    def get_contact_force_from_actor_world(self, other: Actor) -> Real3:
        """Get the total contact force on the actor from another specified actor.

        Args:
            other (Actor): Actor exerting the contact force on this actor.

        Returns:
            Total contact force [N] in world frame from the specified actor.

        Raises:
            :class:`~superdex.physics.Error`: If an error occurs.

        Note:
            Requires registering :class:`TOTAL_CONTACT_FORCE
            <superdex.physics.QueryType>` before the simulation step. Results are
            available after the simulation step completes.

        Note:
            Requires the actor to support :class:`TOTAL_CONTACT_FORCE
            <superdex.physics.QueryType>`.

        Note:
            If the solver diverged in the last simulation step, results may not be
            available or may be stale.

        See Also:
            :meth:`~superdex.physics.Actor.register_query`,
            :meth:`~superdex.physics.Actor.get_contact_force_world`,
            :class:`TOTAL_CONTACT_FORCE <superdex.physics.QueryType>`
        """
    def get_sdf_distances(self) -> SdfDistances:
        """Get SDF distances for contact sample points.

        Returns:
            SDF distance information for contact sample points.

        Raises:
            :class:`~superdex.physics.Error`: If an error occurs.

        Note:
            Requires registering :class:`SDF_DISTANCES <superdex.physics.QueryType>`
            before the simulation step. Results are available after the simulation step
            completes.

        Note:
            Requires the actor to support :class:`SDF_DISTANCES
            <superdex.physics.QueryType>`.

        Note:
            If the solver diverged in the last simulation step, results may not be
            available or may be stale.

        See Also:
            :meth:`~superdex.physics.Actor.register_query`,
            :class:`~superdex.physics.SdfDistances`, :class:`SDF_DISTANCES
            <superdex.physics.QueryType>`
        """
    def register_query(self, type: QueryType | int) -> QueryHandle:
        """Register a query to compute data for this actor.

        Args:
            type (QueryType | int): Type of query to register.

        Returns:
            :class:`~superdex.physics.QueryHandle` for the registered query, or an
            invalid handle if registration fails.

        Raises:
            :class:`~superdex.physics.Error`: If an error occurs.

        Note:
            Queries may add substantial computational overhead. Use only when necessary.

        Note:
            Data is available after the next simulation step, even if its time step is
            zero.

        Note:
            Call :meth:`~superdex.physics.Actor.cancel_query` when the query is not
            needed again in the future.

        Note:
            Queries are reference counted internally, so they will be available as long
            as there is one outstanding :class:`~superdex.physics.QueryHandle`.

        Note:
            Registration is all-or-nothing: if registration fails, no query is
            registered.

        See Also:
            :meth:`~superdex.physics.Actor.cancel_query`,
            :meth:`~superdex.physics.Actor.register_query_and_compute`,
            :meth:`~superdex.physics.Actor.is_query_supported`,
            :meth:`~superdex.physics.Constraint.register_query`
        """
    def register_query_and_compute(self, type: QueryType | int) -> QueryHandle:
        """Register a query for this actor and compute the result immediately.

        Args:
            type (QueryType | int): Type of query to register.

        Returns:
            :class:`~superdex.physics.QueryHandle` for the registered query.

        Raises:
            :class:`~superdex.physics.Error`: If an error occurs.

        Note:
            Same as :meth:`~superdex.physics.Actor.register_query`, except that the data
            is computed immediately (blocking) rather than at the next simulation step.

        Note:
            Immediate computation is not supported for all query types. For unsupported
            types, the query is still registered successfully, immediate computation is
            skipped, and data becomes available after the next simulation step. See
            :class:`~superdex.physics.QueryType` documentation for which types support
            immediate computation.

        Note:
            Stepping the scene with zero time step is another way to force queries to
            update, without advancing the state of the simulation.

        See Also:
            :meth:`~superdex.physics.Actor.register_query`,
            :meth:`~superdex.physics.Actor.cancel_query`
        """
    def cancel_query(self, handle: QueryHandle) -> None:
        """Cancel a previously registered query.

        Args:
            handle (QueryHandle): :class:`~superdex.physics.QueryHandle` from
                :meth:`~superdex.physics.Actor.register_query` or
                :meth:`~superdex.physics.Actor.register_query_and_compute`.

        Note:
            Cancelling an invalid or already-cancelled handle has no effect.

        Note:
            Cancelling queries may improve memory usage and/or performance if the query
            is no longer needed.

        See Also:
            :meth:`~superdex.physics.Actor.register_query`,
            :meth:`~superdex.physics.Actor.register_query_and_compute`
        """
    def is_query_supported(self, type: QueryType | int) -> bool:
        """Check whether a query type is supported for this actor.

        Args:
            type (QueryType | int): Type of query to check.

        Returns:
            True if :meth:`~superdex.physics.Actor.register_query` would succeed for
            this query type, false otherwise.

        See Also:
            :meth:`~superdex.physics.Actor.register_query`
        """
    def get_nested_link_actors(self) -> SpanConstActorHandle:
        """Get the actor handles for the nested rigid link actors.

        Returns:
            Span of actor handles for the nested rigid link actors.

        Raises:
            :class:`~superdex.physics.Error`: If an error occurs.

        Note:
            Only applicable to articulated actors.

        See Also:
            :meth:`~superdex.physics.Actor.get_nested_soft_actors`,
            :meth:`~superdex.physics.Actor.is_nested_link_actor`,
            :meth:`~superdex.physics.Actor.get_articulated_actor`
        """
    def get_nested_soft_actors(self) -> SpanConstActorHandle:
        """Get the actor handles for the nested soft actors (if any).

        Returns:
            Span of actor handles for the nested soft actors, or empty if there are
            none.

        Raises:
            :class:`~superdex.physics.Error`: If an error occurs.

        Note:
            Only applicable to articulated actors.

        See Also:
            :meth:`~superdex.physics.Actor.get_nested_link_actors`,
            :meth:`~superdex.physics.Actor.is_nested_soft_actor`,
            :meth:`~superdex.physics.Actor.get_articulated_actor`
        """
    def get_articulated_shape_info(self) -> ArticulatedShapeInfo:
        """Get articulated shape information about the articulated actor.

        Returns:
            Articulated shape information.

        Raises:
            :class:`~superdex.physics.Error`: If an error occurs.

        Note:
            Only applicable to articulated actors. Sets error otherwise.

        Note:
            This information never changes throughout the actor's lifetime.

        See Also:
            :class:`~superdex.physics.ArticulatedShapeInfo`,
            :func:`~superdex.physics.get_articulated_shape_info`
        """
    def get_articulated_joint_limit_constraints(self) -> SpanConstraintConst:
        """Get the joint-limit constraints of the articulated actor.

        Returns:
            Span of pointers to joint limit constraints.

        Raises:
            :class:`~superdex.physics.Error`: If an error occurs.

        Note:
            Only applicable to articulated actors.

        Note:
            These constraints are owned by the articulated actor and cannot be destroyed
            directly with :meth:`~superdex.physics.Scene.destroy_constraint`. They are
            destroyed with the actor.

        See Also:
            :meth:`~superdex.physics.Actor.get_articulated_dof_limits`
        """
    def get_articulated_joint_friction_params(
        self,
    ) -> SpanConstArticulatedJointFrictionParams:
        """Get the per-joint friction parameters of the articulated actor.

        Returns:
            Span of friction parameters, one per joint.

        Raises:
            :class:`~superdex.physics.Error`: If an error occurs.

        Note:
            Only applicable to articulated actors.

        See Also:
            :meth:`~superdex.physics.Actor.set_articulated_joint_friction_params`,
            :class:`~superdex.physics.ArticulatedJointFrictionParams`
        """
    def set_articulated_joint_friction_params(
        self,
        friction: ArrayLikeArticulatedJointFrictionParams,
    ) -> None:
        """Set the per-joint friction parameters of the articulated actor.

        Args:
            friction (ArrayLikeArticulatedJointFrictionParams): Friction parameters, one
                per joint. Must be of size equal to the number of joints (matching
                :meth:`~superdex.physics.Actor.get_articulated_joint_friction_params`);
                all values must be finite and non-negative.

        Raises:
            :class:`~superdex.physics.Error`: If an error occurs.

        Note:
            Only applicable to articulated actors.

        See Also:
            :meth:`~superdex.physics.Actor.get_articulated_joint_friction_params`,
            :class:`~superdex.physics.ArticulatedJointFrictionParams`
        """
    def get_articulated_joint_inertia_params(self) -> SpanConstReal:
        """Get the per-joint inertia coefficients of the articulated actor.

        Returns:
            Span of inertia coefficients, one per joint.

        Raises:
            :class:`~superdex.physics.Error`: If an error occurs.

        Note:
            Only applicable to articulated actors.

        See Also:
            :meth:`~superdex.physics.Actor.set_articulated_joint_inertia_params`
        """
    def set_articulated_joint_inertia_params(self, inertia: ArrayLikeReal) -> None:
        """Set the per-joint inertia coefficients of the articulated actor.

        Args:
            inertia (ArrayLikeReal): Inertia coefficients, one per joint. Must be of
                size equal to the number of joints (matching
                :meth:`~superdex.physics.Actor.get_articulated_joint_inertia_params`);
                all values must be finite and non-negative.

        Raises:
            :class:`~superdex.physics.Error`: If an error occurs.

        Note:
            Only applicable to articulated actors.

        See Also:
            :meth:`~superdex.physics.Actor.get_articulated_joint_inertia_params`
        """
    def get_articulated_dof_limits(self, out_dof_limits: ArrayLikeReal2) -> None:
        """Get the limit values on all DoFs of the articulated actor.

        Args:
            out_dof_limits (ArrayLikeReal2): Output span for DoF limits. Must be of size
                :meth:`~superdex.physics.Actor.get_num_dofs`.

        Raises:
            :class:`~superdex.physics.Error`: If an error occurs.

        Note:
            Only applicable to articulated actors.

        Note:
            Each element contains [min, max] limits for the corresponding DoF: [m] for
            translational DoFs and [rad] for rotational DoFs.

        Note:
            Unconstrained DoFs have limits of ±infinity.

        See Also:
            :meth:`~superdex.physics.Actor.get_articulated_joint_limit_constraints`
        """
    def get_articulated_pose(self, out_pose: ArrayLikeReal) -> None:
        """Get the pose of an articulated actor expressed as joint DoFs.

        Args:
            out_pose (ArrayLikeReal): Output span for pose as joint DoFs: position [m]
                for translational DoFs and angle [rad] for rotational DoFs. Must be of
                size :meth:`~superdex.physics.Actor.get_num_dofs`.

        Raises:
            :class:`~superdex.physics.Error`: If an error occurs.

        Note:
            Only applicable to articulated actors.

        See Also:
            :meth:`~superdex.physics.Actor.set_articulated_pose_from_joints`
        """
    def get_articulated_link_transforms(
        self,
        out_world_from_links: ArrayLikeTransformRT,
    ) -> None:
        """Get the pose of an articulated actor expressed as world-from-local link
        transforms.

        Args:
            out_world_from_links (ArrayLikeTransformRT): Output span for pose as
                world-from-local links transforms. Must be of size equal to the number
                of links.

        Raises:
            :class:`~superdex.physics.Error`: If an error occurs.

        Note:
            Only applicable to articulated actors.

        See Also:
            :meth:`~superdex.physics.Actor.set_articulated_pose_from_links`,
            :meth:`~superdex.physics.Actor.get_articulated_pose`
        """
    def get_articulated_joint_velocities(self, out_velocities: ArrayLikeReal) -> None:
        """Get time derivatives of the articulated actor DoFs.

        Args:
            out_velocities (ArrayLikeReal): Output span for DoF velocities: [m/s] for
                translational DoFs and [rad/s] for rotational DoFs. Must be of size
                :meth:`~superdex.physics.Actor.get_num_dofs`.

        Raises:
            :class:`~superdex.physics.Error`: If an error occurs.

        Note:
            Only applicable to articulated actors.

        See Also:
            :meth:`~superdex.physics.Actor.set_articulated_joint_velocities`
        """
    def set_articulated_pose_from_links(
        self,
        world_from_links: ArrayLikeTransformRT,
    ) -> None:
        """Set the pose of an articulated actor from the world-from-local link
        transforms.

        Args:
            world_from_links (ArrayLikeTransformRT): World-from-local link transforms.
                Must be of size equal to the number of links.

        Raises:
            :class:`~superdex.physics.Error`: If an error occurs.

        Note:
            Only applicable to articulated actors.

        Note:
            Transforms are automatically projected onto valid joint space.

        Note:
            If the actor has a pose controller, its target pose is reset to the provided
            transforms and its target velocity is set to zero. Call
            :meth:`~superdex.physics.Actor.set_articulated_target_velocity` afterward to
            use a non-zero target velocity.

        Note:
            Does not affect the actor's velocities.

        Note:
            Resets multi-step time integrators, e.g. BDF2 falls back to backward Euler
            in the first time step after this method is called.

        Warning:
            Round-trip error:
            :meth:`~superdex.physics.Actor.set_articulated_pose_from_links` followed by
            :meth:`~superdex.physics.Actor.get_articulated_link_transforms` may not
            return identical values due to internal representation differences.

        See Also:
            :meth:`~superdex.physics.Actor.get_articulated_link_transforms`,
            :meth:`~superdex.physics.Actor.set_articulated_pose_from_joints`
        """
    def set_articulated_pose_from_joints(self, pose: ArrayLikeReal) -> None:
        """Set the pose of an articulated actor from joint DoFs.

        Args:
            pose (ArrayLikeReal): Joint DoF values to set. Must be of size
                :meth:`~superdex.physics.Actor.get_num_dofs`.

        Raises:
            :class:`~superdex.physics.Error`: If an error occurs.

        Note:
            Only applicable to articulated actors.

        Note:
            If the actor has a pose controller, its target pose is reset to the provided
            pose and its target velocity is set to zero. Call
            :meth:`~superdex.physics.Actor.set_articulated_target_velocity` afterward to
            use a non-zero target velocity.

        Note:
            Does not affect the actor's velocities.

        Note:
            Resets multi-step time integrators, e.g. BDF2 falls back to backward Euler
            in the first time step after this method is called.

        Warning:
            Round-trip error:
            :meth:`~superdex.physics.Actor.set_articulated_pose_from_joints` followed by
            :meth:`~superdex.physics.Actor.get_articulated_pose` may not return
            identical values due to internal representation differences.

        See Also:
            :meth:`~superdex.physics.Actor.get_articulated_pose`,
            :meth:`~superdex.physics.Actor.set_articulated_pose_from_links`
        """
    def set_articulated_joint_velocities(self, velocities: ArrayLikeReal) -> None:
        """Set time derivatives of the articulated actor DoFs.

        Args:
            velocities (ArrayLikeReal): Joint DoF velocities to set. Must be of size
                :meth:`~superdex.physics.Actor.get_num_dofs`.

        Raises:
            :class:`~superdex.physics.Error`: If an error occurs.

        Note:
            Only applicable to articulated actors.

        Note:
            If the actor has a pose controller, target velocity is also set to the
            provided velocities.

        Note:
            Resets multi-step time integrators, e.g. BDF2 falls back to backward Euler
            in the first time step after this method is called.

        See Also:
            :meth:`~superdex.physics.Actor.get_articulated_joint_velocities`
        """
    def add_articulated_delta_to_pose(
        self,
        pose: ArrayLikeReal,
        delta_dofs: ArrayLikeReal,
        out_pose: ArrayLikeReal,
    ) -> None:
        """Add a pose delta to a pose using joint-aware composition.

        Args:
            pose (ArrayLikeReal): Base pose as joint DoFs. Must be of size
                :meth:`~superdex.physics.Actor.get_num_dofs`.
            delta_dofs (ArrayLikeReal): Pose delta to add. Must be of size
                :meth:`~superdex.physics.Actor.get_num_dofs`.
            out_pose (ArrayLikeReal): Output pose after adding deltaDofs. Must be of
                size :meth:`~superdex.physics.Actor.get_num_dofs`.

        Raises:
            :class:`~superdex.physics.Error`: If an error occurs.

        Note:
            Only applicable to articulated actors.

        Note:
            Deltas cannot be simply added to poses. This function uses joint information
            to correctly add the delta.

        Note:
            Useful for adding time-stepped velocity to a reference pose.

        Note:
            For 3D rotations, delta values are interpreted as Lie-algebra
            rotation-vector increments.

        Note:
            For 3D rotations, angular velocity is not directly the time derivative of
            the rotation vector representation.

        See Also:
            :meth:`~superdex.physics.Actor.compute_articulated_pose_delta`,
            :meth:`~superdex.physics.Actor.get_articulated_pose`,
            :meth:`~superdex.physics.Actor.get_articulated_pose_distance`
        """
    def compute_articulated_pose_delta(
        self,
        pose_base: ArrayLikeReal,
        pose_target: ArrayLikeReal,
        out_delta_dofs: ArrayLikeReal,
    ) -> None:
        """Compute the pose delta from a base pose to a target pose.

        Args:
            pose_base (ArrayLikeReal): Base pose as joint DoFs. Must be of size
                :meth:`~superdex.physics.Actor.get_num_dofs`.
            pose_target (ArrayLikeReal): Target pose as joint DoFs. Must be of size
                :meth:`~superdex.physics.Actor.get_num_dofs`.
            out_delta_dofs (ArrayLikeReal): Pose delta. Must be of size
                :meth:`~superdex.physics.Actor.get_num_dofs`.

        Raises:
            :class:`~superdex.physics.Error`: If an error occurs.

        Note:
            Only applicable to articulated actors.

        Note:
            Deltas cannot be simply subtracted between poses. This function uses joint
            information to correctly subtract the delta.

        See Also:
            :meth:`~superdex.physics.Actor.add_articulated_delta_to_pose`,
            :meth:`~superdex.physics.Actor.get_articulated_pose`,
            :meth:`~superdex.physics.Actor.get_articulated_pose_distance`
        """
    def get_articulated_pose_distance(
        self,
        pose_a: ArrayLikeReal,
        pose_b: ArrayLikeReal,
        out_trans_distances: ArrayLikeReal,
        out_rot_distances: ArrayLikeReal,
    ) -> None:
        """Compute per-joint distances between two poses.

        Args:
            pose_a (ArrayLikeReal): First pose as joint DoFs. Must be of size
                :meth:`~superdex.physics.Actor.get_num_dofs`.
            pose_b (ArrayLikeReal): Second pose as joint DoFs. Must be of size
                :meth:`~superdex.physics.Actor.get_num_dofs`.
            out_trans_distances (ArrayLikeReal): Translation distances [m] for each
                joint. Must be of size equal to the number of joints.
            out_rot_distances (ArrayLikeReal): Rotation distances [rad] for each joint.
                Must be of size equal to the number of joints.

        Raises:
            :class:`~superdex.physics.Error`: If an error occurs.

        Note:
            Only applicable to articulated actors.

        Note:
            Uses joint-specific distance metrics (Euclidean for translation, angle for
            rotation).

        See Also:
            :meth:`~superdex.physics.Actor.get_articulated_pose`,
            :meth:`~superdex.physics.Actor.add_articulated_delta_to_pose`,
            :meth:`~superdex.physics.Actor.compute_articulated_pose_delta`
        """
    def has_articulated_pose_controller(self) -> bool:
        """Check if the articulated actor has a pose controller.

        Returns:
            True if a pose controller exists, false otherwise.

        Raises:
            :class:`~superdex.physics.Error`: If an error occurs.

        Note:
            Only applicable to articulated actors.

        See Also:
            :meth:`~superdex.physics.Actor.add_articulated_pose_controller`,
            :meth:`~superdex.physics.Actor.remove_articulated_pose_controller`
        """
    def add_articulated_pose_controller(self, params: PoseControllerParams) -> None:
        """Add a pose controller to an articulated actor.

        Args:
            params (PoseControllerParams): Parameters defining the pose controller.

        Raises:
            :class:`~superdex.physics.Error`: If an error occurs.

        Note:
            Only applicable to articulated actors.

        Note:
            Only one pose controller is allowed per actor. Adding a second controller
            raises an error.

        Note:
            The initial target pose and target velocity are set to the actor's current
            pose and velocity.

        Note:
            Empty tracking arrays are allowed. Default zero-gain parameters are used for
            any empty array.

        See Also:
            :meth:`~superdex.physics.Actor.remove_articulated_pose_controller`,
            :meth:`~superdex.physics.Actor.has_articulated_pose_controller`
        """
    def remove_articulated_pose_controller(self) -> None:
        """Remove the pose controller.

        Raises:
            :class:`~superdex.physics.Error`: If an error occurs.

        Note:
            Only applicable to articulated actors.

        Note:
            Returns an error if no pose controller exists.

        See Also:
            :meth:`~superdex.physics.Actor.add_articulated_pose_controller`,
            :meth:`~superdex.physics.Actor.has_articulated_pose_controller`
        """
    def set_articulated_target_link_transforms(
        self,
        world_from_targets: ArrayLikeTransformRT,
    ) -> None:
        """Set the target pose for the controller expressed as link transforms.

        Args:
            world_from_targets (ArrayLikeTransformRT): Target world-from-link transforms
                to set. Must be of size equal to the number of links.

        Raises:
            :class:`~superdex.physics.Error`: If an error occurs.

        Note:
            Only supported for articulated actors with a pose controller.

        Note:
            Also affects target velocity, which depends on how the target changes
            between simulation steps.

        See Also:
            :meth:`~superdex.physics.Actor.reset_articulated_target_link_transforms`,
            :meth:`~superdex.physics.Actor.set_articulated_target_pose`
        """
    def set_articulated_target_pose(self, pose: ArrayLikeReal) -> None:
        """Set the target pose for the controller expressed as joint DoFs.

        Args:
            pose (ArrayLikeReal): Target pose as joint DoFs to set. Must be of size
                :meth:`~superdex.physics.Actor.get_num_dofs`.

        Raises:
            :class:`~superdex.physics.Error`: If an error occurs.

        Note:
            Only applicable to articulated actors with a pose controller.

        Note:
            Also affects target velocity, which depends on how the target changes
            between simulation steps.

        See Also:
            :meth:`~superdex.physics.Actor.reset_articulated_target_pose`,
            :meth:`~superdex.physics.Actor.set_articulated_target_link_transforms`,
            :meth:`~superdex.physics.Actor.get_articulated_target_pose`,
            :meth:`~superdex.physics.Actor.get_articulated_target_link_transforms`,
            :meth:`~superdex.physics.Actor.set_articulated_target_velocity`
        """
    def reset_articulated_target_link_transforms(
        self,
        world_from_targets: ArrayLikeTransformRT,
    ) -> None:
        """Reset the target pose of the controller expressed as link transforms.

        Args:
            world_from_targets (ArrayLikeTransformRT): Target world-from-link transforms
                to reset to. Must be finite and of size equal to the number of links.

        Raises:
            :class:`~superdex.physics.Error`: If an error occurs.

        Note:
            Only applicable to articulated actors with a pose controller.

        Note:
            Sets the target to the provided transforms and the target velocity to zero,
            so no velocity kick is introduced.

        Note:
            To use a non-zero target velocity, call
            :meth:`~superdex.physics.Actor.set_articulated_target_velocity` after this
            function.

        See Also:
            :meth:`~superdex.physics.Actor.set_articulated_target_link_transforms`,
            :meth:`~superdex.physics.Actor.reset_articulated_target_pose`
        """
    def reset_articulated_target_pose(self, pose: ArrayLikeReal) -> None:
        """Reset the target pose of the controller expressed as joint DoFs.

        Args:
            pose (ArrayLikeReal): Target pose as joint DoFs to reset to. Must be finite
                and of size :meth:`~superdex.physics.Actor.get_num_dofs`.

        Raises:
            :class:`~superdex.physics.Error`: If an error occurs.

        Note:
            Only applicable to articulated actors with a pose controller.

        Note:
            Sets the target to the provided pose and the target velocity to zero, so no
            velocity kick is introduced.

        Note:
            To use a non-zero target velocity, call
            :meth:`~superdex.physics.Actor.set_articulated_target_velocity` after this
            function.

        See Also:
            :meth:`~superdex.physics.Actor.set_articulated_target_pose`,
            :meth:`~superdex.physics.Actor.reset_articulated_target_link_transforms`
        """
    def set_articulated_target_velocity(self, velocity: ArrayLikeReal) -> None:
        """Set the target velocity of the controller expressed as joint velocities for
        the
        next simulation step.

        Args:
            velocity (ArrayLikeReal): Target DoF velocities to set. [m/s] for
                translational DoFs and [rad/s] for rotational DoFs. Must be finite and
                of size :meth:`~superdex.physics.Actor.get_num_dofs`.

        Raises:
            :class:`~superdex.physics.Error`: If an error occurs.

        Note:
            Only applicable to articulated actors with a pose controller.

        Note:
            Does not change target pose.

        Note:
            The velocity override is applied once at the start of the next simulation
            step to compute damping, then cleared automatically. To maintain a constant
            target velocity, call this function before each step.

        Note:
            :meth:`~superdex.physics.Actor.reset_articulated_target_pose` and
            :meth:`~superdex.physics.Actor.reset_articulated_target_link_transforms`
            overwrite any pending target velocity with zero. Call this function after
            reset to use a non-zero target velocity.

        See Also:
            :meth:`~superdex.physics.Actor.set_articulated_target_pose`,
            :meth:`~superdex.physics.Actor.reset_articulated_target_pose`
        """
    def get_articulated_pose_constraints(self) -> SpanConstPoseConstraintInfo:
        """Get the pose controller's constraints.

        Returns:
            Span of pose constraint information.

        Raises:
            :class:`~superdex.physics.Error`: If an error occurs.

        Note:
            Only applicable to articulated actors with a pose controller.

        Note:
            These constraints are owned by the pose controller and cannot be destroyed
            individually with :meth:`~superdex.physics.Scene.destroy_constraint`.
            Removing the pose controller with
            :meth:`~superdex.physics.Actor.remove_articulated_pose_controller` destroys
            all of its constraints.

        See Also:
            :class:`~superdex.physics.PoseConstraintInfo`,
            :meth:`~superdex.physics.Actor.get_articulated_pose_controller_params`,
            :meth:`~superdex.physics.Actor.set_articulated_pose_controller_params`
        """
    def get_articulated_pose_controller_params(
        self,
        out_params: PoseControllerParams,
    ) -> None:
        """Get the pose controller's tracking parameters.

        Args:
            out_params (PoseControllerParams): Output tracking parameters grouped by
                constraint type
                (:attr:`~superdex.physics.PoseControllerParams.link_pos_tracking`,
                :attr:`~superdex.physics.PoseControllerParams.link_rot_tracking`,
                :attr:`~superdex.physics.PoseControllerParams.joint_tracking`). All
                three arrays are link-indexed and must be pre-sized to numLinks; they
                are not resized. Entries in
                :attr:`~superdex.physics.PoseControllerParams.joint_tracking` for links
                without a controllable joint are reported as zero gains.

        Raises:
            :class:`~superdex.physics.Error`: If an error occurs.

        Note:
            Only applicable to articulated actors with a pose controller.

        See Also:
            :meth:`~superdex.physics.Actor.get_articulated_pose_constraints`,
            :meth:`~superdex.physics.Actor.set_articulated_pose_controller_params`
        """
    def set_articulated_pose_controller_params(
        self,
        params: PoseControllerParams,
    ) -> None:
        """Set the pose controller's tracking parameters.

        Args:
            params (PoseControllerParams): Tracking parameters grouped by constraint
                type. Each array must be empty, size 1 (broadcast to all links), or size
                numLinks (per-link). An empty array will broadcast using a
                default-constructed PoseTrackingParams (zero gains) to all links.

        Raises:
            :class:`~superdex.physics.Error`: If an error occurs.

        Note:
            Only applicable to articulated actors with a pose controller.

        Note:
            Setting zero stiffness and damping disables the constraint but does not
            destroy it.

        See Also:
            :meth:`~superdex.physics.Actor.get_articulated_pose_controller_params`,
            :meth:`~superdex.physics.Actor.get_articulated_pose_constraints`
        """
    def get_articulated_target_pose(self, out_pose: ArrayLikeReal) -> None:
        """Get the last target pose set to the pose controller.

        Args:
            out_pose (ArrayLikeReal): Output span for the target pose as joint DoFs.
                Must be of size :meth:`~superdex.physics.Actor.get_num_dofs`.

        Raises:
            :class:`~superdex.physics.Error`: If an error occurs.

        Note:
            Only applicable to articulated actors with a pose controller.

        See Also:
            :meth:`~superdex.physics.Actor.set_articulated_target_pose`,
            :meth:`~superdex.physics.Actor.set_articulated_target_link_transforms`,
            :meth:`~superdex.physics.Actor.get_articulated_target_link_transforms`
        """
    def get_articulated_target_link_transforms(
        self,
        out_world_from_targets: ArrayLikeTransformRT,
    ) -> None:
        """Get the last target pose set to the pose controller, expressed as link
        transforms.

        Args:
            out_world_from_targets (ArrayLikeTransformRT): Output span for the target
                world-from-link transforms. Must be of size equal to the number of
                links.

        Raises:
            :class:`~superdex.physics.Error`: If an error occurs.

        Note:
            Only applicable to articulated actors with a pose controller.

        See Also:
            :meth:`~superdex.physics.Actor.set_articulated_target_pose`,
            :meth:`~superdex.physics.Actor.set_articulated_target_link_transforms`,
            :meth:`~superdex.physics.Actor.get_articulated_target_pose`
        """
    def get_articulated_controller_force(self) -> SpanConstReal:
        """Get the generalized forces applied by the pose controller on each joint DoF.

        Returns:
            Flattened generalized-force vector for each joint DoF. Size equals
            :meth:`~superdex.physics.Actor.get_num_dofs`. Units are [N] for
            translational DoFs and [N·m] for rotational DoFs.

        Raises:
            :class:`~superdex.physics.Error`: If an error occurs.

        Note:
            Only applicable to articulated actors with a pose controller.

        Note:
            Requires registering :class:`ARTICULATED_CONTROLLER_FORCE
            <superdex.physics.QueryType>` before the simulation step. Results are
            available after the simulation step completes.

        Note:
            Requires the actor to support :class:`ARTICULATED_CONTROLLER_FORCE
            <superdex.physics.QueryType>`.

        Note:
            Equivalent to using :class:`CONSTRAINT_FORCE <superdex.physics.QueryType>`
            on each of the pose controller's constraints and projecting the constraint
            forces through the link Jacobians to joint DoF space (i.e., Σ Jᵢᵀ · fᵢ for
            each pose constraint i).

        Note:
            If the solver diverged in the last simulation step, the returned generalized
            forces may be stale.

        See Also:
            :meth:`~superdex.physics.Actor.register_query`,
            :class:`ARTICULATED_CONTROLLER_FORCE <superdex.physics.QueryType>`
        """
    def is_nested_link_actor(self) -> bool:
        """Check if the actor is a rigid link within an articulated actor.

        Returns:
            True if the actor is a nested link actor, false otherwise.

        See Also:
            :meth:`~superdex.physics.Actor.get_nested_link_actors`,
            :meth:`~superdex.physics.Actor.get_articulated_actor`
        """
    def is_nested_soft_actor(self) -> bool:
        """Check if the actor is a nested soft actor within a soft-skinned actor.

        Returns:
            True if the actor is a nested soft actor, false otherwise.

        See Also:
            :meth:`~superdex.physics.Actor.get_nested_soft_actors`,
            :meth:`~superdex.physics.Actor.get_articulated_actor`
        """
    def get_articulated_actor(self) -> ActorHandle:
        """Get the owning articulated actor.

        Returns:
            Handle to the owning articulated actor.

        Raises:
            :class:`~superdex.physics.Error`: If an error occurs.

        Note:
            Only applicable to nested link or nested soft actors within articulated
            actors.

        See Also:
            :meth:`~superdex.physics.Actor.is_nested_link_actor`,
            :meth:`~superdex.physics.Actor.get_nested_link_actors`,
            :meth:`~superdex.physics.Actor.is_nested_soft_actor`,
            :meth:`~superdex.physics.Actor.get_nested_soft_actors`
        """
    def get_articulated_jacobian(self) -> SpanConstReal:
        """Get the Jacobian with respect to the articulated DoFs in world frame.

        Returns:
            Jacobian matrix in row-major order with shape [6, numArticulatedDoFs], where
            6 is the number of rigid DoFs of the link (3 translation + 3 rotation) and
            numArticulatedDoFs is :meth:`~superdex.physics.Actor.get_num_dofs` of the
            owning articulated actor.

        Raises:
            :class:`~superdex.physics.Error`: If an error occurs.

        Note:
            Only applicable to nested link actors within articulated actors.

        Note:
            To get the Jacobian in the articulation's root frame, left-multiply the
            Jacobian by the 6×6 block-diagonal of the root-from-world rotation:

            ::

                [ R  0 ] · J
                [ 0  R ]

            where R is the 3×3 root-from-world rotation matrix (i.e., the transpose of
            the owning articulated actor's world-from-root rotation).

        See Also:
            :meth:`~superdex.physics.Actor.is_nested_link_actor`
        """

class Constraint:
    """Represents a constraint that restricts the degrees of freedom of one or more
    actors.

    Constraints enforce relationships between actor degrees of freedom (DoFs), such
    as fixing positions, limiting joint angles, or tracking target poses. They apply
    forces to maintain desired configurations using spring-damper mechanics
    controlled by stiffness and damping parameters.

    Note:
        Each constraint belongs to exactly one scene.

    Note:
        Each constraint acts on the degrees of freedom of one or more actors.

    Note:
        The type of constraint determines which methods are applicable.

    Warning:
        Concurrent access to a constraint is illegal.
    """
    def get_type(self) -> ConstraintType:
        """Get the type of the constraint.

        Returns:
            Type of the constraint.

        Note:
            Some methods are only valid for certain constraint types.

        See Also:
            :class:`~superdex.physics.ConstraintType`
        """
    def get_handle(self) -> ConstraintHandle:
        """Get the handle associated with this constraint.

        Returns:
            Handle to this constraint.

        Note:
            Handles are safe to store and remain valid throughout the constraint
            lifespan.

        Note:
            Call :meth:`~superdex.physics.Scene.get_constraint` to look up the
            constraint's pointer from the constraint's handle.

        See Also:
            :meth:`~superdex.physics.Scene.get_constraint`
        """
    def get_stiffness(self) -> float:
        """Get the constraint's stiffness parameter.

        Returns:
            Stiffness value. [N/m] for translation constraints, [N·m/rad] for rotation
            constraints.

        Note:
            Higher stiffness results in stronger constraint forces/torques.

        See Also:
            :meth:`~superdex.physics.Constraint.set_stiffness`,
            :meth:`~superdex.physics.Constraint.get_damping`,
            :meth:`~superdex.physics.Constraint.get_saturation`
        """
    def set_stiffness(self, stiffness: float) -> None:
        """Set the constraint's stiffness parameter.

        Args:
            stiffness (float): Stiffness value. [N/m] for translation constraints,
                [N·m/rad] for rotation constraints. Must be non-negative and finite.

        Raises:
            :class:`~superdex.physics.Error`: If an error occurs.

        See Also:
            :meth:`~superdex.physics.Constraint.get_stiffness`,
            :meth:`~superdex.physics.Constraint.set_damping`,
            :meth:`~superdex.physics.Constraint.set_saturation`
        """
    def get_damping(self) -> float:
        """Get the constraint's damping parameter.

        Returns:
            Damping value. [N·s/m] for translation constraints, [N·m·s/rad] for rotation
            constraints.

        See Also:
            :meth:`~superdex.physics.Constraint.set_damping`,
            :meth:`~superdex.physics.Constraint.get_stiffness`,
            :meth:`~superdex.physics.Constraint.get_saturation`
        """
    def set_damping(self, damping: float) -> None:
        """Set the constraint's damping parameter.

        Args:
            damping (float): Damping value. [N·s/m] for translation constraints,
                [N·m·s/rad] for rotation constraints. Must be non-negative and finite.

        Raises:
            :class:`~superdex.physics.Error`: If an error occurs.

        See Also:
            :meth:`~superdex.physics.Constraint.get_damping`,
            :meth:`~superdex.physics.Constraint.set_stiffness`,
            :meth:`~superdex.physics.Constraint.set_saturation`
        """
    def get_saturation(self) -> float:
        """Get the constraint's saturation parameter.

        Returns:
            Saturation distance [m] for translation constraints or angle [rad] for
            rotation constraints, or a negative value if saturation is disabled.

        Note:
            A positive value smoothly limits the magnitude of the elastic (stiffness)
            contribution to stiffness * saturation. The damping contribution is separate
            and is not limited by saturation.

        See Also:
            :meth:`~superdex.physics.Constraint.set_saturation`,
            :meth:`~superdex.physics.Constraint.get_stiffness`,
            :meth:`~superdex.physics.Constraint.get_damping`
        """
    def set_saturation(self, saturation: float) -> None:
        """Set the constraint's saturation parameter.

        Args:
            saturation (float): Saturation distance [m] for translation constraints or
                angle [rad] for rotation constraints, or any negative value to disable
                saturation. Must be finite and non-zero.

        Raises:
            :class:`~superdex.physics.Error`: If an error occurs.

        Note:
            A positive value smoothly limits the magnitude of the elastic (stiffness)
            contribution to stiffness * saturation. The damping contribution is separate
            and is not limited by saturation.

        See Also:
            :meth:`~superdex.physics.Constraint.get_saturation`,
            :meth:`~superdex.physics.Constraint.set_stiffness`,
            :meth:`~superdex.physics.Constraint.set_damping`
        """
    def get_deviation(self) -> DynamicArrayReal:
        """Compute the constraint's current deviation from its target, limits, or
        coupling
        condition.

        Returns:
            Vector of constraint deviation values, one per constraint dimension. Units:
            [m] for translation constraints or [rad] for rotation constraints. Size
            equals the number of scalar constraint equations (e.g., 3 for
            position/rotation pivots and joints, 1 for single-DoF range/target
            constraints).

        Note:
            For constraints with targets: deviation is the distance/angle error from
            target.

        Note:
            For constraints with range limits: deviation indicates how far the current
            value is from the valid range.

        Note:
            For coupling constraints without a target or range: deviation is the
            constraint violation, i.e. how far the coupling is from being satisfied
            (zero when exactly satisfied).
        """
    def get_force(self) -> SpanConstReal:
        """Get the force/torque applied by the constraint.

        Values are ordered first by actor (in the order returned by
        :meth:`~superdex.physics.Constraint.get_actor`), then by DoF within each actor
        (in the order returned by
        :meth:`~superdex.physics.Constraint.get_dof_indices_for_actor`). Each entry has
        units conjugate to its DoF: [N] for translational DoFs and [N·m] for rotational
        DoFs (so a single returned vector may mix both, e.g. for
        :class:`RIGID_PIVOT_POSITION <superdex.physics.ConstraintType>` the 6 entries
        are 3 force [N] followed by 3 torque [N·m] components on the rigid actor).

        Returns:
            Flattened generalized-force vector. One entry per constrained DoF.

        Raises:
            :class:`~superdex.physics.Error`: If an error occurs.

        Note:
            Requires registering :class:`CONSTRAINT_FORCE <superdex.physics.QueryType>`
            before the simulation step. Results are available after the simulation step
            completes.

        Note:
            If the solver diverged in the last simulation step, the returned force may
            be inaccurate.

        See Also:
            :meth:`~superdex.physics.Constraint.register_query`,
            :class:`CONSTRAINT_FORCE <superdex.physics.QueryType>`,
            :meth:`~superdex.physics.Constraint.get_dof_indices_for_actor`
        """
    def get_num_actors(self) -> int:
        """Get the number of actors affected by the constraint.

        Returns:
            Number of actors affected by the constraint.
        """
    def get_actor(self, actor_index: int) -> Actor:
        """Get one of the actors affected by this constraint.

        Args:
            actor_index (int): Actor index in the range [0,
                :meth:`~superdex.physics.Constraint.get_num_actors`).

        Returns:
            Pointer to the actor.

        See Also:
            :meth:`~superdex.physics.Constraint.get_num_actors`
        """
    def get_dof_indices_for_actor(self, actor_index: int) -> SpanConstInt:
        """Get the degrees of freedom (DoF) indices affected by this constraint on a
        specific actor.

        Args:
            actor_index (int): Actor index in the range [0,
                :meth:`~superdex.physics.Constraint.get_num_actors`).

        Returns:
            Span of DoF indices for the specified actor.

        See Also:
            :meth:`~superdex.physics.Constraint.get_num_actors`,
            :meth:`~superdex.physics.Constraint.get_actor`
        """
    def set_target_position(self, position: Real3Like) -> None:
        """Set the constraint's target position in world frame.

        Args:
            position (Real3Like): Target position [m] in world frame. Must be finite.

        Raises:
            :class:`~superdex.physics.Error`: If an error occurs.

        Note:
            Only applicable to position-target constraints: :class:`RIGID_PIVOT_POSITION
            <superdex.physics.ConstraintType>`, :class:`DEFORMABLE_NODE_POSITION
            <superdex.physics.ConstraintType>`, and :class:`RIGID_PIVOT_TO_RIGID_TARGET
            <superdex.physics.ConstraintType>`. For :class:`RIGID_PIVOT_TO_RIGID_TARGET
            <superdex.physics.ConstraintType>`, this updates only the translation of the
            target center-of-mass transform.

        Note:
            Also affects target velocity, which depends on the difference between
            current and old target position.

        See Also:
            :meth:`~superdex.physics.Constraint.set_target_rotation`,
            :meth:`~superdex.physics.Constraint.set_target_dof`
        """
    def set_target_rotation(self, rotation: QuaternionLike) -> None:
        """Set the constraint's target rotation.

        The target is interpreted according to the constraint type. For
        :class:`RIGID_PIVOT_ROTATION <superdex.physics.ConstraintType>`, it is the
        target pivot-frame rotation in world coordinates. For
        :class:`RIGID_PIVOT_TO_RIGID_TARGET <superdex.physics.ConstraintType>`, it is
        the rotation of the target center-of-mass transform in world coordinates. For
        :class:`ARTICULATED3D_ROTATION_TARGET <superdex.physics.ConstraintType>`, it is
        the target joint-local rotation. For :class:`JOINT_ROTATION_TRACKING
        <superdex.physics.ConstraintType>`, it is the target rotation of
        :attr:`~superdex.physics.JointRotationTrackingConstraintParams.actor_b` relative
        to :attr:`~superdex.physics.JointRotationTrackingConstraintParams.actor_a`. For
        :class:`ROD_ELEMENT_ROTATION_TO_RIGID <superdex.physics.ConstraintType>`, it is
        the target rotation of the constrained rod element relative to the rigid actor.

        Args:
            rotation (QuaternionLike): Target rotation quaternion [x, y, z, w]. Must be
                finite and non-zero. Normalized before use.

        Raises:
            :class:`~superdex.physics.Error`: If an error occurs.

        Note:
            Also affects target velocity, which depends on the difference between
            current and old target rotation.

        See Also:
            :meth:`~superdex.physics.Constraint.set_target_position`,
            :meth:`~superdex.physics.Constraint.set_target_dof`,
            :meth:`~superdex.physics.Constraint.set_ref_relative_rotation`
        """
    def set_target_dof(self, target: float) -> None:
        """Set the constraint's target DoF.

        Args:
            target (float): Target DoF value. Units: [m] for translation DoFs, [rad] for
                rotation DoFs.

        Raises:
            :class:`~superdex.physics.Error`: If an error occurs.

        Note:
            Only applicable to :class:`ARTICULATED_SINGLE_DOF_TARGET
            <superdex.physics.ConstraintType>`.

        Note:
            Also affects target velocity, which depends on the difference between
            current and old target DoF.

        See Also:
            :meth:`~superdex.physics.Constraint.set_target_position`,
            :meth:`~superdex.physics.Constraint.set_target_rotation`
        """
    def update_old_target(self) -> None:
        """Update the old target to match the current target.

        Call this method after changing a target discontinuously, and before stepping
        the scene, to prevent that change from contributing to damping during the next
        simulation step.

        Raises:
            :class:`~superdex.physics.Error`: If an error occurs.

        Note:
            Only applicable to constraints with a target.
        """
    def set_ref_relative_rotation(
        self,
        rotation_a: QuaternionLike,
        rotation_b: QuaternionLike,
    ) -> None:
        """Set a relative-rotation target from two rotations expressed in a common
        frame.

        Sets the target to inverse(``rotation_a``) * ``rotation_b``.

        Args:
            rotation_a (QuaternionLike): First rotation. Represents
                :attr:`~superdex.physics.JointRotationTrackingConstraintParams.actor_a`
                for :class:`JOINT_ROTATION_TRACKING <superdex.physics.ConstraintType>`
                and the rigid actor for :class:`ROD_ELEMENT_ROTATION_TO_RIGID
                <superdex.physics.ConstraintType>`. Must be finite and non-zero.
                Normalized before use.
            rotation_b (QuaternionLike): Second rotation. Represents
                :attr:`~superdex.physics.JointRotationTrackingConstraintParams.actor_b`
                for :class:`JOINT_ROTATION_TRACKING <superdex.physics.ConstraintType>`
                and the selected rod element for :class:`ROD_ELEMENT_ROTATION_TO_RIGID
                <superdex.physics.ConstraintType>`. Must be finite and non-zero.
                Normalized before use.

        Raises:
            :class:`~superdex.physics.Error`: If an error occurs.

        Note:
            Only applicable to :class:`JOINT_ROTATION_TRACKING
            <superdex.physics.ConstraintType>` and :class:`ROD_ELEMENT_ROTATION_TO_RIGID
            <superdex.physics.ConstraintType>`.

        Note:
            Also affects target velocity, which depends on the difference between
            current and old target relative rotation.

        See Also:
            :meth:`~superdex.physics.Constraint.set_target_rotation`,
            :meth:`~superdex.physics.Constraint.update_old_target`
        """
    def get_limit_min_values(self) -> SpanConstReal:
        """Get the constraint's minimum limit values.

        Returns:
            Span of minimum values for each constrained DoF. Units: [m] for translation
            DoFs, [rad] for rotation DoFs.

        Raises:
            :class:`~superdex.physics.Error`: If an error occurs.

        Note:
            Only applicable to range-based constraints
            (:class:`ARTICULATED_SINGLE_DOF_RANGE <superdex.physics.ConstraintType>`,
            :class:`ARTICULATED3D_ROTATION_RANGE <superdex.physics.ConstraintType>`,
            :class:`JOINT_ROTATION_RANGE <superdex.physics.ConstraintType>`).

        See Also:
            :meth:`~superdex.physics.Constraint.get_limit_max_values`
        """
    def get_limit_max_values(self) -> SpanConstReal:
        """Get the constraint's maximum limit values.

        Returns:
            Span of maximum values for each constrained DoF. Units: [m] for translation
            DoFs, [rad] for rotation DoFs.

        Raises:
            :class:`~superdex.physics.Error`: If an error occurs.

        Note:
            Only applicable to range-based constraints
            (:class:`ARTICULATED_SINGLE_DOF_RANGE <superdex.physics.ConstraintType>`,
            :class:`ARTICULATED3D_ROTATION_RANGE <superdex.physics.ConstraintType>`,
            :class:`JOINT_ROTATION_RANGE <superdex.physics.ConstraintType>`).

        See Also:
            :meth:`~superdex.physics.Constraint.get_limit_min_values`
        """
    def register_query(self, type: QueryType | int) -> QueryHandle:
        """Register a query to compute data for this constraint.

        Args:
            type (QueryType | int): Type of query to register.

        Returns:
            :class:`~superdex.physics.QueryHandle` for the registered query.

        Raises:
            :class:`~superdex.physics.Error`: If an error occurs.

        Note:
            Queries may add substantial computational overhead. Use only when necessary.

        Note:
            Data becomes available after the next simulation step (even if the time-step
            is zero).

        Note:
            Call :meth:`~superdex.physics.Constraint.cancel_query` when the query is not
            needed again in the future.

        Note:
            Queries are reference counted internally, so they will be available as long
            as there is one outstanding :class:`~superdex.physics.QueryHandle`.

        See Also:
            :meth:`~superdex.physics.Constraint.cancel_query`,
            :meth:`~superdex.physics.Constraint.is_query_supported`,
            :meth:`~superdex.physics.Actor.register_query`
        """
    def cancel_query(self, handle: QueryHandle) -> None:
        """Cancel a previously registered query.

        Args:
            handle (QueryHandle): :class:`~superdex.physics.QueryHandle` from a previous
                :meth:`~superdex.physics.Constraint.register_query` call.

        Note:
            Cancelling queries may improve memory usage and/or performance if the query
            is no longer needed.

        See Also:
            :meth:`~superdex.physics.Constraint.register_query`,
            :meth:`~superdex.physics.Actor.cancel_query`
        """
    def is_query_supported(self, type: QueryType | int) -> bool:
        """Check whether a query type is supported for this constraint.

        Args:
            type (QueryType | int): Type of query to check.

        Returns:
            True if :meth:`~superdex.physics.Constraint.register_query` would succeed
            for this query type, false otherwise.

        See Also:
            :meth:`~superdex.physics.Constraint.register_query`
        """

class Scene:
    """Represents a simulation scene.

    Note:
        Each scene manages the simulation of a series of actors.

    Note:
        Actors from different scenes do not interact with each other.

    Warning:
        Concurrent access to the scene and its actors and constraints is illegal.
    """
    DEFAULT_CALLBACK_PRIORITY: int
    """Default priority for callback ordering. Lower values execute earlier (higher
    priority).
    """
    def get_handle(self) -> SceneHandle:
        """Get the scene handle.

        Returns:
            Scene's handle.

        Note:
            Handles are safe to store and remain valid throughout the scene lifespan.

        Note:
            Call :func:`~superdex.physics.get_scene` to look up the scene's pointer from
            the scene's handle.

        See Also:
            :func:`~superdex.physics.get_scene`
        """
    def get_name(self) -> str:
        """Get the name of the scene.

        Returns:
            Name of the scene.
        """
    def get_gravity(self) -> Real3:
        """Get the gravity vector in world frame.

        Returns:
            Gravity vector [m/s^2] in world frame.

        See Also:
            :meth:`~superdex.physics.Scene.set_gravity`
        """
    def set_gravity(self, gravity: Real3Like) -> None:
        """Set the gravity vector in world frame.

        Args:
            gravity (Real3Like): Gravity vector [m/s^2] in world frame.

        See Also:
            :meth:`~superdex.physics.Scene.get_gravity`
        """
    def get_solver_params(self) -> SolverParams:
        """Get the solver parameters.

        Returns:
            Current solver parameters.

        See Also:
            :meth:`~superdex.physics.Scene.set_solver_params`
        """
    def set_solver_params(self, params: SolverParams) -> None:
        """Set the solver parameters.

        Args:
            params (SolverParams): Solver parameters to set.

        Raises:
            :class:`~superdex.physics.Error`: If an error occurs.

        See Also:
            :meth:`~superdex.physics.Scene.get_solver_params`,
            :class:`~superdex.physics.SolverParams`
        """
    def step(self, time_step_sec: float) -> None:
        """Advance the simulation of the :class:`~superdex.physics.Scene` one step.

        SuperDex Physics uses fully-implicit time integration, enabling substantially
        larger stable time steps than physics engines using explicit or semi-implicit
        methods, particularly for stiff systems. Fewer steps per simulated second often
        improve simulation performance and real-time factor.

        Args:
            time_step_sec (float): Time step size [s]. Must be non-negative.

        Note:
            Time steps of 10–25 ms (40–100 physics steps per simulated second) run
            robustly in most scenes, including complex contact-rich and deformable
            simulations. This is a practical starting range, not a guarantee. Smaller
            steps may still be required to resolve fast motion, accurately capture
            short-duration contact dynamics without excessive numerical dissipation,
            resolve dynamics associated with small geometric or discretization length
            scales, or improve nonlinear-solver convergence.

        Note:
            It is OK to use a different time step each time.

        Note:
            If the time step is zero, then the state of the simulation will not change,
            but pre-step and post-step callbacks will still be called, and queries will
            be updated.

        Note:
            If the time step is negative or NaN, no step is taken and an error is
            logged.

        See Also:
            :class:`~superdex.physics.IntegrationMethod`
        """
    def get_last_time_step(self) -> float:
        """Returns the time step size [s] from the most recent positive call to
        :meth:`~superdex.physics.Scene.step`.

        Returns:
            The time step size [s] from the most recent positive
            :meth:`~superdex.physics.Scene.step` call.

        Note:
            In a newly created scene, the return value is unspecified until the first
            positive :meth:`~superdex.physics.Scene.step` call.

        Note:
            :meth:`~superdex.physics.Scene.restore_state` and
            :meth:`~superdex.physics.Scene.restore_state_from_bytes` restore this value
            from the captured state.

        Note:
            Calling :meth:`~superdex.physics.Scene.step` with a zero time step (e.g. to
            refresh queries without advancing the simulation) does not update this value
            (i.e. it retains the value from the last positive time step).
        """
    def get_total_simulation_time(self) -> float:
        """Returns the total simulation time [s] in the scene.

        Returns:
            The total simulation time [s].

        Note:
            May not match the real-time that has elapsed.
        """
    def get_solver_stats(self) -> SolverStats:
        """Get solver convergence metrics from the last simulation step.

        Returns:
            The :class:`~superdex.physics.SolverStats` from the last simulation step.

        See Also:
            :class:`~superdex.physics.SolverStats`,
            :meth:`~superdex.physics.Actor.get_convergence_status`
        """
    def get_performance_stats(self) -> PerformanceStats:
        """Get timing and profiling metrics from the last simulation step.

        Returns:
            The :class:`~superdex.physics.PerformanceStats` from the last simulation
            step.

        See Also:
            :class:`~superdex.physics.PerformanceStats`
        """
    def set_force_single_island(self, force_single_island: bool) -> None:
        """Enables/disables forcing all actors in the scene to be in a single simulation
        island.

        An island is a group of actors and constraints that are solved together. Islands
        are managed automatically based on proximity and other factors. Islands can be
        solved in parallel, which can greatly improve the performance of the scene.
        However, if you suspect that island partitioning might be causing a problem,
        then you can use this function to force all actors in the scene to be simulated
        in a single island.

        Args:
            force_single_island (bool): Whether to force all actors to be in a single
                simulation island.

        Warning:
            Forcing single-island is a debugging feature. It will hurt performance.

        See Also:
            :meth:`~superdex.physics.Scene.get_force_single_island`
        """
    def get_force_single_island(self) -> bool:
        """Get whether all actors are forced to be in a single simulation island.

        Returns:
            True if all actors are forced into a single simulation island.

        See Also:
            :meth:`~superdex.physics.Scene.set_force_single_island`
        """
    def capture_state(self) -> StateHandle:
        """Capture the simulation state that changes from step to step for later
        restoration.

        Captured state includes actor solution, transforms, and velocities; current and
        previous constraint/controller targets; and external forces.

        Returns:
            :class:`~superdex.physics.StateHandle` to be used with
            :meth:`~superdex.physics.Scene.restore_state`. Call
            :meth:`~superdex.physics.Scene.release_state` or
            :meth:`~superdex.physics.Scene.release_all_states` when you're done with it.

        Raises:
            :class:`~superdex.physics.Error`: If an error occurs.

        Warning:
            Not supported for scenes with ROM actors.

        See Also:
            :meth:`~superdex.physics.Scene.restore_state`,
            :meth:`~superdex.physics.Scene.release_state`,
            :meth:`~superdex.physics.Scene.release_all_states`,
            :meth:`~superdex.physics.Scene.capture_state_to_file`,
            :meth:`~superdex.physics.Scene.capture_state_to_bytes`
        """
    def restore_state(self, handle: StateHandle, release_immediately: bool) -> None:
        """Restore captured simulation state. Optionally release the
        :class:`~superdex.physics.StateHandle` at the same time.

        Args:
            handle (StateHandle): :class:`~superdex.physics.StateHandle` from a
                successful call to :meth:`~superdex.physics.Scene.capture_state`.
            release_immediately (bool): If true, release the state handle immediately
                (as if :meth:`~superdex.physics.Scene.release_state` were called), even
                if the restore fails.

        Raises:
            :class:`~superdex.physics.Error`: If an error occurs.

        Note:
            The :class:`~superdex.physics.StateHandle` must have been captured from this
            :class:`~superdex.physics.Scene`.

        Note:
            The scene composition (actors, constraints) must be the same as when the
            state was captured.

        Warning:
            Does not create or destroy actors or constraints, or restore shapes,
            materials, or scene parameters. Captured mutable state on existing actors,
            including static actor transforms, is restored; if topology or persistent
            configuration changes, simulation results may differ.

        Warning:
            Restoration is not transactional. If restoration fails, the scene may be
            partially modified. Restore a known-good checkpoint or recreate the scene
            before continuing.

        See Also:
            :meth:`~superdex.physics.Scene.capture_state`,
            :meth:`~superdex.physics.Scene.release_state`,
            :meth:`~superdex.physics.Scene.release_all_states`,
            :meth:`~superdex.physics.Scene.restore_state_from_bytes`
        """
    def capture_state_to_bytes(self, out_data: ArrayLikeUint8) -> None:
        """Capture simulation state to a binary byte buffer.

        Captures the same simulation state as
        :meth:`~superdex.physics.Scene.capture_state`, but writes the binary
        representation to caller-owned memory instead of storing it internally. This is
        useful for transferring state between processes or caching state outside the
        scene.

        Args:
            out_data (ArrayLikeUint8): Output byte buffer. Capture data is appended to
                the buffer.

        Raises:
            :class:`~superdex.physics.Error`: If an error occurs.

        Note:
            On failure, the output buffer may contain a partial capture. Discard the
            buffer or truncate it to its original size before reuse.

        Warning:
            Has the same capture restrictions as
            :meth:`~superdex.physics.Scene.capture_state`, including no support for
            scenes with ROM actors.

        Warning:
            The binary representation uses an internal capture format. It is intended
            only for compatible scenes with the same actors and constraints created in
            the same order and the same component layouts, using the same SuperDex
            Physics build. It is not a stable long-term serialization or asset
            interchange format.

        See Also:
            :meth:`~superdex.physics.Scene.restore_state_from_bytes`,
            :meth:`~superdex.physics.Scene.capture_state`,
            :meth:`~superdex.physics.Scene.capture_state_to_file`
        """
    def restore_state_from_bytes(self, data: ArrayLikeUint8) -> None:
        """Restore simulation state from a binary byte buffer produced by
        :meth:`~superdex.physics.Scene.capture_state_to_bytes`.

        Args:
            data (ArrayLikeUint8): State data from a successful call to
                :meth:`~superdex.physics.Scene.capture_state_to_bytes`.

        Raises:
            :class:`~superdex.physics.Error`: If an error occurs.

        Note:
            The byte buffer must have been captured from this or a compatible scene via
            :meth:`~superdex.physics.Scene.capture_state_to_bytes`.

        Note:
            The capture and restore scenes must use the same SuperDex Physics build.

        Note:
            The restore requirements from :meth:`~superdex.physics.Scene.restore_state`
            apply, including scene composition (actors, constraints) being the same as
            when the state was captured.

        Warning:
            Has the same restore scope and non-transactional failure behavior as
            :meth:`~superdex.physics.Scene.restore_state`.

        See Also:
            :meth:`~superdex.physics.Scene.capture_state_to_bytes`,
            :meth:`~superdex.physics.Scene.restore_state`
        """
    def release_state(self, handle: StateHandle) -> None:
        """Release a :class:`~superdex.physics.StateHandle` when you are done with it.
        This
        frees up memory.

        Args:
            handle (StateHandle): :class:`~superdex.physics.StateHandle` to release.

        Note:
            Redundant calls to ReleaseState will be ignored.
        """
    def release_all_states(self) -> None:
        """Release memory for all captured state and invalidate all prior
        :class:`~superdex.physics.StateHandle` objects.
        """
    def is_equal_state(self, state_a: StateHandle, state_b: StateHandle) -> bool:
        """Return true if both StateHandles are valid and refer to identical state
        information (full precision).

        Args:
            state_a (StateHandle): First StateHandle to compare
            state_b (StateHandle): Second StateHandle to compare

        Returns:
            True if both states are valid and identical.
        """
    def capture_state_to_file(self, file_path: str) -> None:
        """Captures a snapshot of the state of a :class:`~superdex.physics.Scene`,
        similar
        to :meth:`~superdex.physics.Scene.capture_state`, then writes the data to a JSON
        file for manual inspection.

        Args:
            file_path (str): Path of the output file to write/overwrite (case sensitive
                on some filesystems).

        Raises:
            :class:`~superdex.physics.Error`: If an error occurs.

        Warning:
            Not supported for scenes with ROM actors.

        See Also:
            :meth:`~superdex.physics.Scene.capture_state`
        """
    @overload
    def create_rigid_actor(self, params: RigidActorParams) -> Optional[Actor]:
        """Create a rigid body actor.

        Args:
            params (RigidActorParams): Parameters defining the rigid actor.

        Returns:
            Pointer to the created :class:`~superdex.physics.Actor`, or None on error.

        Raises:
            :class:`~superdex.physics.Error`: If an error occurs.

        See Also:
            :class:`~superdex.physics.RigidActorParams`,
            :meth:`~superdex.physics.Scene.destroy_actor`
        """
    @overload
    def create_rigid_actor(
        self,
        name: str = ...,
        layer: str = ...,
        shape: ShapeHandle = ...,
        world_from_local: TransformRT = ...,
        collider_type: ColliderType | int = ...,
        is_static: bool = ...,
        contact: ContactParams = ...,
        sdf: GridSdfParams = ...,
        has_gravity: bool = ...,
        density: Optional[float] = ...,
        mass: Optional[float] = ...,
        center_of_mass: Optional[Real3Like] = ...,
        moment_of_inertia: Optional[Real6Like] = ...,
        boundary_element_type: ActorBoundaryElementType | int = ...,
        boundary_subsampling: Optional[BoundarySubsamplingParams] = ...,
        linear_velocity: Optional[Real3Like] = ...,
        angular_velocity: Optional[Real3Like] = ...,
    ) -> Optional[Actor]: ...
    @overload
    def create_soft_actor(self, params: SoftActorParams) -> Optional[Actor]:
        """Create a soft deformable actor.

        Args:
            params (SoftActorParams): Parameters defining the soft actor.

        Returns:
            Pointer to the created :class:`~superdex.physics.Actor`, or None on error.

        Raises:
            :class:`~superdex.physics.Error`: If an error occurs.

        See Also:
            :class:`~superdex.physics.SoftActorParams`,
            :meth:`~superdex.physics.Scene.destroy_actor`
        """
    @overload
    def create_soft_actor(
        self,
        name: str = ...,
        layer: str = ...,
        world_from_local: TransformRT = ...,
        shape: ShapeHandle = ...,
        material: SoftMaterialParams = ...,
        contact: ContactParams = ...,
        has_gravity: bool = ...,
        has_inertia: bool = ...,
        has_stress: bool = ...,
        boundary_element_type: ActorBoundaryElementType | int = ...,
    ) -> Optional[Actor]: ...
    @overload
    def create_articulated_actor(
        self,
        params: ArticulatedActorParams,
    ) -> Optional[Actor]:
        """Create an articulated actor.

        Args:
            params (ArticulatedActorParams): Parameters defining the articulated actor.

        Returns:
            Pointer to the created :class:`~superdex.physics.Actor`, or None on error.

        Raises:
            :class:`~superdex.physics.Error`: If an error occurs.

        Note:
            To access nested link actors, use
            :meth:`~superdex.physics.Actor.get_nested_link_actors`.

        Warning:
            A zero-DOF skeleton (every joint is :class:`HARD
            <superdex.physics.ArticulatedJointType>`) is a static welded structure and
            cannot carry a skin (:attr:`~superdex.physics.ArticulatedActorParams.skin`).

        See Also:
            :class:`~superdex.physics.ArticulatedActorParams`,
            :meth:`~superdex.physics.Scene.destroy_actor`
        """
    @overload
    def create_articulated_actor(
        self,
        name: str = ...,
        world_from_root: TransformRT = ...,
        cycles: ArrayLikeArticulatedCycleJointParams = ...,
        joints: ArrayLikeArticulatedJointParams = ...,
        links: ArrayLikeArticulatedLinkParams = ...,
        skin: Optional[ArticulatedSkinParams] = ...,
        joint_velocities: Optional[ArrayLikeReal] = ...,
    ) -> Optional[Actor]: ...
    @overload
    def create_soft_skinned_actor(
        self,
        params: SoftSkinnedActorParams,
    ) -> Optional[Actor]:
        """Create a soft-skinned actor with an articulated skeleton.

        Args:
            params (SoftSkinnedActorParams): Parameters defining the soft-skinned actor.

        Returns:
            Pointer to the created :class:`~superdex.physics.Actor`, or None on error.

        Raises:
            :class:`~superdex.physics.Error`: If an error occurs.

        Note:
            To access nested soft actors, use
            :meth:`~superdex.physics.Actor.get_nested_soft_actors`.

        Note:
            To access nested link actors, use
            :meth:`~superdex.physics.Actor.get_nested_link_actors`.

        Warning:
            The skeleton must have at least one non-Hard joint.

        See Also:
            :class:`~superdex.physics.SoftSkinnedActorParams`,
            :meth:`~superdex.physics.Scene.destroy_actor`
        """
    @overload
    def create_soft_skinned_actor(
        self,
        skeleton_params: ArticulatedActorParams = ...,
        soft_params: ArrayLikeSoftActorParams = ...,
        soft_attach_links: ArrayLikeString = ...,
        enable_colliding_links: bool = ...,
        has_gravity: bool = ...,
        has_inertia: bool = ...,
        has_stress: bool = ...,
    ) -> Optional[Actor]: ...
    @overload
    def destroy_actor(self, actor: Optional[Actor]) -> None:
        """Destroy an actor and remove it from the scene.

        Args:
            actor (Optional[Actor]): Pointer to the actor to destroy.

        Note:
            Nested actors cannot be destroyed individually. When you destroy the parent
            actor (e.g. an articulation), all the nested actors will be destroyed
            automatically.

        Note:
            Constraints attached to the actor are automatically destroyed.

        Note:
            If ``actor`` is None, this function has no effect.

        Note:
            After the actor is destroyed, do not use the pointer or its handle.

        Warning:
            The pointer must be None or point to a live actor owned by this scene.
            Passing a pointer to an actor that has been destroyed is undefined behavior.

        See Also:
            :meth:`~superdex.physics.Scene.get_actor`,
            :meth:`~superdex.physics.Actor.get_handle`
        """
    @overload
    def destroy_actor(self, actor: ActorHandle) -> None:
        """Destroy an actor and remove it from the scene.

        Args:
            actor (ActorHandle): Handle of the actor to destroy. If valid, it must be
                owned by this scene.

        Note:
            Nested actors cannot be destroyed individually. When you destroy the parent
            actor (e.g. an articulation), all the nested actors will be destroyed
            automatically.

        Note:
            Constraints attached to the actor are automatically destroyed.

        Note:
            An invalid handle or one that does not currently identify an actor in the
            scene has no effect.

        Note:
            After the actor is destroyed, do not use its handle.

        See Also:
            :meth:`~superdex.physics.Scene.get_actor`,
            :meth:`~superdex.physics.Actor.get_handle`
        """
    def get_actor(self, actor: ActorHandle) -> Optional[Actor]:
        """Get the :class:`~superdex.physics.Actor` pointer associated with an
        :class:`~superdex.physics.ActorHandle`.

        Args:
            actor (ActorHandle): Handle of the actor to retrieve. If valid, it must be
                owned by this scene.

        Returns:
            Pointer to the :class:`~superdex.physics.Actor`, or None if the handle does
            not currently identify an actor in this scene.

        See Also:
            :class:`~superdex.physics.Actor`, :class:`~superdex.physics.ActorHandle`,
            :meth:`~superdex.physics.Scene.destroy_actor`,
            :meth:`~superdex.physics.Actor.get_handle`
        """
    def get_num_actors(self) -> int:
        """Get the number of actors in the scene, including nested link actors and
        nested
        soft actors.

        Returns:
            Number of actors in the scene.

        Note:
            To count only top-level actors, iterate with
            :meth:`~superdex.physics.Scene.for_each_actor` and skip actors for which
            :meth:`~superdex.physics.Actor.is_nested_link_actor` or
            :meth:`~superdex.physics.Actor.is_nested_soft_actor` returns true.
        """
    def for_each_actor(self, callback: Callable[[Actor], None]) -> None:
        """Iterate over all the actors in the scene using a callback. Includes nested
        link
        actors and nested soft actors.

        Args:
            callback (Callable[[Actor], None]): Function called once for each actor.

        Note:
            To skip nested actors, check
            :meth:`~superdex.physics.Actor.is_nested_link_actor` and
            :meth:`~superdex.physics.Actor.is_nested_soft_actor` inside the callback.

        Warning:
            Destroying other actors or constraints from within the callback is illegal.
        """
    @overload
    def create_articulated_single_dof_range_constraint(
        self,
        params: ArticulatedSingleDofRangeConstraintParams,
    ) -> Optional[Constraint]:
        """Create a range constraint on a single articulated DoF.

        Args:
            params (ArticulatedSingleDofRangeConstraintParams): Parameters defining the
                DoF range constraint.

        Returns:
            Pointer to the created :class:`~superdex.physics.Constraint`, or None on
            error.

        Raises:
            :class:`~superdex.physics.Error`: If an error occurs.

        Note:
            Use this API for runtime creation/deletion of joint limits. For static joint
            limits, use the :meth:`~superdex.physics.Scene.create_articulated_actor` API
            instead.

        See Also:
            :class:`ARTICULATED_SINGLE_DOF_RANGE <superdex.physics.ConstraintType>`,
            :class:`~superdex.physics.ArticulatedSingleDofRangeConstraintParams`,
            :meth:`~superdex.physics.Scene.destroy_constraint`
        """
    @overload
    def create_articulated_single_dof_range_constraint(
        self,
        stiffness: float = ...,
        damping: float = ...,
        saturation: float = ...,
        actor: ActorHandle = ...,
        joint_index: int = ...,
        dof_index: int = ...,
        min_value: float = ...,
        max_value: float = ...,
    ) -> Optional[Constraint]: ...
    @overload
    def create_articulated3d_rotation_range_constraint(
        self,
        params: Articulated3dRotationRangeConstraintParams,
    ) -> Optional[Constraint]:
        """Create a range constraint on the DoFs of a 3D rotation joint of an
        articulated
        actor.

        Args:
            params (Articulated3dRotationRangeConstraintParams): Parameters defining the
                DoF range constraint.

        Returns:
            Pointer to the created :class:`~superdex.physics.Constraint`, or None on
            error.

        Raises:
            :class:`~superdex.physics.Error`: If an error occurs.

        Note:
            Use this API for runtime creation/deletion of joint limits. For static joint
            limits, use the :meth:`~superdex.physics.Scene.create_articulated_actor` API
            instead.

        See Also:
            :class:`ARTICULATED3D_ROTATION_RANGE <superdex.physics.ConstraintType>`,
            :class:`~superdex.physics.Articulated3dRotationRangeConstraintParams`,
            :meth:`~superdex.physics.Scene.destroy_constraint`
        """
    @overload
    def create_articulated3d_rotation_range_constraint(
        self,
        stiffness: float = ...,
        damping: float = ...,
        saturation: float = ...,
        actor: ActorHandle = ...,
        joint_index: int = ...,
        min_values: Real3Like = ...,
        max_values: Real3Like = ...,
    ) -> Optional[Constraint]: ...
    @overload
    def create_articulated_single_dof_target_constraint(
        self,
        params: ArticulatedSingleDofTargetConstraintParams,
    ) -> Optional[Constraint]:
        """Create a target constraint on a single articulated DoF.

        Args:
            params (ArticulatedSingleDofTargetConstraintParams): Parameters defining the
                single DoF target constraint.

        Returns:
            Pointer to the created :class:`~superdex.physics.Constraint`, or None on
            error.

        Raises:
            :class:`~superdex.physics.Error`: If an error occurs.

        See Also:
            :class:`ARTICULATED_SINGLE_DOF_TARGET <superdex.physics.ConstraintType>`,
            :class:`~superdex.physics.ArticulatedSingleDofTargetConstraintParams`,
            :meth:`~superdex.physics.Scene.destroy_constraint`
        """
    @overload
    def create_articulated_single_dof_target_constraint(
        self,
        stiffness: float = ...,
        damping: float = ...,
        saturation: float = ...,
        actor: ActorHandle = ...,
        joint_index: int = ...,
        dof_index: int = ...,
        target_value: float = ...,
    ) -> Optional[Constraint]: ...
    @overload
    def create_articulated3d_rotation_target_constraint(
        self,
        params: Articulated3dRotationTargetConstraintParams,
    ) -> Optional[Constraint]:
        """Create a target constraint on an articulated 3D rotation.

        Args:
            params (Articulated3dRotationTargetConstraintParams): Parameters defining
                the 3D rotation target constraint.

        Returns:
            Pointer to the created :class:`~superdex.physics.Constraint`, or None on
            error.

        Raises:
            :class:`~superdex.physics.Error`: If an error occurs.

        See Also:
            :class:`ARTICULATED3D_ROTATION_TARGET <superdex.physics.ConstraintType>`,
            :class:`~superdex.physics.Articulated3dRotationTargetConstraintParams`,
            :meth:`~superdex.physics.Scene.destroy_constraint`
        """
    @overload
    def create_articulated3d_rotation_target_constraint(
        self,
        stiffness: float = ...,
        damping: float = ...,
        saturation: float = ...,
        actor: ActorHandle = ...,
        joint_index: int = ...,
        target: QuaternionLike = ...,
    ) -> Optional[Constraint]: ...
    @overload
    def create_joint_rotation_range_constraint(
        self,
        params: JointRotationRangeConstraintParams,
    ) -> Optional[Constraint]:
        """Create a range constraint on joint rotation.

        Args:
            params (JointRotationRangeConstraintParams): Parameters defining the joint
                rotation range constraint.

        Returns:
            Pointer to the created :class:`~superdex.physics.Constraint`, or None on
            error.

        Raises:
            :class:`~superdex.physics.Error`: If an error occurs.

        See Also:
            :class:`JOINT_ROTATION_RANGE <superdex.physics.ConstraintType>`,
            :class:`~superdex.physics.JointRotationRangeConstraintParams`,
            :meth:`~superdex.physics.Scene.destroy_constraint`
        """
    @overload
    def create_joint_rotation_range_constraint(
        self,
        stiffness: float = ...,
        damping: float = ...,
        saturation: float = ...,
        ref_frame_rot_vec: Real3Like = ...,
        angle_range_x: Real2Like = ...,
        angle_range_y: Real2Like = ...,
        angle_range_z: Real2Like = ...,
        actor_a: ActorHandle = ...,
        actor_b: ActorHandle = ...,
        range_around_rest: bool = ...,
    ) -> Optional[Constraint]: ...
    @overload
    def create_joint_rotation_tracking_constraint(
        self,
        params: JointRotationTrackingConstraintParams,
    ) -> Optional[Constraint]:
        """Create a tracking constraint on joint rotation.

        Args:
            params (JointRotationTrackingConstraintParams): Parameters defining the
                joint rotation tracking constraint.

        Returns:
            Pointer to the created :class:`~superdex.physics.Constraint`, or None on
            error.

        Raises:
            :class:`~superdex.physics.Error`: If an error occurs.

        See Also:
            :class:`JOINT_ROTATION_TRACKING <superdex.physics.ConstraintType>`,
            :class:`~superdex.physics.JointRotationTrackingConstraintParams`,
            :meth:`~superdex.physics.Scene.destroy_constraint`
        """
    @overload
    def create_joint_rotation_tracking_constraint(
        self,
        stiffness: float = ...,
        damping: float = ...,
        saturation: float = ...,
        ref_frame_rot_vec: Real3Like = ...,
        actor_a: ActorHandle = ...,
        actor_b: ActorHandle = ...,
    ) -> Optional[Constraint]: ...
    @overload
    def create_rod_element_rotation_to_rigid_constraint(
        self,
        params: RodElementRotationToRigidConstraintParams,
    ) -> Optional[Constraint]:
        """Create a rotation constraint between a rigid actor and a rod element.

        Args:
            params (RodElementRotationToRigidConstraintParams): Parameters defining the
                rod element rotation constraint.

        Returns:
            Pointer to the created :class:`~superdex.physics.Constraint`, or None on
            error.

        Raises:
            :class:`~superdex.physics.Error`: If an error occurs.

        See Also:
            :class:`ROD_ELEMENT_ROTATION_TO_RIGID <superdex.physics.ConstraintType>`,
            :class:`~superdex.physics.RodElementRotationToRigidConstraintParams`,
            :meth:`~superdex.physics.Scene.destroy_constraint`
        """
    @overload
    def create_rod_element_rotation_to_rigid_constraint(
        self,
        stiffness: float = ...,
        damping: float = ...,
        saturation: float = ...,
        ref_frame_rot_vec: Real3Like = ...,
        rigid_actor: ActorHandle = ...,
        rod_actor: ActorHandle = ...,
        element_index: int = ...,
    ) -> Optional[Constraint]: ...
    @overload
    def create_rigid_pivot_position_constraint(
        self,
        params: RigidPivotPositionConstraintParams,
    ) -> Optional[Constraint]:
        """Create a position constraint on a rigid actor's pivot point.

        Args:
            params (RigidPivotPositionConstraintParams): Parameters defining the rigid
                pivot position constraint.

        Returns:
            Pointer to the created :class:`~superdex.physics.Constraint`, or None on
            error.

        Raises:
            :class:`~superdex.physics.Error`: If an error occurs.

        See Also:
            :class:`RIGID_PIVOT_POSITION <superdex.physics.ConstraintType>`,
            :class:`~superdex.physics.RigidPivotPositionConstraintParams`,
            :meth:`~superdex.physics.Scene.destroy_constraint`
        """
    @overload
    def create_rigid_pivot_position_constraint(
        self,
        stiffness: float = ...,
        damping: float = ...,
        saturation: float = ...,
        target_position: Real3Like = ...,
        local_position: Real3Like = ...,
        actor: ActorHandle = ...,
    ) -> Optional[Constraint]: ...
    @overload
    def create_rigid_pivot_to_rigid_target_constraint(
        self,
        params: RigidPivotToRigidTargetConstraintParams,
    ) -> Optional[Constraint]:
        """Create a position constraint between a rigid actor's pivot point and the
        world
        position of that pivot under a target center-of-mass transform.

        Args:
            params (RigidPivotToRigidTargetConstraintParams): Parameters defining the
                rigid pivot to rigid target constraint.

        Returns:
            Pointer to the created :class:`~superdex.physics.Constraint`, or None on
            error.

        Raises:
            :class:`~superdex.physics.Error`: If an error occurs.

        See Also:
            :class:`RIGID_PIVOT_TO_RIGID_TARGET <superdex.physics.ConstraintType>`,
            :class:`~superdex.physics.RigidPivotToRigidTargetConstraintParams`,
            :meth:`~superdex.physics.Scene.destroy_constraint`
        """
    @overload
    def create_rigid_pivot_to_rigid_target_constraint(
        self,
        stiffness: float = ...,
        damping: float = ...,
        saturation: float = ...,
        target_transform: TransformRT = ...,
        local_position: Real3Like = ...,
        actor: ActorHandle = ...,
    ) -> Optional[Constraint]: ...
    @overload
    def create_rigid_pivot_rotation_constraint(
        self,
        params: RigidPivotRotationConstraintParams,
    ) -> Optional[Constraint]:
        """Create a rotation constraint on a rigid actor's pivot frame.

        Args:
            params (RigidPivotRotationConstraintParams): Parameters defining the rigid
                pivot rotation constraint.

        Returns:
            Pointer to the created :class:`~superdex.physics.Constraint`, or None on
            error.

        Raises:
            :class:`~superdex.physics.Error`: If an error occurs.

        See Also:
            :class:`RIGID_PIVOT_ROTATION <superdex.physics.ConstraintType>`,
            :class:`~superdex.physics.RigidPivotRotationConstraintParams`,
            :meth:`~superdex.physics.Scene.destroy_constraint`
        """
    @overload
    def create_rigid_pivot_rotation_constraint(
        self,
        stiffness: float = ...,
        damping: float = ...,
        saturation: float = ...,
        target_rotation: Real3Like = ...,
        local_rotation: Real3Like = ...,
        actor: ActorHandle = ...,
    ) -> Optional[Constraint]: ...
    @overload
    def create_rigid_prismatic_joint_constraint(
        self,
        params: RigidPrismaticJointConstraintParams,
    ) -> Optional[Constraint]:
        """Create a prismatic joint constraint between two rigid actors.

        Args:
            params (RigidPrismaticJointConstraintParams): Parameters defining the
                prismatic joint constraint.

        Returns:
            Pointer to the created :class:`~superdex.physics.Constraint`, or None on
            error.

        Raises:
            :class:`~superdex.physics.Error`: If an error occurs.

        See Also:
            :class:`RIGID_PRISMATIC_JOINT <superdex.physics.ConstraintType>`,
            :class:`~superdex.physics.RigidPrismaticJointConstraintParams`,
            :meth:`~superdex.physics.Scene.destroy_constraint`
        """
    @overload
    def create_rigid_prismatic_joint_constraint(
        self,
        stiffness: float = ...,
        damping: float = ...,
        saturation: float = ...,
        free_axis: Real3Like = ...,
        actor_a: ActorHandle = ...,
        actor_b: ActorHandle = ...,
        max: Optional[float] = ...,
        min: Optional[float] = ...,
    ) -> Optional[Constraint]: ...
    @overload
    def create_rigid_spherical_joint_constraint(
        self,
        params: RigidSphericalJointConstraintParams,
    ) -> Optional[Constraint]:
        """Create a spherical joint constraint between two rigid actors.

        Args:
            params (RigidSphericalJointConstraintParams): Parameters defining the
                spherical joint constraint.

        Returns:
            Pointer to the created :class:`~superdex.physics.Constraint`, or None on
            error.

        Raises:
            :class:`~superdex.physics.Error`: If an error occurs.

        See Also:
            :class:`RIGID_SPHERICAL_JOINT <superdex.physics.ConstraintType>`,
            :class:`~superdex.physics.RigidSphericalJointConstraintParams`,
            :meth:`~superdex.physics.Scene.destroy_constraint`
        """
    @overload
    def create_rigid_spherical_joint_constraint(
        self,
        stiffness: float = ...,
        damping: float = ...,
        saturation: float = ...,
        local_pos_a: Real3Like = ...,
        local_pos_b: Real3Like = ...,
        actor_a: ActorHandle = ...,
        actor_b: ActorHandle = ...,
    ) -> Optional[Constraint]: ...
    @overload
    def create_deformable_node_position_constraint(
        self,
        params: DeformableNodePositionConstraintParams,
    ) -> Optional[Constraint]:
        """Create a position constraint on a deformable actor node.

        Args:
            params (DeformableNodePositionConstraintParams): Parameters defining the
                deformable node position constraint.

        Returns:
            Pointer to the created :class:`~superdex.physics.Constraint`, or None on
            error.

        Raises:
            :class:`~superdex.physics.Error`: If an error occurs.

        See Also:
            :class:`DEFORMABLE_NODE_POSITION <superdex.physics.ConstraintType>`,
            :class:`~superdex.physics.DeformableNodePositionConstraintParams`,
            :meth:`~superdex.physics.Scene.destroy_constraint`
        """
    @overload
    def create_deformable_node_position_constraint(
        self,
        stiffness: float = ...,
        damping: float = ...,
        saturation: float = ...,
        node_index: int = ...,
        position: Real3Like = ...,
        actor: ActorHandle = ...,
    ) -> Optional[Constraint]: ...
    @overload
    def create_deformable_node_to_deformable_node_constraint(
        self,
        params: DeformableNodeToDeformableNodeConstraintParams,
    ) -> Optional[Constraint]:
        """Create a constraint connecting two deformable actor nodes.

        Args:
            params (DeformableNodeToDeformableNodeConstraintParams): Parameters defining
                the deformable-node-to-deformable-node constraint.

        Returns:
            Pointer to the created :class:`~superdex.physics.Constraint`, or None on
            error.

        Raises:
            :class:`~superdex.physics.Error`: If an error occurs.

        See Also:
            :class:`DEFORMABLE_NODE_TO_DEFORMABLE_NODE
            <superdex.physics.ConstraintType>`,
            :class:`~superdex.physics.DeformableNodeToDeformableNodeConstraintParams`,
            :meth:`~superdex.physics.Scene.destroy_constraint`
        """
    @overload
    def create_deformable_node_to_deformable_node_constraint(
        self,
        stiffness: float = ...,
        damping: float = ...,
        saturation: float = ...,
        node_index_a: int = ...,
        node_index_b: int = ...,
        actor_a: ActorHandle = ...,
        actor_b: ActorHandle = ...,
        find_closest: bool = ...,
    ) -> Optional[Constraint]: ...
    @overload
    def create_deformable_node_to_rigid_constraint(
        self,
        params: DeformableNodeToRigidConstraintParams,
    ) -> Optional[Constraint]:
        """Create a constraint connecting a deformable node to a rigid actor.

        Args:
            params (DeformableNodeToRigidConstraintParams): Parameters defining the
                deformable-node-to-rigid constraint.

        Returns:
            Pointer to the created :class:`~superdex.physics.Constraint`, or None on
            error.

        Raises:
            :class:`~superdex.physics.Error`: If an error occurs.

        See Also:
            :class:`DEFORMABLE_NODE_TO_RIGID <superdex.physics.ConstraintType>`,
            :class:`~superdex.physics.DeformableNodeToRigidConstraintParams`,
            :meth:`~superdex.physics.Scene.destroy_constraint`
        """
    @overload
    def create_deformable_node_to_rigid_constraint(
        self,
        stiffness: float = ...,
        damping: float = ...,
        saturation: float = ...,
        rigid_local_pos: Real3Like = ...,
        deformable_node_index: int = ...,
        rigid_actor: ActorHandle = ...,
        deformable_actor: ActorHandle = ...,
        find_closest: bool = ...,
        fix_to_deformable_pos: bool = ...,
    ) -> Optional[Constraint]: ...
    def get_constraint(self, constraint: ConstraintHandle) -> Optional[Constraint]:
        """Get the :class:`~superdex.physics.Constraint` pointer associated with a
        :class:`~superdex.physics.ConstraintHandle`.

        Args:
            constraint (ConstraintHandle): Handle of the constraint to retrieve. If
                valid, it must be owned by this scene.

        Returns:
            Pointer to the :class:`~superdex.physics.Constraint`, or None if the handle
            does not currently identify a constraint in this scene.

        See Also:
            :class:`~superdex.physics.Constraint`,
            :class:`~superdex.physics.ConstraintHandle`,
            :meth:`~superdex.physics.Scene.destroy_constraint`,
            :meth:`~superdex.physics.Constraint.get_handle`
        """
    @overload
    def destroy_constraint(self, constraint: Optional[Constraint]) -> None:
        """Destroy a constraint and remove it from the scene.

        Args:
            constraint (Optional[Constraint]): Pointer to the constraint to destroy.

        Note:
            If ``constraint`` is None, this function has no effect.

        Note:
            Use this function to destroy constraints created through the scene's
            constraint-creation APIs. It has no effect on constraints created
            automatically while creating or configuring an actor, such as joint-limit,
            cycle-joint, or pose-controller constraints. To remove such a constraint,
            remove the corresponding actor feature, if supported, or destroy the actor.

        Note:
            After the constraint is destroyed, do not use the pointer or its handle.

        Warning:
            The pointer must be None or point to a live constraint owned by this scene.
            Passing a pointer to a constraint that has been destroyed is undefined
            behavior.

        See Also:
            :meth:`~superdex.physics.Scene.get_constraint`,
            :meth:`~superdex.physics.Constraint.get_handle`
        """
    @overload
    def destroy_constraint(self, constraint: ConstraintHandle) -> None:
        """Destroy a constraint and remove it from the scene.

        Args:
            constraint (ConstraintHandle): Handle of the constraint to destroy. If
                valid, it must be owned by this scene.

        Note:
            An invalid handle or one that does not currently identify a constraint in
            the scene has no effect.

        Note:
            Use this function to destroy constraints created through the scene's
            constraint-creation APIs. It has no effect on constraints created
            automatically while creating or configuring an actor, such as joint-limit,
            cycle-joint, or pose-controller constraints. To remove such a constraint,
            remove the corresponding actor feature, if supported, or destroy the actor.

        Note:
            After the constraint is destroyed, do not use its handle.

        See Also:
            :meth:`~superdex.physics.Scene.get_constraint`,
            :meth:`~superdex.physics.Constraint.get_handle`
        """
    def get_num_constraints(self) -> int:
        """Get the number of constraints in the scene.

        Returns:
            Number of constraints in the scene.

        Note:
            Includes constraints created implicitly by
            :meth:`~superdex.physics.Scene.create_articulated_actor` (e.g. articulated
            joint limits).
        """
    def for_each_constraint(self, callback: Callable[[Constraint], None]) -> None:
        """Iterate over all the constraints in the scene using a callback.

        Args:
            callback (Callable[[Constraint], None]): Function called once for each
                constraint.

        Note:
            Includes constraints created implicitly by
            :meth:`~superdex.physics.Scene.create_articulated_actor` (e.g. articulated
            joint limits).

        Warning:
            Destroying other constraints or actors from within the callback is illegal.
        """
    def get_debug_draw(self) -> DebugDraw:
        """Get the debug draw interface for this scene.

        Returns:
            Reference to the :class:`~superdex.physics.DebugDraw` interface.

        See Also:
            :class:`~superdex.physics.DebugDraw`
        """
    def update_debugger(self) -> None:
        """This gives the debugger a chance to process messages that require reading or
        writing scene state.

        Please call this periodically if you are not actively stepping the scene. If
        this function is never called and the scene is never stepped, then some debugger
        features will not work.

        Note:
            Must be called on the scene's owning thread (like other
            :class:`~superdex.physics.Scene` methods).
        """
    def is_recording(self) -> bool:
        """Check if the scene is currently recording.

        Returns:
            True if recording is active, false otherwise.

        Note:
            A recording write failure does not end the recording session.

        See Also:
            :meth:`~superdex.physics.Scene.start_recording`,
            :meth:`~superdex.physics.Scene.stop_recording`
        """
    def start_recording(self, file_path: str, params: RecordingParams) -> None:
        """Create or replace the specified file and start recording the scene to an HDF5
        file.

        Args:
            file_path (str): Path to the output recording file (case sensitive on some
                filesystems). File will be created or replaced.
            params (RecordingParams): Parameters controlling what data to record.

        Raises:
            :class:`~superdex.physics.Error`: If an error occurs.

        Note:
            Enabling additional recording features will generally increase the recording
            file size and degrade performance.

        Note:
            If the scene is already recording, the previous recording is stopped before
            starting the new one.

        Warning:
            Requires a build with HDF5 support.

        Warning:
            The recording system may undergo a substantial refactor to expand its
            capabilities. Its API and recorded file format may change in future
            releases.

        See Also:
            :meth:`~superdex.physics.Scene.stop_recording`,
            :meth:`~superdex.physics.Scene.is_recording`,
            :class:`~superdex.physics.RecordingParams`
        """
    def stop_recording(self) -> None:
        """Stop recording the scene and close the recording file.

        See Also:
            :meth:`~superdex.physics.Scene.start_recording`,
            :meth:`~superdex.physics.Scene.is_recording`
        """
    def enable_layer_contact_asymmetric(
        self,
        layer_a: str,
        layer_b: str,
        enable: bool,
    ) -> None:
        """Enable or disable collision detection for an ordered pair of contact layer
        names. Order matters: controls only the specified direction (actors in layerA
        checking against actors in layerB).

        Args:
            layer_a (str): Name of the layer of the "colliding" actor (the one checking
                for contact). Must be non-empty.
            layer_b (str): Name of the layer of the "collider" actor (the one with a
                :class:`~superdex.physics.ColliderType` being tested). Must be
                non-empty.
            enable (bool): True to enable contact, false to disable it.

        Raises:
            :class:`~superdex.physics.Error`: If an error occurs.

        Note:
            Contact is enabled by default. You only need to call this method if you wish
            to disable (or re-enable) contact for a specific layer pair.

        Note:
            Both layer-to-layer and actor-to-actor contact must be enabled for contact
            to occur.

        Note:
            Call twice with swapped arguments or use
            :meth:`~superdex.physics.Scene.enable_layer_contact_symmetric` to affect
            collision checks in both directions.

        Note:
            Calling this method with a layer name not previously seen in the scene
            registers it as a new contact layer, increasing
            :meth:`~superdex.physics.Scene.get_num_contact_layers` and adding the name
            to :meth:`~superdex.physics.Scene.enumerate_contact_layer_names`.

        See Also:
            :meth:`~superdex.physics.Scene.enable_layer_contact_symmetric`,
            :meth:`~superdex.physics.Scene.enable_actor_contact_asymmetric`,
            :meth:`~superdex.physics.Scene.is_layer_contact_enabled`
        """
    def enable_layer_contact_symmetric(
        self,
        layer_a: str,
        layer_b: str,
        enable: bool,
    ) -> None:
        """Equivalent to calling
        :meth:`~superdex.physics.Scene.enable_layer_contact_asymmetric` with ``layer_a``
        and ``layer_b`` in both orders.

        Args:
            layer_a (str): Name of the first contact layer. Must be non-empty.
            layer_b (str): Name of the second contact layer. Must be non-empty.
            enable (bool): True to enable contact, false to disable it.

        Raises:
            :class:`~superdex.physics.Error`: If an error occurs.

        See Also:
            :meth:`~superdex.physics.Scene.enable_layer_contact_asymmetric`,
            :meth:`~superdex.physics.Scene.enable_actor_contact_symmetric`,
            :meth:`~superdex.physics.Scene.is_layer_contact_enabled`
        """
    def is_layer_contact_enabled(self, layer_a: str, layer_b: str) -> bool:
        """Check if collision detection is enabled for an ordered pair of contact layer
        names. Order matters: refers only to the specified direction (actors in layerA
        checking against actors in layerB).

        Args:
            layer_a (str): Name of the layer of the "colliding" actor (the one checking
                for contact).
            layer_b (str): Name of the layer of the "collider" actor (the one with a
                :class:`~superdex.physics.ColliderType` being tested).

        Returns:
            True if layer-to-layer contact is enabled, false if layer-to-layer contact
            is disabled.

        Note:
            Contact is enabled by default, so you will get true for any pair of layers
            unless overridden via
            :meth:`~superdex.physics.Scene.enable_layer_contact_asymmetric` or
            :meth:`~superdex.physics.Scene.enable_layer_contact_symmetric`.

        Note:
            This only checks the layer filter. Actor-to-actor contact can still be
            disabled with
            :meth:`~superdex.physics.Scene.enable_actor_contact_asymmetric` or
            :meth:`~superdex.physics.Scene.enable_actor_contact_symmetric`.

        See Also:
            :meth:`~superdex.physics.Scene.enable_layer_contact_asymmetric`,
            :meth:`~superdex.physics.Scene.enable_layer_contact_symmetric`
        """
    def get_num_contact_layers(self) -> int:
        """Get the number of unique contact layer names in the scene.

        Returns:
            Number of unique layer names in the scene.

        Note:
            The count may include layer names not currently assigned to an actor.

        See Also:
            :meth:`~superdex.physics.Scene.enumerate_contact_layer_names`
        """
    def enumerate_contact_layer_names(self, callback: Callable[[str], None]) -> None:
        """Enumerate all unique contact layer names in the scene.

        Args:
            callback (Callable[[str], None]): Function called once for each layer name.

        Note:
            The callback fires :meth:`~superdex.physics.Scene.get_num_contact_layers`
            times.

        Note:
            Enumeration order is unspecified.

        Warning:
            Do not register new contact layers from inside the callback. Doing so may
            invalidate the enumeration and cause undefined behavior.

        See Also:
            :meth:`~superdex.physics.Scene.get_num_contact_layers`
        """
    def enable_actor_contact_asymmetric(
        self,
        colliding: ActorHandle,
        collider: ActorHandle,
        enable: bool,
        include_nested_actors: IncludeNestedActors | int,
    ) -> None:
        """Enable or disable collision detection between two actors. Order matters:
        controls only the specified direction (colliding actor checking against collider
        actor).

        Args:
            colliding (ActorHandle): Handle of the colliding actor.
            collider (ActorHandle): Handle of the collider actor.
            enable (bool): True to enable contact, false to disable it.
            include_nested_actors (IncludeNestedActors | int): Whether nested actors
                should be affected by this contact setting. With :class:`NO
                <superdex.physics.IncludeNestedActors>`, only the exact handles are
                affected. With :class:`YES <superdex.physics.IncludeNestedActors>`,
                parent actors with nested actors resolve to the parent plus nested
                actors. The setting is applied to every ordered pair in the
                cross-product of the two resolved handle sets; no pairs outside that
                cross-product are affected. If the resolved sets overlap, pairs in the
                overlap, including self-pairs, are affected.

        Raises:
            :class:`~superdex.physics.Error`: If an error occurs.

        Note:
            Contact is enabled by default. You only need to call this method if you wish
            to disable (or re-enable) contact for a specific actor pair.

        Note:
            Both layer-to-layer and actor-to-actor contact must be enabled for contact
            to occur.

        Note:
            Contact between adjacent links is automatically disabled when an articulated
            or soft-skinned actor is created. A later actor-contact setting can override
            that automatic disable for any pair included in the resolved actor sets. In
            particular, enabling contact between a parent actor and itself with
            :class:`YES <superdex.physics.IncludeNestedActors>` enables contact between
            that parent's nested actors, including adjacent links, unless a later
            actor-contact setting disables those pairs again.

        Note:
            Call twice with swapped arguments or use
            :meth:`~superdex.physics.Scene.enable_actor_contact_symmetric` to affect
            collision checks in both directions.

        See Also:
            :meth:`~superdex.physics.Scene.enable_actor_contact_symmetric`,
            :meth:`~superdex.physics.Scene.enable_layer_contact_asymmetric`
        """
    def enable_actor_contact_symmetric(
        self,
        actor_a: ActorHandle,
        actor_b: ActorHandle,
        enable: bool,
        include_nested_actors: IncludeNestedActors | int,
    ) -> None:
        """Equivalent to calling
        :meth:`~superdex.physics.Scene.enable_actor_contact_asymmetric` with ``actor_a``
        and ``actor_b`` in both orders using the same ``include_nested_actors``.

        Args:
            actor_a (ActorHandle): Handle of the first actor.
            actor_b (ActorHandle): Handle of the second actor.
            enable (bool): True to enable contact, false to disable it.
            include_nested_actors (IncludeNestedActors | int): Whether nested actors
                should be affected by this contact setting. See
                :meth:`~superdex.physics.Scene.enable_actor_contact_asymmetric` for
                details.

        Raises:
            :class:`~superdex.physics.Error`: If an error occurs.

        See Also:
            :meth:`~superdex.physics.Scene.enable_actor_contact_asymmetric`,
            :meth:`~superdex.physics.Scene.enable_layer_contact_symmetric`
        """
    def set_contact_pair_params_override(
        self,
        actor_a: ActorHandle,
        actor_b: ActorHandle,
        params_override: ContactPairParamsOverride,
    ) -> None:
        """Set contact parameter overrides for an unordered actor pair.

        Args:
            actor_a (ActorHandle): Handle of the first actor.
            actor_b (ActorHandle): Handle of the second actor.
            params_override (ContactPairParamsOverride): Parameter override with at
                least one present field. This replaces any existing override for the
                pair; absent fields use the normal actor-parameter combination.

        Raises:
            :class:`~superdex.physics.Error`: If an error occurs.

        Note:
            Both actors must have contact parameters.

        Note:
            The exact actors are used; nested actors are not included automatically.

        See Also:
            :meth:`~superdex.physics.Scene.clear_contact_pair_params_override`,
            :meth:`~superdex.physics.Scene.has_contact_pair_params_override`,
            :meth:`~superdex.physics.Scene.get_contact_pair_params_override`
        """
    def clear_contact_pair_params_override(
        self,
        actor_a: ActorHandle,
        actor_b: ActorHandle,
    ) -> None:
        """Clear contact parameter overrides for an unordered actor pair.

        Args:
            actor_a (ActorHandle): Handle of the first actor.
            actor_b (ActorHandle): Handle of the second actor.

        Raises:
            :class:`~superdex.physics.Error`: If an error occurs.

        Note:
            Clearing a valid pair without an override succeeds without changing the
            scene.

        See Also:
            :meth:`~superdex.physics.Scene.set_contact_pair_params_override`
        """
    def has_contact_pair_params_override(
        self,
        actor_a: ActorHandle,
        actor_b: ActorHandle,
    ) -> bool:
        """Check whether the exact unordered actor pair has a parameter override.

        Args:
            actor_a (ActorHandle): Handle of the first actor.
            actor_b (ActorHandle): Handle of the second actor.

        Returns:
            True if the exact pair has a stored override.

        Raises:
            :class:`~superdex.physics.Error`: If an error occurs.

        See Also:
            :meth:`~superdex.physics.Scene.get_contact_pair_params_override`
        """
    def get_contact_pair_params_override(
        self,
        actor_a: ActorHandle,
        actor_b: ActorHandle,
    ) -> ContactPairParamsOverride:
        """Get the parameter override for the exact unordered actor pair.

        Args:
            actor_a (ActorHandle): Handle of the first actor.
            actor_b (ActorHandle): Handle of the second actor.

        Returns:
            The complete stored override.

        Raises:
            :class:`~superdex.physics.Error`: If an error occurs.

        Note:
            Reports an error when a valid pair has no stored override.

        See Also:
            :meth:`~superdex.physics.Scene.set_contact_pair_params_override`,
            :meth:`~superdex.physics.Scene.has_contact_pair_params_override`
        """
    def register_pre_step_callback(
        self,
        debug_name: str,
        callback: Callable[[StepInfo], None],
        priority: int = ...,
    ) -> CallbackHandle:
        """Register a callback to execute before each simulation step.

        Args:
            debug_name (str): Descriptive name for debugging and profiling.
            callback (Callable[[StepInfo], None]): Callback function receiving
                :class:`~superdex.physics.StepInfo`.
            priority (int): Execution order. Lower values execute first (higher
                priority).

        Returns:
            :class:`~superdex.physics.CallbackHandle` for the registered callback.

        Note:
            Callbacks run sequentially on the thread that calls
            :meth:`~superdex.physics.Scene.step`, in increasing priority order (lower
            values first).

        Note:
            The relative order of callbacks registered with equal priority is
            unspecified. Assign distinct priorities when ordering matters.

        Note:
            Callbacks must be thread-safe if they access shared state.

        Note:
            Callbacks should not modify scene structure (add/remove actors/constraints).

        See Also:
            :meth:`~superdex.physics.Scene.cancel_callback`,
            :meth:`~superdex.physics.Scene.register_post_step_callback`
        """
    def register_post_step_callback(
        self,
        debug_name: str,
        callback: Callable[[StepInfo], None],
        priority: int = ...,
    ) -> CallbackHandle:
        """Register a callback to execute after each simulation step.

        Args:
            debug_name (str): Descriptive name for debugging and profiling.
            callback (Callable[[StepInfo], None]): Callback function receiving
                :class:`~superdex.physics.StepInfo`.
            priority (int): Execution order. Lower values execute first (higher
                priority).

        Returns:
            :class:`~superdex.physics.CallbackHandle` for the registered callback.

        Note:
            Callbacks run sequentially on the thread that calls
            :meth:`~superdex.physics.Scene.step`, in increasing priority order (lower
            values first).

        Note:
            The relative order of callbacks registered with equal priority is
            unspecified. Assign distinct priorities when ordering matters.

        Note:
            Callbacks must be thread-safe if they access shared state.

        Note:
            Callbacks should not modify scene structure (add/remove actors/constraints).

        See Also:
            :meth:`~superdex.physics.Scene.cancel_callback`,
            :meth:`~superdex.physics.Scene.register_pre_step_callback`
        """
    def cancel_callback(self, handle: CallbackHandle) -> None:
        """Cancel a previously registered callback.

        Args:
            handle (CallbackHandle): :class:`~superdex.physics.CallbackHandle` from
                :meth:`~superdex.physics.Scene.register_pre_step_callback` or
                :meth:`~superdex.physics.Scene.register_post_step_callback`. Must be
                owned by this scene.

        Note:
            Invalid and already-cancelled handles are ignored.

        See Also:
            :meth:`~superdex.physics.Scene.register_pre_step_callback`,
            :meth:`~superdex.physics.Scene.register_post_step_callback`
        """

DEFAULT_GRAVITY: Real3
"""Default gravity acceleration in world frame [m/s²]."""

class DebugDraw:
    """Manages debug visualization features for a scene.

    Provides a feature-based system for visualizing various aspects of the physics
    simulation. The client application is responsible for rendering the generated
    primitives using its own graphics API. Features can be individually enabled or
    disabled, such as visualizing contact points, normals, velocities, or AABBs.

    Note:
        Must be accessed on the same thread as the owning
        :class:`~superdex.physics.Scene`.

    Note:
        Debug draw is disabled by default.

    Note:
        If consuming the data on a different thread, the client must deep-copy the
        underlying data into client-owned buffers before the scene thread calls
        :meth:`gather_data` again or modifies the :class:`~superdex.physics.Scene`.
        Copying :class:`~superdex.physics.DebugDrawData` itself is a shallow copy of
        ``mochi::Span`` views into internal buffers and does not transfer ownership.

    Warning:
        This documentation references the following C++ API, which is not available
        in Python: ``mochi::Span``.

    Warning:
        Debug drawing has significant performance overhead due to geometry
        generation and data gathering.

    See Also:
        :meth:`~superdex.physics.Scene.get_debug_draw`,
        :class:`~superdex.physics.DebugDrawData`
    """
    def is_enabled(self) -> bool:
        """Check if debug drawing is enabled for the scene.

        Returns:
            True if debug drawing is enabled, false otherwise.

        Note:
            When disabled, :meth:`~superdex.physics.DebugDraw.gather_data` returns empty
            data regardless of feature settings.

        See Also:
            :meth:`~superdex.physics.DebugDraw.enable`
        """
    def enable(self, enable: bool) -> None:
        """Enable or disable debug drawing for the entire scene.

        Args:
            enable (bool): True to enable, false to disable.

        Note:
            This is a master switch that overrides all individual feature settings.

        See Also:
            :meth:`~superdex.physics.DebugDraw.is_enabled`
        """
    def get_num_features(self) -> int:
        """Get the number of debug draw features available.

        Returns:
            Number of features that can be individually enabled or disabled.

        See Also:
            :meth:`~superdex.physics.DebugDraw.find_feature`,
            :meth:`~superdex.physics.DebugDraw.get_feature_name`
        """
    def find_feature(self, name: str) -> int:
        """Find a feature by name.

        Args:
            name (str): Feature name (case sensitive).

        Returns:
            Feature index if found, or -1 if not found.

        See Also:
            :meth:`~superdex.physics.DebugDraw.get_feature_name`,
            :meth:`~superdex.physics.DebugDraw.enable_feature`
        """
    def get_feature_name(self, index: int) -> str:
        """Get the name of a feature by index.

        Args:
            index (int): Feature index in the range [0,
                :meth:`~superdex.physics.DebugDraw.get_num_features`).

        Returns:
            Name of the feature.

        See Also:
            :meth:`~superdex.physics.DebugDraw.get_num_features`,
            :meth:`~superdex.physics.DebugDraw.find_feature`
        """
    def get_feature_description(self, index: int) -> str:
        """Get the description of a feature by index.

        Args:
            index (int): Feature index in the range [0,
                :meth:`~superdex.physics.DebugDraw.get_num_features`).

        Returns:
            Human-readable description of the feature.

        See Also:
            :meth:`~superdex.physics.DebugDraw.get_num_features`,
            :meth:`~superdex.physics.DebugDraw.get_feature_name`
        """
    def is_feature_enabled(self, index: int) -> bool:
        """Check if a feature is currently enabled.

        Args:
            index (int): Feature index in the range [0,
                :meth:`~superdex.physics.DebugDraw.get_num_features`).

        Returns:
            True if the feature is enabled, false otherwise.

        See Also:
            :meth:`~superdex.physics.DebugDraw.enable_feature`
        """
    def enable_feature(self, index: int, enable: bool) -> None:
        """Enable or disable a specific feature.

        Args:
            index (int): Feature index in the range [0,
                :meth:`~superdex.physics.DebugDraw.get_num_features`).
            enable (bool): True to enable the feature, false to disable.

        Note:
            No debug-draw data is generated while the master switch
            (:meth:`~superdex.physics.DebugDraw.is_enabled`) is false, but per-feature
            settings are remembered and take effect when the master switch is enabled.

        See Also:
            :meth:`~superdex.physics.DebugDraw.is_feature_enabled`,
            :meth:`~superdex.physics.DebugDraw.enable`
        """
    def gather_data(self) -> DebugDrawData:
        """Generate debug draw primitives for all currently enabled features.

        Returns:
            Collection of primitives to be rendered by the client application. The
            returned :class:`~superdex.physics.DebugDrawData` contains Spans that
            reference internal buffers.

        Note:
            Data is valid only until the next call to
            :meth:`~superdex.physics.DebugDraw.gather_data` or any modification to the
            :class:`~superdex.physics.Scene`.

        Note:
            Returns empty data if :meth:`~superdex.physics.DebugDraw.is_enabled` is
            false.

        See Also:
            :class:`~superdex.physics.DebugDrawData`
        """
    def enable_actor(self, actor: ActorHandle, enable: bool) -> None:
        """Enable or disable debug drawing for a specific actor.

        Args:
            actor (ActorHandle): Handle of the actor to enable or disable debug drawing
                for.
            enable (bool): True to enable, false to disable.

        Raises:
            :class:`~superdex.physics.Error`: If an error occurs.

        Note:
            Enabled by default for all actors, including nested actors (child actors of
            articulated, soft-skinned and blended actors).

        Note:
            Debug draw data won't be generated for any actor if the master switch, i.e.
            :meth:`~superdex.physics.DebugDraw.is_enabled`, is false.
        """

class DebugServer:
    """Server API to enable remote debugging and visualization.

    See Also:
        :func:`~superdex.physics.get_debug_server`
    """
    def start(self, preferred_port: int = ...) -> None:
        """Start accepting client connections on a TCP port.

        Args:
            preferred_port (int): Preferred TCP port to listen on. If the port is
                already in use, the next available port is chosen automatically.

        Note:
            No-op if the server has already been started.

        See Also:
            :meth:`~superdex.physics.DebugServer.stop`,
            :meth:`~superdex.physics.DebugServer.has_started`,
            :meth:`~superdex.physics.DebugServer.get_port`
        """
    def stop(self) -> None:
        """Stop the server and disconnect any connected client.

        Note:
            No-op if the server has not started.

        See Also:
            :meth:`~superdex.physics.DebugServer.start`,
            :meth:`~superdex.physics.DebugServer.has_started`
        """
    def has_started(self) -> bool:
        """Check whether the server has started.

        Returns:
            True if :meth:`~superdex.physics.DebugServer.start` has been called, even if
            the server is not actually able to accept connections due to issues with
            permissions or firewalls.

        See Also:
            :meth:`~superdex.physics.DebugServer.start`,
            :meth:`~superdex.physics.DebugServer.stop`
        """
    def has_connection(self) -> bool:
        """Check whether a debugger client is currently connected.

        Returns:
            True if any debugger client is connected.

        See Also:
            :meth:`~superdex.physics.DebugServer.start`,
            :meth:`~superdex.physics.DebugServer.stop`
        """
    def get_port(self) -> int:
        """Get the actual TCP port the server is listening on.

        May differ from the port requested in
        :meth:`~superdex.physics.DebugServer.start` if that port was already in use.

        Returns:
            The TCP port currently in use, or 0 if the server is not listening on a TCP
            socket.

        See Also:
            :meth:`~superdex.physics.DebugServer.start`
        """
    def set_coordinate_space(self, space: CoordinateSpace) -> None:
        """Declare the coordinate space this application's scene data is expressed in.

        Sent to the debugger client so it can render the scene in the correct
        orientation. This is purely descriptive metadata: it does not affect simulation.
        The default is X-right, Y-up, Z-backward.

        Args:
            space (CoordinateSpace): Coordinate space convention and unit scale of this
                application's scenes.

        Note:
            Must be called before :meth:`~superdex.physics.DebugServer.start`.

        See Also:
            :meth:`~superdex.physics.DebugServer.start`
        """

DEFAULT_BACK_PROPAGATION_EPS_FINITE_DIFF: float
"""Default finite-difference epsilon used by back-propagation solvers."""

TRANSFORM_RT_IDENTITY: TransformRT
"""Identity transform (no rotation, no translation)."""

QUATERNION_IDENTITY: Quaternion
"""Identity quaternion (no rotation)."""

GRID_SDF_PARAMS_DEFAULT: GridSdfParams
"""Default grid SDF construction parameters."""

# Core types (defined in mochi_physics_pybind.cpp)
class Error(RuntimeError):
    '''Exception raised when a SuperDex Physics operation fails.'''
    ...

class Int3:
    '''Fixed-size three-element integer array.'''
    @overload
    def __init__(self) -> None: ...
    @overload
    def __init__(self, x: int = ..., y: int = ..., z: int = ...) -> None: ...
    @overload
    def __init__(self, seq: Sequence[int]) -> None: ...
    @overload
    def __init__(self, seq: npt.NDArray[np.signedinteger]) -> None: ...
    def __getitem__(self, index: int) -> int: ...
    def __setitem__(self, index: int, value: int) -> None: ...
    def __len__(self) -> int: ...
    def __repr__(self) -> str: ...
    def __str__(self) -> str: ...
    def __eq__(self, other: object) -> bool: ...
    def __ne__(self, other: object) -> bool: ...
    def __array__(self, dtype: Any = ..., copy: Any = ...) -> Any: ...
    def tolist(self) -> list[int]: ...

type Int3Like = Int3 | Sequence[int] | npt.NDArray[np.signedinteger]

class Real2:
    '''Fixed-size two-element floating-point array.'''
    @overload
    def __init__(self) -> None: ...
    @overload
    def __init__(self, x: float = ..., y: float = ...) -> None: ...
    @overload
    def __init__(self, seq: Sequence[float]) -> None: ...
    @overload
    def __init__(self, seq: npt.NDArray[np.floating]) -> None: ...
    def __getitem__(self, index: int) -> float: ...
    def __setitem__(self, index: int, value: float) -> None: ...
    def __len__(self) -> int: ...
    def __repr__(self) -> str: ...
    def __str__(self) -> str: ...
    def __add__(self, other: Real2) -> Real2: ...
    def __sub__(self, other: Real2) -> Real2: ...
    def __mul__(self, other: Real2 | float) -> Real2: ...
    def __truediv__(self, other: Real2 | float) -> Real2: ...
    def __radd__(self, other: float) -> Real2: ...
    def __rsub__(self, other: float) -> Real2: ...
    def __rmul__(self, other: float) -> Real2: ...
    def __rtruediv__(self, other: float) -> Real2: ...
    def __iadd__(self, other: Real2 | float) -> Real2: ...
    def __isub__(self, other: Real2 | float) -> Real2: ...
    def __imul__(self, other: Real2 | float) -> Real2: ...
    def __itruediv__(self, other: Real2 | float) -> Real2: ...
    def __neg__(self) -> Real2: ...
    def __eq__(self, other: object) -> bool: ...
    def __ne__(self, other: object) -> bool: ...
    def __array__(self, dtype: Any = ..., copy: Any = ...) -> Any: ...
    def tolist(self) -> list[float]: ...

type Real2Like = Real2 | Sequence[float] | npt.NDArray[np.floating]

class Real3:
    '''Fixed-size three-element floating-point array.'''
    @overload
    def __init__(self) -> None: ...
    @overload
    def __init__(self, x: float = ..., y: float = ..., z: float = ...) -> None: ...
    @overload
    def __init__(self, seq: Sequence[float]) -> None: ...
    @overload
    def __init__(self, seq: npt.NDArray[np.floating]) -> None: ...
    def __getitem__(self, index: int) -> float: ...
    def __setitem__(self, index: int, value: float) -> None: ...
    def __len__(self) -> int: ...
    def __repr__(self) -> str: ...
    def __str__(self) -> str: ...
    def __add__(self, other: Real3) -> Real3: ...
    def __sub__(self, other: Real3) -> Real3: ...
    def __mul__(self, other: Real3 | float) -> Real3: ...
    def __truediv__(self, other: Real3 | float) -> Real3: ...
    def __radd__(self, other: float) -> Real3: ...
    def __rsub__(self, other: float) -> Real3: ...
    def __rmul__(self, other: float) -> Real3: ...
    def __rtruediv__(self, other: float) -> Real3: ...
    def __iadd__(self, other: Real3 | float) -> Real3: ...
    def __isub__(self, other: Real3 | float) -> Real3: ...
    def __imul__(self, other: Real3 | float) -> Real3: ...
    def __itruediv__(self, other: Real3 | float) -> Real3: ...
    def __neg__(self) -> Real3: ...
    def __eq__(self, other: object) -> bool: ...
    def __ne__(self, other: object) -> bool: ...
    def __array__(self, dtype: Any = ..., copy: Any = ...) -> Any: ...
    def tolist(self) -> list[float]: ...

type Real3Like = Real3 | Sequence[float] | npt.NDArray[np.floating]

class Real6:
    '''Fixed-size six-element floating-point array.'''
    @overload
    def __init__(self) -> None: ...
    @overload
    def __init__(
        self,
        a: float = ...,
        b: float = ...,
        c: float = ...,
        d: float = ...,
        e: float = ...,
        f: float = ...,
    ) -> None: ...
    @overload
    def __init__(self, seq: Sequence[float]) -> None: ...
    @overload
    def __init__(self, seq: npt.NDArray[np.floating]) -> None: ...
    def __getitem__(self, index: int) -> float: ...
    def __setitem__(self, index: int, value: float) -> None: ...
    def __len__(self) -> int: ...
    def __repr__(self) -> str: ...
    def __str__(self) -> str: ...
    def __add__(self, other: Real6) -> Real6: ...
    def __sub__(self, other: Real6) -> Real6: ...
    def __mul__(self, other: Real6 | float) -> Real6: ...
    def __truediv__(self, other: Real6 | float) -> Real6: ...
    def __radd__(self, other: float) -> Real6: ...
    def __rsub__(self, other: float) -> Real6: ...
    def __rmul__(self, other: float) -> Real6: ...
    def __rtruediv__(self, other: float) -> Real6: ...
    def __iadd__(self, other: Real6 | float) -> Real6: ...
    def __isub__(self, other: Real6 | float) -> Real6: ...
    def __imul__(self, other: Real6 | float) -> Real6: ...
    def __itruediv__(self, other: Real6 | float) -> Real6: ...
    def __neg__(self) -> Real6: ...
    def __eq__(self, other: object) -> bool: ...
    def __ne__(self, other: object) -> bool: ...
    def __array__(self, dtype: Any = ..., copy: Any = ...) -> Any: ...
    def tolist(self) -> list[float]: ...

type Real6Like = Real6 | Sequence[float] | npt.NDArray[np.floating]

class Color:
    '''RGBA color representation using 4 bytes (0-255 per channel) in RGBA order.'''
    @overload
    def __init__(self) -> None: ...
    @overload
    def __init__(
        self,
        r: int = ...,
        g: int = ...,
        b: int = ...,
        a: int = ...,
    ) -> None: ...
    @overload
    def __init__(self, seq: Sequence[int]) -> None: ...
    @overload
    def __init__(self, seq: npt.NDArray[np.unsignedinteger]) -> None: ...
    def __getitem__(self, index: int) -> int: ...
    def __setitem__(self, index: int, value: int) -> None: ...
    def __len__(self) -> int: ...
    def __repr__(self) -> str: ...
    def __str__(self) -> str: ...
    def __eq__(self, other: object) -> bool: ...
    def __ne__(self, other: object) -> bool: ...
    def __array__(self, dtype: Any = ..., copy: Any = ...) -> Any: ...
    def tolist(self) -> list[int]: ...

type ColorLike = Color | Sequence[int] | npt.NDArray[np.unsignedinteger]

class Quaternion:
    '''Used for 3D rotations.'''
    @overload
    def __init__(self) -> None: ...
    @overload
    def __init__(
        self,
        x: float = ...,
        y: float = ...,
        z: float = ...,
        w: float = ...,
    ) -> None: ...
    @overload
    def __init__(self, seq: Sequence[float]) -> None: ...
    def __getitem__(self, index: int) -> float: ...
    def __setitem__(self, index: int, value: float) -> None: ...
    def __len__(self) -> int: ...
    def __repr__(self) -> str: ...
    def __str__(self) -> str: ...
    def __mul__(self, other: Quaternion) -> Quaternion: ...
    def __add__(self, other: Quaternion) -> Quaternion: ...
    def __sub__(self, other: Quaternion) -> Quaternion: ...
    def __rmul__(self, other: float) -> Quaternion: ...
    def __truediv__(self, other: float) -> Quaternion: ...
    def __neg__(self) -> Quaternion: ...
    def __eq__(self, other: object) -> bool: ...
    def __ne__(self, other: object) -> bool: ...
    def to_rotation_vector(self) -> Real3: ...
    def tolist(self) -> list[float]: ...
    @staticmethod
    def identity() -> Quaternion: ...
    @staticmethod
    def zero() -> Quaternion: ...
    @staticmethod
    def from_axis_angle(axis: Real3Like, angle: float) -> Quaternion: ...
    @staticmethod
    def from_rotation_vector(rotation_vector: Real3Like) -> Quaternion: ...
    @staticmethod
    def rotation_x(angle: float) -> Quaternion: ...
    @staticmethod
    def rotation_y(angle: float) -> Quaternion: ...
    @staticmethod
    def rotation_z(angle: float) -> Quaternion: ...

type QuaternionLike = Quaternion | Sequence[float] | npt.NDArray[np.floating]

class TransformRT:
    '''A 3D affine transform (rotation and translation, but no scale).'''
    @overload
    def __init__(self) -> None: ...
    @overload
    def __init__(self, rotation: QuaternionLike) -> None: ...
    @overload
    def __init__(self, translation: Real3Like) -> None: ...
    @overload
    def __init__(self, rotation: QuaternionLike, translation: Real3Like) -> None: ...
    def __repr__(self) -> str: ...
    def __str__(self) -> str: ...
    def __mul__(self, other: TransformRT) -> TransformRT: ...
    def __imul__(self, other: TransformRT) -> TransformRT: ...
    def __eq__(self, other: object) -> bool: ...
    def __ne__(self, other: object) -> bool: ...
    @property
    def rotation(self) -> Quaternion: ...
    @rotation.setter
    def rotation(self, value: QuaternionLike) -> None: ...
    @property
    def translation(self) -> Real3: ...
    @translation.setter
    def translation(self, value: Real3Like) -> None: ...
    @staticmethod
    def identity() -> TransformRT: ...
    def inverse(self) -> TransformRT: ...

class Aabb:
    '''Axis-aligned bounding box in 3D space.'''
    @overload
    def __init__(self) -> None: ...
    @overload
    def __init__(self, min: Real3Like, max: Real3Like) -> None: ...
    def __repr__(self) -> str: ...
    def __str__(self) -> str: ...
    def __eq__(self, other: object) -> bool: ...
    def __ne__(self, other: object) -> bool: ...
    @property
    def min(self) -> Real3: ...
    @min.setter
    def min(self, value: Real3Like) -> None: ...
    @property
    def max(self) -> Real3: ...
    @max.setter
    def max(self, value: Real3Like) -> None: ...
    def get_center(self) -> Real3: ...
    def get_size(self) -> Real3: ...
class Obb:
    '''Oriented bounding box in 3D space.'''
    @overload
    def __init__(self) -> None: ...
    @overload
    def __init__(self, transform: TransformRT, half_extents: Real3Like) -> None: ...
    def __repr__(self) -> str: ...
    def __str__(self) -> str: ...
    def get_center(self) -> Real3: ...
    def get_half_extents(self) -> Real3: ...
    def get_size(self) -> Real3: ...
# Module functions (defined in mochi_physics_pybind.cpp)
def is_debug() -> bool:
    '''Return whether the loaded native library is a debug build.'''
    ...
def is_initialized() -> bool:
    '''Return whether SuperDex Physics is currently initialized.'''
    ...
def initialize(num_worker_threads: int) -> None:
    '''Initialize SuperDex Physics by creating the process-wide context.

    Only one context is needed for the entire process. It can be used to load shapes and
create any
    number of scenes. Call shutdown() to clean everything up when you are done.

    Args:
        num_worker_threads: The number of asynchronous worker threads to initialize, or
zero for
            single-threaded execution. Negative values (for example, -1) let SuperDex
Physics
            choose a default based on the CPU hardware. Positive values are clamped to
the number
            of logical processors.

    Note:
        Simulation performance does not necessarily improve by using more threads. For
simple
        scenes, using a small number of threads, or even single-threaded execution, may
be fastest.

    Raises:
        RuntimeError: If SuperDex Physics is already initialized.
    '''
    ...
def shutdown() -> None:
    '''Shut down SuperDex Physics by destroying the process-wide context.

    Any remaining state is immediately destroyed, including all scenes, actors, and
constraints.
    Registered dependent contexts are destroyed first.

    Warning:
        Must be called on the same thread that called initialize().

    Raises:
        RuntimeError: If SuperDex Physics is not initialized.
    '''
    ...
def uses_double_precision() -> bool:
    '''Return whether the loaded native library uses double-precision floating-point
(FP64) values.'''
    ...
def uses_hdf5() -> bool:
    '''Return whether the loaded native library includes HDF5 support.'''
    ...
@overload
def normalize(v: Real2Like) -> Real2:
    '''Return a unit-length copy of the vector. A zero vector remains zero.'''
    ...
@overload
def normalize(v: Real3Like) -> Real3:
    '''Return a unit-length copy of the vector. A zero vector remains zero.'''
    ...
@overload
def normalize(q: QuaternionLike) -> Quaternion:
    '''Return a unit-length copy of the quaternion. A zero quaternion remains zero.'''
    ...
def log(message: str, channel: LogChannel = ...) -> None: ...
